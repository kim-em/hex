import HexGf2.Core
import HexGf2.FiniteExtension
import HexGf2.Ops

/-!
`HexGf2` exposes packed `GF(2)` polynomial scaffolding, the carry-less
multiply boundary, the first XOR/shift polynomial operations, and the
finite-extension carrier records used by later optimized binary-field code.
-/
