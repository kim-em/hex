## Current state

`HexPolyZMathlib` is at `done_through: 2` and ready for Phase 3 (conformance testing). The library exposes:

- `Hex.ZPoly ≃+* Polynomial ℤ` via `toPolynomial`, `ofPolynomial`, `equiv` (`HexPolyZMathlib/Basic.lean`).
- Mignotte bound and supporting `coeff_map_intCast`, `natDegree_map_intCast`, `support_map_intCast`, `norm_coeff_map_intCast`, `sq_norm_coeff_map_intCast`, `mahlerMeasure_le_l2norm`, `mignotte_bound` (`HexPolyZMathlib/Mignotte.lean`).

`HexPolyZMathlib/Basic.lean` and `HexPolyZMathlib/Mignotte.lean` are fully proven (zero `sorry`/`axiom`). There is no `HexPolyZMathlib/Conformance.lean` yet.

## Deliverables

One PR adding `HexPolyZMathlib/Conformance.lean`, mirroring the established noncomputable-bridge conformance pattern from `HexPolyMathlib/Conformance.lean`:

1. **Module docstring** declaring `Oracle: none`, `Mode: always`, plus the standard "Covered operations / Covered properties / Covered edge cases" lists (per `SPEC/testing.md` § per-library module contract). Cover at minimum:
   - operations: `toPolynomial`, `ofPolynomial`, `equiv`, `equiv.symm`, plus the `@[simp]` simp-bridge lemmas (`coeff_toPolynomial`, `toPolynomial_zero`, `toPolynomial_C`, `toPolynomial_add`, `toPolynomial_mul`, `toPolynomial_ofPolynomial`, `ofPolynomial_toPolynomial`, `equiv_apply`, `equiv_symm_apply`).
   - properties: round trips in both directions; `equiv` agrees with the named conversion functions; addition and multiplication are preserved by the bridge.
2. **`example` proofs** discharged by the named bridge lemmas (one per advertised operation, ≥3 cases per operation: typical / edge / adversarial). Use `noncomputable section`. Use private `Hex.ZPoly` and `Polynomial ℤ` fixtures; size-pick fixtures toward the upper end of `SPEC/testing.md` § "Profile sizes" (degree 8-12).
3. **Import** `HexPolyZMathlib/Conformance.lean` from `HexPolyZMathlib.lean` so `lake build HexPolyZMathlib` elaborates every check.
4. **Out of scope here**: writing fixtures that exercise the Mignotte bound itself (the bound is over `ℝ` and not amenable to `example`-style conformance — leave for a later cross-check or oracle issue if needed).
5. **Bump** `libraries.yml`: `HexPolyZMathlib.done_through: 3` in the same PR.
6. CI: confirm `lake build HexPolyZMathlib` is green and that the `Conformance.yml` job picks the new module up via the auto-derivation script (no extra matrix entry expected).

## Context

- `HexPolyZMathlib/Basic.lean` (bridge surfaces and simp lemmas).
- `HexPolyZMathlib/Mignotte.lean` (Mignotte bound; out of scope for this conformance module).
- `HexPolyMathlib/Conformance.lean` — canonical pattern to mirror (noncomputable section, `example : LHS = RHS := named_lemma args`).
- `HexMatrixMathlib/Conformance.lean` and `HexGramSchmidtMathlib/Conformance.lean` — additional reference patterns.
- `SPEC/testing.md` — per-library module contract, profile sizes, banned anti-patterns.
- `PLAN/Phase3.md` — exit criteria and reviewer checklist.

## Verification

- `lake build HexPolyZMathlib` green; conformance file elaborates in <2s.
- `git diff --check` clean.
- No `axiom` / `native_decide` / `TODO` / `FIXME` / `sorry` introduced.
- Module docstring matches contract; ≥3 cases per advertised operation; no banned anti-patterns (no `expected*` dead fields, no LHS-equals-its-evaluation `#guard`, no `decide`-on-noncomputable).
- `libraries.yml HexPolyZMathlib.done_through` is `3`.

## Out of scope

- Mignotte-bound oracle or numerical cross-check.
- `HexPolyZ` (non-Mathlib) conformance work.
- Any new `def` or `theorem` in `HexPolyZMathlib/Basic.lean` or `Mignotte.lean`.
