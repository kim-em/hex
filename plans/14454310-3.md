## Current state

`HexLLLMathlib` is at `done_through: 0` and ready for Phase 1 (library scaffolding). The library already has:

- `HexLLLMathlib/Basic.lean`: `latticeSubmodule`, `mem_latticeSubmodule_iff`, `memLattice_iff_mem_latticeSubmodule`, `row_mem_latticeSubmodule`, `intRowToEuclidean`, `intVectorToEuclidean`, plus the Mathlib-Euclidean-norm formulation of the LLL short-vector guarantee.
- 2 `sorry` placeholders in `Basic.lean`, both in `theorem` bodies (`mem_latticeSubmodule_iff` itself; `lake build` succeeds today).
- `lake build HexLLLMathlib` is green at HEAD.

The done_through counter is at `0`, but no Phase 1 audit and bump has been done.

## Deliverables

One PR auditing Phase 1 completeness against `SPEC/Libraries/hex-lll-mathlib.md` and bumping `libraries.yml`:

1. **Audit**: walk every named SPEC declaration in `SPEC/Libraries/hex-lll-mathlib.md` (the Mathlib bridge for `hex-lll`) and verify, for each one already in Lean:
   - signature matches the SPEC,
   - body correctly implements the SPEC contract (no wrong-but-plausible trivial body, no `sorry` in any `def` or data-carrying `structure` field, no fake `@[extern]`).
   For SPEC declarations NOT yet in Lean, list them in the PR description and open one narrow follow-up issue per gap (Phase 1 follow-on).
2. **Module docstring**: confirm `HexLLLMathlib/Basic.lean` carries a module docstring summarising contents (Phase 1 exit criterion). Add or extend if missing.
3. **Forbidden placeholders**: confirm no `TODO` / `FIXME` / `...` / wrong-shape body in any `def`. Existing `sorry` in `theorem` bodies is fine; do not attempt to discharge them in this PR.
4. **Bump `libraries.yml`**: set `HexLLLMathlib.done_through: 1` in the same PR.
5. PR description records the audit outcome in one paragraph: which declarations were reviewed, which remain SPEC-only (with follow-up issue numbers), and which (if any) were repaired in this PR.

## Context

- `SPEC/Libraries/hex-lll-mathlib.md` — full SPEC for the bridge library.
- `PLAN/Phase1.md` — Phase 1 exit criteria.
- `HexLLLMathlib/Basic.lean` — current scaffolding.
- `libraries.yml` — `done_through` counter; deps: `[HexLLL, HexMatrixMathlib]`. Both at `done_through ≥ 2` and `done_through ≥ 3` respectively, so the dep gate is satisfied.
- Sibling reference: `HexGramSchmidtMathlib`, `HexMatrixMathlib`, `HexModArithMathlib` already at `done_through: 3`.

## Verification

- `lake build HexLLLMathlib` green.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- `libraries.yml HexLLLMathlib.done_through` is `1`.
- PR description names each SPEC declaration audited; deferred declarations have linked follow-up issues; repairs made in this PR are documented one-line each.
- No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` introduced.

## Out of scope

- Discharging the existing 2 theorem `sorry`s in `Basic.lean`.
- Phase 2 / Phase 3 work for `HexLLLMathlib`.
- Any change to dependent libraries (`HexLLL`, `HexMatrixMathlib`).
- Modifying `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.

## Decomposition note

If the audit surfaces more than three substantive missing SPEC declarations or wrong-shape bodies, prefer to `coordination skip N 'needs planner decomposition: N missing decls; need separate Phase 1 issues per stage'` rather than fixing them all here. This issue's headline goal is the audit, the documentation of gaps, and the bump on a known-clean surface.
