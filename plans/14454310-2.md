## Current state

`HexBerlekampZassenhaus` is at `done_through: 0` and ready for Phase 1 (library scaffolding). The library already has substantial committed scaffolding:

- `HexBerlekampZassenhaus/Basic.lean` (~850 lines): `X`, `extractXPower`, `XPowerData`, `leadingCoeffModP`, `leadingCoeffAdmissible`, `squareFreeModP`, `isGoodPrime`, `PrimeCandidateScore`, `choosePrimeScore?`, `choosePrime`, `PrimeChoiceData`, `LiftData`, `FactorNormalizationData`, `normalizeForFactor`, `normalizationPrefixFactors`, `reassembleNormalizedFactors`, `PrimeFactorData`, `DegreeObstruction`, `ZPolyIrreducibilityCertificate`, `degreeSum`, `factorProduct`, `containsDegree`, `hasSubsetDegree`, `checkCertAtFactor`, `checkFactorCerts`, `checkForPolynomial`, `candidateFactorDegrees`, `primeDataAt?`, `checkForCertificate`, `hasObstructionFor`, `checkDegreeObstructions`, `checkIrreducibleCert`, `choosePrimeData`, `henselLiftData`, `recombineLLL?`, `recombinationSearch`, `LiftedFactorsRecombineTo`, `recombine`, `factorWithBound`, `factor`, plus the proof-side theorems (`factor_product_of_bound`, `normalizeForFactor_reassembles`, `reassembleNormalizedFactors_product`, `normalizedConstantFactors_product`, `recombinationSearch_product`, `recombine_product_of_lifted_factors`, `checkIrreducibleCert_*`, `degreeObstruction_no_subset_degree`).
- `HexBerlekampZassenhaus/EmitFixtures.lean` and `HexBerlekampZassenhaus/CrossCheck.lean` from earlier oracle and BZ-specific work (#2023, #1986 follow-ups).
- 26 `sorry` placeholders in `Basic.lean` — all in `theorem` bodies, which is permitted under `PLAN/Phase1.md` ("`sorry`'d theorem proofs are expected").

The done_through counter is at `0`, but `lake build HexBerlekampZassenhaus` succeeds today. The Phase 1 audit and bump have not been done.

## Deliverables

One PR auditing Phase 1 completeness against `SPEC/Libraries/hex-berlekamp-zassenhaus.md` and bumping `libraries.yml`:

1. **Audit**: walk every named SPEC declaration (`factorWithBound`, `factor`, `isGoodPrime`, `choosePrime`, `PrimeChoiceData`, `LiftData`, `choosePrimeData`, `henselLiftData`, `recombine`, plus any narrative-fixed pipeline records) and verify:
   - signature matches the SPEC,
   - body correctly implements the SPEC contract (no wrong-but-plausible trivial body, no `sorry` in any `def` or data-carrying `structure` field, no fake `@[extern]`),
   - any data-level definitions in Lean that are NOT in the SPEC are noted in the PR description,
   - any SPEC declarations NOT yet implemented in Lean are listed in the PR description **and** have a follow-up issue opened (one per gap).
2. **Module docstrings**: every committed `.lean` file under `HexBerlekampZassenhaus/` carries a module docstring summarising contents (Phase 1 exit criterion). Add any that are missing.
3. **Forbidden placeholders**: confirm no `TODO` / `FIXME` / `...` / wrong-shape body. Existing `sorry` in `theorem` bodies is fine; do not attempt to discharge them in this PR.
4. **Bump `libraries.yml`**: set `HexBerlekampZassenhaus.done_through: 1` in the same PR.
5. PR description records the audit outcome in one paragraph: which declarations were reviewed, which remain SPEC-only (with follow-up issue numbers), and which (if any) were repaired in this PR.

## Context

- `SPEC/Libraries/hex-berlekamp-zassenhaus.md` — full SPEC for the capstone library.
- `PLAN/Phase1.md` — Phase 1 exit criteria and the "no wrong-but-plausible bodies" rule.
- `HexBerlekampZassenhaus/Basic.lean`, `HexBerlekampZassenhaus/CrossCheck.lean`, `HexBerlekampZassenhaus/EmitFixtures.lean` — current scaffolding.
- `libraries.yml` — `done_through` counter.
- Sibling: HexBerlekampZassenhausMathlib is blocked on this Phase 1 bump (deps: `[HexBerlekampZassenhaus, HexPolyZMathlib]`).

## Verification

- `lake build HexBerlekampZassenhaus` green.
- `python3 scripts/check_dag.py` clean.
- `git diff --check` clean.
- `libraries.yml HexBerlekampZassenhaus.done_through` is `1`.
- PR description names each SPEC declaration audited; deferred declarations have linked follow-up issues; repairs made in this PR are documented one-line each.
- No new `sorry` / `axiom` / `native_decide` / `TODO` / `FIXME` introduced.

## Out of scope

- Discharging any of the existing 26 theorem `sorry`s.
- Phase 2 / Phase 3 work for `HexBerlekampZassenhaus`.
- Any change to dependent libraries (`HexBerlekamp`, `HexHensel`, `HexLLL`).
- Modifying `SPEC/`, top-level `PLAN.md`, or top-level `AGENTS.md`.
- Performance benchmarking (Phase 4).

## Decomposition note

If the audit surfaces more than three substantive missing SPEC declarations or wrong-shape bodies, prefer to `coordination skip N 'needs planner decomposition: N missing decls; need separate Phase 1 issues per stage'` rather than fixing them all here. This issue's headline goal is the audit, the documentation of gaps, and the bump on a known-clean surface.
