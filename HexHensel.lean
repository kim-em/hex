import HexHensel.Basic

/-!
The `HexHensel` library provides the executable bridge layer shared by later
Hensel-lifting algorithms, starting with conversions between `ZPoly` and
`FpPoly p` and coefficientwise reduction modulo powers of `p`.
-/
