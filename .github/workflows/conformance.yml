name: Conformance

on:
  workflow_dispatch:

jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase 0 stub
        run: |
          echo "Phase 0 stub: conformance workflow to be expanded per SPEC/testing.md."
      - uses: leanprover/lean-action@v1
