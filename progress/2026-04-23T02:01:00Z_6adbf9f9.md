**Accomplished**

- Claimed issue `#70` and verified the residual still existed: `HexArith.Int.extGcd` was pure Lean, there was no `HexArith/ffi/` shim, and `lakefile.toml` had no native wiring for the extern contract.
- Updated `HexArith/ExtGcd.lean` so `HexArith.Int.extGcd` now uses the spec-mandated `@[extern "lean_hex_mpz_gcdext"]` declaration shape while retaining `Hex.pureIntExtGcd` as the definitional fallback.
- Added `HexArith/ffi/mpz_gcdext.c` as the Phase 1 native scaffold; it currently delegates back to `Hex.pureIntExtGcd`, giving the repository the promised extern boundary without changing logical behavior.
- Wired the shim into the `HexArith` Lake target via `moreLinkArgs` in `lakefile.toml`.
- Rebuilt with `lake build HexArith` successfully after the edits.

**Current frontier**

- The issue is implementation-complete for the scaffold interpretation of the spec. The remaining session work is publication only: commit, PR, and issue handoff.

**Next step**

- Commit the extern-contract changes and open the PR closing `#70`.

**Blockers**

- None.
