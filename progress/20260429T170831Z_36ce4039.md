**Accomplished**
- Claimed #1127 as the critical-path continuation for raw `mulWords` left-XOR coefficient linearity.
- Added compiled same-capacity XOR-linearity support in `HexGF2/Multiply.lean`:
  `coeffWords_xorClmulAt_xor_left`,
  `foldl_xorClmulAt_xor_left_coeff`,
  `foldl_mulWords_xor_left_coeff`, and
  `coeffWords_mulWords_xor_left_same_size`.
- Verified the partial slice with `lake build HexGF2.Multiply`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested
  forbidden-token scan.
- Opened follow-up #1129 for the remaining zero-padding bridge from the
  same-size theorem to the full raw `mulWords` theorem.

**Current frontier**
- The carry-less and nested-fold algebra is now available when the three raw
  accumulators have the same capacity.
- The full theorem
  `coeffWords (mulWords (xorWords xs ys) zs) n =
    (coeffWords (mulWords xs zs) n != coeffWords (mulWords ys zs) n)`
  still needs a proof that extending the smaller raw accumulators with zero
  capacity words preserves every coefficient.

**Next step**
- Claim #1129 and prove the padding/extension bridge, then discharge the full
  private raw coefficient theorem using the same-capacity fold lemmas.

**Blockers**
- No Lean errors in the committed partial slice.
- The remaining work is bookkeeping around differently sized raw accumulators,
  not a missing CLMUL or inner-fold linearity fact.
