**Accomplished**

- Claimed `#915` and added the missing `HexGramSchmidt` theorem/API surface for the core determinant and norm consumers.
- Added `GramSchmidt.Rat.basis_span` mirroring the existing integer prefix-span theorem shape.
- Added Gram-Schmidt-owned integer `memLattice` and `independent` predicates, `gramSchmidtNormProduct`, and theorem declarations for `gramDet_eq_prod_normSq`, `gramDet_pos`, `basis_normSq`, and `normSq_latticeVec_ge_min_basis_normSq`.
- Verified `lake build HexGramSchmidt`, `lake build HexLLL`, `python3 scripts/check_dag.py`, `git diff --check`, the requested theorem-surface grep, and no `axiom`/`native_decide` matches in `HexGramSchmidt`.

**Current frontier**

- `HexGramSchmidt` now exposes the determinant/norm theorem names needed by LLL and bridge consumers, with Phase 2 proof placeholders only.
- The new Gram-Schmidt predicates avoid an upstream dependency on `HexLLL`; downstream code can bridge to the existing LLL `Matrix.memLattice`/`Matrix.independent` predicates later if desired.

**Next step**

- Continue the remaining `HexGramSchmidt` Phase 2 review work, especially the update-surface review called out by the issue.

**Blockers**

- None for `#915`.
