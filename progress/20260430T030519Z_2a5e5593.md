**Accomplished**

- Claimed #1249 and verified its HexPoly Phase 5 context.
- Replaced the direct `sorry` in `mod_sub_self_eq_mul_neg_div` with a coefficientwise proof from `div_mul_add_mod`.
- Isolated the remaining obstacle as `mul_sub_zero_comm`, a smaller executable DensePoly multiplication-negation law.
- Opened follow-up issue #1252 for that multiplication bridge.
- Verified `lake build HexPoly`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` sorry scan.

**Current frontier**

- #1249 is partially complete: the division-identity subtraction helper now reduces to the single DensePoly multiplication law `mul_sub_zero_comm`.
- The remaining local leaf for this path is #1252.

**Next step**

- Prove #1252 by establishing the needed coefficient behavior of executable DensePoly multiplication over `[Lean.Grind.CommRing S]`; then #1249's helper will become sorry-free.

**Blockers**

- None for publishing the partial proof split.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
