**Accomplished**

- Claimed issue `#905`.
- Added private list/permutation fold helpers in `HexPolyFp/Basic.lean`.
- Proved the generic triangular fold reindex lemma over `ZMod64 p` for the associativity follow-up.

**Current frontier**

- `#906` can instantiate the new generic reindex lemma for `FpPoly.mul_assoc` wiring.

**Next step**

- Claim `#906` or `#893` after the dependency chain is updated and use `fold_triangular_assoc_reindex` to finish the coefficient associativity proof.

**Blockers**

- None for this issue.
