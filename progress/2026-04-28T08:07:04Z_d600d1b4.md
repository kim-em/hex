**Accomplished**

- Claimed issue `#476` after the higher-priority `human-oversight` issue `#561` was rejected by coordination as `needs replan`.
- Added [HexHensel/Basic.lean](/home/kim/hex/worktrees/d600d1b4/HexHensel/Basic.lean:1) with the Phase 1 bridge layer between `ZPoly` and `FpPoly p`: `ZPoly.modP`, `FpPoly.liftToZ`, `ZPoly.reduceModPow`, and initial compatibility/congruence lemmas for reduction, addition, and multiplication.
- Updated [HexHensel.lean](/home/kim/hex/worktrees/d600d1b4/HexHensel.lean:1) to export the new bridge module and document the root library surface.
- Verified with `lake build HexHensel`, `lake build`, and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexHensel` now exposes the shared bridge and congruence utilities expected by later lifting work, but the actual linear and quadratic Hensel step algorithms remain open in dependent issues `#489` and `#492`.

**Next step**

- Commit the bridge scaffold and open the PR closing `#476`.

**Blockers**

- None.
