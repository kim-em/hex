**Accomplished**

- Claimed issue `#551` and repaired [HexMatrixMathlib/RankSpanNullspace.lean](/home/kim/hex/worktrees/d988bcdb/HexMatrixMathlib/RankSpanNullspace.lean:1) so its public bridge theorems now live in the canonical `HexMatrixMathlib` namespace.
- Removed the duplicate local `matrixEquiv` definition from that module and reused the canonical bridge from [HexMatrixMathlib/Basic.lean](/home/kim/hex/worktrees/d988bcdb/HexMatrixMathlib/Basic.lean:1), while keeping the row-reduction API available via `HexMatrix.RREF`.
- Verified the repair with `lake build HexMatrixMathlib` and `python3 scripts/check_dag.py`.

**Current frontier**

- The namespace/API divergence reported by review issue `#521` is fixed; the remaining `HexMatrixMathlib` warnings are the pre-existing scaffold `sorry`s in `Basic`, `Determinant`, and `RankSpanNullspace`.

**Next step**

- Commit the namespace repair and open the PR closing `#551`, which should unblock review issue `#560`.

**Blockers**

- None.
