**Accomplished**

- Claimed feature issue `#307` and extended [`HexPolyFp/Conformance.lean`](/home/kim/hex/worktrees/728b4046/HexPolyFp/Conformance.lean:1) with the remaining Phase 3 square-free slice.
- Added deterministic elaboration-time checks for `FpPoly.squareFreeDecomposition` and `FpPoly.squareFreePart`, covering a typical quadratic over `F_3`, a degree-0 edge case, and a trailing-zero adversarial input that normalizes before decomposition.
- Added executable property checks for decomposition-product reconstruction, positive multiplicities, pairwise-coprime factor records, square-free factor records, and square-free-part extraction.
- Verified the library with `lake build HexPolyFp`.

**Current frontier**

- `HexPolyFp` now has committed core conformance coverage for both the modular-power/Frobenius/modular-composition surface and the current square-free shell.
- The next library-level step is review/phase-bump work for `HexPolyFp.done_through = 3`, which unblocks `HexGfqRing` Phase 3 once merged.

**Next step**

- Commit this conformance slice and create the PR closing `#307`.
- After merge, dispatch or claim the `HexPolyFp` review/phase-bump item so the downstream `HexGfqRing` queue can start.

**Blockers**

- None in this session.
