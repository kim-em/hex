**Accomplished**

- Claimed `#951`.
- Proved `GF2Poly.degree?_mulXk_of_degree?_eq_some` in `HexGF2/Basic.lean`.
- Added shifted-coefficient adapters for source words and for out-of-source-word shifted positions, including the final carry-word high-bit case.
- Verified `lake build HexGF2.Basic`, `lake build HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, the requested `mulXk`/`sorry` scan for `HexGF2/Basic.lean`, and the forbidden `axiom|native_decide` scan.

**Current frontier**

- `coeff_mulXk_degree_add`, `coeff_mulXk_division_step`, and `coeff_division_step_cancel` now consume the proved `mulXk` degree theorem.
- `HexGF2/Euclid.lean` still contains the division/gcd theorem placeholders that are intentionally downstream of this issue.

**Next step**

- Proceed to `#828`: use the completed `mulXk` degree theorem in the `divModAux` invariant and remainder-bound proofs.

**Blockers**

- No blockers for the `mulXk` degree theorem.
