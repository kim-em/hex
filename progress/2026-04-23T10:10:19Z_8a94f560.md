**Accomplished**

- Claimed issue `#367` after verifying the earlier `#375` queue entry was stale against open conformance follow-up `#353`.
- Extended `HexPoly/Benchmark.lean` with stable CSV headers, row serializers, and batch CSV runners for the existing multiplication, monic-division, and GCD/XGCD benchmark families.
- Verified `lake build HexPoly` succeeds with the new export surface.

**Current frontier**

- `HexPoly` now exposes machine-readable benchmark rows directly from the library target for the committed Phase 4 cases.
- Remaining `HexPoly` Phase 4 work, if any, is outside this export-only slice.

**Next step**

- Land `#367`, then continue with another ready Phase 4 or Phase 3 issue such as downstream benchmark publication or remaining conformance expansion on the live status frontier.

**Blockers**

- No blocker for this slice.
