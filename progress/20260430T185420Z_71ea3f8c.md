**Accomplished**

- Claimed #1428 and restructured `linearHenselStep_raw_factor_congr_from_correction` in `HexHensel/Linear.lean`.
- Replaced the theorem's direct proof-body `sorry` with a proof that composes three narrower private obligations:
  `linearHenselStep_first_order_congr`,
  `linearHenselStep_scaled_first_order_congr`, and
  `linearHenselStep_product_expansion_congr`.
- Connected the existing `scale_coeffwiseDiv_sub_of_congr` helper to the target theorem via the explicit `eZ`/`first` decomposition.
- Ran `lake build HexHensel`, `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern scan of `HexHensel/Linear.lean`.

**Current frontier**

- The raw factor congruence theorem now has the intended high-level shape:
  finite-field correction gives a first-order congruence, scaling by `p^k` lifts it to modulus `p^(k+1)`, and product expansion discharges the cross term.
- Three private proof obligations remain in `HexHensel/Linear.lean`:
  `linearHenselStep_first_order_congr`,
  `linearHenselStep_scaled_first_order_congr`, and
  `linearHenselStep_product_expansion_congr`.
- Existing unrelated `HexHensel/Linear.lean` proof-body `sorry`s remain.

**Next step**

- Prove `linearHenselStep_first_order_congr` by adding or deriving the local `modP` add/mul compatibility bridge for lifted `FpPoly` terms.
- Then prove `linearHenselStep_scaled_first_order_congr` from coefficient divisibility, and `linearHenselStep_product_expansion_congr` by expanding `(g + p^k*r) * (h + p^k*hCorrection)` and using `hk : 1 ≤ k` to absorb the `p^(2*k)` term modulo `p^(k+1)`.

**Blockers**

- No build blockers.
