**Accomplished**

- Claimed #1250 and proved `mod_add_mul_self` in `HexPoly/Euclid.lean` through the existing `%` invariants.
- Added small polynomial algebra helpers for `p + 0 = p` and `0 * p = 0`.
- Factored the remaining canonical-remainder obligations into narrower leaves, `mod_self_eq_zero` and `zero_mod_eq_zero`.
- Opened follow-up issues #1255 and #1256 for those two leaves.
- Verified `lake build HexPoly`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` sorry scan.

**Current frontier**

- `mod_add_mul_self` itself no longer contains a direct `sorry`.
- The remaining new lower-level leaves are `mod_self_eq_zero` and `zero_mod_eq_zero`.
- Existing unrelated `HexPoly/Euclid.lean` sorries, including CRT congruence leaves, remain outside this issue.

**Next step**

- Prove #1255 and #1256, then return to #1233 once the lower modular-remainder foundation is fully discharged.

**Blockers**

- None for this PR.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
