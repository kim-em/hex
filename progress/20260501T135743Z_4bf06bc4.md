**Accomplished**

- Claimed #1622 and replaced the false arbitrary `c,w`
  `yunFactorsContribution_loop_state_product_invariant` surface with the
  caller-scoped `yunFactorsContribution_initial_state_product_invariant`.
- Rewired `yunFactorsContribution_reconstruct_core` to use the corrected
  initial-state invariant, preserving the public reconstruction theorem and
  the done/tail helper surfaces.
- Verified `HexPolyFp.SquareFree`, `HexPolyFp`, `scripts/status.py HexPolyFp`,
  `scripts/check_dag.py`, `git diff --check`, and the touched-file forbidden
  placeholder scan.

**Current frontier**

- The Yun contribution reconstruction invariant is now stated only for the
  reachable split `c := f / gcd f f'`, `g := gcd f f'` under the caller's
  nonzero, nonzero-derivative, positive-multiplicity, and fuel hypotheses.
- The corrected invariant still carries the existing Phase 5 proof placeholder
  at the invariant body; downstream work no longer depends on the false
  arbitrary `fuel = 0` statement.
- A second-opinion check was attempted but the external `claude` process
  stalled without output and was stopped.

**Next step**

- Continue #1594 by proving or consuming the corrected initial-state invariant
  in the public Yun reconstruction integration path.

**Blockers**

- None for this statement repair. Existing upstream `sorry` warnings remain in
  `HexArith`, `HexModArith`, `HexPoly`, `HexPolyFp.Basic`, and other
  `HexPolyFp.SquareFree` Phase 5 proof bodies.
