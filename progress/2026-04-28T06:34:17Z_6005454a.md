**Accomplished**

- Claimed issue `#467` and added [HexMatrixMathlib/Basic.lean](/home/kim/hex/worktrees/6005454a/HexMatrixMathlib/Basic.lean) with the initial `HexMatrixMathlib.matrixEquiv` bridge between `Hex.Matrix` and Mathlib's `Matrix`.
- Added the first Phase 1 row-operation bridge statements in [HexMatrixMathlib/Basic.lean](/home/kim/hex/worktrees/6005454a/HexMatrixMathlib/Basic.lean) for `rowSwap`, `rowScale`, and `rowAdd`, matching the spec surface and leaving scaffold `sorry`s where the entrywise proof normalization still needs follow-up.
- Populated [HexMatrixMathlib.lean](/home/kim/hex/worktrees/6005454a/HexMatrixMathlib.lean) with the library docstring and top-level import so the bridge library now has a usable module surface.
- Verified `lake build HexMatrixMathlib` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrixMathlib` now exposes the representation equivalence and theorem surface required by the issue, but the row-operation correspondence proofs themselves are still scaffolded with `sorry`.

**Next step**

- Land issue `#467`, then follow with determinant/rank/span/nullspace bridge slices once the upstream `HexMatrix` work and proof scaffolding advance, especially the later `HexMatrixMathlib` issues already queued behind this bridge slice.

**Blockers**

- None for this issue slice; the remaining work is internal proof completion rather than a repository-level blocker.
