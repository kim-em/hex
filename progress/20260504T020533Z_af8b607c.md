**Accomplished**

- Claimed #1973 after #1968 and #1972 were already claimed by other agents.
- Added `Hex.LLLBench.runFirstShortVectorChecksum` to `HexLLL/Bench.lean`, covering the public `lll.firstShortVector` recombination surface.
- Added a BZ-shaped public LLL benchmark fixture with independence checking hoisted into `prep`, plus a proof bridge from the executable check to `Matrix.independent`.
- Tuned the scientific schedule to parameters `5, 6, 7`; the default run reports a verdict consistent with `firstShortVectorComplexity`.

**Current frontier**

- `HexLLL/Bench.lean` now registers six benchmarks, including the downstream short-vector hot path.
- Verification passed:
  - `lake build HexLLL.Bench`
  - `lake exe hexlll_bench run Hex.LLLBench.runFirstShortVectorChecksum`
  - `lake exe hexlll_bench list`
  - `lake exe hexlll_bench verify`
  - `lake build HexLLL`
  - `rg -n "axiom|native_decide|TODO|FIXME" HexLLL/Bench.lean`
  - `git diff --check`

**Next step**

- Let the PR for #1973 merge; HexLLL Phase 4 still must not be marked complete until the full Phase 4 review validates every benchmark under the project rules.

**Blockers**

- None for #1973.
