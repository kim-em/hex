**Accomplished**

- Claimed feature issue `#482` and added [HexBerlekamp/Basic.lean](/home/kim/hex/worktrees/c8b22b3e/HexBerlekamp/Basic.lean:1) with the initial executable Berlekamp surface: `basisSize`, coefficient extraction, Berlekamp columns, the Frobenius matrix `Q_f`, the fixed-space matrix `Q_f - I`, and kernel wrappers that convert `HexMatrix.nullspace` vectors back into `FpPoly` representatives.
- Added the top-level [HexBerlekamp.lean](/home/kim/hex/worktrees/c8b22b3e/HexBerlekamp.lean:1) import and module docstring so the library now exports the Berlekamp matrix/kernel scaffold.
- Verified the slice with `lake build HexBerlekamp` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexBerlekamp` now has the first reusable matrix/kernel scaffold, but later Phase 1 work still needs the irreducibility criterion, factor splitting, and Rabin-facing surfaces.
- The kernel wrapper currently reuses `HexMatrix.nullspace` through a `Lean.Grind.Field (ZMod64 p)` assumption; that matches the intended linear-algebra path without introducing a Berlekamp-local solver.

**Next step**

- Commit this Berlekamp scaffold and open the PR closing `#482`.

**Blockers**

- None.
