**Accomplished**

- Claimed feature issue #1216 for HexBerlekampZassenhaus Phase 3 conformance.
- Expanded `HexBerlekampZassenhaus/Conformance.lean` into a Lean-only core conformance suite with a top contract docstring and committed guards for normalization, good-prime selection, prime data packaging, recombination success/failure, bounded/default factor product behavior, and integer irreducibility certificate positive/negative cases.
- Split the implementation into `HexBerlekampZassenhaus/Basic.lean`, made `HexBerlekampZassenhaus.lean` import both `Basic` and `Conformance`, and changed the conformance module to import `Basic` to avoid an import cycle.
- Bumped `libraries.yml[HexBerlekampZassenhaus].done_through` from 2 to 3.
- After rebasing onto current `origin/main`, kept upstream's HexPolyFp proof repair and dropped this branch's temporary proof-only fallback.
- Verified `lake build HexBerlekampZassenhaus`, `lake build`, `python3 scripts/status.py HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, `git diff --check`, and a manual banned-pattern scan of the conformance file.

**Current frontier**

- HexBerlekampZassenhaus now reports ready for Phase 4, blocked by dependency phase counters: `HexBerlekamp.done_through >= 4` and `HexHensel.done_through >= 4`.
- The conformance root import pattern now matches the other Phase 3 libraries, so `lake build HexBerlekampZassenhaus` reaches the conformance guards.

**Next step**

- Open the PR for #1216 with the conformance expansion, root-module split, phase bump, and proof-only HexPolyFp dependency repair.

**Blockers**

- None for this session.
