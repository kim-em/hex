**Accomplished**

- Claimed feature issue `#1087`.
- Added the first `HexBerlekampZassenhaus` factoring pipeline entry points in
  `HexBerlekampZassenhaus.lean`:
  - `choosePrimeData`
  - `henselLiftData`
  - `recombine`
  - `factorWithBound`
  - `factor`
  - proof-only `factor_product_of_bound`
- Switched the module import to `HexHensel.Multifactor` so the pipeline can use
  the public multifactor lift and ordered product surfaces directly.
- Created follow-up feature issue `#1090` for the remaining irreducibility
  certificate checker surface from the SPEC.

**Current frontier**

- The factoring pipeline now packages selected small-prime data, square-free
  modular factors, Hensel lifted integer factors, and a conservative exact
  recombination step under the stable public API names.
- `HexBerlekampZassenhaus.done_through` remains `0` because the certificate
  API is still absent and is tracked by `#1090`.

**Next step**

- Claim `#1090` to add `PrimeFactorData`, `ZPolyIrreducibilityCertificate`,
  and `checkIrreducibleCert`, then reassess whether Phase 1 can be marked
  complete.

**Blockers**

- None.
