**Accomplished**

- Claimed issue `#919`.
- Inspected the pinned `lean-bench` source at manifest revision `81e2ff683f3fdf6803eafe60251716f9c95c73ea`; it exposes `compare` as a CLI operation over existing `setup_benchmark` registrations, not as a separate compare-registration macro.
- Updated `HexArith/Bench.lean`'s module docstring to distinguish scientific benchmark registrations from the three-way extended-GCD compare group and to state the shared prepared nonnegative bounded-word domain.
- Verified `git diff --check`, `python3 scripts/check_dag.py`, and the forbidden-pattern scan `rg -n "native_decide|axiom" HexArith -S`.

**Current frontier**

- `HexArith.Bench` now documents the extended-GCD compare group separately from benchmark registrations while preserving the existing normalized `(g, s*a + t*b)` observable policy and benchmark names.

**Next step**

- Re-run the Lake verification commands once the local `.lake/packages/mathlib` checkout is clean enough for Lake to reconcile the manifest checkout.

**Blockers**

- `lake build HexArith.Bench` did not reach Lean compilation because Lake attempted to check out mathlib revision `5450b53e5ddc75d46418fabb605edbf36bd0beb6`, but the local `.lake/packages/mathlib` checkout has pre-existing deleted and untracked files that Git refuses to overwrite or remove during checkout.
