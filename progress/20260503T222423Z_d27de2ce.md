**Accomplished**

- Claimed #1852 and added proof-friendly `subtractScaledShift` support in
  `HexPoly/Euclid.lean`.
- Refactored `subtractScaledShift` through an explicit fold step and proved
  frame/cancellation-slot lemmas for unchanged indices, the last shifted divisor
  slot, and indices above the previous leading remainder degree.
- Added `ZMod64.inv_mul_eq_one_of_prime` in `HexModArith/Prime.lean`, deriving
  the nonzero-residue inverse law from the existing prime-modulus and inverse
  machinery.

**Current frontier**

- Verification passed:
  - `lake build HexPoly.Euclid`
  - `lake build HexPolyFp.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- Placeholder scan over touched task files introduced no `axiom`,
  `native_decide`, `TODO`, `FIXME`, or new `sorry`. `HexPolyFp.Basic` still
  reports only pre-existing `sorry` warnings and has no final diff.

**Next step**

- Use the new `subtractScaledShift` frame/cancellation lemmas to prove the
  `divModArrayAux` loop degree invariant for #1844, specializing the scalar
  leading-coefficient cancellation through `ZMod64.inv_mul_eq_one_of_prime`.

**Blockers**

- None for this issue.
