**Accomplished**

- Claimed `#939`.
- Updated `LLLState.potential` in `HexLLL/Basic.lean` to multiply only `d_1` through `d_{n-1}`, matching `SPEC/Libraries/hex-lll.md`.
- Added `LLLState.potential_eq_gramDetProduct` so future termination proofs can unfold the corrected measure through the state's Gram-determinant certificate without depending on `d_n`.
- Verified `lake build HexLLL`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested HexLLL forbidden-language grep.

**Current frontier**

- The direct HexLLL potential-measure blocker for the Phase 2 review is fixed.
- The remaining `rg` hits in `HexLLL/Basic.lean` are existing theorem/proof-field `sorry`s, not new data-level placeholders.

**Next step**

- Re-run the residual HexLLL Phase 2 review tracked by `#932` after this PR lands.

**Blockers**

- No blockers for this slice.
