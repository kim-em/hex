**Accomplished**

- Claimed #1919 for the HexPolyFp `powLinear` binomial recurrence.
- Added private finite binomial term/sum definitions in `HexPolyFp/SquareFree.lean`.
- Proved the bounded Pascal-row recurrence `powLinearBinomSum_succ_row`, including the zero, interior, and top-row term cases using local polynomial/scalar APIs.

**Current frontier**

- Verification passed:
  - `lake build HexPolyFp.SquareFree`
  - `git diff --check`
  - `git diff -U0 -- HexPolyFp/SquareFree.lean | rg -n "\+.*(axiom|native_decide|TODO|FIXME|sorry)" || true`
- `lake build HexPolyFp.SquareFree` still reports pre-existing `sorry` warnings in `HexPolyFp.Basic` and later declarations in `HexPolyFp.SquareFree`; this session did not add new placeholders.

**Next step**

- Downstream #1920 can use `powLinearBinomSum_succ_row` to assemble the full `powLinear` binomial expansion theorem.

**Blockers**

- None for this recurrence slice.
