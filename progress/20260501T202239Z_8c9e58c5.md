**Accomplished**

- Claimed #1641 and narrowed the `HexPolyFp/SquareFree.lean`
  derivative-active Yun split proof cluster.
- Added the proved helper `yunFactorsContribution_initial_state_split`,
  packaging the initial `c * g = f` reconstruction for
  `c = f / gcd f f'` and `g = gcd f f'`.
- Replaced the monolithic
  `yunFactorsContribution_initial_state_product_invariant` placeholder with
  a named private frontier lemma,
  `yunFactorsContribution_derivative_active_split_algebra`, which exposes the
  reconstruction fact separately and delegates the remaining product invariant
  to a narrower proof obligation connected to the existing Yun step/descent
  helpers.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
  `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token scan for `axiom`,
  `native_decide`, `TODO`, and `FIXME` in the touched file.

**Current frontier**

- The derivative-active initial split now has a reusable proved reconstruction
  helper.
- The remaining hard obligation is the proof body of
  `yunFactorsContribution_derivative_active_split_algebra`: showing the
  `yunFactorsContribution` product, together with the possible repeated-part
  `p`-root descent, reconstructs `pow f multiplicity`.

**Next step**

- Prove `yunFactorsContribution_derivative_active_split_algebra` by induction
  on `fuel`, using `hfuel` to discharge the impossible zero-fuel/nonzero case
  and the existing step/descent lemmas for the recursive Yun branch.

**Blockers**

- None; this is partial progress because the derivative-active product
  algebra still needs the recursive proof described above.
