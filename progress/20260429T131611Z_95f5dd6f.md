**Accomplished**

- Claimed feature issue `#1075` for the missing HexHensel multifactor lift API.
- Added `HexHensel/Multifactor.lean` with the public `Array.polyProduct`, executable `ZPoly.multifactorLift`, and proof-level `ZPoly.multifactorLift_spec`.
- Exported the multifactor surface from `HexHensel.lean`.
- Verified `lake build HexHensel`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-token/sorry scans.

**Current frontier**

- `HexHensel.done_through` remains `1`; the issue intentionally does not add the Phase 2 review token.
- The new preservation theorem is present with a proof-only `sorry`, matching the current scaffold phase.

**Next step**

- Run a follow-up HexHensel Phase 2 review to decide whether the repaired surface is sufficient to add `status/hex-hensel.scaffolding-reviewed` and bump `libraries.yml`.

**Blockers**

- None for this issue.
