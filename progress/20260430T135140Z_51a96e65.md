**Accomplished**

- Claimed #1360 and assessed the remaining `HexHensel/Linear.lean` one-step
  theorem surface.
- Added local coefficient and congruence helpers for
  `LinearLiftResult.liftScaledIncrement`.
- Decomposed the rest of #1360 into #1363 for `linearHenselStep_spec` and
  #1364 for the one-step monicity/degree preservation theorems.
- Added #1363 and #1364 as dependencies of #1361 so the iterative wrappers
  remain blocked on the one-step surface.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans on `HexHensel/Linear.lean`.

**Current frontier**

- `LinearLiftResult.coeff_liftScaledIncrement` and
  `LinearLiftResult.congr_liftScaledIncrement_zero` are available for the
  linear Hensel proof cluster.
- The one-step theorem placeholders in `HexHensel/Linear.lean` remain split
  across #1363 and #1364.

**Next step**

- Work #1363 to prove the factor-congruence bridge for `linearHenselStep_spec`,
  then #1364 for structural monicity and degree invariants.

**Blockers**

- None for the landed helper slice. The remaining proofs need dedicated
  reduction, remainder-degree, and canonical-lift bridge lemmas.
