**Accomplished**

- Claimed issue `#624` after the higher-priority `#583` and `#616` targets were unavailable (`#583` marked `replan`, `#616` already claimed).
- Reproduced the runtime distinction between plain `lake env lean` and Lake-driven elaboration for extern-backed `HexArith` declarations using `/tmp/test_extern.lean`.
- Confirmed that `lake build HexArith` produces the expected module dynlibs and that `lake lean /tmp/test_extern.lean` resolves `UInt64.mulHi`, `UInt64.addCarry`, and `HexArith.Int.extGcd` successfully.
- Updated [`PLAN/Conventions.md`](../PLAN/Conventions.md) and [`SPEC/Libraries/hex-arith.md`](../SPEC/Libraries/hex-arith.md) so the repo now documents the real FFI build rule (`extern_lib` for C shims, `moreLinkArgs` only for linker flags) and the supported smoke-test path (`lake lean` or explicit `--load-dynlib`, not plain `lake env lean`).

**Current frontier**

- The branch contains only the documentation/spec correction for `#624` plus this progress note.
- Verification is complete for the intended resolution path: `lake build HexArith`, `lake lean /tmp/test_extern.lean`, and `python3 scripts/check_dag.py` all succeed.

**Next step**

- Commit the `#624` docs/spec correction and open the PR closing the issue.

**Blockers**

- None for this scope. The remaining limitation is upstream Lean/Lake behavior: plain `lake env lean <file>` does not auto-load per-module dynlibs for extern-backed declarations.
