**Accomplished**

- Claimed issue `#504` and added the missing `HexMatrix` row-echelon API declarations in [HexMatrix/RowEchelon.lean](/home/kim/hex/worktrees/69605f26/HexMatrix/RowEchelon.lean): `transform_mul_inv`, `freeCols_injective`, and `pivotCols_disjoint_freeCols`.
- Kept the additions aligned with the existing scaffold style and namespace layout so the review-identified omissions now exist at the root-library API surface.
- Verified `lake build HexMatrix` and `python3 scripts/check_dag.py`.

**Current frontier**

- Issue `#504` is reduced to publishing the branch/PR; the new declarations are present and build cleanly with the repository's existing scaffold warnings.

**Next step**

- Create the PR for `#504`, then let follow-up review decide whether `HexMatrix.done_through` can advance or if any additional row-echelon surface is still missing.

**Blockers**

- None.
