# PR #1817 repair: dedup ratCoeffToIntWithDen_cast after main merge

## Accomplished

- Merged `origin/main` into `agent/4be83fc9`. Main's #1816 / #1827 had
  re-introduced `Hex.ZPoly.ratCoeffToIntWithDen_cast` as a `sorry`, and
  the PR's already-proved version became a duplicate, breaking the build.
- Removed the PR's earlier copy of the lemma and replaced main's `sorry`
  body with the actual proof, leaving a single declaration in the
  position main expects.
- Verified locally: `lake build HexPolyZ.Basic`, `lake build HexPolyZ`
  (conformance target), `lake exe hexpolyz_bench list`, and
  `lake exe hexpolyz_bench verify` all succeed; only pre-existing
  unrelated `sorry` warnings on lines 256, 573, 580 remain.

## Current frontier

PR #1817 ready for re-push as a clean "fill in the sorry" patch.

## Next step

Push and mark salvaged.

## Blockers

None.
