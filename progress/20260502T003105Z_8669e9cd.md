**Accomplished**

- Claimed #1722, confirmed the direct fully-unfolded `bv_decide` source-pair
  route for `clmul` was too expensive, and decomposed it into #1725, #1726,
  and #1727.
- Claimed #1725 and added private right-input bit-fold coefficient helpers in
  `HexGF2/Multiply.lean`: `oneHotBitWord`, `xorWordList_oneHotBitWords_eq`,
  `clmul_rightBitFold_low`, and `clmul_rightBitFold_high`.
- Verified `lake build HexGF2.Multiply`, `lake build HexGF2`,
  `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-placeholder scan for touched HexGF2
  files.

**Current frontier**

- #1725 is ready for PR: `clmul` low/high word bits can now be expanded over
  the set bits of the right input and one-hot products, with `bit < 64`.
- #1726 is the next proof layer: expand each one-hot product over the source
  bits of the left input.
- #1727 is blocked on #1725 and #1726 and should assemble the final
  source-pair coefficient helpers.

**Next step**

- Land #1725, then prove the one-hot source-bit coefficient lemmas in #1726.

**Blockers**

- None for #1725. The original #1722 one-shot source-pair proof path was
  abandoned because it spent several minutes in `HexGF2.Multiply` without
  completing.
