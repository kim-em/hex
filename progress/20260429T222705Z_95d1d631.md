**Accomplished**

- Claimed issue #1172 for the HexGF2 Phase 4 packed-versus-generic comparison benchmark slice.
- Added the `HexGF2Bench.lean` executable root that imports the library-owned HexGF2 benchmark registrations and adds cross-library `GF2Poly` versus `FpPoly 2` comparison registrations.
- Added deterministic shared-domain GCD comparison registrations and Berlekamp-matrix-style Frobenius-column comparison registrations, each returning coefficient-domain checksums for `lean-bench compare`.
- Kept `HexGF2/Bench.lean` inside the declared HexGF2 dependency boundary and pointed `hexgf2_bench` at the executable-root adapter.
- Verified `lake build hexgf2_bench`, `lake exe hexgf2_bench list`, `lake exe hexgf2_bench verify`, both new smoke `compare` groups, `lake build HexGF2`, `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned-pattern scan.

**Current frontier**

- `HexGF2` remains at `done_through: 3`; this issue intentionally adds comparison registrations without completing all Phase 4 exit criteria or bumping `libraries.yml`.
- The new compare registrations agree on shared smoke parameters; scientific timing remains Phase 4 follow-up work.

**Next step**

- Open the PR for #1172 with the comparison benchmark registrations.

**Blockers**

- No technical blockers found.
