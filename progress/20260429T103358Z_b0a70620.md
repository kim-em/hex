**Accomplished**

- Claimed `#971`, determined it combined three independent HexGF2 proof layers, decomposed it into `#975`, `#976`, and `#977`, and released the parent as `replan`.
- Claimed `#968` for HexPoly Phase 4 review.
- Replaced HexPoly benchmark polynomial-result checksums with bounded `UInt64` hash observables so timing no longer includes unbounded `Int`/`Rat` accumulator growth.
- Added explicit `signalFloorMultiplier := 1.0` to HexPoly benchmark registrations so the committed scientific schedules can produce verdicts on this subprocess-based harness.
- Found and filed benchmark follow-ups `#983` for composition scaling and `#984` for Euclidean division scaling.
- Verified `lake build HexPoly.Bench`, `lake exe hexpoly_bench list`, `lake exe hexpoly_bench verify`, representative scientific runs for add/mul/content/CRT, `python3 scripts/status.py HexPoly`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- HexPoly Phase 4 remains incomplete; `libraries.yml` was intentionally left at `HexPoly.done_through: 3`.
- `runAddChecksum`, `runMulChecksum`, `runContent`, and `runPolyCRTChecksum` now produce consistent representative verdicts after the bounded-observable fix.
- `runComposeChecksum` and `runDivModChecksum` remain Phase 4 blockers with narrow follow-up issues.

**Next step**

- Resolve `#983` and `#984`, then rerun the full HexPoly Phase 4 review before bumping `HexPoly.done_through` to 4.

**Blockers**

- `#983`: composition benchmark scaling exceeds the declared `O(n^3)` model.
- `#984`: division benchmark scaling exceeds the declared `O(n^2)` model.
