**Accomplished**

- Claimed #1865 and proved `GramSchmidt.Int.gramDet_zero` in
  `HexGramSchmidt/Int.lean` by reducing the executable empty-prefix
  determinant convention directly.
- Confirmed `gramDetVec_eq_gramDet` is not definitional and needs the
  missing no-pivot Bareiss / Gram-prefix invariant.
- Created follow-up #1868 for the remaining `gramDetVec_eq_gramDet` proof
  and linked it from #1865 with a decomposition breadcrumb.

**Current frontier**

- Verification passed:
  - `lake build HexGramSchmidt.Int`
  - `lake build HexGramSchmidt`
  - `python3 scripts/status.py HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- `HexGramSchmidt.Int` still has the existing `gramDetVec_eq_gramDet`
  placeholder and later Phase 5 placeholders.

**Next step**

- Land this partial PR for #1865, then pick up #1868 to prove
  `gramDetVec_eq_gramDet` using private Bareiss/Gram-prefix helper lemmas.

**Blockers**

- None for the partial `gramDet_zero` proof.
