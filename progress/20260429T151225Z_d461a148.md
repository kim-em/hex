**Accomplished**

- Claimed `#1113`.
- Added the high-word left one-hot `pureClmul` theorem in `HexGF2/Clmul.lean`.
- Added a private high-projection fold helper that reduces the folded pair accumulator to a high-word step function using `clmulAccumulateBit_oneHot_low` and `clmulAccumulateBit_oneHot_high`.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `#1113` is implemented and ready for PR publication.
- The final public split assembly from the parent chain remains out of scope for this issue.

**Next step**

- Publish the PR for `#1113`; the next HexGF2 proof step should expose the final pair theorem that combines the low-word and high-word left one-hot projections.

**Blockers**

- `rg -n "axiom|native_decide" HexGF2 -S` reports the pre-existing `private axiom aesIrreducible` in `HexGF2/Smoke.lean`; this session did not touch that file.
