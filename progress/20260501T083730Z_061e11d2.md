**Accomplished**

- Claimed issue #1567 for the `HexPolyFp` square-free reverse-accumulator coprimeness invariant.
- Added proved structural lemmas showing `yunFactors` appends newly emitted factors independently of the incoming reverse accumulator and leaves the repeated tail independent of that accumulator.
- Added the corresponding `squareFreeAuxRev_reverse_append` lemma and used it to replace the `squareFreeAuxRev_pairwise_coprime_core` proof-body `sorry`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan over `HexPolyFp/SquareFree.lean`.

**Current frontier**

The arbitrary-accumulator theorem now reduces to the empty-accumulator pairwise coprimeness invariant plus the caller-supplied cross-coprimeness hypothesis. The remaining `HexPolyFp/SquareFree.lean` boundary for this issue is `squareFreeAuxRev_pairwise_coprime_nil_core`, which must prove the actual Yun/recursive factors emitted by an empty run are pairwise coprime.

**Next step**

Prove `squareFreeAuxRev_pairwise_coprime_nil_core` by induction on fuel, adding focused algebraic helper lemmas for coprimeness among factors emitted by `yunFactors` and the recursive `pthRoot repeated` tail.

**Blockers**

None.
