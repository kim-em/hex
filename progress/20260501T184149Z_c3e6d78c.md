**Accomplished**

- Claimed #1666 and proved `ZMod64.inv_mul_eq_one` in `HexModArith/Basic.lean`.
- Added a private Int/Nat modular bridge from the `HexArith.Int.extGcd`
  Bezout equation to the reduced inverse representative used by `ZMod64.inv`.
- Preserved executable definitions for `ZMod64.inv`, `mul`, `pow`, and `ofNat`.
- Verified `lake build HexModArith.Basic`, `lake build HexModArith`,
  `python3 scripts/status.py HexModArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned `HexModArith/Basic.lean` for forbidden
  placeholders.

**Current frontier**

- `HexModArith/Basic.lean` no longer has the inverse multiplication contract
  placeholder.
- `HexModArith/Ring.lean` still has pre-existing Phase 5 placeholders outside
  this issue's scope.

**Next step**

- Continue HexModArith Phase 5 by addressing the remaining `Ring.lean`
  commutative-ring proof placeholders, or pick another unclaimed Phase 5 issue
  if one is higher priority.

**Blockers**

- None.
