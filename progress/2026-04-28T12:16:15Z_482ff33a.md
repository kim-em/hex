**Accomplished**

- Claimed issue `#731` after releasing stale review issue `#672`, then added [`HexArith/Conformance.lean`](/home/kim/hex/worktrees/482ff33a/HexArith/Conformance.lean) as the first Phase 3 `core` conformance slice for `hex-arith`.
- Covered the requested executable surface with elaboration-time checks: `HexArith.extGcd` on `Nat`, `HexArith.Int.extGcd`, `HexArith.UInt64.extGcd`, and the `UInt64` wide-word helpers `mulHi`, `mulFull`, `addCarry`, and `subBorrow`.
- Imported the conformance module from [`HexArith.lean`](/home/kim/hex/worktrees/482ff33a/HexArith.lean) so `lake build HexArith` elaborates the new checks.
- Verified with `lake build HexArith` and `python3 scripts/check_dag.py`; the conformance file also passes the requested `rg -n "native_decide|expected[A-Z]" HexArith/Conformance.lean -S` check (no matches).

**Current frontier**

- The first conformance slice is ready to publish as a PR for `#731`. It intentionally stops short of Barrett, Montgomery, modular exponentiation, and primality conformance, so `HexArith.done_through` remains at `2`.

**Next step**

- Commit the `HexArith` conformance slice plus this progress note, then run `coordination create-pr 731 --partial` so the remaining Phase 3 coverage can be replanned as follow-up work.

**Blockers**

- None for this slice. The remaining Phase 3 work is simply larger than this issue’s scoped extGCD/wide-word subset.
