**Accomplished**

- Claimed critical-path feature issue `#170` and verified that `HexPolyFp` was still only a stub on `main`, so the Phase 1 carrier/basic-arithmetic/Frobenius slice remained unimplemented.
- Added [HexPolyFp/Core.lean](/home/kim/hex/worktrees/95034d68/HexPolyFp/Core.lean) with the `FpPoly p` alias over `HexPoly.DensePoly (HexModArith.ZMod64 p)`, normalization-preserving constructors (`ofCoeffs`, `C`, `monomial`, `X`), coefficient/degree accessors, thin arithmetic wrappers, and a fuel-bounded modular-power/Frobenius API over monic reduction.
- Updated [HexPolyFp.lean](/home/kim/hex/worktrees/95034d68/HexPolyFp.lean) to re-export the new core module and document the public surface.
- Ran `lake build HexPolyFp` successfully.

**Current frontier**

- The `HexPolyFp` root scaffold now exists and compiles, so the next frontier is publishing this slice and letting follow-up workers extend the library with modular composition, square-free decomposition, and any review/bump work once Phase 1 coverage is broad enough.

**Next step**

- Create the PR closing `#170`, then let planners/workers pick up the next `HexPolyFp` or newly unblocked downstream tasks (`HexGfqRing`, `HexHensel`, `HexBerlekamp`) as the phase counters advance.

**Blockers**

- None.
