**Accomplished**

- Claimed issue #1157.
- Moved the packed single-word GF2 helper surface used by field wrappers into `HexGF2/Euclid.lean`.
- Added proof-only Euclid bridge statements for reduced xgcd inverses and the packed CLMUL inverse path.
- Replaced the remaining `sorry` bodies for `GF2n.mul_inv_cancel` and `GF2nPoly.mul_inv_cancel` in `HexGF2/Field.lean`.

**Current frontier**

- `HexGF2/Field.lean` is now free of `sorry` placeholders.
- The wrapper cancellation theorems compile by representative equality and delegate proof-only algebraic gaps to Euclid-level bridge statements.

**Next step**

- Land the PR for #1157, then a later proof-focused task can discharge the new Euclid bridge `sorry`s.

**Blockers**

- No technical blockers found.
