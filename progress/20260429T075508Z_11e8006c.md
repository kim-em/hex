**Accomplished**

- Claimed issue `#868`.
- Added proved one-hot `UInt64` helper lemmas for the word used by `GF2Poly.monomial`.
- Proved `GF2Poly.degree? (GF2Poly.monomial n) = some n`.
- Proved monomial coefficient behavior: the defining coefficient is `true`, and all other coefficients are `false`.
- Verified `lake build HexGF2.Basic`, `lake build HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and that the `HexGF2/Basic.lean` diff adds no `axiom` or `native_decide`.

**Current frontier**

- `HexGF2.Basic` now exposes the monomial degree/coefficient bridge needed by the long-division cancellation work.
- Downstream `HexGF2.Euclid` still has the existing `sorry` proof surface for division/gcd correctness.

**Next step**

- Continue with the blocked `mulXk` division-step issue once its remaining dependencies have landed.

**Blockers**

- None for this issue.
