**Accomplished**

- Claimed issue #1570 for the `HexPolyFp` bounded coefficient-fold prime cancellation boundary.
- Moved the existing `powLinear_prime_coeff` proof boundary above the bounded-fold specialization so Lean can use it in declaration order.
- Replaced the `sorry` in `coeffFoldPowerCoeff_prime_coeff` with a proof that rewrites through `powLinear_coeffFold_prime_coeff_expansion`, applies the general prime coefficient boundary, and specializes with `coeffFold_coeff`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan over `HexPolyFp/SquareFree.lean`.

**Current frontier**

`powLinear_coeffFold_prime_coeff` now flows through a proved bounded-fold specialization. The broader dense-polynomial Frobenius boundary `powLinear_prime_coeff` remains an existing proof boundary and is the natural next target for removing the underlying mixed-tuple cancellation assumption.

**Next step**

Prove `powLinear_prime_coeff` or decompose it into a direct fold/add prime-coefficient theorem for finite sums of `coeffTerm`s, then use that result to remove the remaining Frobenius coefficient boundary.

**Blockers**

None.
