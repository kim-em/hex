**Accomplished**

- Claimed `#1114`.
- Added `pureClmul_oneHot_left` in `HexGF2/Clmul.lean` by assembling the landed low-word and high-word left one-hot projection theorems.
- Added public runtime `clmul_oneHot_left`, `clmul_oneHot_left_fst`, and `clmul_oneHot_left_snd` theorems.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `#1114` is implemented and ready for PR publication.
- The next HexGF2 proof-chain issues are the raw left-monomial `mulWords` coefficients and then the public `(monomial k) * q = q.mulXk k` bridge.

**Next step**

- Publish the PR for `#1114`.

**Blockers**

- `rg -n "axiom|native_decide" HexGF2 -S` reports the pre-existing `private axiom aesIrreducible` in `HexGF2/Smoke.lean`; this session did not touch that file.
