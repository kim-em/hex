**Accomplished**

- Claimed issue #1406.
- Added the peel/lower layer for `permutationVectors_complete` in
  `HexMatrix/Determinant.lean`:
  - `lowerFinLast`
  - `finLast_idxOf_lt_of_full_nodup`
  - `peelLastVector`
  - a recursive `permutationVectors_complete` wrapper
- Verified the partial state with `lake build HexMatrix`,
  `lake build HexMatrix.Conformance`, `python3 scripts/status.py HexMatrix`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a placeholder scan
  of `HexMatrix/Determinant.lean`.

**Current frontier**

- The recursive completeness wrapper is now structured around locating
  `Fin.last n` by `idxOf`, lowering all non-last entries into `Fin n`, using
  the induction hypothesis on the peeled vector, and reinserting at the
  located index.
- Three proof-body placeholders remain in the new layer:
  - `peelLastVector_castSucc_toList`
  - `peelLastVector_nodup`
  - `insertAt_peelLastVector`

**Next step**

- Prove the three peel/reinsert list lemmas, then the new
  `permutationVectors_complete` theorem will be sorry-free.

**Blockers**

- None.
