**Accomplished**

- Claimed issue #1142, identified that its SPEC-shaped normalization cannot be completed honestly without a `ZPoly` primitive square-free decomposition API, opened dependency issue #1152, added `depends-on: #1152` to #1142, and skipped #1142 as blocked.
- Claimed issue #1152.
- Added `ZPoly.toRatPoly`, rational denominator-clearing back to primitive integer representatives, `PrimitiveSquareFreeDecomposition`, `SquareFreeRat`, `primitiveSquareFreeDecomposition`, and `squareFreeCore` in `HexPolyZ/Basic.lean`.
- Added proof-facing theorem statements for primitive projection, primitive rational reification, rational reassembly, and square-free-core data flow.
- Extended `HexPolyZ/Conformance.lean` with guards for repeated factors, nontrivial content before square-free normalization, powers of `X`, already square-free inputs, and rational denominator clearing.
- Verified `lake build HexPolyZ`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` scans.

**Current frontier**

- `HexPolyZ` now exposes an executable primitive square-free normalization surface that `HexBerlekampZassenhaus.factorWithBound` can consume once #1152 lands.
- The new theorem bodies are proof-only `sorry`s, matching the existing phase convention.

**Next step**

- Re-open #1142 after #1152 merges and thread `primitiveSquareFreeDecomposition` through the `HexBerlekampZassenhaus.factorWithBound` normalization/reassembly path.

**Blockers**

- #1142 remains blocked on #1152 until this dependency PR merges.
