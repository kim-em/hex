**Accomplished**

- Claimed `#489` and added a dedicated `HexHensel/Linear.lean` module implementing the linear Hensel correction step on top of the existing `ZPoly`/`FpPoly` bridge.
- Added the Phase 1 linear-step surface required by the issue: `ZPoly.coeffwiseDiv`, `LinearLiftResult`, `ZPoly.linearHenselStep`, and theorem statements for the product, monicity, and degree-preservation invariants.
- Wired the new module into `HexHensel.lean` and updated the root library docstring to describe the bridge plus single-step lifting surface.
- Verified the result with `lake build HexHensel` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexHensel` now exposes an executable one-step linear lift from modulus `p^k` to `p^(k+1)`; the proof obligations remain scaffolded with `sorry`, which is expected for Phase 1.

**Next step**

- Publish the `#489` PR and let follow-up workers extend the library with quadratic lifting, iteration, and the mathlib-side correctness theorems.

**Blockers**

- None.
