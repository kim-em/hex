**Accomplished**

- Claimed #1244 and verified its prerequisite #1243 is closed.
- Replaced the `congr_mod_core` placeholder in `HexPoly/Euclid.lean` with a divisibility proof through a narrower helper, `mod_sub_self_eq_mul_neg_div`.
- Replaced the `mod_eq_mod_of_dvd_sub` placeholder with a proof through explicit DensePoly algebra/remainder helpers; proved the coefficientwise cancellation helper `eq_add_mul_of_sub_eq_mul`.
- Opened follow-up issues #1249 and #1250 for the two remaining lower-level helper placeholders exposed by this proof split.
- Verified `lake build HexPoly`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` sorry scan.

**Current frontier**

- #1244 is ready to publish as a proof-graph refinement: the named modular congruence foundation lemmas no longer contain direct `sorry`s.
- The remaining new leaves are `mod_sub_self_eq_mul_neg_div` and `mod_add_mul_self`, tracked by #1249 and #1250.

**Next step**

- Prove #1249 first, then #1250; after those land, #1233 can focus on the CRT Bezout congruence helpers without inheriting this lower-level modular arithmetic gap.

**Blockers**

- None for this PR.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
