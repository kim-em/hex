**Accomplished**

- Claimed #1490 and moved the `squareFree_factors_squareFree` public proof obligation into a private `squareFreeAuxRev_factors_squareFree` accumulator invariant.
- Updated `squareFree_factors_squareFree` to unfold `squareFreeDecomposition`/`squareFreeAux` and discharge the public theorem through the empty-accumulator case.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of touched `HexPolyFp` files.

**Current frontier**

- `squareFree_factors_squareFree` no longer contains a public `sorry`.
- The remaining proof work is concentrated in the private accumulator invariant, alongside the existing private pairwise-coprime invariant and weighted-product theorem placeholder.

**Next step**

- Prove `squareFreeAuxRev_factors_squareFree` by adding recursive `yunFactors` preservation lemmas for discovered factors and carrying the accumulator hypothesis through derivative-zero `pthRoot` descent.

**Blockers**

- None for this issue.
