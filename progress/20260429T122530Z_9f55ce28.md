**Accomplished**

- Claimed review issue `#1040` for `HexBerlekamp` Phase 2 scaffolding review.
- Audited `HexBerlekamp.lean`, `HexBerlekamp/Basic.lean`, `HexBerlekamp/Factor.lean`, and `HexBerlekamp/Irreducibility.lean` against `SPEC/Libraries/hex-berlekamp.md` and `PLAN/Phase2.md`.
- Found that the current executable bodies are not fake externs, trivial aliases, or data-level placeholders, but the scaffold is missing required SPEC surface.
- Filed follow-up feature issues:
  - `#1050` for the irreducibility certificate structure and checker.
  - `#1051` for the full Berlekamp factorization driver and product theorem surface.
  - `#1052` for the distinct-degree factorization API.
- Verified `lake build HexBerlekamp`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested Berlekamp audit grep.

**Current frontier**

- `HexBerlekamp` remains at `done_through: 1` because Phase 2 did not pass.
- The existing matrix/kernel, single split-step, and boolean irreducibility-test surface compiles, but downstream libraries still lack the certificate, full factorization, and distinct-degree APIs promised by the SPEC.

**Next step**

- Implement the follow-up feature issues, then rerun the Phase 2 scaffold review and only then add `status/hex-berlekamp.scaffolding-reviewed` and bump `HexBerlekamp.done_through` to `2`.

**Blockers**

- Phase 2 exit is blocked on `#1050`, `#1051`, and `#1052`.
