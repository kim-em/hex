**Accomplished**

- Claimed issue `#849` and added the `HexGF2.Basic` degree bridge theorem surface for packed `GF2Poly`.
- Added private highest-set-bit helper statements plus public `degree?`, coefficient, and monomial facts needed by downstream division work.
- Verified `lake build HexGF2.Basic`, `lake build HexGF2`, `python3 scripts/check_dag.py`, `rg -n "axiom|native_decide" HexGF2 -S`, and `git diff --check`.

**Current frontier**

- `HexGF2/Basic.lean` now exposes the intended reusable facts, but the hard UInt64/highest-bit proof bodies remain as `sorry` placeholders.
- The only `axiom` found by the requested scan remains the pre-existing private smoke-test axiom in `HexGF2/Smoke.lean`.

**Next step**

- Publish this as a partial PR for `#849` and create a follow-up issue to discharge the new `sorry` bodies in `HexGF2/Basic.lean`.

**Blockers**

- GitHub API calls began failing with an exceeded rate limit before the follow-up issue or partial PR could be created.
