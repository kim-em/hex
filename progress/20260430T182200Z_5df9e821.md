**Accomplished**

- Claimed #1393, verified `permutationVectors_transposeValues_neg_sum` was already proved at HEAD, ran `lake build HexMatrix`, and skipped #1393 as stale for planner triage.
- Claimed #1416, found the linear Hensel raw factor-congruence statement is false at `k = 0`, decomposed it into #1422 and #1423, and skipped #1416 with a decomposition breadcrumb.
- Claimed #1422 and repaired the linear one-step theorem chain in `HexHensel/Linear.lean` by adding the required `hk : 1 ≤ k` hypothesis to the raw, reduced, and public `linearHenselStep_spec` statements.
- Added `ZPoly.modP_eq_of_congr` in `HexHensel/Basic.lean` so congruent integer polynomials reduce to the same `FpPoly`.
- Ran `lake build HexHensel`, `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-pattern scans over the touched Hensel files.

**Current frontier**

- #1422 is ready for PR.
- #1423 is blocked on #1422 and should finish `linearHenselStep_raw_factor_congr` against the repaired statement.
- `modP_eq_of_congr` is available, but a future proof may still need narrower `modP` compatibility lemmas for constants, addition, multiplication, or lifted `FpPoly` products.

**Next step**

- Land #1422, then work #1423 to derive the `FpPoly` Bezout/correction identity and transport it back to the raw `ZPoly.congr` proof.

**Blockers**

- No build blockers.
