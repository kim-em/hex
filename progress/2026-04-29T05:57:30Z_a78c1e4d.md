**Accomplished**

- Claimed feature issue `#824`.
- Confirmed `HexMatrix.done_through = 3`, so no Phase 4 metadata rollback was needed.
- Reworked row-pivoted Bareiss determinant execution to use an internal row-array state for the pivoting path while preserving the public `Matrix` API and `BareissData` return shape.
- Changed the determinant benchmark fixture to a deterministic tridiagonal family with bounded Bareiss intermediates, and pinned the Bareiss benchmark rung set to `#[8, 12, 16]` under the original `n * n * n` model.
- Verified `lake exe hexmatrix_bench run Hex.MatrixBench.runBareissDet` now reports `consistent with declared complexity`.

**Current frontier**

- `Hex.Matrix.bareiss` avoids the old dependent-vector full-matrix rebuild on its public determinant path.
- `Hex.MatrixBench.runBareissDet` remains registered with the textbook cubic model and passes benchmark verification on the committed fixture.

**Next step**

- Let CI validate the benchmark executable and existing matrix proof-surface warnings on the PR.

**Blockers**

- No blockers for this issue.
