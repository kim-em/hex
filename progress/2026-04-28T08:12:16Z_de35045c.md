**Accomplished**

- Claimed issue `#584` and rewrote [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/de35045c/HexArith/ExtGcd.lean:1) so `Hex.pureIntExtGcd` now runs the Euclidean algorithm directly on `Int` with quotient/remainder updates, instead of detouring through `Nat.natAbs` and re-signing afterward.
- Removed forbidden scaffold-oriented wording from the module and `pureIntExtGcd` docstrings in that file.
- Verified with `lake build HexArith`, `lake build`, and `#eval` checks against `Hex.pureIntExtGcd` for the sign cases from the issue body.

**Current frontier**

- The `Int` reference body is now direct and builds cleanly. The sibling `HexArith` human-oversight issues about the fake extern and other scaffold vocabulary remain separate follow-up work.

**Next step**

- Commit the `HexArith.ExtGcd` rewrite and open the PR closing `#584`.

**Blockers**

- Direct interpreter evaluation of `HexArith.Int.extGcd` is not available because that API is marked `@[extern]`; the session verified the changed runtime body via `Hex.pureIntExtGcd` instead.
