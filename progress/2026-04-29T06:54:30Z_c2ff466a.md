**Accomplished**

- Claimed issue `#859`.
- Ran the HexMatrix Phase 4 benchmark path and found the original schedules did not produce usable verdicts for every registration: square multiplication was below the harness signal floor, Leibniz had too few verdict rows, and the Bareiss/Leibniz comparison had no common parameter.
- Tightened `HexMatrix/Bench.lean` benchmark schedules so square multiplication uses high-signal dimensions, Bareiss uses a larger batch budget, and Leibniz uses an explicit small-domain ladder through `n = 8` with warmup trimming.
- Verified the scientific runs: `runSquareMulChecksum`, `runBareissDet`, and `runLeibnizDet` all reported `consistent with declared complexity`; `compare runBareissDet runLeibnizDet` reported common parameter `8` and agreement.
- Bumped `libraries.yml` so `HexMatrix.done_through = 4`.

**Current frontier**

- `HexMatrix` now reports Phase 5 readiness via `python3 scripts/status.py HexMatrix`.
- Existing theorem-level `sorry` warnings remain in HexMatrix proof files; they were not part of this Phase 4 benchmark signoff.

**Next step**

- Open the PR for `#859`.

**Blockers**

- None.
