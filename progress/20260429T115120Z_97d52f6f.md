**Accomplished**

- Claimed issue `#1037` for the `HexPolyZ` Phase 4 binomial benchmark scaling follow-up.
- Fast-forwarded the branch to include the newly landed `HexPolyZ/Bench.lean` harness from `origin/main`.
- Reproduced the central-binomial benchmark at its scientific settings. On this host the original `n` model barely passed with `β=+0.147`, confirming the benchmark was threshold-fragile rather than robust.
- Updated `Hex.PolyZBench.runBinom` to declare the central-binomial fixture's compiled-`Nat` bit-growth model as `n * Nat.log2 (n + 1)`, while documenting that the executable still performs the SPEC-required `O(n)` multiplicative formula steps.
- Verified the revised `runBinom` benchmark reports `consistent with declared complexity` with `β=-0.041`.

**Current frontier**

- `HexPolyZ` remains in Phase 4 with `done_through: 3`; this session resolved the known `runBinom` scaling follow-up but did not perform the full Phase 4 scientific review.

**Next step**

- Run the full `HexPolyZ` Phase 4 review: execute every parametric registration at scientific settings and bump `libraries.yml` only if all registrations return verdict-eligible `consistent with declared complexity` results.

**Blockers**

- None for the `runBinom` follow-up.
