**Accomplished**

- Claimed human-oversight issue #1196.
- Updated `HexPolyZ/Bench.lean` so `runBinom` declares and documents the observed bit-growth-sensitive `n * log^2 n` model.
- Changed the full Mignotte-bound fixture so `factorDegree` scales as `log2 (n + 1)` instead of staying capped at 64, while documenting why the ambient norm scan keeps the registration linear.
- Added explicit worst-case derivation comments for `runGcdChecksum` and `runXGcdChecksum` in `HexPoly/Bench.lean`.
- Created follow-up issue #1202 for noisy/inconclusive Euclidean worst-case scientific verdicts found while verifying #1196.

**Current frontier**

- `runBinom` and `runMignotteCoeffBound` have reviewable local derivation comments and passed filtered smoke checks; their scientific reruns reported consistent verdicts after the updates.
- The Euclidean registrations are documented, but repeated scientific runs were not stable enough to close the performance question without the follow-up issue.

**Next step**

- A worker should take #1202 to decide whether the Euclidean worst-case benchmarks need schedule tuning, an implementation fix, or a declared-model correction under the benchmark-finding workflow.

**Blockers**

- None for #1196; the remaining Euclidean benchmark uncertainty is tracked in #1202.
