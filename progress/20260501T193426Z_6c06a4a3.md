**Accomplished**

- Claimed #1677 and added private Montgomery inverse support lemmas in
  `HexArith/Montgomery/InvNat.lean`.
- Proved the standard Newton/Hensel divisibility identity over `Int`, lifted it
  to Nat powers of two, and packaged a wrapping-subtraction form for
  `2^64 - z + 2`.
- Verified `lake build HexArith.Montgomery.InvNat`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned the touched Montgomery file for forbidden
  placeholders.

**Current frontier**

- The reusable Nat-level Newton congruence ingredient is now present and
  tailored to the UInt64 wrapping subtraction shape.
- `montPosInv_spec` and `montInv_spec` remain the only placeholders in
  `HexArith/Montgomery/InvNat.lean`.

**Next step**

- Use the new wrapped divisibility lemma with `UInt64.toNat_mul_mod_two_pow`
  and `UInt64.toNat_sub_mod_two_pow` to prove the 12→24, 24→48, and 48→64
  refinement chain, then close `montPosInv_spec`.

**Blockers**

- None.
