**Accomplished**

- Claimed #1775, assessed it as two separable proof tasks, and decomposed it into #1796 and #1797.
- Claimed #1796 and added private `projectionCombination` and `reduceAgainstBasis` reconstruction helpers in `HexGramSchmidt/Basic.lean`.
- Verified the helper layer with `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-placeholder scan of `HexGramSchmidt/Basic.lean`.

**Current frontier**

- The local projection-fold algebra for reconstructing a row from `reduceAgainstBasis` is available.
- The remaining #1797 work is the `basisRowsAux` index bridge that connects this helper to `prefixCombination` and proves `basisMatrix_reconstruction_invariant`.

**Next step**

- Work #1797 once #1796 lands: prove the generated-row/index bridge and wire `GramSchmidt.basisMatrix_reconstruction_invariant`.

**Blockers**

- None.
