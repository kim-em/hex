**Accomplished**

- Claimed `#948`.
- Replaced `GFqRing.nsmul` in `HexGfqRing/Operations.lean` with a binary double-and-add loop over the scalar bits.
- Added private proof-only linear `nsmul` specification lemmas and proved the binary implementation extensionally matches the previous successor-recursive behavior.
- Updated local `nsmul`/`zsmul` scalar-law proofs that had relied on definitional successor recursion.
- Verified `lake build HexGfqRing.Operations`, `lake build HexGfqRing`, `python3 scripts/check_dag.py`, the requested implementation/scaffold `rg` scans, and `git diff --check`.

**Current frontier**

- `HexGfqRing` quotient scalar multiplication now has the required `O(log n)` quotient-operation shape while preserving public `SMul Nat` and `SMul Int` behavior.

**Next step**

- Re-run the downstream Phase 2 review issue after this PR lands.

**Blockers**

- No blockers for this slice.
