**Accomplished**

- Claimed issue `#549` and verified that `HexMatrix` was genuinely ready for Phase 3 by checking `scripts/status.py`, the Phase 3 plan, and the current root-module surface.
- Added [HexMatrix/Conformance.lean](/home/kim/hex/worktrees/8ffb5abe/HexMatrix/Conformance.lean:1) with a current-contract core conformance docstring and elaboration-time checks covering dense matrix basics, multiplication, row operations, `det`, `bareiss`, `rref`, `spanCoeffs` / `spanContains`, and `nullspace` across typical, edge, and adversarial fixtures.
- Imported the conformance module from [HexMatrix.lean](/home/kim/hex/worktrees/8ffb5abe/HexMatrix.lean:1) and bumped `libraries.yml` so `HexMatrix.done_through = 3`.
- Verified the result with `lake build HexMatrix`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py`, which now reports `HexMatrix -> Phase 4`.

**Current frontier**

- The `HexMatrix` Phase 3 core conformance module is implemented and verified locally; the remaining step is to publish the branch as the PR for `#549`.

**Next step**

- Commit the `HexMatrix` conformance work and open the PR closing `#549`.

**Blockers**

- None.
