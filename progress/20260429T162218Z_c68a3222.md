**Accomplished**
- Claimed #1107 and added the public `GF2Poly.monomial_mul` theorem in `HexGF2/Multiply.lean`.
- Proved `monomial k * q = q.mulXk k` by coefficient extensionality using the left-oriented raw monomial multiplication coefficient lemmas.
- Verified the touched multiplication module and downstream Euclid import build.

**Current frontier**
- The public left monomial multiplication bridge needed by HexGF2 Euclid callers is now available.
- The downstream blocked issue #977 can use `monomial_mul` alongside the existing right-oriented `mul_monomial`.

**Next step**
- Claim #977 and use the monomial multiplication bridge for the quotient-step cancellation lemma.

**Blockers**
- None for #1107.
