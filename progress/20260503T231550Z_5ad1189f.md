**Accomplished**

- Claimed #1876 and audited the `HexHensel` Phase 1 surface against
  `SPEC/Libraries/hex-hensel.md` and `PLAN/Phase1.md`.
- Rewrote `HexHensel/QuadraticMultifactor.lean` docstrings to describe
  the current quadratic multifactor API and theorem contract without
  process-history wording.
- Bumped `libraries.yml` so `HexHensel.done_through: 1`.

**Current frontier**

- Verification passed:
  - `lake build HexHensel`
  - `python3 scripts/status.py HexHensel`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - targeted scan for forbidden tokens/process-history wording in touched
    files
- `scripts/status.py HexHensel` now reports `HexHensel -> Phase 2`.

**Next step**

- Pick up Phase 2 review work for `HexHensel` when planned, or continue
  with the remaining unclaimed feature proof items.

**Blockers**

- None.
