**Accomplished**

- Claimed feature issue #1293.
- Split the existing `HexGfq` root implementation into `HexGfq/Basic.lean` so the root can import both the API and conformance checks without an import cycle.
- Added `HexGfq/Conformance.lean` with deterministic core checks for the committed `C(2, 1)` generic `GFq` and packed `GF2q` surfaces.
- Added `HexGfq` to the conformance workflow matrix.
- Verified `lake build HexGfq`, `python3 scripts/status.py HexGfq`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` scan.

**Current frontier**

- `HexGfq` now has its core conformance surface wired into `lake build HexGfq` and CI.
- `libraries.yml[HexGfq].done_through` remains at `2` because the existing packed `C(2, 1)` irreducibility witness is still theorem-level `sorry` work outside this issue.

**Next step**

- A follow-up can discharge `Conway.packedGF2Entry_2_1_irreducible` and then bump `HexGfq` to Phase 3 completion.

**Blockers**

- No blocker for this session.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
