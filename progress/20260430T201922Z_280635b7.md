**Accomplished**

- Claimed #1428 and worked in `HexHensel/Linear.lean` on the two remaining
  linear Hensel raw-factor bridge leaves.
- Replaced the direct `sorry` in `modP_add_lift_mul` with a composition of
  narrower private bridge statements for `ZPoly.modP` addition and lifted
  left/right multiplication.
- Replaced the direct `sorry` in
  `linearHenselStep_product_expansion_identity_congr` with a call to a
  narrower private expansion congruence statement, and isolated the scaled
  cross-term congruence as its own private theorem.
- Verified the compiling partial state with `lake build HexHensel.Linear`,
  `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-token scans over `HexHensel/Linear.lean`.

**Current frontier**

- The original #1428 target leaves are no longer direct proof-body placeholders.
- The remaining local proof frontier has been pushed to five narrow private
  statements:
  `modP_add`, `modP_lift_mul_left`, `modP_lift_mul_right`,
  `linearHenselStep_product_expansion_cross_congr`, and
  `linearHenselStep_product_expansion_identity_congr_core`.
- These are still proof-body `sorry`s, so #1428 is only partially complete.

**Next step**

- Prove the `ZPoly.modP`/`FpPoly.liftToZ` ring-hom bridge lemmas
  coefficientwise, then prove the scaled cross-term divisibility and exact
  expansion congruence using the same coefficient-level style.

**Blockers**

- Full completion remains blocked on the Mathlib-free dense-polynomial
  coefficient proofs for `modP` add/mul compatibility and polynomial product
  expansion.
