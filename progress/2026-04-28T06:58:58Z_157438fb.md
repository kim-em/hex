**Accomplished**

- Claimed issue `#469` after releasing stale issue `#456` for replanning because its theorem surface still depended on `Matrix.independent`, which the spec assigns to `hex-lll`.
- Added [HexMatrixMathlib/Determinant.lean](/home/kim/hex/worktrees/157438fb/HexMatrixMathlib/Determinant.lean) with the Phase 1 determinant bridge theorem `HexMatrixMathlib.det_eq` and the supporting permutation-product rewrite lemmas `detProduct_eq_matrixEquiv` / `detTerm_eq_matrixEquiv`.
- Updated [HexMatrixMathlib.lean](/home/kim/hex/worktrees/157438fb/HexMatrixMathlib.lean) to export the determinant bridge module and mention the determinant comparison theorem in the library docstring.
- Verified `lake build HexMatrixMathlib` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrixMathlib` now exposes the determinant bridge slice needed by the issue; the remaining open bridge work in this library is the later proof-filling and any follow-on refinement of the rank/span/nullspace namespace split.

**Next step**

- Commit the `#469` changes, publish the branch, and open the PR closing the issue.

**Blockers**

- None.
