**Accomplished**

- Claimed #1912, assessed it as too broad for one proof step, decomposed it into #1918, #1919, and #1920, then marked #1912 for replan with the required breadcrumb.
- Claimed #1918 and added private `FpPoly` scalar/fold algebra helpers in `HexPolyFp/SquareFree.lean` near the `powLinear` proof cluster.
- Moved the existing scalar helpers earlier in the file so the upcoming binomial-sum proofs can use them before the square-free reconstruction section.

**Current frontier**

- Verification passed:
  - `lake build HexPolyFp.SquareFree`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME" HexPolyFp/SquareFree.lean`
  - `rg -n "sorry" HexPolyFp/SquareFree.lean`
- The remaining `sorry`s in `HexPolyFp/SquareFree.lean` are pre-existing; no new placeholders were introduced.

**Next step**

- Prove #1919 using the new scalar/fold helpers: define the local `powLinear` binomial term/sum and prove the Pascal-row recurrence.

**Blockers**

- None for #1919. #1920 is intentionally blocked on #1919.
