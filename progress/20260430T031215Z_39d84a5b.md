**Accomplished**

- Claimed #1255 and verified the HexPoly Phase 5 context.
- Made the self-remainder invariant explicit in `DensePoly.DivModLaws`.
- Replaced the direct `sorry` in private `mod_self_eq_zero` with the corresponding law projection.
- Updated the concrete `ZMod64` law-package instance to include the new field.
- Verified `lake build HexPoly`, `lake build HexPolyFp`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` sorry scan.

**Current frontier**

- `mod_self_eq_zero` in `HexPoly/Euclid.lean` is no longer admitted.
- The adjacent `zero_mod_eq_zero` leaf remains and is still needed by `mod_mul_self_left`.
- The concrete `ZMod64` `DivModLaws` instance still has admitted proof fields, now including `mod_self_eq_zero`.

**Next step**

- Claim #1256 to prove `zero_mod_eq_zero`, or open a narrower HexPolyFp issue later to discharge the concrete `ZMod64` division-law fields.

**Blockers**

- None.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
