**Accomplished**

- Claimed issue `#291` and added [`HexPolyMathlib/Conformance.lean`](/home/kim/hex/worktrees/17b91a53/HexPolyMathlib/Conformance.lean:1) as the first Phase 3 core-profile conformance slice for the dense-polynomial Mathlib bridge.
- Covered the existing bridge surface with committed `Rat` examples: dense/Mathlib round trips, `equiv` preserving addition and multiplication, and the `gcd` / `xgcd` correspondence theorems on typical, edge, and adversarial fixtures.
- Re-exported the conformance module from [`HexPolyMathlib.lean`](/home/kim/hex/worktrees/17b91a53/HexPolyMathlib.lean:1) so `lake build HexPolyMathlib` now elaborates the checks automatically.
- Verified the slice with `lake build HexPolyMathlib`.

**Current frontier**

- `HexPolyMathlib` now has an initial Phase 3 conformance module wired into the normal build.
- A later review can decide whether this bridge surface is sufficient for a Phase 3 completion bump or whether additional bridge declarations need committed checks first.

**Next step**

- Commit these changes and publish the PR closing `#291`.

**Blockers**

- No blocking errors in this session.
- The build still emits pre-existing `sorry` warnings from upstream `HexPoly` and `HexPolyMathlib` scaffold modules.
