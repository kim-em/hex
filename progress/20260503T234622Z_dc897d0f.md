**Accomplished**

- Claimed #1899 and added bordered-minor reindexing helpers in `HexMatrix.Basic`.
- Added determinant corollaries for the bordered-minor prefix equalities in `HexMatrix.Determinant`.
- Added Leibniz product/term reindexing helpers for permutations that fix the final row/column, splitting the determinant product into a leading-prefix product and the final entry.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Basic.lean HexMatrix/Determinant.lean`
- `lake build HexMatrix` still reports pre-existing warnings/sorries in `HexMatrix.RREF` and `HexMatrix.Bareiss`; no new placeholders were added in touched files.

**Next step**

- Use the new prefix and fixed-final-column reindexing lemmas to build the full bordered-minor determinant expansion needed for Desnanot-Jacobi.

**Blockers**

- None for this slice.
