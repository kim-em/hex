**Accomplished**

- Claimed issue #1797 (GramSchmidt basisRowsAux reconstruction index bridge)
  on branch `agent/2792bc95`.
- Proved `GramSchmidt.basisMatrix_reconstruction_invariant` in
  `HexGramSchmidt/Basic.lean`, removing one of the three remaining `sorry`
  placeholders in that file.
- Added internal helper lemmas:
  - `basisRowsAux_get!_eq_reduceAgainstBasis_take` — index bridge: the
    `basisRev.length + k`-th element of `basisRowsAux basisRev pending`
    is the reduction of `pending[k]` against the previous basis rows.
  - `basisRows_get!_eq_reduceAgainstBasis_take` — specialization to the
    public `basisRows` form.
  - `basisRows_take_pairwise` — first `k` basis rows are pairwise
    orthogonal (sublist of `basisRows_pairwise`).
  - `foldl_vec_acc_split_pointwise`, `projectionCombination_acc_split`,
    `projectionCombination_append`, `projectionCombination_singleton`,
    `projectionCombination_reverse` — algebraic infrastructure for the
    projection-combination sums.
  - `basisRows_get!_eq_reduceAgainstBasis_forward` — bridges the reverse
    reduction order produced by Gram-Schmidt to the forward order needed
    by `prefixCombination`.
  - `prefixSumByRow` and `prefixSumByRow_succ` — recursive shape of the
    row-indexed prefix combination, used to side-step the `Fin.castSucc`
    rewriting issue when peeling off the last term of a `List.finRange`
    fold.
  - `prefixCombination_eq_prefixSumByRow` and
    `prefixSumByRow_eq_projectionCombination` — the two-step bridge from
    `prefixCombination` (with its coefficient-matrix entry lookup) to
    `projectionCombination` over the take of `basisRows`.

**Current frontier**

- `HexGramSchmidt/Basic.lean` builds clean and only retains the two
  unrelated `Rat.basis_span` and `Int.basis_span` `sorry`s (out of scope
  for #1797).
- `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`,
  and `python3 scripts/check_dag.py` all pass.

**Next step**

- Push `agent/2792bc95` and create the PR closing #1797.
- Future work on #1775 / Phase 5 still needs to address `basis_span` and
  the Gram-determinant / `scaledCoeffs` / update-formula placeholders in
  HexGramSchmidt.

**Blockers**

- None.
