**Accomplished**

- Claimed repair PR #1798 and diagnosed the failing CI as stale-base fallout around explicit `DensePoly.DivModLaws` projections.
- Merged `origin/main` into the PR branch to pull in the upstream `HexPolyFp.ModCompose` repair.
- Updated `HexGfqRing.Basic` to use `DensePoly.DivModLaws.mod_add_mod` and `DensePoly.DivModLaws.mod_mul_mod` explicitly, matching the upstream fix pattern.
- Verified the previously failing downstream target set locally.

**Current frontier**

- PR #1798 now builds locally against current `origin/main`; the repair branch has one merge commit plus this narrow follow-up fix.

**Next step**

- Push the repaired PR branch and mark #1798 salvaged.

**Blockers**

- None.
