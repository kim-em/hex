**Accomplished**

- Claimed #1672 and narrowed the false Montgomery inverse proof surface in
  `HexArith/Montgomery/InvNat.lean` by removing the public Nat-level
  `newton_step` theorem, whose statement ignored `UInt64` wrapping.
- Added proved private bridge lemmas showing that `UInt64` multiplication and
  subtraction modulo `2^64` reduce correctly modulo `2^t` for `t ≤ 64`.
- Verified `lake build HexArith.Montgomery.InvNat`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned the touched file for forbidden placeholders.
- Created follow-up issue #1675 for the remaining wrapping Newton refinement
  and inverse specs.

**Current frontier**

- `HexArith/Montgomery/InvNat.lean` no longer exposes the false
  `newton_step` theorem.
- The remaining local placeholders are still `montPosInv_spec` and
  `montInv_spec`.

**Next step**

- Prove a reusable wrapping Newton-step lemma using the new bridge lemmas, then
  connect it to `montPosInv_spec` and `montInv_spec`.

**Blockers**

- None.
