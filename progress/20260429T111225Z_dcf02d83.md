**Accomplished**

- Claimed feature issue `#1006`.
- Repaired `DensePoly.derivative` in `HexPoly/Operations.lean` by replacing additive-only repeated scaling with coefficient multiplication by natural numerals.
- Verified `lake build HexPoly.Bench`.
- Verified `lake exe hexpoly_bench verify Hex.PolyBench.runDerivativeChecksum`.
- Re-ran `lake exe hexpoly_bench run Hex.PolyBench.runDerivativeChecksum`; verdict is `consistent with declared complexity` with `cMin=41.859`, `cMax=42.594`, and `β=+0.005`.
- Verified `python3 scripts/status.py HexPoly` and `git diff --check`.

**Current frontier**

- `HexPoly` remains in Phase 4 with `done_through: 3`.
- The derivative benchmark blocker from `#1006` is resolved in this branch.
- Remaining Phase 4 blockers include the eval benchmark coefficient-growth issue and the primitive-part content recomputation issue.

**Next step**

- Work the remaining HexPoly Phase 4 benchmark findings, then re-run the full Phase 4 review before bumping `HexPoly.done_through` to `4`.

**Blockers**

- None for this session's `#1006` work.
