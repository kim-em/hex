**Accomplished**

- Claimed issue `#866`.
- Added the adjacent-swap exact-division surface in `HexGramSchmidt/Update.lean`.
- Introduced named helpers for the old `d[k]` denominator, pivot `B`, the `d[k]'` numerator/quotient, and the two above-row `nu` update numerators.
- Added focused `dvd` theorem statements for exact divisibility of the `d[k]'`, `nu[i][k-1]'`, and `nu[i][k]'` adjacent-swap numerators by the old `d[k]` denominator.
- Verified with `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`, and the issue grep target.

**Current frontier**

- The theorem surface is now present for future `LLLState.swapStep` work.
- The exactness theorems are stated with `sorry`, matching the surrounding theorem-surface style in `HexGramSchmidt/Update.lean`.

**Next step**

- Open the PR for `#866`.

**Blockers**

- None.
