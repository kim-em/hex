**Accomplished**

- Claimed feature issue `#1018`.
- Extended `HexGramSchmidt/Bench.lean` with prepared update-helper fixtures and
  benchmark registrations for `sizeReduce`, `adjacentSwap`,
  `adjacentSwapDenom`, `adjacentSwapPivotCoeff`,
  `adjacentSwapGramDetNumerator`, `adjacentSwapGramDetQuotient`, and both
  adjacent-swap above-row scaled-coefficient numerator helpers.
- Updated the benchmark module docstring to describe the covered
  `HexGramSchmidt/Update.lean` surfaces.
- Verified `lake build HexGramSchmidt.Bench`.
- Verified `lake exe hexgramschmidt_bench list`.
- Verified `lake exe hexgramschmidt_bench verify`.
- Ran the eight new scientific benchmark registrations. All were inconclusive
  for schedule/harness-resolution reasons, so `libraries.yml` was left
  unchanged.
- Filed follow-up feature issue `#1030` to tune the update-helper scientific
  schedules or hot loops.
- Verified `python3 scripts/status.py HexGramSchmidt`.
- Verified `python3 scripts/check_dag.py`.
- Verified `git diff --check`.
- Verified `rg -n "native_decide" HexGramSchmidt -S` finds no matches.

**Current frontier**

- `HexGramSchmidt` remains in Phase 4 with `done_through: 3`.
- Update-helper benchmark registration coverage is present and smoke-tested,
  but the new registrations do not yet produce scientific verdicts.

**Next step**

- Work issue `#1030`: tune the row-operation hot loops and scalar-helper
  schedules so every new update-helper benchmark has enough verdict-eligible
  rows, then continue the full Phase 4 scientific review.

**Blockers**

- None for registration coverage. Phase 4 completion is blocked on scientific
  verdict tuning for the new update-helper benchmarks.
