**Accomplished**

- Claimed issue #1395.
- Tightened the `HexHensel/Linear.lean` theorem surface for linear Hensel
  invariant preservation.
- Added explicit `1 < p` hypotheses to the step monicity/degree invariants
  and to the iterative monicity wrapper.
- Added explicit computed-correction degree/survival hypotheses for the
  `g` and `h` degree preservation theorem statements.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token scan of `HexHensel/Linear.lean`.

**Current frontier**

- The stale linear invariant theorem surface no longer claims preservation
  for the degenerate `p = 1` modulus case.
- The one-step factor congruence proof remains owned by issue #1363.
- The iterative lift wrapper proof issue #1361 can now target the corrected
  invariant hypotheses.

**Next step**

- Prove the corrected `linearHenselStep_monic`,
  `linearHenselStep_g_degree?_eq`, and `linearHenselStep_h_degree?_eq`
  obligations, or proceed with #1363 before the iterative wrapper.

**Blockers**

- None.
