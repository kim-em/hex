**Accomplished**

- Claimed issue `#350` and re-reviewed `HexMatrix` against `SPEC/Libraries/hex-matrix.md` and `PLAN/Phase2.md`.
- Confirmed the live tree still contains the wrong-but-plausible scaffold bodies already tracked by `#332`, so Phase 2 attestation cannot yet be restored.
- Found an additional review blocker beyond `#332`: [`HexMatrix/Rref.lean`](/home/kim/hex/worktrees/0dab7e18/HexMatrix/Rref.lean:43) defines `IsEchelonForm.freeCols` as the first `m - rank` columns rather than the sorted complement of `pivotCols`, and the executable nullspace path in [`HexMatrix/Nullspace.lean`](/home/kim/hex/worktrees/0dab7e18/HexMatrix/Nullspace.lean:46) builds on that wrong surface.
- Opened follow-up issue `#361` to rewrite the free-column/nullspace scaffolds as honest placeholders, and commented the review outcome on `#350`.
- Verified `lake build HexMatrix` succeeds and confirmed `libraries.yml` still leaves `HexMatrix.done_through = 1` with no `status/hex-matrix.scaffolding-reviewed` token restored.

**Current frontier**

- `HexMatrix` Phase 2 remains blocked on `#332` and `#361`. Once both land, the library needs a fresh review pass before the token and `done_through = 2` can be restored.

**Next step**

- Execute `#332` and `#361`, then re-run the `HexMatrix` Phase 2 review.

**Blockers**

- `python3 scripts/check_dag.py` currently fails outside this review scope on `HexBerlekamp/Rabin.lean:2` importing `HexPoly.Gcd` across the declared library boundary.
