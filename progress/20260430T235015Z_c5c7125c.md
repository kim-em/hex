**Accomplished**

- Claimed #1473 and replaced the `sorry` in
  `addModSquare_monic_of_high_remainder_zero` in
  `HexHensel/Quadratic.lean`.
- Added a narrow private normalized-polynomial helper,
  `monic_of_coeff_eq_one_and_high_coeff_zero`, used to conclude monicity
  from a top coefficient equal to `1` and zero coefficients above it.
- Proved that `addModSquare g r m` preserves monicity when `g` is monic
  and `r` has zero coefficients at and above `g.size - 1`.
- Verified `lake build HexHensel.Quadratic`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scans of `HexHensel/Quadratic.lean` for
  `axiom`, `native_decide`, `TODO`, and `FIXME`.

**Current frontier**

- The representation-level quadratic g-update monicity leaf from #1473 is
  closed.
- `HexHensel/Quadratic.lean` still has the separate divmod support-bound
  proof placeholder
  `divModMonicModSquare_remainder_coeff_eq_zero_of_monic` from #1472.

**Next step**

- Work #1472 to prove the modular monic divmod remainder support bound,
  which should complete the split g-update monicity chain.

**Blockers**

- None for #1473.
