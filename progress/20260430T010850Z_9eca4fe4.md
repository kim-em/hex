**Accomplished**

- Claimed issue #1187 for the HexBerlekampZassenhaus nested Rabin certificate gap.
- Extended `PrimeFactorData` with concrete modular `factorPolys`.
- Updated the per-prime checker so each aligned `(degree, factor, certificate)` entry checks factor degree, monicity, and `Berlekamp.checkIrreducibilityCertificate` on the concrete modular factor.
- Required the recorded modular factor product to equal `ZPoly.modP p f`.
- Added `HexBerlekampZassenhaus.Conformance` guards showing a malformed nested Rabin certificate is rejected even when prime and degree metadata match.

**Current frontier**

- The executable Z[x] irreducibility certificate checker now validates nested Rabin certificates against concrete modular factors instead of metadata alone.
- `HexBerlekampZassenhaus` remains at Phase 2; this PR fixes the blocker found by #1170/#1187 but does not add the Phase 2 review token or bump `libraries.yml`.

**Next step**

- Re-run the HexBerlekampZassenhaus Phase 2 scaffolding review and, if no new blockers are found, add the review token and advance `HexBerlekampZassenhaus.done_through` to 2.

**Blockers**

- None for this issue.
