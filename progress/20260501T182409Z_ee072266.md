**Accomplished**

- Claimed #1661 and added the symmetric right-input raw-product expansion layer in
  `HexGF2/Multiply.lean`.
- Proved private helpers for right-linearity at the coefficient level:
  `clmul_xor_right_snd_bit`, `clmul_xor_right_fst_bit`,
  `clmulCoeffAt_zero_right`, `clmulCoeffAt_xor_right`,
  `clmulCoeffAt_xorWordList_right`, and
  `clmulCoeffAt_mulWords_right_contrib`.
- Decomposed the residual associativity bridge into #1667 and #1668, with the
  required breadcrumb on #1661.
- Verified `lake build HexGF2.Multiply`, `lake build HexGF2`,
  `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned `HexGF2/Multiply.lean` for forbidden
  placeholders.

**Current frontier**

- `GF2Poly.mul_assoc` still has the pre-existing public proof placeholder.
- The raw associativity bridge now has both left- and right-intermediate
  expansion helpers available. The next missing layer is packaging each
  associated raw product into source-triple XOR expansions.

**Next step**

- Work #1667: define/package the source-triple expansion forms for
  `coeffWords (mulWords (mulWords xs ys) zs) n` and
  `coeffWords (mulWords xs (mulWords ys zs)) n`.

**Blockers**

- None.
