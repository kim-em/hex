**Accomplished**

- Claimed issue `#909` and added a `HexArith.powMod` Phase 4 benchmark registration in `HexArith/Bench.lean`.
- Added deterministic prepared `PowModInput` values over the fixed odd word-sized modulus `65537`, parameterized by exponent bit length, with an `O(n)` repeated-squaring model.
- Updated the bench module docstring so the scientific registration list includes `runPowMod`.
- Verified `lake build HexArith`, `lake build HexArith.Bench`, `lake exe hexarith_bench list`, `lake exe hexarith_bench verify`, `python3 scripts/check_dag.py`, `git diff --check`, and confirmed `rg -n "native_decide|axiom" HexArith -S` has no matches.

**Current frontier**

- `HexArith.Bench` now lists six registered benchmarks, including `Hex.ArithBench.runPowMod`.
- This slice intentionally does not bump `libraries.yml[HexArith].done_through`; broader Phase 4 completion coverage remains out of scope.

**Next step**

- Continue reviewing the remaining HexArith Phase 4 surface before deciding whether the library can advance through Phase 4.

**Blockers**

- None for this issue.
