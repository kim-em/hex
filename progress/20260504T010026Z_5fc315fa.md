**Accomplished**

- Claimed #1946 and completed the Rat monic/general division agreement slice.
- Added `DensePoly.divModArray_eq_zero_self_of_degree_lt` in `HexPoly.Euclid` for the array-backed early degree shortcut.
- Proved `ratDivModLaws.divModMonic_eq_divMod_of_monic` in `HexPolyZ.Basic`.
- Decomposed the remaining Rat reconstruction work into #1954 and left the required breadcrumb on #1946.

**Current frontier**

- Verification passed:
  - `lake build HexPolyZ.Basic`
  - `lake build HexPolyZ`
  - `python3 scripts/status.py HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check HEAD`
- `ratDivModLaws.divMod_spec` remains open and is now tracked by #1954.
- Existing unrelated `sorry`s remain in Rat mod laws, Rat gcd laws, and square-free-core proof code.

**Next step**

- Prove the array-backed `divModArrayAux` reconstruction invariant needed for `ratDivMod_spec_core`, then close `ratDivModLaws.divMod_spec`.

**Blockers**

- The reconstruction proof needs a quotient-array invariant showing each `set!` writes the next unused quotient slot, so that the array update corresponds to polynomial addition by the long-division term.
