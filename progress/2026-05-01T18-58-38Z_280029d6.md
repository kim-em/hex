**Accomplished**

- Claimed #1642, confirmed the old `newton_step` surface is too broad for
  one session, and decomposed it into #1672 and #1673.
- Claimed #1672 and added proved private `UInt64` refinement lemmas for the
  executable wrapping Montgomery inverse step from 3 to 6 bits and from 6 to
  12 bits in `HexArith/Montgomery/InvNat.lean`.
- Verified `lake build HexArith.Montgomery.InvNat`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned the touched file for forbidden tokens.

**Current frontier**

- `montPosInvStep_mod_3_to_6` and `montPosInvStep_mod_6_to_12` are closed.
- The higher refinements still need Nat modular algebra rather than direct
  `bv_decide`; exploratory attempts at 12-to-24 and beyond timed out.
- The original placeholders for `newton_step`, `montPosInv_spec`, and
  `montInv_spec` remain.

**Next step**

- Prove the general wrapping/modular Newton step for `montPosInvStep`, making
  the 64-bit word boundary explicit, then use it to close #1673.

**Blockers**

- None.
