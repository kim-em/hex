**Accomplished**

- Claimed #1345 and replaced the `ZPoly.coeff_coeffwiseDiv` proof placeholder in
  `HexHensel/Linear.lean` with a direct `DensePoly.ofCoeffs`/`List.range`
  coefficient proof.
- Added a local `list_getD_map_range` helper in `HexHensel/Linear.lean` because
  the analogous helper in `HexHensel/Basic.lean` is private to that module.
- Decomposed the remaining #1345 proof surface into #1360 for the one-step
  linear Hensel invariants and #1361 for the iterative wrappers, with #1361
  depending on #1360.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans on the touched
  `HexHensel/Linear.lean` file.

**Current frontier**

- `ZPoly.coeff_coeffwiseDiv` is proved.
- `HexHensel/Linear.lean` still has proof-body placeholders for the one-step
  correction invariants and iterative `henselLift` wrappers.

**Next step**

- Work #1360 to prove `linearHenselStep_spec`, monicity preservation, and the
  degree preservation lemmas before attempting the wrapper issue #1361.

**Blockers**

- None for the coefficient lemma. The remaining linear Hensel proof surface
  needs dedicated degree/remainder and reduction-preservation helpers.
