**Accomplished**

- Claimed `#1105`, confirmed the direct `bv_decide` route still fails on the folded `pureClmul` definition, and decomposed the issue into `#1109` and `#1110`.
- Claimed `#1109`.
- Added private `HexGF2/Clmul.lean` helper lemmas for left one-hot accumulator contributions:
  low-word contribution when `hot + bitIdx < 64`, high-word contribution when `64 <= hot + bitIdx`, and the supporting one-hot shift-left/shift-right facts.
- Verified `lake build HexGF2.Clmul`, `python3 scripts/check_dag.py`, and `git diff --check`.
- Ran `rg -n "axiom|native_decide" HexGF2 -S`; the only hit is the pre-existing `HexGF2/Smoke.lean` private axiom.

**Current frontier**

- `#1109` is complete and ready to publish.
- `#1110` is blocked on `#1109` and should use the new accumulator helpers to prove the fold invariant and public left one-hot split/projection lemmas.

**Next step**

- Publish the `#1109` PR, then the next worker should take `#1110`.

**Blockers**

- None for `#1109`.
