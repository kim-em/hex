**Accomplished**

- Claimed `#964`.
- Added `HexGramSchmidt/Bench.lean` with `lean-bench` registrations for deterministic integer `gramDetVec` and `scaledCoeffs` checksum targets.
- Registered the `hexgramschmidt_bench` Lake executable.
- Added CI smoke commands for `lake exe hexgramschmidt_bench list` and `lake exe hexgramschmidt_bench verify`.
- Verified `lake build HexGramSchmidt.Bench`, `lake exe hexgramschmidt_bench list`, `lake exe hexgramschmidt_bench verify`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested benchmark-registration scan.

**Current frontier**

- `HexGramSchmidt` now has its first Phase 4 benchmark slice for the executable integer determinant surface.
- Row-operation update-helper benchmarks remain intentionally outside this slice.

**Next step**

- Review the new HexGramSchmidt benchmark coverage against the full Phase 4 exit criteria before any `libraries.yml` phase bump.

**Blockers**

- No blockers for the benchmark harness slice.
