**Accomplished**

- Claimed #1562 and added a local `HexPolyFp/SquareFree.lean` coefficient expansion for powers of `coeffFold`.
- Introduced `coeffFoldPowerCoeff`, proved `powLinear_coeffFold_coeff_expansion`, and added the named `p`-exponent specialization `powLinear_coeffFold_prime_coeff_expansion`.
- Kept the change scoped to proof helpers; no executable square-free definitions or phase counters were changed.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and a touched-file forbidden-placeholder scan.

**Current frontier**

- #1562 is complete and ready for PR.
- The new expansion gives follow-up cancellation work a stable finite-fold coefficient surface for `(powLinear (coeffFold g m) p).coeff n`.

**Next step**

- Continue with #1563, the prime mixed-term cancellation over the expansion, then #1564 for the two-summand Frobenius additivity integration.

**Blockers**

- None for this session.
