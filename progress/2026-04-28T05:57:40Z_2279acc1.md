**Accomplished**

- Claimed issue `#436` and implemented the first `HexMatrix` Phase 1 scaffold in [HexMatrix/Basic.lean](/home/kim/hex/worktrees/2279acc1/HexMatrix/Basic.lean).
- Added the dense `Hex.Matrix` representation as `Vector (Vector R m) n`, with entry construction, row/column accessors, transpose, zero, and identity helpers.
- Added executable dot product, matrix-vector multiplication, matrix-matrix multiplication, and `Int`/`Rat` norm-squared wrappers.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/2279acc1/HexMatrix.lean) to export the new module and verified `lake build HexMatrix` plus `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrix` now has the dense core needed for later row operations and row-reduction work, but those later APIs from the spec are still unimplemented.

**Next step**

- Publish the issue `#436` PR, then follow up with row-operation and echelon/RREF scaffolding issues against this new core surface.

**Blockers**

- None.
