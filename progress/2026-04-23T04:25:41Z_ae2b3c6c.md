**Accomplished**

- Claimed feature issue `#152` and verified the nullspace-bridge gap against `SPEC/Libraries/hex-matrix-mathlib.md`, `PLAN/Phase1.md`, `HexMatrix/Nullspace.lean`, and the existing `HexMatrixMathlib` bridge modules.
- Added `HexMatrixMathlib/Nullspace.lean` with the Phase 1 theorem scaffold `nullspace_span_eq_ker`, relating the span of Hex's computed nullspace basis vectors to `LinearMap.ker (Matrix.mulVecLin ...)`.
- Added `vectorEquiv` to `HexMatrixMathlib/MatrixEquiv.lean` so dense Hex vectors can be transported into Mathlib's `Fin n → R` representation alongside `matrixEquiv`.
- Updated `HexMatrixMathlib.lean` to re-export the new nullspace bridge module and mention the nullspace/kernel correspondence in the root docstring.
- Ran `lake build HexMatrixMathlib.Nullspace` and `lake build HexMatrixMathlib` successfully.

**Current frontier**

- `HexMatrixMathlib` now exposes the nullspace/kernel bridge theorem surface requested by issue `#152`; the remaining work on this slice is proof completion and any later span-membership bridge follow-up tracked separately.

**Next step**

- Publish the `#152` scaffold as a PR so downstream matrix-mathlib work can depend on the new nullspace bridge declarations.

**Blockers**

- None.
