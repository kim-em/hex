**Accomplished**

- Claimed #1767 for the Gram-Schmidt dot-product linearity helper slice.
- Added Rat-specific `Vector.dotProduct` fold normalization over
  `List.finRange` in `HexMatrix/Basic.lean`.
- Exposed `Matrix.dot_sub_smul_rat` and zero-specialized wrappers for
  subtracting a scalar multiple in the left dot argument.
- Added private Gram-Schmidt-facing lemmas for `subtractProjection` dot
  rewrites and zero-orthogonal specializations.
- Verified `lake build HexMatrix.Basic`, `lake build HexGramSchmidt.Basic`,
  `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`,
  `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token
  scans on touched Lean files.

**Current frontier**

- #1767's helper surface is available for downstream Gram-Schmidt projection
  preservation work.
- `GramSchmidt.Rat.basis_decomposition` and the larger reconstruction
  invariant remain out of scope for this slice.

**Next step**

- Work #1768: prove the Gram-Schmidt projection preservation invariant using
  the new `Matrix.dot_sub_smul_rat` and `dot_subtractProjection` helper
  surface.

**Blockers**

- None.
