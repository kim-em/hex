**Accomplished**

- Claimed and completed issue `#283` by adding [`HexGfqField/Core.lean`](/home/kim/hex/worktrees/4b3c0fd5/HexGfqField/Core.lean:1), which introduces the Phase 1 `FiniteField p f hf hirr` carrier as a thin wrapper over `HexGfqRing.PolyQuotient p f hf`.
- Added the immediate conversion surface and theorem layer needed by downstream field-operation issues: a local irreducibility scaffold, quotient round trips, `ofPoly`, `repr`, and extensionality/injectivity lemmas.
- Updated [`HexGfqField.lean`](/home/kim/hex/worktrees/4b3c0fd5/HexGfqField.lean:1) to re-export the new carrier module.
- Verified the slice with `lake build HexGfqField` and `python3 scripts/status.py`; `HexGfqField` remains on the active Phase 1 frontier as expected after this narrow carrier-only issue.

**Current frontier**

- `HexGfqField` now has the shared carrier/conversion boundary promised by the spec, but still lacks field operations such as inverse, division, exponentiation, Frobenius, and any field/typeclass law surface.
- The downstream blocked libraries (`HexGF2Mathlib`, eventual `HexGfq`) still depend on finishing the remaining `HexGfqField` Phase 1 slices and then bumping `libraries.yml`.

**Next step**

- Publish the branch as the PR for `#283`, then let the queue continue onto the next `HexGfqField`, `HexHensel`, `HexLLL`, or benchmark-scaffolding issue.

**Blockers**

- None in this session.
