**Accomplished**

- Claimed feature issue `#1055`.
- Rebased onto `origin/main` after the merged `#1059` pow benchmark fix so the session included the current HexModArith Phase 4 baseline.
- Tuned `HexModArith/Bench.lean` for the residual non-pow blockers:
  - changed `runAddChecksum` to use local per-sample hot-loop amplification and a mid-range scientific schedule,
  - reduced `runMulChecksum` hot-loop amplification and schedule so it has stable verdict-eligible rows without hitting the cap.
- Verified all HexModArith scientific registrations individually or in the full verdict sweep; the final explicit reruns for the previously problematic cases reported consistent verdicts.
- Bumped `libraries.yml` so `HexModArith.done_through: 4`.
- Verified:
  - `lake build HexModArith.Bench`
  - `lake exe hexmodarith_bench verify`
  - `lake exe hexmodarith_bench run Hex.ModArithBench.runAddChecksum`
  - `lake exe hexmodarith_bench run Hex.ModArithBench.runMulChecksum`
  - `lake exe hexmodarith_bench run Hex.ModArithBench.runPow`
  - `lake exe hexmodarith_bench run Hex.ModArithBench.runInvChecksum`
  - full verdict sweep over all 11 HexModArith registrations
  - `python3 scripts/status.py HexModArith`
  - `python3 scripts/status.py`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "native_decide" HexModArith -S`

**Current frontier**

- `HexModArith` now reports Phase 5 ready in `scripts/status.py`.
- The worktree still has a pre-existing unstaged `.claude/CLAUDE.md` change that this session did not create and should not include in the PR.

**Next step**

- Open the PR for `#1055` with the benchmark tuning, `libraries.yml` bump, and this progress file.

**Blockers**

- None.
