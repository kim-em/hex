**Accomplished**

- Claimed issue #1578 for the non-circular `powLinear_add_prime_coeff` bridge.
- Refactored `HexPolyFp/SquareFree.lean` so `powLinear_add_prime_coeff` no longer rewrites through `powLinear_prime_coeff`.
- Moved the remaining prime-characteristic finite-convolution cancellation boundary to `coeffFoldPowerCoeff_prime_coeff`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over `HexPolyFp/SquareFree.lean`.

**Current frontier**

Issue #1578 is complete. The `powLinear_add_prime_coeff` theorem now depends on the bounded coefficient-fold Frobenius coefficient theorem instead of the general `powLinear_prime_coeff` theorem. The hard finite coefficient-fold cancellation statement remains as the proof-body `sorry` at `coeffFoldPowerCoeff_prime_coeff`.

**Next step**

Use the finite coefficient-fold cancellation boundary to finish the upstream Frobenius coefficient proof tracked by #1556.

**Blockers**

None.
