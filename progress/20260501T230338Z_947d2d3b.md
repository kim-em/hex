**Accomplished**

- Claimed issue #1711 for the HexArith prime-power modulo cancellation follow-up.
- Proved `add_pow_prime_mod_of_choose_dvd` in `HexArith/Nat/Prime.lean`.
- Added private helpers showing middle binomial terms divide their full
  `chooseTerm`, reduce to zero modulo `p`, and leave the prefix sum congruent
  to `a ^ p`.
- Preserved the public `add_pow_prime_mod` and `pow_prime_mod` wrappers.
- Verified `lake build HexArith.Nat.Prime`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token scan on
  `HexArith/Nat/Prime.lean`.

**Current frontier**

- The targeted HexArith placeholder for `add_pow_prime_mod_of_choose_dvd` is
  closed.
- `HexArith` remains Phase 5 with `done_through: 4`; this turn did not evaluate
  every Phase 5 exit criterion, so `libraries.yml` was not changed.

**Next step**

- Continue with the remaining unclaimed Phase 5 issues outside HexArith, such
  as the HexGF2 associativity bridge or HexPolyFp Yun successor-fuel algebra.

**Blockers**

- None.
