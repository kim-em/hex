**Accomplished**

- Claimed #1570 after confirming the older #1538 path still depends on blocked #1556.
- Added a private `coeffFoldPowerCoeff_prime_coeff` boundary in `HexPolyFp/SquareFree.lean` that states the exact prime mixed-tuple cancellation target for the recursive coefficient expansion.
- Added proved case wrappers for the divisible and non-divisible coefficient indices, plus a `powLinear_coeffFold_prime_coeff` wrapper in the downstream coefficient form.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token scan over touched HexPolyFp files.

**Current frontier**

- The cancellation shape is now explicit at the `coeffFoldPowerCoeff` boundary and exposed in the `(powLinear (coeffFold g m) p).coeff n` form needed by the later Frobenius coefficient integration.
- The central proof of `coeffFoldPowerCoeff_prime_coeff` is still a proof-body `sorry`; #1570 is therefore only partially advanced.

**Next step**

- Prove `coeffFoldPowerCoeff_prime_coeff` by connecting the recursive convolution expansion to the finite-index prime-characteristic cancellation helpers added by #1563, without using the downstream `powLinear_add_prime_coeff` theorem.

**Blockers**

- No external blocker. The remaining work is the internal combinatorial cancellation proof over the recursive coefficient expansion.
