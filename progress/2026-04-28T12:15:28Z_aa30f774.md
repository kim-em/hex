**Accomplished**

- Claimed human-oversight issue `#728` and updated `HexArith/Montgomery/InvNat.lean` so `montPosInv` now performs five Newton refinement steps instead of six.
- Revised the Montgomery inverse docstring to match the spec's `2^3 -> 2^96 >= 2^64` refinement argument.
- Verified `lake build HexArith` and `python3 scripts/check_dag.py`.
- Ran a Lean smoke script comparing the new five-step `montInv` against the previous six-step chain for `p = 2^31 - 1` and `p = 2^63 + 29`; both produced identical `p'` values and satisfied `p * p' ≡ -1 (mod 2^64)`.

**Current frontier**

- The implementation for `#728` is complete and ready to publish.

**Next step**

- Commit `HexArith/Montgomery/InvNat.lean` with this progress note and run `coordination create-pr 728`.

**Blockers**

- None for this issue.
