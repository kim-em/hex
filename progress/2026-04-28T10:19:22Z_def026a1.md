**Accomplished**

- Claimed `#662` and implemented the missing `HexLLL` Phase 1 update layer in [HexLLL/Basic.lean](/home/kim/hex/worktrees/def026a1/HexLLL/Basic.lean).
- Added executable `LLLState.sizeReduceColumn`, `LLLState.sizeReduce`, and `LLLState.swapStep` definitions using the integer-state formulas from `SPEC/Libraries/hex-lll.md`.
- Added the immediate theorem surface for the new update layer (`sizeReduce_d`, `sizeReduce_basis`, `swapStep_memLattice_iff`, `swapStep_ν_eq`, `swapStep_d_eq`) and updated the top-level [HexLLL.lean](/home/kim/hex/worktrees/def026a1/HexLLL.lean) docstring to advertise the new API.
- Bumped `libraries.yml` so `HexLLL.done_through = 1`.
- Verified with `lake build HexLLL`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py`.

**Current frontier**

- `HexLLL` now reaches Phase 1 in the project status view, and `scripts/status.py` reports `HexLLL -> Phase 2 (scaffolding review)` as ready.
- The next newly unblocked downstream library is `HexLLLMathlib -> Phase 1`.

**Next step**

- Commit the `HexLLL` changes and this progress note.
- Publish the work for `#662` with `coordination create-pr 662 "feat: add HexLLL size-reduction and swap scaffolding"`.

**Blockers**

- The worktree already contained an unrelated tracked modification to `.claude/CLAUDE.md`; it was left untouched and is not part of this issue's changes.
