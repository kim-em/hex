**Accomplished**

- Claimed `#638` and verified the remaining Phase 1 gap in `HexArith` was the missing spec-level `BarrettCtx.mk` constructor.
- Updated `HexArith/Barrett/Reduce.lean` so `BarrettCtx` now uses the internal constructor name `mkCtx` and exposes the public `BarrettCtx.mk (p) hp hlt` wrapper promised by the spec, computing `pinv` as `UInt64.ofNat (barrettRadix / p.toNat)`.
- Verified the change with `lake build HexArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- Issue `#638` is implemented and ready to publish as a narrow API completion for the Barrett context surface.
- The remaining work is procedural: commit the Lean change and open the PR that closes `#638`.

**Next step**

- Create the commit including `HexArith/Barrett/Reduce.lean` and this progress file, then run `coordination create-pr 638` to publish the fix.

**Blockers**

- None for this issue.
