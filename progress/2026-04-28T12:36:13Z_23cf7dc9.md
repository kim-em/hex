**Accomplished**

- Claimed issue `#766` and added the first `HexConway` module slice in `HexConway/Basic.lean`, including the Tier 1 `luebeckConwayPolynomial?` lookup, the committed `(p, n) = (2, 1)` entry `X + 1`, and lookup-hit / miss lemmas for that initial table slice.
- Wired the new module through `HexConway.lean` with a library docstring describing the current Tier 1 scope.
- Moved `FpPoly.Irreducible` from `HexGfqField/Basic.lean` down to `HexPolyFp/Basic.lean` so `HexConway` can state irreducibility witnesses without introducing a downstream dependency on `HexGfqField`.
- Verified `lake build HexConway` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexConway` now has the baseline imported-table API shape and one committed entry, but the entry's irreducibility theorem is still a proof-level `sorry`; the current executable checker path is not yet set up for direct interpreter-time proof reduction against the extern-backed `ZMod64.mul`.

**Next step**

- Commit these `HexConway` / `HexPolyFp` layering changes and publish the PR for `#766`.

**Blockers**

- None for landing this Phase 1 scaffold slice; richer checker-backed imported-entry proofs remain follow-up work.
