**Accomplished**

- Claimed issue `#592` and confirmed the current live gap was the fake `HexArith/ffi/mpz_gcdext.c` trampoline rather than missing source files.
- Replaced the boxed-Lean trampoline in `HexArith/ffi/mpz_gcdext.c` with a real GMP-backed `mpz_gcdext` bridge, including Lean `Int`/`Nat` marshaling and a C-side fallback path for non-GMP builds.
- Verified `lake build HexArith` succeeds, `python3 scripts/check_dag.py` stays clean, `nm -g .lake/build/lib/libhexarithffi.a` shows `lean_hex_mpz_gcdext`, and `grep -RIn 'l_Hex_.*___boxed' HexArith/ffi/*.c` is empty.
- Confirmed the new extern runs correctly via explicit plugin loading (`lake env lean --load-dynlib=.lake/build/lib/lean/Hex_HexArith_ExtGcd.so /tmp/hex_extgcd_only.lean`).
- Opened follow-up issue `#616` for the remaining plain-`lake env lean` plugin autoload failure affecting `HexArith` externs.

**Current frontier**

- The GMP bridge itself is implemented and verified.
- Plain `lake env lean <file>` still does not auto-load `HexArith` module plugins, so the combined runtime smoke test from the issue body is still blocked by loader/configuration behavior outside `mpz_gcdext.c`.

**Next step**

- Commit the `mpz_gcdext.c` bridge change, create a partial PR for `#592`, and link follow-up issue `#616` as the residual runtime-loading work.

**Blockers**

- `lake env lean` still reports missing native implementations for `UInt64.mulHi`, `UInt64.addCarry`, and `HexArith.Int.extGcd` unless the relevant module `.so` files are loaded explicitly.
