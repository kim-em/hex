**Accomplished**

- Claimed #1920 and implemented the private full binomial expansion theorem for `powLinear (f + g) n` in `HexPolyFp/SquareFree.lean`.
- Added local support lemmas proving above-row binomial terms vanish and the completed row sum is stable past the top index.
- Verified the new proof with `lake build HexPolyFp.SquareFree`, `git diff --check -- HexPolyFp/SquareFree.lean`, and a zero-context diff scan for forbidden placeholders.

**Current frontier**

- `powLinear_add_binom_sum` now rewrites `powLinear (f + g) n` to `powLinearBinomSum f g n (n + 1)`, the finite sum over `k ≤ n` using `Hex.Nat.choose`.
- The module still reports pre-existing `sorry` warnings in `HexPolyFp.Basic` and later `HexPolyFp/SquareFree.lean` declarations outside this proof cluster.

**Next step**

- Use the new private theorem in the follow-on `powLinear_add_prime_coeff`/`coeffFoldPowerCoeff_prime_coeff` work item if it replaces the existing coefficient-fold argument cleanly.

**Blockers**

- None for #1920.
