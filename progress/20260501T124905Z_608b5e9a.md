**Accomplished**

- Claimed issue #1606 for the HexPolyFp Yun loop-state product invariant.
- Added `yunFactorsContribution_loop_state_product_invariant` in
  `HexPolyFp/SquareFree.lean` as the stronger private theorem surface over
  arbitrary `(c, w, multiplicity, fuel)` Yun loop states.
- Rewired `yunFactorsContribution_reconstruct_core` to consume the loop-state
  invariant and specialize it back to the initial `f / gcd f f'` state via
  `div_gcd_mul_reconstruct`.
- Verified `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the banned-token scan
  for `HexPolyFp/SquareFree.lean`.

**Current frontier**

- The reconstruction core now has the requested stronger induction target.
- The remaining proof-body placeholder has moved from
  `yunFactorsContribution_reconstruct_core` to the general loop-state invariant.
- `HexPolyFp` remains in Phase 5 with existing proof-body `sorry`s.

**Next step**

- Prove `yunFactorsContribution_loop_state_product_invariant` by fuel induction,
  using the named terminal, split, step, and repeated-tail descent surfaces.

**Blockers**

- None.
