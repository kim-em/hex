**Accomplished**

- Claimed #1236 for HexArith Phase 5 prime/binomial proof work.
- Proved the local `Prime.dvd_mul` using the existing mathlib-free `Prime`
  predicate plus Lean core `Nat.gcd`/`Nat.Coprime` facts.
- Replaced the direct `choose_prime_dvd` admission with a proof through a
  narrower Pascal-row multiplication identity helper and a local
  non-divisibility bound for `0 < k < p`.
- Replaced the direct `add_pow_prime_mod` and `pow_prime_mod` admissions with
  narrower private helper lemmas that isolate the missing binomial expansion
  and induction steps.
- Verified `lake build HexArith`, `python3 scripts/status.py HexArith`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested
  focused `rg` sorry scan.

**Current frontier**

- `HexArith/Nat/Prime.lean` now has three private proof obligations:
  `choose_succ_mul_eq`, `add_pow_prime_mod_of_choose_dvd`, and
  `pow_prime_mod_from_add_pow`.
- The public theorem statements are wired through those helpers, so the
  remaining work is closer to the local combinatorial foundations rather than
  sitting at the exported API surface.

**Next step**

- Prove `choose_succ_mul_eq` from the Pascal recursion, then discharge
  `add_pow_prime_mod_of_choose_dvd` via a local binomial expansion lemma and
  `pow_prime_mod_from_add_pow` by induction on `a`.

**Blockers**

- No build blockers.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
