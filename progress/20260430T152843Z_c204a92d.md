**Accomplished**

- Claimed issue #1396.
- Tightened the `HexHensel/Quadratic.lean` theorem surface so the raw factor
  congruence requires `0 < m`, while raw Bezout, monic update, combined spec,
  and monic wrapper require `1 < m`.
- Preserved the executable quadratic Hensel definitions.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-token scan of
  `HexHensel/Quadratic.lean`.

**Current frontier**

- The false `m = 0` and `m = 1` theorem surfaces are repaired.
- The underlying proof bodies in `HexHensel/Quadratic.lean` remain existing
  Phase 5 `sorry`s for successor proof issues.

**Next step**

- Successor workers can prove the raw factor, raw Bezout, and monic-update
  obligations against the corrected modulus hypotheses.

**Blockers**

- None.
