**Accomplished**

- Claimed feature issue `#160` and added [HexMatrixMathlib/Span.lean](/home/kim/hex/worktrees/8dd5741c/HexMatrixMathlib/Span.lean) to scaffold the Mathlib row-span bridge for dense Hex matrices.
- Introduced `HexMatrixMathlib.rowSpan` together with the theorem declarations `mem_rowSpan_of_spanCoeffs_eq_some` and `spanContains_iff_mem_rowSpan`, connecting Hex's executable span witness surface to `Submodule.span` over transported matrix rows.
- Updated [HexMatrixMathlib.lean](/home/kim/hex/worktrees/8dd5741c/HexMatrixMathlib.lean) to re-export the new span module and mention the row-span bridge in the root docstring.
- Verified the slice with `lake build HexMatrixMathlib`.

**Current frontier**

- `HexMatrixMathlib` now covers determinant, rank, nullspace, row operations, matrix equivalence, and row-span membership at the Phase 1 theorem-statement layer.
- The remaining work in this library is proof completion for the scaffolded bridge theorems and any later completeness review/planner bookkeeping outside this issue's scope.

**Next step**

- Land the PR for `#160` so downstream work can rely on the new `rowSpan` definition and span-membership bridge statements when extending Mathlib-facing matrix reasoning.

**Blockers**

- None locally; `lake build HexMatrixMathlib` completed successfully, with only the expected scaffold `sorry` warnings in this library and its dependencies.
