**Accomplished**

- Claimed issue `#8` and added [HexPoly/Arithmetic.lean](/home/kim/hex/worktrees/1578d5c4/HexPoly/Arithmetic.lean:1) with Phase 1 dense polynomial arithmetic scaffolding: raw coefficient-array combinators, public `DensePoly.add` / `sub` / `mul`, and the corresponding typeclass instances.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/1578d5c4/HexPoly.lean:1) to re-export the arithmetic module while preserving the root-module docstring.
- Verified this slice with `lake build HexPoly`; the build succeeds, with only the pre-existing `sorry` warning in `HexPoly/DensePoly.lean`.

**Current frontier**

- `HexPoly` now exposes the core arithmetic operators over the dense representation, but division-with-remainder, Euclidean APIs, evaluation/composition/derivative, and `Int`-specific content work remain unscaffolded.

**Next step**

- Add the next Phase 1 `HexPoly` slice for division/remainder or another standalone algebraic API block from `SPEC/Libraries/hex-poly.md`.

**Blockers**

- None.
