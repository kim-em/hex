**Accomplished**

- Claimed `#975`.
- Added coefficient extensionality for `GF2Poly`, including stored-word-count equality from coefficient equality and word-level extensionality by bit tests.
- Proved public additive algebra lemmas in `HexGF2/Basic.lean`: `zero_add`, `add_zero`, `add_comm`, `add_assoc`, `add_add_cancel_left`, and `add_add_cancel_right`.
- Verified `lake build HexGF2.Basic`, `lake build HexGF2.Euclid`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- The GF2 coefficient/addition proof layer is now available for later Euclid invariant and quotient-step cancellation work.
- The requested full `rg -n "axiom|native_decide" HexGF2 -S` scan still reports the pre-existing trusted AES smoke-test fixture in `HexGF2/Smoke.lean`; this session did not introduce any `axiom` or `native_decide`.

**Next step**

- Continue with the dependent GF2 Euclid prerequisite issues, especially monomial multiplication and quotient-step cancellation.

**Blockers**

- No blockers for the coefficient extensionality and additive algebra slice.
