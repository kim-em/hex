**Accomplished**

- Claimed issue `#4` and scaffolded [HexPoly/DensePoly.lean](/home/kim/hex/worktrees/87bc44c9/HexPoly/DensePoly.lean:1) with the Phase 1 dense representation: `DensePoly`, normalization helpers, `ofArray`, and basic coefficient/degree accessors.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/87bc44c9/HexPoly.lean:1) to import the new module and carry a root module doc comment.
- Verified the slice with `lake build HexPoly`; the only remaining warning is the expected `sorry` proof for normalization.

**Current frontier**

- `HexPoly` now has its core dense data model in place, but arithmetic APIs and proofs from the spec remain unscaffolded.

**Next step**

- Land this scaffold PR, then queue the next narrow `HexPoly` Phase 1 slice such as addition/multiplication signatures or division/GCD-facing API declarations.

**Blockers**

- None.
