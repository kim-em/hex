**Accomplished**

- Claimed PR #1816 for repair after #1798 was freshly claimed by another repair session.
- Diagnosed the failed CI as `HexGfqRing.Basic` using the generic `DensePoly.mod_add_mod` and `DensePoly.mod_mul_mod` wrappers, which failed to synthesize `DensePoly.DivModLaws (ZMod64 p)` in downstream quotient proofs.
- Repaired `HexGfqRing/Basic.lean` by rewriting the two affected proof steps through the concrete `DensePoly.DivModLaws` projection fields.
- Verified with `lake build HexGfqRing.Basic`, `lake build`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, `lake exe hexgfqring_bench list`, and `lake exe hexgfqring_bench verify`.

**Current frontier**

- PR #1816's local failed-check symptom is repaired; the branch is ready to push and mark salvaged.

**Next step**

- Push the repaired PR branch with `--force-with-lease` and mark PR #1816 salvaged.

**Blockers**

- None.
