**Accomplished**

- Claimed `#492` and verified that `HexHensel` still lacked the quadratic doubling-step module described in `SPEC/Libraries/hex-hensel.md`.
- Added `HexHensel/Quadratic.lean` with the executable `QuadraticLiftResult` and `ZPoly.quadraticHenselStep` definitions, including coefficient reduction modulo `m^2`, factor correction, and Bezout-witness update logic.
- Added the initial theorem surface for the quadratic step: separate factor and Bezout congruence theorems, the combined `quadraticHenselStep_spec`, and monicity preservation.
- Exported the new module from `HexHensel.lean` and updated the root library docstring to mention both linear and quadratic lifting.
- Verified with `lake build HexHensel` and `python3 scripts/check_dag.py`.

**Current frontier**

- The quadratic Hensel-doubling scaffold requested by `#492` is implemented locally and builds cleanly.
- `HexHensel` still has the larger Phase 1 frontier around the separate linear-step refinements and the out-of-scope multifactor/public-lift layers.

**Next step**

- Commit the quadratic-step scaffold and open the PR for `#492`.

**Blockers**

- None in this session.
