**Accomplished**

- Claimed `#931`.
- Added `HexGramSchmidt/Conformance.lean` with Lean-only Phase 3 core checks for `GramSchmidt.Int.gramDet`, `gramDetVec`, and `scaledCoeffs`.
- Covered typical, zero-edge, and dependent integer matrix fixtures, including determinant-vector agreement, selected Gram determinant values, diagonal/upper scaled-coefficient shape, and lower scaled-coefficient entries.
- Imported the conformance module from `HexGramSchmidt.lean`.

**Current frontier**

- `HexGramSchmidt` now has an initial conformance slice for the integer Gram-determinant and scaled-coefficient surface.
- `libraries.yml[HexGramSchmidt].done_through` remains `2` because this slice does not cover the full public `HexGramSchmidt` conformance surface.

**Next step**

- Add a focused follow-up conformance slice for the remaining `HexGramSchmidt` public surface, especially noncomputable `basis` / `coeffs` behavior and row-update APIs.

**Blockers**

- No blocker for this slice.
