**Accomplished**

- Claimed issue `#61` (`Scaffold HexMatrix residual echelon partition theorems`) and verified the remaining Phase 1 gap was localized to [HexMatrix/Rref.lean](/home/kim/hex/worktrees/7af74f6a/HexMatrix/Rref.lean).
- Added the missing `IsEchelonForm` scaffold theorems for `transform_mul_inv`, `pivotCols_injective`, `freeCols_injective`, and `pivotCols_disjoint_freeCols`, keeping them aligned with the existing row-echelon API and using `sorry` proofs as Phase 1 scaffolding permits.
- Verified the updated `HexMatrix` target with `lake build HexMatrix`.

**Current frontier**

- Issue `#61` is implemented locally and ready to package into a PR from this worktree.

**Next step**

- Create the PR for `#61`, then continue chipping away at the remaining `HexMatrix` Phase 1 gap around the Bareiss determinant surface (`#62`).

**Blockers**

- None.
