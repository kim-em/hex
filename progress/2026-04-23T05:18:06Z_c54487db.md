**Accomplished**

- Claimed critical-path feature issue `#186` and verified the current `HexPolyFp` scaffold against `SPEC/Libraries/hex-poly-fp.md`, `HexPolyFp/Core.lean`, and `HexPolyFp/ModCompose.lean`.
- Added [HexPolyFp/SquareFree.lean](/home/kim/hex/worktrees/c54487db/HexPolyFp/SquareFree.lean) with an executable square-free decomposition scaffold: factor/decomposition records, derivative and reconstructed-product helpers, an executable Yun-style shell built on the existing derivative/gcd/division surfaces, and theorem statements for product, multiplicity positivity, square-freeness, and pairwise coprimeness.
- Updated [HexPolyFp.lean](/home/kim/hex/worktrees/c54487db/HexPolyFp.lean) to re-export the new square-free module and advertise the expanded public surface.
- Ran `lake build HexPolyFp` successfully after fixing computable `DecidableEq` support for `FpPoly`.

**Current frontier**

- The square-free module is now scaffolded, but the executable path still uses a conservative shell for derivative-zero / perfect-power cases; later Phase 1 or proof phases can refine that branch into a fuller Yun implementation.

**Next step**

- Commit this branch and publish the PR for `#186` so the critical-path `HexPolyFp` square-free slice can merge and unblock downstream `HexGfqRing`, `HexBerlekamp`, and `HexHensel` scaffolding.

**Blockers**

- None in the local build; only the usual existing `sorry` scaffolding warnings remain in upstream modules and the new theorem surface.
