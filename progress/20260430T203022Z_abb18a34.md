**Accomplished**

- Claimed #1441 and completed the `ZMod64.neg` normalization proof work in
  `HexModArith/Ring.lean`.
- Added private helper lemmas for the nonzero, non-word-modulus negation branch,
  proving the wrapped UInt64 expression has representative `p - a.toNat` and is
  still below `p`.
- Replaced the proof placeholder in `ZMod64.neg` and the `sorry` in
  `ZMod64.toNat_neg`.
- Verified with `lake build HexModArith.Ring`, `lake build HexModArith`,
  `lake build HexModArith.Conformance`, `python3 scripts/status.py HexModArith`,
  `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans
  over `HexModArith/Ring.lean`.

**Current frontier**

- The #1441 target leaves are complete.
- `HexModArith/Ring.lean` still has the existing downstream `Lean.Grind`
  semiring/ring/commutative-ring proof placeholders, which were explicitly out
  of scope for this issue.

**Next step**

- Continue with the remaining Phase 5 `HexModArith` proof obligations, especially
  the downstream `Lean.Grind` ring laws that can now use `toNat_neg`.

**Blockers**

- None for #1441.
