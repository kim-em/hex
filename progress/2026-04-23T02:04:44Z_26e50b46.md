**Accomplished**

- Claimed issue `#74` and confirmed `HexPoly.Crt` was stating CRT remainder theorems over `DensePoly.modMonic` without the monicity hypotheses that API requires.
- Updated [HexPoly/Crt.lean](/home/kim/hex/worktrees/26e50b46/HexPoly/Crt.lean) so `polyCRT_mod_fst` requires `DensePoly.Monic a` and `polyCRT_mod_snd` requires `DensePoly.Monic b`, with docstrings aligned to the executable remainder contract.
- Verified the change with `lake build HexPoly`.

**Current frontier**

- The branch is ready to publish for issue `#74`; only commit and PR creation remain.

**Next step**

- Commit `HexPoly/Crt.lean` and this progress file, then open the feature PR closing `#74`.

**Blockers**

- None.
