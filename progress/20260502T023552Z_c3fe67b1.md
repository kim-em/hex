**Accomplished**

- Claimed #1733 for the HexGF2 left-middle carry-less multiply source-triple helper.
- Proved `xorBoolList_left_middle_sourceTriple_collapse` in `HexGF2/Multiply.lean`.
- Added local XOR-list congruence helpers, middle low/high source-pair index collapses, and masked low/high list collapses to combine the split middle contribution.
- Verified `lake build HexGF2.Multiply`, `lake build HexGF2`, `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-placeholder scan of `HexGF2/Multiply.lean`.

**Current frontier**

- `HexGF2/Multiply.lean` still has the existing fixed source-triple associativity placeholder in `xorBoolList_assoc_fixed_sourceTriple`.
- The left-middle source-triple helper and wrapper are now fully proved.

**Next step**

- Use the completed low, middle, and high two-word source-triple helpers to finish `xorBoolList_assoc_fixed_sourceTriple`.

**Blockers**

- None.
