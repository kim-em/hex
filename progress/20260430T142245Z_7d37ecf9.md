**Accomplished**

- Claimed #1370, found it stale against `main` because its target helper only
  exists in open PR #1372, and released it for replanning with the dependency
  reason.
- Claimed #1353 for the determinant row-add duplicate-row cancellation.
- Split the opaque `permutationVectors_rowAdd_sum` proof boundary in
  `HexMatrix/Determinant.lean` into:
  - `detProduct_rowAdd` for the row-add product expansion;
  - `permutationVectors_duplicateRow_sum` for the duplicate-row Leibniz
    cancellation.
- Added `rowAddDuplicate`, row-add/duplicate-row access lemmas, and determinant
  sum-add fold helpers so the public `det_rowAdd` path now exposes the intended
  multilinear proof structure.
- Opened successor issues #1373 and #1374 for the remaining row-add proof
  leaves, with #1374 depending on #1373.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-pattern scan on
  `HexMatrix/Determinant.lean`.

**Current frontier**

- `permutationVectors_rowAdd_sum` is no longer a single opaque placeholder. It
  now reduces row-add determinant invariance to product expansion plus
  duplicate-row cancellation.
- The determinant row-swap product/sign issues #1370 and #1371 should wait for
  PR #1372 or be marked with an explicit dependency.

**Next step**

- Claim #1373 to prove `detProduct_rowAdd`, then #1374 to prove the
  duplicate-row Leibniz cancellation over `permutationVectors`.

**Blockers**

- #1370 was stale when claimed because its target declaration was only present
  in open PR #1372, not on `main`.
