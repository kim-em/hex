**Accomplished**

- Claimed `#828`.
- Added `GF2Poly.division_step_degree_lt` in `HexGF2/Basic.lean`, packaging the long-division subtraction descent fact from the existing leading-coefficient cancellation and `mulXk` degree theorem.
- Decomposed the remaining `#828` work into `#971` for monomial multiplication/additive algebra prerequisites and `#972` for the final `divModAux` invariant and remainder-bound proofs.
- Left the required decomposition breadcrumb on `#828`.
- Verified `lake build HexGF2.Basic`, `lake build HexGF2.Euclid`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexGF2/Euclid.lean` still has the intended downstream placeholders for `divMod_spec`, `mod_degree_lt`, and gcd/xgcd theorems.
- The `divModAux` invariant now has a proved degree-descent step available, but still needs the algebraic bridge between `monomial k * q` and `q.mulXk k`.

**Next step**

- Work `#971`: prove the multiplication-by-monomial bridge and minimal additive laws needed to rewrite the long-division quotient/remainder update.

**Blockers**

- No technical blocker for the descent lemma.
- Full completion of `#828` is blocked on the algebraic prerequisites now tracked by `#971`.
