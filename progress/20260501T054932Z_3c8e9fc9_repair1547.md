**Accomplished**

- Claimed PR #1547 for repair and reproduced the CI failures locally.
- Diagnosed the downstream failures as missing `DensePoly.DivModLaws` / `DensePoly.GcdLaws` instances for `ZMod64 p` consumers in `HexGfqRing` and `HexGfqField`.
- Restored the `ZMod64 p` division-law instance to downstream-compatible availability and added the matching gcd-law instance scaffold in `HexPolyFp/Basic.lean`.
- Verified `lake build HexGfqRing`, `lake build HexGfqField`, `lake build`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexPolyFp`, and `git diff --check`.

**Current frontier**

- PR #1547 is locally repaired and ready to push.

**Next step**

- Commit the repair with this progress file, force-push the PR branch, and mark PR #1547 salvaged.

**Blockers**

- None.
