**Accomplished**

- Claimed review issue `#1085`.
- Reviewed `HexBerlekamp.lean` and `HexBerlekamp/*.lean` against
  `SPEC/Libraries/hex-berlekamp.md`, `PLAN/Phase1.md`,
  `PLAN/Phase2.md`, `PLAN/Conventions.md`, and downstream certificate
  expectations in `SPEC/Libraries/hex-berlekamp-zassenhaus.md`.
- Found a Phase 2 blocker in the public irreducibility certificate
  surface: the Lean type is indexed by `p` and uses `prime`/`degree`
  fields, while the specs require a self-contained certificate with
  `p` and `n` fields so downstream arrays can hold certificates from
  different primes.
- Opened follow-up feature issue `#1091` for the certificate-shape
  repair.
- Left `status/hex-berlekamp.scaffolding-reviewed` absent and left
  `HexBerlekamp.done_through: 1` unchanged.
- Verified with:
  - `lake build HexBerlekamp`
  - `python3 scripts/status.py HexBerlekamp`
  - `python3 scripts/status.py`
  - `python3 scripts/check_dag.py`
  - `rg -n "scaffold|for now|eventual|placeholder|Phase 1|bridge for|axiom|native_decide" HexBerlekamp HexBerlekamp.lean -S`
  - `rg -n "\bsorry\b" HexBerlekamp HexBerlekamp.lean -S`

**Current frontier**

- HexBerlekamp's executable matrix, Rabin, factor, and distinct-degree
  bodies otherwise look like real scaffold bodies, with no forbidden
  scaffold vocabulary, data-level `sorry`, `axiom`, fake extern, or
  hidden native-type detour found in this review.
- Existing `sorry`s in HexBerlekamp are proof-level theorem placeholders.
- Phase 2 remains open until `#1091` lands and a reviewer can re-run the
  token/bump step.

**Next step**

- Repair `IrreducibilityCertificate` per `#1091`, then rerun the
  HexBerlekamp Phase 2 review checks and add the scaffolding-reviewed
  token if no further blockers remain.

**Blockers**

- `#1091` blocks marking HexBerlekamp Phase 2 complete.
