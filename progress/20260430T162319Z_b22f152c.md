**Accomplished**

- Claimed issue #1402.
- Added `inversionCount_transposePermutationValues_parity` in
  `HexMatrix/Determinant.lean`, using the separated append-block bridge
  from #1401.
- Replaced the unrestricted `detSign_transposeValues` placeholder with a
  restricted proof requiring `perm.toList.Nodup` and `i ≠ j`.
- Threaded `permutationVectors_nodup` through the local row-swap term call.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

**Current frontier**

- The determinant row-swap term path now has a proved sign flip under nodup
  evidence.
- The remaining row-swap work is the product reindexing lemma and the
  permutation-vector sum reindexing, which are outside #1402.
- Existing unrelated `.claude/CLAUDE.md` worktree modification remains
  uncommitted.

**Next step**

- Use the restricted sign lemma with the row-swap product lemma in issue #1379.

**Blockers**

- None.
