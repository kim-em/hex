**Accomplished**

- Claimed #1485 and proved the public `henselLift_spec` and `henselLift_monic`
  wrappers in `HexHensel/Linear.lean`.
- Instantiated the completed `henselLiftLoop_invariant` for the successor
  precision case and projected product congruence and `g` monicity from it.
- Verified `lake build HexHensel.Linear`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-placeholder scan of
  `HexHensel/Linear.lean`.

**Current frontier**

- `HexHensel/Linear.lean` has no remaining placeholders.
- `HexHensel` still has its independent Phase 5 placeholder in
  `HexHensel/Multifactor.lean`.

**Next step**

- Prove the remaining multifactor Phase 5 theorem in
  `HexHensel/Multifactor.lean` in a separate issue.

**Blockers**

- None.
