**Accomplished**

- Claimed #1563 and verified its #1562 dependency was closed.
- Added complete finite-index prime-characteristic cancellation helpers in `HexPolyFp/SquareFree.lean`, including the `coeffFold`/`coeffTerm` coefficient bridge needed before the stronger expansion cancellation proof.
- Created follow-up #1570 for the remaining `coeffFoldPowerCoeff` prime mixed-tuple cancellation and linked it from #1563 with the required decomposition breadcrumb.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token scan for touched HexPolyFp code.

**Current frontier**

- `zmod64_index_fold_add_pow_prime` and `coeffFold_coeff_index_fold_pow_prime` now expose cancellation for bounded coefficient-index folds.
- The remaining blocker for the downstream Frobenius-additivity integration is the recursive `coeffFoldPowerCoeff g m p n` mixed-tuple cancellation tracked by #1570.

**Next step**

- Prove #1570 by connecting `powLinear_coeffFold_prime_coeff_expansion` to the finite-index cancellation helpers, showing that mixed p-tuples vanish and only pure diagonal coefficient contributions survive.

**Blockers**

- No external blocker. The remaining work is proof-local but needs a stronger combinatorial invariant over the recursive coefficient expansion.
