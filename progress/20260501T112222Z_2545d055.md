**Accomplished**
- Claimed issue #1603 for the HexPolyFp Yun terminal/descent helper theorem surfaces.
- Added `yunFactorsContribution_terminal_of_isOne`, packaging the `isOne c` stop branch with `eq_one_of_isOne_true`.
- Added `yunFactorsContribution_tail_repeated_descent`, exposing the repeated-tail `pthRoot` descent alignment for `squareFreeAuxRevContribution`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, and `git diff --check`.
- Scanned touched `HexPolyFp` files for forbidden terms; no `axiom`, `native_decide`, `TODO`, or `FIXME` were present, and the `sorry` count remained unchanged.

**Current frontier**
- `yunFactorsContribution_reconstruct_core` remains the integration target for issue #1593, now with explicit local helper surfaces for the terminal and repeated-tail branches.

**Next step**
- Open the PR for issue #1603.

**Blockers**
- None.
