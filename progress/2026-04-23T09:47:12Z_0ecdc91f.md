**Accomplished**

- Claimed issue `#355` after the higher-priority human-oversight follow-ups were already in flight.
- Re-checked `HexMatrixMathlib` against `SPEC/Libraries/hex-matrix-mathlib.md`, `SPEC/testing.md`, and the live conformance module to confirm determinant coverage was still missing.
- Extended `HexMatrixMathlib/Conformance.lean` with a determinant-only conformance slice for `det_eq`, using committed typical, edge, and adversarial fixtures plus transport-equality `#guard`s.
- Updated the conformance docstring so the covered operations, properties, and edge cases now mention determinant coverage.
- Verified `lake build HexMatrixMathlib` succeeds and `python3 scripts/status.py` remains consistent.

**Current frontier**

- `HexMatrixMathlib` now has determinant bridge conformance coverage, but its Phase 3 frontier remains blocked on `HexMatrix.done_through ≥ 3`.

**Next step**

- Publish the `#355` PR so the determinant slice can merge and unblock future `HexMatrixMathlib` review work once `HexMatrix` advances again.

**Blockers**

- `HexMatrixMathlib` cannot progress beyond this narrow determinant slice until `HexMatrix` completes the rewind/fix/re-review path and reaches Phase 3.
