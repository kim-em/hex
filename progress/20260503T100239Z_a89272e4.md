**Accomplished**

- Claimed #1834 and added the Phase 1 quadratic multifactor Hensel lift
  scaffolding in a new module `HexHensel/QuadraticMultifactor.lean`,
  exposed via the `HexHensel` umbrella.
- New surface: `iterateQuadraticHensel` (a doubling-step iterator that
  reuses `quadraticHenselStep` as the per-step primitive),
  `henselLiftQuadratic` (the per-split lifter from modulus `p` to `p^k`
  with final reduction modulo `p^k`), `multifactorLiftQuadratic` (mirrors
  the sequential split tree of the linear `multifactorLift`), and the
  `multifactorLiftQuadratic_spec` companion theorem with the same
  hypothesis package as `multifactorLift_spec`.
- `multifactorLiftQuadratic_spec`'s proof is a single `sorry`, as
  permitted by the issue. Phase 5 will discharge it; lift uniqueness
  (linear-vs-quadratic agreement) lives in `hex-hensel-mathlib`.

**Current frontier**

- `lake build HexHensel.QuadraticMultifactor` succeeds with a single
  expected `declaration uses 'sorry'` warning on the spec theorem.
- The linear `multifactorLift` surface is unchanged.
- `python3 scripts/check_dag.py` is clean.

**Next step**

- Wire `multifactorLiftQuadratic` into `HexBerlekampZassenhaus` (issue C
  in the Phase 1 BZ critical path).
- Discharge `multifactorLiftQuadratic_spec` (Phase 5 obligation).

**Blockers**

- Local `lake build` of `HexArith.Montgomery.InvNat` consistently times
  out at the existing `bv_decide (config := { timeout := 30 })` setting
  on this worktree's machine; raising the timeout to 240 lets the build
  complete in ~3 minutes. Pre-existing on `main`, unrelated to this
  change, and CI presumably has more headroom — flagged here for whoever
  picks up the InvNat performance follow-up.
