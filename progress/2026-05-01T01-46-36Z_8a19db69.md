**Accomplished**

- Claimed #1497 and replaced the three `sorry` proofs in `HexArith/Montgomery/RedcNat.lean`.
- Added private Nat-level helpers for the Montgomery correction congruence, exact divisibility of the REDC numerator by `UInt64.word`, and the unreduced quotient bound.
- Proved `redcNat_eq_mod`, `redcNat_lt`, and `redcNat_u_lt_two_p` without changing the executable `redcNat` definition or public theorem statements.
- Verified `lake build HexArith.Montgomery.RedcNat`, `lake build HexArith.Montgomery.Redc`, `lake build HexArith`, `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`, `git diff --check`, and a focused placeholder scan of `HexArith/Montgomery/RedcNat.lean`.

**Current frontier**

- `HexArith/Montgomery/RedcNat.lean` has no remaining placeholders.
- `HexArith` remains in Phase 5 because other files still contain existing proof placeholders, including `ExtGcd`, `Montgomery/InvNat`, `Montgomery/Redc`, `Montgomery/Context`, `Nat/Prime`, and Barrett bridge/context files.

**Next step**

- Continue Phase 5 on the next unclaimed HexArith proof cluster, with the executable Montgomery bridge proofs in `HexArith/Montgomery/Redc.lean` now able to depend on completed Nat-level REDC lemmas.

**Blockers**

- None.
