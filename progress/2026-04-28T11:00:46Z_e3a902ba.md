**Accomplished**

- Claimed `#699`, confirmed the project-wide Mathlib-free split in `SPEC/design-principles.md`, and verified that `SPEC/Libraries/hex-gf2.md` was the lone spec still promising core-side `Fintype` for `GF2n`.
- Updated `SPEC/Libraries/hex-gf2.md` to keep `HexGF2` responsible only for executable `GF(2^n)` field operations/algebraic laws, with finiteness/cardinality moved to the Mathlib bridge side.
- Updated `SPEC/Libraries/hex-gf2-mathlib.md` and `SPEC/Libraries/README.md` so the bridge library explicitly owns packed-field `Fintype`/cardinality.
- Opened follow-up issue `#710` for the actual `HexGF2Mathlib` implementation of the packed-field finiteness/cardinality bridge.
- Verified the landing with `lake build HexGF2` and `python3 scripts/check_dag.py`.

**Current frontier**

- `#699` is resolved at the spec/coordination layer: the ownership split is now consistent between `hex-gf2`, `hex-gfq-field`, and the library index.
- The remaining executable work for this topic is the bridge implementation tracked in `#710`, which will need to extend `HexGF2Mathlib` beyond the current `GF2Poly` equivalence foothold.

**Next step**

- Commit these spec-alignment edits and publish the PR closing `#699`.
- A later worker can pick up `#710`, implement the `GF2n` / `GF2nPoly` bridge plus transported finiteness/cardinality, and then `#672` can re-run the `HexGF2` Phase 2 review on the corrected spec surface.

**Blockers**

- None for `#699` itself.
- The new bridge issue `#710` will likely need additional `HexGF2Mathlib` equivalence machinery before it can expose packed-field cardinality results.
