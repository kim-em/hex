**Accomplished**

- Claimed #1329 after confirming there were no unclaimed `human-oversight` issues.
- Reproduced the original `Hex.PolyZBench.runBinom` mismatch at the committed `32..512` schedule: the `n * Nat.log2 (n + 1) * Nat.log2 (n + 1)` model remained inconclusive with a negative residual slope.
- Probed larger `runBinom` schedules. The `512..8192` range showed the old model becoming too low, and the `8192..65536` range auto-fit most strongly to `n^2`.
- Updated `HexPolyZ/Bench.lean` so `runBinom` declares `n * n`, with a local cost-model comment explaining the `O(n)` loop over a linearly growing `Nat` accumulator, and shifted the scientific schedule to `16384..131072`.
- Verified the checked-in `runBinom` settings report `consistent with declared complexity` with `β=-0.065`.
- Ran the full `HexPolyZ` parametric benchmark suite; all ten registrations reported `consistent with declared complexity`.
- Ran `lake exe hexpolyz_bench verify`, `lake build HexPolyZ`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_phase4.py`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexPolyZ` still reports ready for Phase 4 with `done_through: 3`; this issue fixes the known `runBinom` blocker but does not bump `libraries.yml`.
- A follow-up Phase 4 completion issue can now rerun/record the final checks and bump `HexPolyZ.done_through` to `4` if no new blocker appears.

**Next step**

- Open a PR for #1329 with the `runBinom` benchmark model correction.

**Blockers**

- None for #1329.
- The worktree still has a pre-existing unrelated `.claude/CLAUDE.md` modification that this session did not touch.
