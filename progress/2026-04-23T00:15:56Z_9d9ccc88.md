**Accomplished**

- Claimed issue `#5` and added [HexArith/UInt64/Wide.lean](/home/kim/hex/worktrees/9d9ccc88/HexArith/UInt64/Wide.lean:1) with executable Phase 1 scaffolding for `UInt64.mulHi`, `UInt64.addCarry`, and `UInt64.subBorrow`, plus theorem shells for the requested natural-number bridge lemmas.
- Updated [HexArith.lean](/home/kim/hex/worktrees/9d9ccc88/HexArith.lean:1) to import the new module and document the library root.
- Verified the slice with `lake build HexArith`; the remaining warnings are the expected `sorry` theorem placeholders from the scaffold.

**Current frontier**

- `HexArith` now has the shared wide-word layer needed before Barrett- and Montgomery-specific Phase 1 files can be scaffolded.

**Next step**

- Land this PR, then queue the next narrow `HexArith` slice for Nat-level modular arithmetic lemmas or one of the reducer/context modules that depends on the new `UInt64` helpers.

**Blockers**

- None.
