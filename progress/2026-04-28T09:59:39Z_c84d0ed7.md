**Accomplished**

- Claimed `#646` and added the missing `HexModArith` hot-loop wrapper surface in new module `HexModArith/HotLoop.lean`.
- Introduced typed `Hex.BarrettCtx`, `Hex.MontCtx`, and `Hex.MontResidue` wrappers over the existing `hex-arith` contexts, with `ZMod64`-level Barrett multiplication and Montgomery conversion/multiplication APIs plus immediate transport lemmas.
- Added the characteristic-`p` surface in `HexModArith/Ring.lean`: `natCast_eq_natCast_iff`, `natCast_eq_zero_iff_dvd`, `natCast_self`, and `instance : Lean.Grind.IsCharP (ZMod64 p) p`.
- Exported the new hot-loop module from `HexModArith.lean`.
- Verified with `lake build HexModArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- The `#646` implementation is complete in `HexModArith`.
- The branch is ready to commit and publish as the PR closing `#646`.

**Next step**

- Commit `HexModArith.lean`, `HexModArith/Ring.lean`, `HexModArith/HotLoop.lean`, and this progress file.
- Run `coordination create-pr 646 "feat: add HexModArith hot-loop wrappers"` to open the closing PR.

**Blockers**

- None in this worktree after the local Lean proof adjustments; build and DAG verification both pass.
