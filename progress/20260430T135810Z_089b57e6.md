**Accomplished**

- Claimed #1363 for the HexHensel linear one-step factor congruence proof.
- Added `congr_mul_reduceModPow_pair` to transport multiplication through
  canonical `reduceModPow` on both factors.
- Refactored `linearHenselStep_spec` so the public theorem is proved by a
  reduced-product bridge over the executable `linearHenselStep` shape.
- Moved the remaining hard algebraic obligation to the private
  `linearHenselStep_raw_factor_congr` boundary, where the correction terms,
  `modP` bridge, and `DensePoly.divMod` reconstruction are all explicit.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans on `HexHensel/Linear.lean`.

**Current frontier**

- `linearHenselStep_spec` no longer contains the proof-body placeholder.
- The remaining #1363 proof work is concentrated in
  `linearHenselStep_raw_factor_congr`, which must show the unreduced corrected
  factors multiply to `f` modulo `p^(k+1)`.

**Next step**

- Prove `linearHenselStep_raw_factor_congr` by combining the quotient/remainder
  identity for `DensePoly.divMod (t * eMod) gMod`, the Bezout congruence, and
  the coefficientwise division bridge from `hprod`.

**Blockers**

- None. Lower-level congruence and `divMod_spec` APIs are available, though
  several of their own proof bodies remain placeholders elsewhere in the Phase
  5 surface.
