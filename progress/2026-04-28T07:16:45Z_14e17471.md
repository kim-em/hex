**Accomplished**

- Claimed issue `#526` after the higher-priority `human-oversight` item `#541` proved unclaimable (`needs replan`), and verified that `HexMatrix/Bareiss.lean` still lacked the spec-promised `bareiss_eq_det` theorem surface.
- Updated [HexMatrix/Bareiss.lean](/home/kim/hex/worktrees/14e17471/HexMatrix/Bareiss.lean:1) to import the determinant API, extend the module docstring to mention the determinant correspondence surface, and add `theorem bareiss_eq_det (M : Matrix Int n n) : bareiss M = det M := by sorry`.
- Verified the scaffold with `lake build HexMatrix` and `python3 scripts/check_dag.py`.

**Current frontier**

- Issue `#526` is ready to publish; the missing root-library Bareiss/determinant theorem declaration now exists and compiles.

**Next step**

- Commit the `HexMatrix` Bareiss theorem-surface addition and open the PR closing `#526`.

**Blockers**

- None.
