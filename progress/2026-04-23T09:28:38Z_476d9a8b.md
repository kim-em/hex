**Accomplished**

- Claimed `#354` after checking the current unclaimed queue and verifying that `#351` was still stale against the open wrong-scaffold fix `#332`, while `HexGfqField`'s prerequisite fix `#341` was already closed.
- Re-reviewed `HexGfqField` against `SPEC/Libraries/hex-gfq-field.md` and `PLAN/Phase2.md`; confirmed the remaining gaps are honest `sorry` placeholders rather than wrong-but-plausible data-level implementations.
- Restored the Phase 2 attestation by adding `status/hex-gfq-field.scaffolding-reviewed` and bumping `HexGfqField.done_through` from `1` to `2` in `libraries.yml`.
- Verified `lake build HexGfqField`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py` all succeed; `status.py` now moves `HexGfqField` out of the ready-for-review set and correctly reports it blocked on `HexGfqRing.done_through ≥ 3` for Phase 3.

**Current frontier**

- The issue work is complete locally. Remaining session work is to commit the attestation change plus this progress note and open the PR for `#354`.

**Next step**

- Commit `libraries.yml`, `status/hex-gfq-field.scaffolding-reviewed`, and this progress file, then run `coordination create-pr 354`.

**Blockers**

- No technical blocker on `#354`.
