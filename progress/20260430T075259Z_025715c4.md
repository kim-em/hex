**Accomplished**

- Claimed feature issue #1307.
- Added `HexGfqField/Bench.lean` with lean-bench registrations for `ofPoly`/`repr`, `add`, `mul`, `neg`/`sub`, `pow`, `inv`/`div`, `zpow`, and `frob`.
- Used deterministic dense `F_8191` fixtures so the Frobenius exponent is dense and the square-and-multiply path exercises the multiply branch.
- Added the `hexgfqfield_bench` Lake executable and CI smoke commands.
- Verified `lake build HexGfqField`, `lake build hexgfqfield_bench`, `lake exe hexgfqfield_bench list`, `lake exe hexgfqfield_bench verify`, `python3 scripts/status.py HexGfqField`, `python3 scripts/check_dag.py`, `python3 scripts/check_phase4.py`, `git diff --cached --check`, and a `native_decide`/`sorry` scan of `HexGfqField/Bench.lean`.

**Current frontier**

- `HexGfqField` now has a Phase 4 benchmark executable smoke surface, but the issue intentionally did not run the full scientific benchmark suite or bump `libraries.yml`.
- `scripts/check_phase4.py --all` still reports unrelated pre-existing benchmark-comment gaps outside `HexGfqField`.

**Next step**

- Run the full scientific `hexgfqfield_bench` registrations when ready and only then decide whether `HexGfqField.done_through` can advance to `4`.

**Blockers**

- No blocker for this session.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
