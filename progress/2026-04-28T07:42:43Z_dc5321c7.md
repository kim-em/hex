**Accomplished**

- Claimed `#541` and reshaped `Hex.ZMod64` around the spec-mandated `ZMod64.Bounds` typeclass instead of threading `(hp : 0 < p)` and `(hword : p ≤ UInt64.word)` through every operation.
- Added the default extern-backed `Hex.ZMod64.mul` contract in [HexModArith/Basic.lean](/home/kim/hex/worktrees/dc5321c7/HexModArith/Basic.lean) together with the pure Lean fallback body and updated `toNat_*` lemmas for `zero`, `one`, `add`, `sub`, and `mul`.
- Added the Phase 1 reference C shim [HexModArith/ffi/zmod64_mul.c](/home/kim/hex/worktrees/dc5321c7/HexModArith/ffi/zmod64_mul.c) using `__uint128_t` modular reduction, and wired `HexModArith` precompiled native builds in [lakefile.lean](/home/kim/hex/worktrees/dc5321c7/lakefile.lean).
- Added [HexModArith/Smoke.lean](/home/kim/hex/worktrees/dc5321c7/HexModArith/Smoke.lean) with native smoke checks for `p = 2^31 - 1` and `p = 2^63 + 29`; the build printed the expected residues `2` and `1288`.
- Verified with `lake build HexModArith`, `lake build`, `python3 scripts/check_dag.py`, and a standalone `cc -Wall -Wextra` compile of the new C shim.

**Current frontier**

- The `#541` repair is implemented and verified locally. The remaining task is to commit and publish the branch as the closing PR for the issue.

**Next step**

- Commit the `ZMod64.Bounds`/`mul` repair and run `coordination create-pr 541`.

**Blockers**

- None.
