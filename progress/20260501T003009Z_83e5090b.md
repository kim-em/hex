**Accomplished**

- Claimed #1481 and proved `linearHenselStep_h_degree?_eq` in
  `HexHensel/Linear.lean`.
- Threaded the supplied raw and reduced `h` degree hypotheses through the
  executable `linearHenselStep` definition, mirroring the existing `g` degree
  preservation proof.
- Verified `lake build HexHensel.Linear`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a targeted forbidden-placeholder scan of
  `HexHensel/Linear.lean`.

**Current frontier**

- The linear step now has proved `g` monicity, `g` degree preservation, and `h`
  degree preservation.
- Remaining local `HexHensel/Linear.lean` placeholders are the loop invariant,
  `henselLift_spec`, and `henselLift_monic`.
- `HexHensel/Multifactor.lean` still has its Phase 5 placeholder.

**Next step**

- Continue HexHensel Phase 5 by proving `henselLiftLoop_invariant`, using the
  now-available per-step `g` and `h` preservation theorems.

**Blockers**

- None.
