**Accomplished**

- Claimed #1483 and proved `henselLiftLoop_invariant` in
  `HexHensel/Linear.lean`.
- Moved the private loop-invariant theorem below the per-step spec and monicity
  lemmas so the proof can reuse the established single-step surface.
- Verified `lake build HexHensel.Linear`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a targeted forbidden-placeholder scan of
  `HexHensel/Linear.lean`.

**Current frontier**

- The linear Hensel loop now threads product congruence, fixed Bezout
  congruence, and `g` monicity across all iterations.
- Remaining local `HexHensel/Linear.lean` placeholders are the public wrappers
  `henselLift_spec` and `henselLift_monic`.
- `HexHensel/Multifactor.lean` still has its Phase 5 placeholder.

**Next step**

- Prove `henselLift_spec` and `henselLift_monic` from the completed loop
  invariant and the existing start/step hypotheses.

**Blockers**

- None.
