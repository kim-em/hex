**Accomplished**

- Claimed feature issue `#1052` for the `HexBerlekamp` distinct-degree factorization surface.
- Added `HexBerlekamp/DistinctDegree.lean` with executable degree buckets, result/product helpers, a gcd candidate/step API, and a fuel-bounded `distinctDegreeFactor` loop over increasing positive degrees.
- Added SPEC-facing theorem surfaces for product preservation and Frobenius-degree bucket invariants, with proof bodies left as permitted `sorry`s.
- Re-exported the new module from `HexBerlekamp.lean`.
- Verified `lake build HexBerlekamp`, `git diff --check`, and the requested banned-word/native-decision scan for `HexBerlekamp`.

**Current frontier**

- `HexBerlekamp` now exposes Berlekamp split-step factoring, full Berlekamp factorization, Rabin/Rank irreducibility tests, and a distinct-degree factorization API.
- The new DDF surface records each discovered degree bucket as the product of factors for that degree plus an explicit residual.

**Next step**

- Prove the DDF loop product invariant by threading exact-division preservation through `distinctDegreeStep`, then refine the bucket invariant toward exact irreducible-degree separation in the mathlib-facing layer.

**Blockers**

- None.
