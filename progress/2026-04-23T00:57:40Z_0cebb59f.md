**Accomplished**

- Claimed issue `#28` and added `HexPoly/Gcd.lean`, introducing executable Phase 1 scaffolding for dense-polynomial `gcd`, `xgcd`, the `XgcdResult` record, and the adjacent divisibility/Bezout theorem declarations.
- Updated `HexPoly.lean` to re-export the new GCD module and keep the root-module documentation aligned with the expanded API surface.
- Verified the slice with `lake build HexPoly`; the build succeeds with only theorem-level `sorry` warnings in the expected scaffold modules.

**Current frontier**

- Issue `#28` is ready to be packaged as a PR from this worktree.
- Remaining `HexPoly` Phase 1 work still includes CRT and any later `DensePoly Int` content/primitive-part slices, which were intentionally left out of this issue.

**Next step**

- Commit the `HexPoly` GCD scaffold, create the PR for issue `#28`, and let auto-merge advance the library queue.

**Blockers**

- None.
