**Accomplished**

- Claimed `#989`, determined it combined the right-monomial raw fold bridge with the Euclid-oriented left-monomial bridge, decomposed it into `#999` and `#1001`, and marked the parent for replan.
- Claimed `#999` and added two narrow helper lemmas: `GF2Poly.words_monomial` in `HexGF2/Basic.lean` and `GF2Poly.coeff_mul` in `HexGF2/Multiply.lean`.
- Filed residual issue `#1003` for the remaining right-monomial raw fold proof and made `#1001` depend on it.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned-pattern scan.

**Current frontier**

- The helper surface now exposes the normalized monomial word array and the raw `mulWords` coefficient target.
- The remaining `#1003` goal is the raw coefficient/fold equality between `mulWords q.words ((Array.replicate (k / 64) 0).push oneHot)` and the raw `shiftLeft` storage.

**Next step**

- Prove `q * monomial k = q.mulXk k` in `HexGF2/Multiply.lean` using `words_monomial`, `coeff_mul`, and the one-hot CLMUL projection lemmas.

**Blockers**

- No external blockers. The remaining work is a nontrivial Lean raw-array/fold proof.
