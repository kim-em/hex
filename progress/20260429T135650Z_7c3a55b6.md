**Accomplished**

- Claimed feature issue `#1097`.
- Added `HexHensel/Conformance.lean` with the Phase 3 docstring and Lean-only core checks for `ZPoly.modP`, `FpPoly.liftToZ`, `ZPoly.reduceModPow`, and `Array.polyProduct`.
- Imported the conformance module from `HexHensel.lean`.
- Bumped `HexHensel.done_through` to `3`.
- Verified `lake build HexHensel`, `python3 scripts/status.py HexHensel`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-token scan.

**Current frontier**

- `HexHensel` is now ready for Phase 4 once its dependency phase gates are satisfied.
- `python3 scripts/status.py HexHensel` reports Phase 4 blocked on `HexPolyFp.done_through >= 4` and `HexPolyZ.done_through >= 4`.

**Next step**

- Advance the `HexPolyFp` and `HexPolyZ` Phase 4 benchmarking gates before returning to `HexHensel` Phase 4.

**Blockers**

- None for this issue.
