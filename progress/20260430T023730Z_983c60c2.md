**Accomplished**

- Claimed #1231 and confirmed the two target HexGramSchmidt Phase 4 scientific benchmark registrations were below the lean-bench child-process signal floor.
- Added fixed hot-loop wrappers for `runGramDetVecChecksum` and `runScaledCoeffsChecksum` while keeping their declared textbook complexity models unchanged.
- Retuned the two scientific schedules so both now produce verdict-eligible rows and return `consistent with declared complexity`.
- Bumped `libraries.yml[HexGramSchmidt].done_through` to `4`.
- Verified `lake build HexGramSchmidt`, `lake exe hexgramschmidt_bench list`, `lake exe hexgramschmidt_bench verify`, both retuned scientific runs, `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- HexGramSchmidt now reports ready for Phase 5 implementation work.
- The eight benchmark registrations not touched by this session remain as in the parent audit; smoke verification covers all ten registrations.

**Next step**

- Pick up the next HexGramSchmidt Phase 5 proof or implementation issue once planned.

**Blockers**

- None for this session.
