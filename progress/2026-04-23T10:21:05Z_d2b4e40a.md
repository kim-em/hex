**Accomplished**

- Claimed issue `#355` after confirming the higher-priority human-oversight issue `#332` was already claimed.
- Extended `HexMatrixMathlib/Conformance.lean` with determinant-bridge coverage only: updated the conformance docstring, added determinant fixtures for typical/edge/adversarial square matrices, added computational equality checks, and instantiated `det_eq` on those fixtures.
- Verified `lake build HexMatrixMathlib` succeeds on the live tree.

**Current frontier**

- `HexMatrixMathlib` now has a determinant-only conformance slice alongside the existing equivalence and row-operation coverage.
- Remaining matrix-mathlib follow-up, if any, is outside this determinant bridge slice.

**Next step**

- Land `#355` and then return to the unclaimed queue for the next ready feature or summarize issue.

**Blockers**

- No blocker for this slice.
