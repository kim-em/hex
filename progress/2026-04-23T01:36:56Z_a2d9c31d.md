**Accomplished**

- Claimed and completed issue `#52` (`feature`): extended [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/a2d9c31d/HexArith/ExtGcd.lean) with the pure-Lean `_root_.Hex.pureUInt64ExtGcd` helper, the public `HexArith.UInt64.extGcd` API, and matching scaffold theorem declarations for the gcd projection and Bezout identity.
- Updated [HexArith.lean](/home/kim/hex/worktrees/a2d9c31d/HexArith.lean) to describe the re-exported Nat and `UInt64` extended-GCD surfaces in the root-module docstring.
- Verified with `lake build HexArith` (success; only existing scaffold `sorry` warnings remain, including the new `UInt64` theorem placeholders).

**Current frontier**

- `HexArith` now exposes Nat and `UInt64` extended-GCD scaffolds, but the `Int` pure/public surface promised by the spec is still outstanding in issue `#47`.

**Next step**

- Land this PR, then continue the remaining `HexArith` Phase 1 queue, most directly the `Int`/`mpz_gcdext` scaffold in `#47` or the remaining Montgomery context follow-through that still builds on placeholder executable arithmetic.

**Blockers**

- `reflect` was not available in this session, so no extra post-task workflow changes were applied.
