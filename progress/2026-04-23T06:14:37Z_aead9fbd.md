**Accomplished**

- Claimed issue `#214` and added [`HexLLL/State.lean`](/home/kim/hex/worktrees/aead9fbd/HexLLL/State.lean) with the Phase 1 integer state scaffold: `LLLState`, the `ν_eq` / `d_eq` tracking hypotheses, the Nat-indexed `gramDetEntry` accessor, the proof-facing `gramSchmidtCoeff` helper, and the executable multiplicative `potential`.
- Updated [`HexLLL.lean`](/home/kim/hex/worktrees/aead9fbd/HexLLL.lean) to re-export the new state module and describe the expanded root surface accurately.
- Verified the slice with `lake build HexLLL`, which succeeded with only the existing scaffold-stage `sorry` warnings in upstream modules.

**Current frontier**

- `HexLLL` now exposes the integer state/potential surface promised by the spec and is ready for later size-reduction, swap-step, and main-loop scaffolding work.

**Next step**

- Publish the branch as the issue `#214` PR, then let a later worker take the next `HexLLL` algorithm slice or a newly highest-priority ready issue.

**Blockers**

- None in this slice.
