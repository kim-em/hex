**Accomplished**

- Claimed issue `#442` and added [HexMatrix/RREF.lean](/home/kim/hex/worktrees/5f156af4/HexMatrix/RREF.lean) with an executable Gauss-Jordan `rref` pass over decidable fields.
- Added the Phase 1 row-span and nullspace API surface on top of the echelon scaffold: `rowCombination`, `IsEchelonForm.spanCoeffs`, `IsEchelonForm.spanContains`, `IsRREF.nullspaceMatrix`, `IsRREF.nullspace`, the convenience wrappers, and the corresponding theorem stubs.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/5f156af4/HexMatrix.lean) to export the new module and corrected the affected contracts in [SPEC/Libraries/hex-matrix.md](/home/kim/hex/worktrees/5f156af4/SPEC/Libraries/hex-matrix.md) so the executable APIs explicitly require `DecidableEq` and the span theorems use row combinations rather than the dimensionally invalid `M * c = v`.
- Verified `lake build HexMatrix` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrix` now has the executable reduction, row-span, and nullspace surface needed for the remaining Phase 1 matrix work, but the proof obligations in `HexMatrix/RowEchelon.lean` and the new `HexMatrix/RREF.lean` theorems remain scaffolded with `sorry`.

**Next step**

- Land issue `#442` so determinant (`#450`) and Bareiss (`#452`) work can build on the completed reduction/nullspace layer.

**Blockers**

- None.
