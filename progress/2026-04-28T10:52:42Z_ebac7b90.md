**Accomplished**

- Claimed and completed issue `#697` by adding the iterative `Hex.ZPoly.henselLift` wrapper in [HexHensel/Linear.lean](/home/kim/hex/worktrees/ebac7b90/HexHensel/Linear.lean:61), reusing `linearHenselStep` to lift a factorization from modulus `p` to modulus `p^k`.
- Added the initial theorem surface for the wrapper (`henselLift_spec` and `henselLift_monic`) and updated the top-level [HexHensel.lean](/home/kim/hex/worktrees/ebac7b90/HexHensel.lean:5) docstring to describe the exported iterative API.
- Bumped `libraries.yml` so `HexHensel.done_through = 1`, then verified `lake build HexHensel`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py`. The status frontier now advances `HexHensel` to Phase 2 and unblocks `HexHenselMathlib` Phase 1.

**Current frontier**

- `HexHensel` is ready for its Phase 2 scaffolding review.
- `HexBerlekampZassenhaus` remains blocked only on `HexBerlekamp.done_through >= 1`.

**Next step**

- Publish the branch as the PR closing `#697`, then let a later worker handle either the `HexHensel` Phase 2 review or the remaining `HexBerlekamp` / `HexBerlekampZassenhaus` critical-path work.

**Blockers**

- None.
