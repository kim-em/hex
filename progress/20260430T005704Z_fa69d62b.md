**Accomplished**

- Claimed human-oversight issue #1197 after confirming its blocker #1194 was closed.
- Added `scripts/check_phase4.py` to gate changed `setup_benchmark` registrations on adjacent cost-model derivation comments and derivation-bearing commit messages.
- Wired the new Phase 4 gate into CI beside the benchmark smoke steps, with full checkout history so branch-diff checks have a merge base.
- Updated `PLAN/Phase4.md` exit criteria to require derivation comments and commit-message derivations for new or changed parametric registrations.

**Current frontier**

- The new checker intentionally scans only registrations changed on the current branch by default, so it does not fail CI on older benchmark registrations that predate the derivation-comment rule.
- `python3 scripts/check_phase4.py --all` still reports older untouched registrations without adjacent derivation comments; those are not remediated by this issue.

**Next step**

- Future Phase 4 benchmark PRs that change `setup_benchmark` declarations should include adjacent derivation comments and commit-message derivations so the new CI gate passes.

**Blockers**

- None.
