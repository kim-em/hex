**Accomplished**

- Claimed #1773 first, oriented on `HexGramSchmidt/Basic.lean`, and confirmed
  the private reconstruction invariant depends on a missing
  `basisRowsAux` orthogonality layer.
- Decomposed #1773 into #1774 for the Gram-Schmidt orthogonality invariant and
  #1775 for the reconstruction fold, with #1775 blocked on #1774.
- Claimed #1758 and added `HexManual/Phase2345Checkpoint-19.md`, summarizing
  the merged PR wave after checkpoint 18 through PR #1772.
- Verified `python3 scripts/status.py`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

**Current frontier**

- `HexPolyFp` has unclaimed #1752 for the Yun derivative-active loop
  invariant; its dependency #1747 is closed.
- `HexGramSchmidt` has unclaimed #1774 for `basisRowsAux` orthogonality and
  blocked #1775 for `basisMatrix_reconstruction_invariant`.
- `coordination orient` reports no open PRs and no PRs needing repair.

**Next step**

- Publish the checkpoint PR closing #1758.

**Blockers**

- None for the checkpoint.
