**Accomplished**

- Claimed #1259 as the next HexPoly Euclid proof item after #1258 closed.
- Proved `mul_sub_zero_comm` in `HexPoly/Euclid.lean` using the generic `DensePoly.coeff_mul` theorem.
- Added private coefficient-fold helper lemmas that collapse the executable loop-order multiplication sum to a diagonal convolution, reindex the diagonal over commutative rings, and handle multiplication by `0 - q`.
- Kept executable polynomial operations unchanged.
- Verified `lake build HexPoly`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` sorry scan.

**Current frontier**

- `mod_sub_self_eq_mul_neg_div` now depends on a proved multiplication-negation bridge instead of the local admission.
- The remaining `HexPoly/Euclid.lean` sorries are the pre-existing division, modular-reduction, and CRT-related admissions.

**Next step**

- Continue reducing the remaining Euclid admissions, likely starting with the modular addition/multiplication preservation lemmas or the CRT congruence helpers that depend on `congr_mod`.

**Blockers**

- No build blockers.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
