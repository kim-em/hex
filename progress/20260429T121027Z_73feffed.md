**Accomplished**

- Claimed feature issue `#1034` for `HexGfqField` Phase 3 conformance.
- Added `HexGfqField/Conformance.lean` with Lean-only core checks for the finite-field wrapper operations over a fixed `F_5[x] / (x^2 + x + 1)` fixture.
- Re-exported the conformance module from `HexGfqField.lean`.
- Added `HexGfqField` to the conformance workflow matrix.
- Bumped `HexGfqField.done_through` from `2` to `3` in `libraries.yml`.

**Current frontier**

- `HexGfqField` now reports Phase 4 as its next phase, blocked on `HexGfqRing.done_through >= 4`.
- The conformance fixture uses a private irreducibility witness because `FiniteField` requires the proof argument while executable operations erase it.

**Next step**

- Advance `HexGfqRing` Phase 4 before starting `HexGfqField` Phase 4 benchmarking.

**Blockers**

- None for this issue.
