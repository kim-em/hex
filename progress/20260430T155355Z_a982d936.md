**Accomplished**

- Claimed issue #1385.
- Added private inversion-count append/cross helpers in `HexMatrix/Determinant.lean`.
- Proved adjacent-swap inversion parity and the separated-swap parity theorem
  `inversionCount_swap_separated_parity`.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

**Current frontier**

- The separated list-swap parity lemma is now available for the successor
  transpose bridge work in #1384.
- Remaining determinant placeholders are still in row-swap product/sign/sum
  helpers and duplicate-row cancellation, outside this issue's scope.
- An unrelated pre-existing `.claude/CLAUDE.md` worktree modification remains
  uncommitted.

**Next step**

- Continue the row-swap path by using this separated-swap parity theorem to
  bridge `transposePermutationValues` to a private `detSign` flip lemma.

**Blockers**

- None.
