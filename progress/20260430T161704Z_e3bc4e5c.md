**Accomplished**

- Claimed issue #1401.
- Added private list/vector split helpers in `HexMatrix/Determinant.lean`.
- Proved `transposePermutationValues_toList_of_lt` in the separated swap shape.
- Proved the symmetric `transposePermutationValues_toList_of_gt` bridge by commuting the value transpose.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

**Current frontier**

- The separated toList bridge is now available for packaging the determinant
  sign parity flip in blocked issue #1402.
- Existing determinant row-swap product/sign/sum and duplicate-row placeholders
  remain outside this issue's scope.
- An unrelated pre-existing `.claude/CLAUDE.md` worktree modification remains
  uncommitted.

**Next step**

- Use these toList bridges with `inversionCount_swap_separated_parity` to prove
  the private `detSign` flip for `transposePermutationValues`.

**Blockers**

- None.
