**Accomplished**

- Claimed repair PR `#887`.
- Merged `origin/main` into `agent/aa303716` and resolved the single conflict in `HexPolyFp/Basic.lean`.
- Kept the PR's `FpPoly.one_mul` / `FpPoly.mul_one` proofs and main's coefficient-formula / distributivity block.
- Verified `lake build HexPolyFp`, `lake build`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexPolyFp`, the `native_decide` scan, and `git diff --check`.

**Current frontier**

- PR `#887` is locally repaired and ready to push.

**Next step**

- Push the repaired PR branch and mark the PR salvaged through coordination.

**Blockers**

- None.
