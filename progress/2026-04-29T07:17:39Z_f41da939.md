**Accomplished**

- Claimed issue #861 for HexMatrixMathlib Phase 3 conformance checks.
- Added `HexMatrixMathlib.Conformance` with always-on guards for executable-to-Mathlib round trips, Mathlib-to-executable round trips, row-operation bridge behavior, determinant bridge behavior, and core zero/identity/singular/pivot edge cases.
- Imported the conformance module from `HexMatrixMathlib.lean`.
- Advanced `HexMatrixMathlib.done_through` from 2 to 3 in `libraries.yml`.
- Verified `lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexMatrixMathlib`, and the local banned-placeholder scan for the touched HexMatrixMathlib files.

**Current frontier**

- `HexMatrixMathlib` now reports Phase 4 as its next frontier and is blocked by `HexMatrix.done_through >= 4`.

**Next step**

- Continue with Phase 4 only after the executable `HexMatrix` library has advanced far enough to satisfy the dependency gate.

**Blockers**

- None for this issue.
