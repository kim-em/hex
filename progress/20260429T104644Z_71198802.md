**Accomplished**

- Claimed `#980` and repaired `HexArith.powMod`'s word-sized odd-modulus hot path by replacing repeated big-`Nat` `% 2` and `/ 2` exponent traversal with `Nat.testBit` over a precomputed binary length.
- Applied the same bit-position traversal shape to the Nat fallback exponentiation loop.
- Verified `runPowMod` now reports `consistent with declared complexity` with `β=+0.069` on the committed scientific schedule.
- Re-ran HexArith benchmark smoke and compare checks; all six benchmark registrations verify, and both registered compare groups agree on common parameters.

**Current frontier**

- `HexArith.done_through` remains `3` as required by issue scope; this PR resolves the `runPowMod` Phase 4 blocker but does not bump the library to Phase 4 complete.
- Existing HexArith proof `sorry` warnings remain unchanged.

**Next step**

- Review/merge the `#980` fix, then run the final HexArith Phase 4 completion issue that can update `libraries.yml` if no other HexArith findings remain.

**Blockers**

- None for this issue.
