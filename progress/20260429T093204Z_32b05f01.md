**Accomplished**

- Claimed `#935`.
- Extended `HexPoly/Bench.lean` with deterministic nontrivial-content integer polynomial inputs.
- Added `runContent` and `runPrimitivePartChecksum` benchmark targets with `O(n)` registrations.
- Updated the HexPoly benchmark module docstring so only polynomial CRT remains listed for a later Phase 4 slice.
- Verified `lake build HexPoly.Bench`, `lake exe hexpoly_bench list`, `lake exe hexpoly_bench verify`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-pattern scan.

**Current frontier**

- HexPoly Phase 4 now covers dense integer core operations, Rat Euclidean operations, and integer content/primitive-part helpers in the bench executable.

**Next step**

- Add the remaining HexPoly Phase 4 polynomial CRT benchmark slice in a separate issue.

**Blockers**

- No blockers for this slice.
