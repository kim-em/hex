**Accomplished**

- Claimed feature issue `#1023` for the active one-hot monomial fold contribution in `HexGF2/Multiply.lean`.
- Added private bit-level lemmas specializing `clmul_oneHot_snd` and `clmul_oneHot_fst` to no-carry and carry word positions.
- Added private coefficient-update lemmas for `xorClmulAt` with an in-word one-hot multiplier.
- Added active monomial-word update lemmas for `(monomial k).words[k / 64]!`, covering both no-carry and carry-to-next-word cases at target coefficient `n + k`.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- The zero-prefix and active-word raw update pieces are now available for the successor assembly of the right monomial multiplication theorem.

**Next step**

- Use these helpers in the successor issue to assemble the full raw coefficient bridge for `mulWords q.words (words_monomial k)`, then expose the public right-oriented theorem.

**Blockers**

- The requested full `rg -n "axiom|native_decide|sorry|admit" HexGF2 -S` still reports pre-existing proof placeholders and the smoke-test axiom in `HexGF2/Field.lean`, `HexGF2/Euclid.lean`, and `HexGF2/Smoke.lean`; this session did not add new hits.
