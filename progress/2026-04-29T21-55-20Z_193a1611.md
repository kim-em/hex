**Accomplished**

- Claimed issue #1148 and added the Mathlib-facing Rabin finite-field glue theorem surface in `HexBerlekampMathlib.Basic`.
- Introduced the local `Rabin.frobeniusPolynomial` surface and theorem statements for `AdjoinRoot` vanishing, quotient maps into `GaloisField`, finrank/natDegree, degree divisibility, irreducible non-coprimality to divisibility, divisor arithmetic, and executable Rabin-test bridges.
- Added `rabin_irreducible_of_positive_degree` with explicit prime-field and positive-degree hypotheses while leaving the existing `rabin_irreducible` statement intact.
- Verified with `lake build HexBerlekampMathlib`, `python3 scripts/status.py HexBerlekampMathlib`, `python3 scripts/check_dag.py`, `git diff --check`, the banned-pattern scan, and a `sorry` scan confirming only proof-position obligations.

**Current frontier**

`HexBerlekampMathlib.Basic` now exposes the Rabin theorem names and types needed by downstream Mathlib proof work, with proof bodies intentionally left as `sorry` where the issue allowed proof placeholders.

**Next step**

Replace the new Rabin `sorry` proof bodies using Mathlib finite-field results, executable transport lemmas, and the existing Berlekamp/Rabin implementation invariants.

**Blockers**

None for the theorem-surface work. Full proof completion still depends on proving or importing the executable-to-Mathlib transport facts for Frobenius remainders, gcd/coprimality, and degree bookkeeping.
