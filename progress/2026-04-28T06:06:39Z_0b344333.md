**Accomplished**

- Claimed issue `#441` and added [HexMatrix/RowEchelon.lean](/home/kim/hex/worktrees/0b344333/HexMatrix/RowEchelon.lean) with executable row operations: `rowSwap`, `rowScale`, and `rowAdd`.
- Added the Phase 1 echelon-form surface in the same module: `RowEchelonData`, `IsEchelonForm`, `IsRREF`, and the attached `freeCols` / column-partition theorem API.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/0b344333/HexMatrix.lean) to export the new module and verified `lake build HexMatrix` plus `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrix` now has the row-operation layer and echelon-form data contracts needed by the later matrix issues, but the proof obligations in the column-partition API remain scaffolded with `sorry`.

**Next step**

- Land this issue and let the blocked follow-ons (`rref`/span/nullspace and determinant/Bareiss) build against the new surface.

**Blockers**

- None.
