**Accomplished**

- Claimed issue #1705 for the HexArith extended-GCD bridge proofs.
- Corrected the pure Int extended-GCD terminal case so negative final
  remainders return negated Bezout coefficients matching the reported
  nonnegative gcd.
- Proved the shared `pureIntExtGcd.go` gcd/Bezout loop invariant, then closed
  `Hex.pureIntExtGcd_fst`, `Hex.pureIntExtGcd_bezout`,
  `HexArith.Int.extGcd_fst`, `HexArith.Int.extGcd_bezout`,
  `HexArith.UInt64.extGcd_fst`, and `HexArith.UInt64.extGcd_bezout`.
- Verified `lake build HexArith.ExtGcd`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token scan on `HexArith/ExtGcd.lean`.

**Current frontier**

- The extended-GCD proof bridge in `HexArith/ExtGcd.lean` is complete.
- HexArith Phase 5 still has existing Nat prime placeholders in
  `HexArith/Nat/Prime.lean`.

**Next step**

- Continue HexArith Phase 5 by claiming a focused issue for the Nat prime
  binomial/Fermat proof cluster.

**Blockers**

- None.
