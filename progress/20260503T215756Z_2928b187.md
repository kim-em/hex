**Accomplished**

- Claimed #1826 and added the private rational scaling helper
  `rat_scale_mul_scale` in `HexPolyZ/Basic.lean`.
- Proved supporting fold lemmas for `DensePoly.mulCoeffStep` /
  `DensePoly.mulCoeffSum`, including the nonzero-scalar size preservation
  needed because `DensePoly.scale 0` normalizes to zero.
- Preserved executable dense-polynomial behavior; changes are proof-only.

**Current frontier**

- `lake build HexPolyZ.Basic` passes with only pre-existing `sorry` warnings.
- `lake build HexPolyZ` passes.
- `python3 scripts/status.py HexPolyZ` reports HexPolyZ ready in Phase 5.
- `python3 scripts/check_dag.py` passes.
- `git diff --check` passes.
- Touched Lean file scan found no `axiom`, `native_decide`, `TODO`, or `FIXME`.

**Next step**

- Use `rat_scale_mul_scale` in the downstream rational division
  reconstruction work for #1824.

**Blockers**

- None.
