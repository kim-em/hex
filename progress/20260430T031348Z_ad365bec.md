**Accomplished**

- Claimed #1256 and proved `zero_mod_eq_zero` in `HexPoly/Euclid.lean`.
- Kept the public statement surfaces for `mod_mul_self_left`, `mod_add_mul_self`,
  `mod_eq_mod_of_dvd_sub`, and `mod_eq_mod_of_congr` unchanged.
- Verified `lake build HexPoly`, `python3 scripts/status.py HexPoly`,
  `python3 scripts/check_dag.py`, `git diff --check`, and the requested
  `rg` sorry scan.

**Current frontier**

- The zero-dividend canonical remainder leaf is discharged.
- Upstream has discharged the sibling `mod_self_eq_zero` leaf.
- CRT congruence helpers `polyCRT_congr_fst` and `polyCRT_congr_snd` remain
  admitted outside this issue.

**Next step**

- Return to the CRT Bezout congruence helpers in #1233.

**Blockers**

- None for this issue.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
