**Accomplished**

- Claimed feature issue #1322 for the `HexGfqField` inverse xgcd proof support cluster.
- Added public `FpPoly.scale_add` and `FpPoly.scale_mul_left` lemmas for distributing `DensePoly.scale` over addition and left multiplication.
- Removed the redundant local `ZMod64` division instance from `HexGfqRing.Basic`, keeping quotient code on the shared `HexPolyFp.Basic` division instance.
- Added `GFqRing.reduceMod_self` and `GFqRing.reduceMod_add_mul_self_right` for eliminating an added right multiple of the modulus under quotient reduction.
- Replaced the `sorry`s in `scaled_xgcd_repr_bezout` and `reduceMod_repr_mul_invPoly_eq_scaled_gcd`.
- Verified `lake build HexGfqField`, `python3 scripts/status.py HexGfqField`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexGfqField/Operations.lean` now has the scaled Bezout and quotient-reduction bridge needed before the final inverse normalization proof.
- The remaining local `HexGfqField` placeholder is `reduceMod_repr_mul_invPoly_eq_one`, which depends on the irreducible-gcd normalization theorem and remains intentionally out of scope for #1322.

**Next step**

- Work #1323: prove the irreducible-modulus normalization result that reduces the normalized xgcd gcd witness to one, then close the remaining `reduceMod_repr_mul_invPoly_eq_one` gap.

**Blockers**

- No blocker for #1322.
- The touched files still report pre-existing proof placeholders in `HexPolyFp.Basic`; `HexGfqField/Operations.lean` retains only the out-of-scope inverse-normalization `sorry`.
- The worktree still has a pre-existing unrelated `.claude/CLAUDE.md` modification that this session did not touch.
