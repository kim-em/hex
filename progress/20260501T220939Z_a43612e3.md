**Accomplished**

- Claimed issue #1703 for `HexArith.powMod_eq`.
- Proved `powModNat_eq` for the Nat repeated-squaring fallback via a factored
  `powModNatGo` loop invariant.
- Proved `powModWordOdd_eq` for the Montgomery path via a `powMontBitsGo`
  invariant using the existing `MontCtx.fromMont_toMont` and
  `MontCtx.mulMont_repr` bridge theorems.
- Proved the public theorem `HexArith.powMod_eq` by dispatching over the
  zero-modulus, word-sized odd Montgomery, word-sized fallback, and large-Nat
  fallback branches.
- Verified `lake build HexArith.Montgomery.Context`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token scan on
  `HexArith/Montgomery/Context.lean`.

**Current frontier**

- The Montgomery-facing `powMod` correctness theorem is complete.
- Existing out-of-scope HexArith Phase 5 placeholders remain in
  `HexArith/Nat/Prime.lean` and `HexArith/ExtGcd.lean`.

**Next step**

- Continue HexArith Phase 5 by claiming a focused issue for the remaining
  Nat prime or extended-GCD proof placeholders.

**Blockers**

- None.
