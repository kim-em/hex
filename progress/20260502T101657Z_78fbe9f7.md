**Accomplished**

- Claimed #1818, identified that its target statement is stale without a prime-modulus field assumption, added a corrected issue note, and released it for replan.
- Claimed #1813 and replaced the false general `yunFactorsContribution_derivative_active_loop_invariant` surface with an initial-state invariant specialized to `g = DensePoly.gcd f (DensePoly.derivative f)` and `c = f / g`.
- Split the remaining derivative-active Yun proof work into the concrete `yunFactorsContribution_initial_state_done` and `yunFactorsContribution_initial_state_tail` helper obligations.
- Routed `yunFactorsContribution_derivative_active_split_algebra_succ` through the specialized initial-state invariant instead of a free `c * w = f` loop invariant.
- Verified with `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over touched polynomial files.

**Current frontier**

- The derivative-active reconstruction path no longer depends on the false `c * w = f`-only invariant.
- The remaining hard proof holes are now the terminal and recursive-tail cases for the true Yun initial state.

**Next step**

- Prove `yunFactorsContribution_initial_state_done` and `yunFactorsContribution_initial_state_tail`, using the initial gcd/division split and the existing Yun step-combiner lemmas.

**Blockers**

- #1818 needs replan before implementation because the concrete `divMod` remainder-degree statement needs a prime-modulus or equivalent field assumption.
