**Accomplished**

- Claimed issue `#877`.
- Extended `HexModArith/Conformance.lean` with hot-loop checks for `BarrettCtx.mulMod`, `MontCtx.toMont`, `MontCtx.mulMont`, and `MontCtx.fromMont`.
- Added typical, edge, and adversarial Barrett/Montgomery modulus fixtures and checked results against core `ZMod64` or Nat modular contracts.
- Bumped `libraries.yml` so `HexModArith.done_through = 3`.
- Verified `lake build HexModArith`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexModArith`, the requested forbidden-pattern scan, and `git diff --check`.

**Current frontier**

- `HexModArith` now reports Phase 4 readiness, blocked on `HexArith.done_through >= 4`.
- Hot-loop conformance coverage is in the main conformance module imported by `HexModArith.lean`.

**Next step**

- Continue with the next unclaimed feature/review issue, or advance `HexArith` so `HexModArith` Phase 4 can proceed.

**Blockers**

- None.
