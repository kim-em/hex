**Accomplished**

- Claimed #1371, found its unrestricted row-swap/sign lemma shape is false for
  arbitrary vectors with duplicate entries, and decomposed it into #1377,
  #1378, and #1379.
- Claimed #1377 and added private `HexMatrix/Determinant.lean` helpers proving
  `permutationVectors` entries have `toList.Nodup`.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-pattern scan on
  `HexMatrix/Determinant.lean`.

**Current frontier**

- Row-swap determinant proof now has the permutation-vector distinctness fact
  needed to state the term/sign lemmas truthfully.
- Remaining row-swap successors are the inversion parity flip under index
  transposition (#1378) and the restricted row-swap term/sum proof (#1379).

**Next step**

- Prove the `detSign` flip for `transposePermutationValues` under a distinctness
  hypothesis, then use it to repair the row-swap term lemma with membership
  evidence from `permutationVectors_nodup`.

**Blockers**

- None for #1377.
