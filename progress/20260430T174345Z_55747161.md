**Accomplished**

- Claimed issue #1363 and continued the HexHensel linear one-step factor
  congruence proof path.
- Added `FpPoly.modP_liftToZ`, proving that reducing a canonical integer lift
  back modulo `p` recovers the original `FpPoly`.
- Added `FpPoly.congr_liftToZ_modP`, proving that every `ZPoly` is congruent
  modulo `p` to the canonical integer lift of its `modP` reduction.
- Added the local `congr_liftToZ_of_modP_eq` transport helper in
  `HexHensel/Linear.lean`.
- Verified the partial slice with `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern
  scan over touched HexHensel files.

**Current frontier**

- `linearHenselStep_raw_factor_congr` remains the active placeholder.
- The `modP`/`liftToZ` congruence bridge now exists for transporting an `FpPoly`
  equality back to a `ZPoly.congr` modulo `p`.
- The next missing proof layer is the actual one-step correction identity using
  `DensePoly.divMod_spec` for `DensePoly.divMod (t * eMod) gMod`, followed by
  scaling from modulo `p` to modulo `p^(k+1)`.

**Next step**

- Prove the field-polynomial identity behind the correction:
  `r * hMod + gMod * (s * eMod + q * hMod) = eMod`, using the Bezout
  congruence and `DensePoly.divMod_spec`.
- Transport that identity through `congr_liftToZ_of_modP_eq`, combine it with
  `scale_coeffwiseDiv_sub_of_congr`, and discharge the `p^(k+1)` divisibility
  calculation for the raw theorem.

**Blockers**

- No build blockers.
- The full raw theorem is still larger than the bridge slice completed here;
  this session should land as partial progress for planner narrowing.
