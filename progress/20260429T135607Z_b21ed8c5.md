**Accomplished**

- Claimed review issue `#1098`.
- Re-reviewed `HexBerlekamp` after the irreducibility certificate repair from PR `#1095`, with focus on the self-contained Rabin certificate surface and downstream `HexBerlekampZassenhaus` expectations.
- Confirmed the Phase 2 forbidden-vocabulary grep is clean, all `HexBerlekamp` `sorry` hits are proof-only theorem bodies, there are no extern shims, and the only native-shaped hit is the `Fin` index used by the committed `Vector` API.
- Added `status/hex-berlekamp.scaffolding-reviewed` and bumped `HexBerlekamp.done_through` to `2`.
- Verified `lake build HexBerlekamp`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested review greps.

**Current frontier**

- `HexBerlekamp` now advances to Phase 3 conformance testing.
- `HexConway` remains blocked until `HexBerlekamp.done_through >= 3`.

**Next step**

- Add the Phase 3 conformance module/checks for `HexBerlekamp`.

**Blockers**

- None.
