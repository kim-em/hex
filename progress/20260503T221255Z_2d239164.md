**Accomplished**

- Claimed #1824 and added the narrow `Rat[x]` reconstruction helper
  `rat_div_gcd_mul_reconstruct` in `HexPolyZ/Basic.lean`.
- Added local proof-only `DensePoly.DivModLaws Rat` and
  `DensePoly.GcdLaws Rat` bridges needed to reuse the generic
  `div_mul_add_mod`, `mod_eq_zero_of_dvd`, and `gcd_dvd_left` APIs.
- Preserved executable polynomial division, gcd, rational arithmetic,
  and integer primitive-part behavior.

**Current frontier**

- Verification passed:
  - `lake build HexPolyZ.Basic`
  - `lake build HexPolyZ`
  - `python3 scripts/status.py HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- Builds report placeholder warnings for the new local Rat law packages
  and the existing HexPolyZ proof frontier.

**Next step**

- Use `rat_div_gcd_mul_reconstruct` in
  `primitiveSquareFreeDecomposition_reassembly_over_rat` to rewrite the
  nonzero-derivative branch from
  `ratPrimitive / gcd ratPrimitive derivative` times the gcd back to
  `ratPrimitive`.

**Blockers**

- None for this issue.
