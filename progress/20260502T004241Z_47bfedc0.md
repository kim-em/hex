**Accomplished**

- Claimed #1726 and added private source-bit coefficient lemmas in `HexGF2/Multiply.lean` for one-hot right inputs:
  `clmul_oneHot_source_low` and `clmul_oneHot_source_high`.
- Added supporting finite XOR range lemmas that reduce the `List.range 64`
  source-bit fold to the unique contributing source bit, or to `false` when
  the target bit has no in-word source.
- Verified `lake build HexGF2.Multiply`, `lake build HexGF2`,
  `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scans for newly added forbidden placeholders.

**Current frontier**

- #1726 is ready for PR. The one-hot leaf now expands
  `clmul x ((1 : UInt64) <<< b.toUInt64)` low/high selected bits over
  source bits of `x`, matching the bit-fold layer from #1725.
- Existing downstream source-triple placeholders in `HexGF2/Multiply.lean`
  remain untouched and are outside this issue.

**Next step**

- Claim #1727 after #1726 lands and assemble the full source-pair bit
  expansion lemmas from the right-bit-fold and one-hot source-bit layers.

**Blockers**

- None for #1726.
