**Accomplished**

- Claimed #1880, verified it was stale because the no-pivot loop bordered-minor invariant is still open as #1893, and marked it for replan with that dependency noted.
- Claimed #1909 and added `det_borderedMinor_succ_finalColumn_expansion` in `HexMatrix.Determinant`, specializing final-column expansion to the next bordered minor and rewriting the diagonal term into Bareiss pivot shape.
- Created residual issue #1925 for the remaining off-diagonal/product algebra needed to finish the general bordered-minor Desnanot-Jacobi theorem.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix.Bareiss`
  - `lake build HexMatrix`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Determinant.lean`
- `lake build HexMatrix` still reports pre-existing warnings/sorries in `HexMatrix.Bareiss` and `HexMatrix.RREF`; no new placeholders were added in `HexMatrix.Determinant`.

**Next step**

- Use #1925 to prove the residual off-diagonal identity and then package the full Bareiss-friendly bordered-minor Desnanot-Jacobi theorem.

**Blockers**

- None for the landed expansion slice.
