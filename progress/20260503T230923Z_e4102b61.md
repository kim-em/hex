**Accomplished**

- Claimed #1853 and proved `HexPolyFp.Basic`'s
  `divMod_remainder_degree_lt_core`.
- Added generic `DensePoly.divModArray`/`divMod` remainder-degree helpers
  in `HexPoly/Euclid.lean`, parameterized by the leading-term cancellation
  law needed by field-style division.
- Instantiated the cancellation law for `ZMod64 p` using prime-modulus
  inverse cancellation and the normalized-polynomial leading coefficient.

**Current frontier**

- Verification passed:
  - `lake build HexPoly.Euclid`
  - `lake build HexPolyFp.Basic`
  - `lake build HexPolyFp`
  - `python3 scripts/status.py HexPolyFp`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- The target `divMod_remainder_degree_lt_core` no longer has a `sorry`.
  Other pre-existing `HexPolyFp.Basic` placeholders remain outside this
  issue's scope.

**Next step**

- Continue with the remaining `DensePoly.DivModLaws` and `GcdLaws`
  placeholders in separate scoped work items.

**Blockers**

- None.
