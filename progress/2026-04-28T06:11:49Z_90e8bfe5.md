**Accomplished**

- Claimed issue `#440` and added the Montgomery Phase 1 scaffold under `HexArith/Montgomery/`.
- Implemented executable definitions for `redcNat`, `montPosInv` / `montInv`, `redc`, `MontCtx.toMont` / `fromMont` / `mulMont`, and `powMod`.
- Added the specified theorem surface for the Nat layer, the `UInt64` bridge, the user-facing Montgomery API, and `powMod_eq`, then wired the new modules into `HexArith.lean`.
- Verified with `lake build HexArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexArith` now has the Montgomery scaffold and executable path, but the separate Barrett scaffold from issue `#439` is still outstanding.
- The new Montgomery theorem statements are scaffolded with `sorry`; later proof work will need to discharge the REDC, inverse, and user-facing correctness lemmas.

**Next step**

- Land the `#440` PR, then pick up `#439` or split follow-up proof work for the Montgomery correctness layer if Phase 1 needs narrower residual tasks.

**Blockers**

- None.
