**Accomplished**

- Claimed #1815 and proved `ratPolyPrimitivePart_rational_associate_core`
  in `HexPolyZ/Basic.lean`.
- Factored the denominator-cleared integer polynomial into a private
  `ratPolyPrimitivePartCleared` helper so the proof can reuse the same
  executable coefficients as `ratPolyPrimitivePart`.
- Added private proof helpers for common-denominator positivity,
  coefficient denominator divisibility, rational reconstruction from
  `content_mul_primitivePart`, and the `normalizePrimitiveSign` negative
  sign branch.

**Current frontier**

- Verification passed:
  - `lake build HexPolyZ.Basic`
  - `lake build HexPolyZ`
  - `python3 scripts/status.py HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- `HexPolyZ.Basic` still reports existing placeholder warnings for later
  primitive square-free proof obligations.

**Next step**

- Continue with the claimed square-free rational reassembly work in #1825
  once that session is ready, now using the proof-backed
  `ratPolyPrimitivePart_rational_associate` theorem.

**Blockers**

- None for #1815.
