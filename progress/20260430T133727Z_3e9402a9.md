**Accomplished**

- Claimed #1357 and added the last-insertion identity determinant helper
  cluster in `HexMatrix/Determinant.lean`.
- Proved product preservation for inserting `Fin.last n` at the last slot
  over the identity matrix.
- Proved inversion-count/sign preservation for appending the last index, and
  combined it into `detTerm_identity_insertAt_last`.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans for the touched diff.

**Current frontier**

- The last-insertion identity determinant term preservation needed by #1356 is
  now available as private helper lemmas.
- `permutationVectors_identity_sum`, row-swap, and row-add cancellation remain
  the active `HexMatrix/Determinant.lean` proof placeholders.

**Next step**

- Work #1356 to combine the non-last zero case and last-insertion preservation
  into the identity Leibniz fold proof.

**Blockers**

- None.
