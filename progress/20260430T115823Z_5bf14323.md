**Accomplished**

- Claimed issue #1326 and threaded explicit `Hex.Nat.Prime p` evidence through
  the `HexGfqField.FiniteField` type, constructors, casts, operations, field
  instances, Frobenius surface, and characteristic-`p` instance.
- Updated `HexConway.SupportedEntry`, the public `HexGfq` wrapper, GF2 mathlib
  bridge adapters, `HexGfqField` conformance checks, and benchmark inputs to
  provide prime evidence at call sites.
- Kept `HexGfqField` mathlib-free by certifying the benchmark modulus `8191`
  with a local finite divisor checker instead of importing Mathlib.
- Verified the affected targets and policy checks:
  `lake build HexGfqField`, `lake build HexGfq`,
  `lake build HexGF2Mathlib`, `lake build HexGfqField.Conformance`,
  `lake exe hexgfqfield_bench list`,
  `lake exe hexgfqfield_bench verify`,
  `python3 scripts/status.py HexGfqField`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `FiniteField` now carries both the prime-modulus proof and irreducibility
  proof explicitly, so downstream field APIs no longer advertise field
  structure over arbitrary bounded moduli.
- Existing proof placeholders and benchmark/conformance irreducibility axioms
  remain as they were; this session did not introduce new proof escapes.

**Next step**

- Review and merge the PR for issue #1326.

**Blockers**

- None for this issue. A pre-existing unrelated `.claude/CLAUDE.md` worktree
  modification remains unstaged.
