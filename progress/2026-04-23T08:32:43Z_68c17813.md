**Accomplished**

- Claimed feature issue `#314` and added [`HexGfqField/Instances.lean`](/home/kim/hex/worktrees/68c17813/HexGfqField/Instances.lean:1) to package `FiniteField p f hf hirr` with the missing additive, cast, `Lean.Grind.Field`, integer-power, and `IsCharP` scaffolding promised by the Phase 1 spec.
- Updated [`HexGfqField.lean`](/home/kim/hex/worktrees/68c17813/HexGfqField.lean:1) to re-export the new instance module and document the expanded wrapper-level surface.
- Verified the issue slice with `lake build HexGfqField`; the target now compiles successfully with the new wrapper instance layer in place.

**Current frontier**

- `HexGfqField` now exposes the spec-facing typeclass boundary needed to move its Phase 1 scaffold forward, while still leaving the executable inverse/law proofs as explicit stubs for later refinement.
- The immediate downstream unblock is planner/review work to bump `HexGfqField.done_through` once this slice lands, which then opens the `HexGF2Mathlib` and `HexGfq` dependency chain.

**Next step**

- Publish the branch as the PR for `#314`.
- After merge, queue the `HexGfqField` review/phase-bump issue or the next downstream Phase 1 consumer.

**Blockers**

- None in this session.
