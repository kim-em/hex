**Accomplished**

- Claimed issue `#437` and added the Phase 1 `DensePoly` arithmetic layer in [HexPoly/Operations.lean](/home/kim/hex/worktrees/b4f7ac2f/HexPoly/Operations.lean).
- Implemented executable addition, subtraction, schoolbook multiplication, Horner evaluation, composition, derivative, and supporting `Add`/`Sub`/`Mul` instances while preserving normalization through `ofCoeffs`.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/b4f7ac2f/HexPoly.lean) to export the new operations module.
- Verified `lake build HexPoly`, `lake build`, and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexPoly` now exposes the core executable arithmetic API needed before the division/GCD/CRT follow-up issue `#443`.

**Next step**

- Commit and publish the `#437` PR so the downstream polynomial and finite-field work blocked on `#443` can proceed once this lands.

**Blockers**

- None.
