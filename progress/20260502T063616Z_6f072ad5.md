**Accomplished**

- Claimed #1790 and proved local Mathlib-free rational zero-norm helpers in `HexGramSchmidt/Basic.lean`.
- Added the dot-product annihilation lemma for `Matrix.dot v v = 0`.
- Removed the nonzero-norm assumption from `dot_reduceAgainstBasis_of_mem` by splitting the projection proof into zero-norm and nonzero-norm cases.
- Verified with `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and placeholder scans on `HexGramSchmidt/Basic.lean`.

**Current frontier**

- The reduce-against-basis orthogonality helper now handles degenerate accumulated rows.
- #1791 can use this to wire the `basisRowsAux` orthogonality invariant toward public `basis_orthogonal`.

**Next step**

- Work #1791: prove the `basisRowsAux` pairwise orthogonality invariant and connect it to the public Gram-Schmidt basis orthogonality theorem.

**Blockers**

- None.
