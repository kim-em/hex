**Accomplished**

- Claimed #1499 and repaired the public `DensePoly.divMod_spec` theorem boundary in `HexPoly/Euclid.lean`.
- Moved the division reconstruction invariant into the existing `DensePoly.DivModLaws` law package, then proved `divMod_spec` and `div_mul_add_mod` by projecting that law.
- Narrowed the modular congruence helper surface that depends on reconstruction to require `DivModLaws`.
- Added the corresponding `ZMod64` law-field obligation to the existing `HexPolyFp.Basic` instance so downstream typeclass resolution still works.
- Verified `lake build HexPoly.Euclid`, `lake build HexPoly`, `lake build HexPolyFp.Basic`, `lake build HexHensel.Linear`, `python3 scripts/status.py HexPoly`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexPoly/Euclid.lean` no longer has a `sorry` in `divMod_spec`; the remaining `HexPoly.Euclid` warnings are the pre-existing gcd/xgcd and modular reduction placeholders outside #1499.
- The concrete finite-field `DensePoly.DivModLaws (ZMod64 p)` instance now includes reconstruction as an explicit proof obligation, alongside its existing admitted fields.

**Next step**

- Prove the concrete `ZMod64` `DivModLaws` fields, starting with `divMod_spec`, or continue the remaining `HexPoly.Euclid` Phase 5 placeholders tracked by separate issues.

**Blockers**

- None.
