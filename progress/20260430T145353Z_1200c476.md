**Accomplished**

- Claimed #1381, assessed the row-swap parity task, and decomposed it into
  #1383 for list-level separated-swap parity and #1384 for the
  `transposePermutationValues`/`detSign` bridge.
- Claimed #1383 and added private `HexMatrix/Determinant.lean` foundations:
  `crossInversionCount`, `inversionFold_start`, `inversionFold_append`, and
  `inversionCount_append`.
- Created residual issue #1385 for the remaining separated-swap parity theorem
  over the new append/cross-inversion decomposition.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-pattern scan for `axiom`,
  `native_decide`, `TODO`, and `FIXME` in `HexMatrix/Determinant.lean`.

**Current frontier**

- `inversionCount_append` now expands inversion counts across list append
  blocks with an explicit cross-inversion term.
- The actual separated-swap odd-parity theorem is not yet proved and is tracked
  by #1385.
- The vector/index bridge to `transposePermutationValues` and the private
  `detSign` flip lemma remain blocked on #1385 and are tracked by #1384.

**Next step**

- Prove #1385 by comparing the cross-inversion contributions for
  `pre ++ a :: mid ++ b :: post` and `pre ++ b :: mid ++ a :: post`, then use
  that result in #1384.

**Blockers**

- None for the append decomposition landed in this session.
