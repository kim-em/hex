**Accomplished**

- Claimed feature issue `#83` after checking the unclaimed queue and choosing the downstream-facing `HexMatrix` API gap over the newer `HexMatrixMathlib` scaffold.
- Added `Vector.normSq` to [HexMatrix/RowEchelon.lean](/home/kim/hex/worktrees/b97a453e/HexMatrix/RowEchelon.lean) as the stable norm-squared helper built from the existing dense-matrix dot product, plus a `normSq_eq_dot` simp theorem.
- Updated the root [HexMatrix.lean](/home/kim/hex/worktrees/b97a453e/HexMatrix.lean) docstring to reflect that the library now re-exports the vector norm-squared surface.
- Verified the change with `lake build HexMatrix`.

**Current frontier**

- Session work is implementation-complete. Remaining work is to commit the `HexMatrix` helper addition and open the PR closing `#83`.

**Next step**

- Stage the two `HexMatrix` edits and this progress note, commit with a feature-scoped message, then run `coordination create-pr 83`.

**Blockers**

- None.
