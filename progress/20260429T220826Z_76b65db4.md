**Accomplished**

- Claimed issue #1168 for the HexGF2 Phase 4 field-wrapper benchmark slice.
- Extended `HexGF2/Bench.lean` with deterministic `GF2n` AES-modulus benchmark registrations for addition, multiplication, inversion, division, and square-and-multiply powering.
- Added deterministic `GF2nPoly` benchmark registrations over a committed degree-128 packed modulus fixture for multiplication, inversion, division, and square-and-multiply powering.
- Verified `lake build hexgf2_bench`, `lake exe hexgf2_bench list`, `lake exe hexgf2_bench verify`, `lake build HexGF2`, `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned-pattern scan.

**Current frontier**

- `HexGF2` remains at `done_through: 3`; this issue intentionally adds field-wrapper registrations without completing every Phase 4 exit criterion or bumping `libraries.yml`.
- The banned-pattern scan still reports pre-existing hits in `HexGF2/Euclid.lean` and `HexGF2/Smoke.lean`.

**Next step**

- Open the PR for #1168 with the field-wrapper benchmark registrations.

**Blockers**

- No technical blockers found.
