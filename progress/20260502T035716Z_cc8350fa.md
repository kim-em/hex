**Accomplished**

- Claimed #1750 for the HexMatrix echelon transform inverse theorem surface.
- Strengthened `IsEchelonForm` with an explicit `transform_right_inv` contract.
- Replaced the `IsEchelonForm.transform_mul_inv` proof placeholder with a proof from the strengthened contract.
- Updated `SPEC/Libraries/hex-matrix.md` so the row-echelon API description matches the repaired theorem surface.
- Verified `lake build HexMatrix.RowEchelon`, `lake build HexMatrix.RREF`, `lake build HexMatrix`, `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-placeholder scan of touched HexMatrix files.

**Current frontier**

- `HexMatrix/RowEchelon.lean` no longer has a proof placeholder for `transform_mul_inv`.
- The computational row-reduction behavior is unchanged.
- HexMatrix remains in Phase 5 because the broader RREF, span, nullspace, and Bareiss proof placeholders are outside this issue's scope.

**Next step**

- Continue HexMatrix Phase 5 by proving the remaining RREF/span/nullspace or Bareiss correctness placeholders selected by future worker issues.

**Blockers**

- None.
