**Accomplished**

- Claimed #1438 and added the initial `HexBerlekamp/Bench.lean` LeanBench
  harness.
- Registered deterministic benchmark targets for Berlekamp matrix construction,
  Rabin irreducibility testing, Berlekamp split-step factorization, and
  distinct-degree factorization over `F_5`, each with an adjacent cost-model
  derivation comment.
- Added the `hexberlekamp_bench` Lake executable and CI smoke commands for
  `list` and `verify`.
- Verified `lake build HexBerlekamp`, `lake build HexBerlekamp.Bench`,
  `lake exe hexberlekamp_bench list`, `lake exe hexberlekamp_bench verify`,
  `python3 scripts/status.py HexBerlekamp`, `python3 scripts/check_phase4.py`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden
  placeholder scan over the touched benchmark/config files.

**Current frontier**

- `HexBerlekamp` now has Phase 4 benchmark wiring and CI smoke coverage.
- The benchmark uses the existing split-step factoring surface because the
  public full `berlekampFactor` API currently requires a `Lean.Grind.Field
  (ZMod64 5)` instance that `HexModArith` does not yet expose.

**Next step**

- Later Phase 4 work can run the new benchmark scientifically and decide
  whether to add comparator runs or a follow-up issue for the missing
  `ZMod64` field instance needed by full kernel-based Berlekamp factorization.

**Blockers**

- None for the initial benchmark harness.
