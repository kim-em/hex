**Accomplished**

- Claimed #1523 and re-read the linked HexPolyFp/HexPoly/HexModArith SPEC and Phase 5 guidance.
- Replaced the opaque body of `pthRoot_pow_mul_prime_of_derivative_zero` with a proof through narrower private lemmas for derivative-zero coefficient support, Frobenius reconstruction of `pthRoot`, and exponent composition for the local square-and-multiply `pow`.
- Split the remaining helper leaves into #1526, #1527, and #1528, and left the required decomposition breadcrumb on #1523.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan for `axiom`, `native_decide`, `TODO`, and `FIXME` in `HexPolyFp/SquareFree.lean`.

**Current frontier**

- `pthRoot_pow_mul_prime_of_derivative_zero` is no longer an opaque proof placeholder.
- The remaining proof leaves introduced for #1523 are:
  - #1526: `pow_pow_mul`
  - #1527: `derivative_zero_coeff_non_pmultiple`
  - #1528: `pthRoot_frobenius_of_derivative_zero`
- Existing unrelated proof frontiers remain for contribution recursion, Yun contribution reconstruction, pairwise coprimeness, and Yun-factor square-freeness.

**Next step**

- Prove the three new helper leaves, starting with `pow_pow_mul` as a generic exponent law and then the coefficient-support/Frobenius pair for `pthRoot`.

**Blockers**

- No build blockers.
- The coefficient-support leaf needs the formal derivative coefficient law connected to `ZMod64` prime characteristic, and the Frobenius leaf needs coefficient-level reconstruction for the sparse `p`-multiple support induced by zero derivative.
