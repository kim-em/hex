**Accomplished**

- Claimed #1787 and proved `ZPoly.ratPolyPrimitivePart_primitive`.
- Added narrow `DensePoly` helpers for content under `-1` scaling, zero scaling, zero content, and zero primitive parts.
- Preserved the executable definitions of `ratPolyPrimitivePart`, `normalizePrimitiveSign`, and primitive square-free decomposition.
- Verified `lake build HexPolyZ.Basic`, `lake build HexPolyZ`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, `git diff --check`, and placeholder scans for touched files.

**Current frontier**

- `ratPolyPrimitivePart` now carries the intended primitive proof through denominator clearing, primitive-part extraction, and sign normalization.
- `HexPolyZ/Basic.lean` still has the existing out-of-scope placeholders for rational reassembly and square-free-core correctness.

**Next step**

- A follow-up HexPolyZ worker can tackle `primitiveSquareFreeDecomposition_reassembly_over_rat` or `primitiveSquareFreeDecomposition_squareFreeCore`.

**Blockers**

- None for #1787.
