**Accomplished**

- Claimed issue `#901` and added `HexPolyZ/Conformance.lean` for the Phase 3 core Lean-only conformance profile.
- Covered the advertised `hex-poly-z` surface: `ZPoly`, `ZPoly.congr`, `ZPoly.coprimeModP`, content/primitive-part APIs, and executable Mignotte helpers.
- Imported the conformance module from `HexPolyZ.lean`, added `HexPolyZ` to the conformance workflow matrix, and bumped `libraries.yml[HexPolyZ].done_through` from `2` to `3`.
- Verified `lake build HexPolyZ`, `python3 scripts/check_dag.py`, `python3 scripts/status.py`, `git diff --check`, and confirmed `rg -n "native_decide" HexPolyZ -S` has no matches.

**Current frontier**

- `scripts/status.py` now treats `HexPolyZ` as Phase 3 complete and lists `HexPolyZ -> Phase 4` as blocked only on `HexPoly.done_through >= 4`.
- The conformance checks intentionally remain core-only; external python-flint/Sage oracle wiring is still deferred to future oracle profiles.

**Next step**

- Continue with `HexPoly` Phase 4 or other ready queue items; `HexPolyZ` Phase 4 can start after `HexPoly` advances through Phase 4.

**Blockers**

- None for this issue.
