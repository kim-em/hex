**Accomplished**

- Claimed #1825 and proved
  `ZPoly.primitiveSquareFreeDecomposition_reassembly_over_rat` in
  `HexPolyZ/Basic.lean`.
- Split the proof across the executable zero-primitive, derivative-zero,
  and gcd quotient/repeated-part branches.
- Reused the rational primitive-part associate witnesses, rational scale
  product helper, and `rat_div_gcd_mul_reconstruct` without changing
  executable polynomial definitions.

**Current frontier**

- Verification passed:
  - `lake build HexPolyZ.Basic`
  - `lake build HexPolyZ`
  - `python3 scripts/status.py HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
- Placeholder scan shows only the pre-existing HexPolyZ proof frontier:
  `ratPolyPrimitivePart_rational_associate_core`, local Rat law packages,
  and `primitiveSquareFreeDecomposition_squareFreeCore`.

**Next step**

- Prove `primitiveSquareFreeDecomposition_squareFreeCore` as the remaining
  Phase 5 square-free decomposition proof item.

**Blockers**

- None for #1825.
