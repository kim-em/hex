**Accomplished**

- Claimed feature issue `#1003`, inspected the raw monomial-fold bridge, and decomposed it into `#1015` and `#1016` after confirming the missing proof splits at the `xorClmulAt` coefficient surface.
- Marked `#1003` as `replan` with the required `Decomposed into #1015, #1016` breadcrumb.
- Claimed feature issue `#1015`.
- Added private coefficient helper lemmas in `HexGF2/Multiply.lean` for the low word, high word, and unaffected word cases of `xorClmulAt`.
- Verified `lake build HexGF2.Multiply`, `git diff --check`, and the target-file scan for `sorry`, `admit`, `axiom`, and `native_decide`.

**Current frontier**

- `#1015` is ready for PR publication.
- `#1016` is blocked on `#1015` and should use these helpers to carry the raw monomial fold through to `q * monomial k = q.mulXk k`.

**Next step**

- Open the PR for `#1015`, then let the blocked follow-up `#1016` proceed after merge.

**Blockers**

- None for `#1015`.
