**Accomplished**

- Claimed issue `#452` and added [HexMatrix/Bareiss.lean](/home/kim/hex/worktrees/e496ceb9/HexMatrix/Bareiss.lean) with an executable Bareiss scaffold over `Int`: a no-pivot recurrence, a row-pivoting wrapper, and `BareissData` for swap/sign bookkeeping.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/e496ceb9/HexMatrix.lean) to export the new determinant module and mention the executable Bareiss path in the library docstring.
- Verified `lake build HexMatrix` and spot-checked the executable API with small `#eval` examples, including a case that requires one row swap.

**Current frontier**

- The root `HexMatrix` library now has the executable determinant path needed for later proof work, but the generic determinant definition and row-operation determinant lemmas remain in issue `#450`.

**Next step**

- Land issue `#452` so the matrix determinant bridge and downstream Gram determinant work can build on the executable Bareiss scaffold.

**Blockers**

- `python3 scripts/check_dag.py` currently fails on a pre-existing repository issue: `HexArith/Montgomery/Context.lean:1 imports Mathlib but HexArith is not a mathlib bridge`.
