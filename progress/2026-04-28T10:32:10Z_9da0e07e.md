**Accomplished**

- Claimed `#680` and tightened the exported `HexPolyFp` square-free API to require an explicit `Nat.Prime p` hypothesis.
- Updated `HexPolyFp/SquareFree.lean` so the executable decomposition entrypoint and its public theorem surface no longer overclaim `F_p[x]` behavior for composite moduli.
- Verified the change with `lake build HexPolyFp` and `python3 scripts/check_dag.py`.

**Current frontier**

- The square-free API now matches the field-dependent contract expected by downstream consumers, and no downstream caller updates were needed because the current tree has no in-repo uses of this surface yet.
- The fix is committed on `agent/9da0e07e` and pushed to `origin`, but PR creation did not complete because the GitHub GraphQL API rate limit was exhausted during `coordination create-pr`.

**Next step**

- Re-run `coordination create-pr 680 "fix: require prime modulus for HexPolyFp square-free API"` once the GitHub API rate limit resets so the pushed branch can be published as the closing PR for `#680`.

**Blockers**

- GitHub GraphQL API rate limiting blocked `coordination create-pr` after the branch push.
