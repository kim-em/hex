**Accomplished**

- Claimed issue #1321 for the `HexMatrix` determinant row-operation proof cluster.
- Added proved determinant-style fold lemmas in `HexMatrix/Determinant.lean`:
  `foldl_det_sum_congr`, `foldl_det_sum_mul_left`, and
  `foldl_det_sum_mul_left_zero`.
- Reworked `det_rowScale_leibniz` so the theorem is now proved from the
  per-term row-scaling obligation `detTerm_rowScale` plus the fold lemmas.
- Replaced the broad public helper bodies with named private foundation
  obligations for identity, row swap, row scale, and row addition.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-pattern scan for `native_decide`,
  `axiom`, `TODO`, and `FIXME` in `HexMatrix/Determinant.lean`.

**Current frontier**

- `HexMatrix/Determinant.lean` still has four private proof obligations:
  `permutationVectors_identity_sum`, `permutationVectors_rowSwap_sum`,
  `detTerm_rowScale`, and `permutationVectors_rowAdd_sum`.
- The public determinant API and executable determinant definitions were
  unchanged.

**Next step**

- Prove `detTerm_rowScale` first; it is now isolated from the fold algebra.
- Then split the permutation-vector obligations into enumeration completeness,
  sign under transposition, and duplicate-row cancellation lemmas.

**Blockers**

- None.
