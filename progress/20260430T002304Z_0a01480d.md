**Accomplished**

- Claimed issue #1188 for the HexArith UInt64 wide-word bridge proofs.
- Proved `toNat_mulHi`, `toNat_mulFull`, `mulHi_mulLo`, and `toNat_addCarry` in `HexArith/UInt64/Wide.lean`.
- Corrected the false `toNat_subBorrow` reconstruction statement to the standard borrow law `d + rhs = a + bout * word`, matching existing conformance usage, and proved it.
- Verified `lake build HexArith.UInt64.Wide`, `lake build HexArith`, `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`, `git diff --check`, the required target `sorry` grep, and the forbidden-placeholder grep.

**Current frontier**

- `HexArith/UInt64/Wide.lean` has no remaining `sorry` occurrences.
- `HexArith` remains in Phase 5 with other proof obligations outside the wide-word layer.

**Next step**

- Continue with the remaining HexArith Phase 5 proof clusters, especially the Nat modular arithmetic and downstream Barrett/Montgomery bridge proofs that consume the wide-word lemmas.

**Blockers**

- None for this issue.
