**Accomplished**

- Claimed #1819 and discharged item 1
  (`canonical_remainder_unique_of_pos_degree`) in `HexPolyFp/Basic.lean`.
- Added local helper lemmas for the proof:
  - `foldl_mulCoeffStep_select` (inner fold of `mulCoeffStep` collapses
    to the matching diagonal term),
  - `foldl_mulCoeffStep_outer` (outer schoolbook fold rewritten via the
    diagonal form),
  - `foldl_select_index` (foldl over a select-one-index `if`),
  - `coeff_mul_at_top` (coefficient at index `f.size-1 + (g.size-1)`
    equals the product of leading coefficients for nonzero `f`, `g`).
- Added `[PrimeModulus p]` to the
  `canonical_remainder_unique_of_pos_degree` signature so the proof can
  rule out the `k ≠ 0` branch via
  `ZMod64.eq_zero_or_eq_zero_of_mul_eq_zero`.
- Filed sub-issue #1882 covering the residual item 2
  (`mod_eq_mod_of_congr_not_pos_degree`); left a `Decomposed into
  #1882` breadcrumb on the parent issue.

**Current frontier**

- Verification:
  - `lake build HexPolyFp.Basic` — succeeds with only pre-existing
    `sorry` warnings (none added).
  - `lake build HexPolyFp` — succeeds.
  - `python3 scripts/status.py HexPolyFp` — `done_through: 4, ready: yes`.
  - `python3 scripts/check_dag.py` — clean.
  - `git diff --check HEAD~1` — clean.
- Touched files contain no `axiom`, `native_decide`, `TODO`, or
  `FIXME`; no new `sorry` outside pre-existing proof bodies.

**Next step**

- Submit partial PR for #1819 (item 1 only) and let #1882 carry item 2.
- Item 3 of #1819 (the `mod_eq_mod_of_congr_core` wiring) auto-closes
  once items 1, 2, and #1853 land — no work needed beyond ensuring the
  existing dispatch in `mod_eq_mod_of_congr_core` stays correct.

**Blockers**

- Item 2 (constant-divisor case) needs `divModArrayAux` loop reasoning
  comparable to #1853 and was deferred to #1882.
- Item 3 stays blocked behind #1853.
