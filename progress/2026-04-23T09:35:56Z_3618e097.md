**Accomplished**

- Claimed issue `#313` and added `HexPoly/Benchmark.lean` as the first `HexPoly` Phase 4 benchmark-fixture slice.
- Added stable named small and medium cases plus executable batch runners for dense multiplication, monic `divMod`, and field `gcd`/`xgcd`.
- Re-exported the benchmark module from `HexPoly.lean` and verified the slice with `lake build HexPoly`.

**Current frontier**

- `HexPoly` now has committed benchmark fixtures for its core runtime-heavy operations, but Phase 4 is not complete yet because broader benchmark publication/reporting work and downstream `HexPolyFp` benchmarking remain open.

**Next step**

- Commit the `HexPoly` benchmark slice and this progress note, then open the PR for `#313`.
- Follow-on work can extend Phase 4 reporting/export infrastructure or move to the next ready library benchmark/conformance frontier.

**Blockers**

- No blocker on this slice.
- The workspace still contains unrelated pre-existing modifications in `.claude/CLAUDE.md` and the untracked `.pod-codex-home/` tree, so commit only the `HexPoly` files plus this progress note.
