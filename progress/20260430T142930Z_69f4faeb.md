**Accomplished**

- Claimed #1373 for the determinant row-add product expansion.
- Added private product-fold helpers for splitting a single additive factor in
  a determinant-style left fold.
- Replaced the `detProduct_rowAdd` proof-body placeholder in
  `HexMatrix/Determinant.lean` using `rowAdd_get`, `rowAddDuplicate_get`, and
  the new single-additive-factor fold lemma.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-pattern scan on
  `HexMatrix/Determinant.lean`.

**Current frontier**

- `detTerm_rowAdd` now depends on a proven `detProduct_rowAdd`; the row-add
  determinant proof is reduced to the existing duplicate-row Leibniz
  cancellation obligation.
- Remaining `HexMatrix/Determinant.lean` proof placeholders are the row-swap
  helper cluster, identity permutation sum, and `permutationVectors_duplicateRow_sum`.

**Next step**

- Claim the dependent duplicate-row cancellation issue after #1373 lands, or
  continue with the independent row-swap permutation/sign issues.

**Blockers**

- None for this session.
