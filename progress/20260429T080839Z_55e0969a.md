**Accomplished**

- Claimed issue `#833`.
- Added proof-complete `GFqRing` projection and reduction lemmas in `HexGfqRing/Operations.lean` for reduced representatives, `ofPoly` negation/subtraction, quotient additive laws, quotient multiplicative zero/one/commutativity/distributivity, natural scalar recursion, integer casts, and integer scalar multiplication.
- Kept the existing `Lean.Grind.*` instance placeholders unchanged; no new `sorry`, `axiom`, or `native_decide` stand-ins were introduced.
- Verified `lake build HexGfqRing`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexGfqRing/Operations.lean` now has more representative-level facts available for the eventual quotient `Lean.Grind.Semiring`, `Ring`, and `CommRing` instance proofs.
- Multiplicative associativity for quotient representatives remains blocked on the upstream `FpPoly` multiplication associativity work.

**Next step**

- After the upstream polynomial associativity issue lands, use these projection lemmas to discharge the remaining quotient ring instance fields or create a narrower follow-up for any missing reduction lemma.

**Blockers**

- `FpPoly.mul_assoc` is still not available in this worktree, so the quotient multiplication associativity field remains out of scope for this issue.
