**Accomplished**

- Claimed issue `#448` and added the initial `HexGramSchmidt` scaffold in [HexGramSchmidt/Basic.lean](/home/kim/hex/worktrees/b85d0073/HexGramSchmidt/Basic.lean).
- Implemented executable Gram-Schmidt basis construction for rational row matrices, plus the integer-to-rational wrapper and coefficient-matrix surface for both `Hex.GramSchmidt.Int` and `Hex.GramSchmidt.Rat`.
- Added the initial structural theorem names from the spec (`basis_zero`, `basis_orthogonal`, `basis_decomposition`, `coeffs_diag`, `coeffs_upper`) with scaffolded proofs, and exported the module from [HexGramSchmidt.lean](/home/kim/hex/worktrees/b85d0073/HexGramSchmidt.lean).
- Verified with `lake build HexGramSchmidt` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexGramSchmidt` now exposes the core basis/coefficient API, but later Phase 1 declarations from the spec are still absent: Gram determinants, scaled coefficients, and update formulas under row operations.

**Next step**

- Publish the `#448` PR, then follow with narrower issues for `gramDet`/`gramDetVec`/`scaledCoeffs` and the swap/size-reduction update formulas.

**Blockers**

- None.
