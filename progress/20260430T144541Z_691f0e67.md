**Accomplished**

- Claimed #1378 and added private `HexMatrix/Determinant.lean` helpers for
  `finTranspose` point behavior, involutivity, injectivity, and inequality
  reflection.
- Added `transposePermutationValues_get`,
  `transposePermutationValues_involutive`, and the sign-level double-transpose
  consequence `detSign_transposePermutationValues_involutive`.
- Split the remaining single-transposition odd-parity proof into #1381 and
  linked it from #1378 as the residual combinatorial work.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-pattern scan for `axiom`,
  `native_decide`, `TODO`, and `FIXME` in `HexMatrix/Determinant.lean`.

**Current frontier**

- Row-swap parity work now has proved structural transpose facts available in
  `HexMatrix/Determinant.lean`.
- The actual parity-flip lemma for
  `inversionCount (transposePermutationValues perm i j).toList` remains open
  and is tracked by #1381.

**Next step**

- Prove #1381: establish the odd parity change under `perm.toList.Nodup` and
  `i ≠ j`, then package it as the private `detSign` flip lemma needed by the
  restricted row-swap term proof.

**Blockers**

- None for the structural helpers landed in this session.
