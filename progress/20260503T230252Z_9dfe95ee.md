**Accomplished**

- Claimed #1870 and fixed the derivative-zero branch of
  `primitiveSquareFreeDecomposition` so its `squareFreeCore` uses
  `normalizePrimitiveSign primitive` instead of the raw primitive part.
- Added `toRatPoly_normalizePrimitiveSign_rational_associate` and updated
  `primitiveSquareFreeDecomposition_reassembly_over_rat` for the normalized
  derivative-zero branch.
- Added a conformance guard covering the negative-constant `-2` case.
- Created follow-up #1871 for the remaining
  `primitiveSquareFreeDecomposition_squareFreeCore` proof and linked it from
  #1870 with a decomposition breadcrumb.

**Current frontier**

- Verification passed:
  - `lake build HexPolyZ.Basic`
  - `lake build HexPolyZ`
  - `python3 scripts/status.py HexPolyZ`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME" HexPolyZ/Basic.lean HexPolyZ/Conformance.lean`
- `HexPolyZ.Basic` still has the pre-existing Rat law placeholders and the
  existing `primitiveSquareFreeDecomposition_squareFreeCore` placeholder.

**Next step**

- Land this partial PR for #1870, then prove #1871 by establishing the
  quotient-by-gcd square-free theorem for the current decomposition branch
  structure.

**Blockers**

- None for the executable normalization fix.
