**Accomplished**

- Claimed #1774 and added compiling private `HexGramSchmidt/Basic.lean` helpers for Gram-Schmidt orthogonality:
  dot symmetry, preservation of zero dot through `reduceAgainstBasis`, and a nonzero-norm `dot_reduceAgainstBasis_of_mem` lemma over pairwise-orthogonal accumulated rows.
- Verified the partial helper layer with `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and placeholder scans on the touched file.
- Decomposed the remaining #1774 work into #1790 for the mathlib-free zero-norm annihilation lemma and #1791 for final `basisRowsAux`/public `basis_orthogonal` wiring.

**Current frontier**

- The reduction-level invariant is proved when the target accumulated row has nonzero self-dot.
- The main blocker is now isolated: in the computational core, prove that a `Vector Rat m` with `Matrix.dot v v = 0` annihilates all dot products, without importing Mathlib.

**Next step**

- Work #1790, then use it in #1791 to remove the nonzero-norm assumption and prove the `basisRowsAux` orthogonality invariant.

**Blockers**

- None for the partial helper PR; full #1774 completion depends on #1790 and #1791.
