**Accomplished**

- Claimed `#960`.
- Reviewed `HexGramSchmidt/Conformance.lean` against `SPEC/testing.md`,
  `PLAN/Phase3.md`, and `SPEC/Libraries/hex-gram-schmidt.md`.
- Expanded the conformance boundary to cover rational Gram determinants and
  the executable size-reduction / adjacent-swap update surface.
- Bumped `HexGramSchmidt.done_through` from `2` to `3`.
- Verified `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`,
  `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/status.py`,
  `git diff --check`, and the requested banned-pattern scan.

**Current frontier**

- `HexGramSchmidt` is ready for Phase 4 benchmarking.
- `HexLLL` is now unblocked for Phase 3 conformance testing.

**Next step**

- Dispatch or claim the newly ready `HexLLL -> Phase 3` conformance work,
  or start `HexGramSchmidt -> Phase 4` benchmarking.

**Blockers**

- No blockers for this review/bump.
