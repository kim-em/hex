**Accomplished**

- Claimed feature issue `#93` and added the initial `HexGramSchmidt` scaffold module at `HexGramSchmidt/Int.lean`.
- Introduced executable `Hex.GramSchmidt.Int.basis` and `Hex.GramSchmidt.Int.coeffs` definitions, plus Nat-indexed `HexMatrix.Matrix.row` and `HexMatrix.Matrix.entry` helpers to support the spec-style theorem surface.
- Declared the immediate theorem API for the integer basis/coefficients slice: `basis_zero`, `basis_orthogonal`, `basis_decomposition`, `coeffs_diag`, and `coeffs_upper`.
- Updated `HexGramSchmidt.lean` to re-export the new module and describe the current library surface.
- Verified the scaffold with `lake build HexGramSchmidt`.

**Current frontier**

- This session completed the requested Phase 1 slice for the integer Gram-Schmidt basis/coefficients core.
- Remaining `HexGramSchmidt` Phase 1 work is still open in follow-up areas from the spec: Gram determinants, scaled coefficients, rational-input APIs, and update formulas.

**Next step**

- Publish the branch as the PR for issue `#93` so later workers can continue the remaining `HexGramSchmidt` scaffold slices on top of this API.

**Blockers**

- None.
