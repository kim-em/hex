**Accomplished**

- Claimed issue `#525` and audited the current `HexMatrix` API against `SPEC/Libraries/hex-matrix.md`, `SPEC/Libraries/hex-gram-schmidt.md`, and `SPEC/Libraries/hex-lll.md`.
- Added the spec-facing vector aliases `Hex.Vector.dotProduct` and `Hex.Vector.normSq` plus their `Int`/`Rat` specializations in `HexMatrix/Basic.lean`, while preserving the existing `Matrix.dot` / `Matrix.normSq` helpers as thin wrappers.
- Added `Matrix.gramMatrix` and the leading-principal `Matrix.submatrix` helper needed for the downstream `b.gramMatrix.submatrix k` shape.
- Verified the slice with `lake build HexMatrix` and `python3 scripts/check_dag.py`.

**Current frontier**

- The implementation work for `#525` is complete locally and ready for publication.

**Next step**

- Commit `HexMatrix/Basic.lean` together with this progress file and run `coordination create-pr 525`.

**Blockers**

- None for this issue.
