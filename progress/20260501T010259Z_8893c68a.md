**Accomplished**

- Claimed #1455 and added `HexManual/Phase2345Checkpoint-17.md`.
- Summarized the merged PR wave after #1297 through #1486, covering the
  `HexHensel` linear/quadratic proof stream, determinant follow-through,
  `HexPolyFp` and `HexModArith` proof updates, and the `HexBerlekamp`
  benchmark harness.
- Cross-checked the checkpoint against live `coordination orient`, open
  agent-plan issues, `python3 scripts/status.py`, recent progress notes, and
  the relevant SPEC/PLAN context.
- Verified `python3 scripts/status.py`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a scan confirming no committed `SPEC/` or `PLAN/`
  file contains the forbidden process-note phrases.

**Current frontier**

- Unclaimed feature work is now the three split `HexPolyFp/SquareFree.lean`
  issues: #1488, #1489, and #1490.
- `HexHensel` has one remaining represented Phase 5 frontier,
  `multifactorLift_spec`, tracked by replan issue #1487.
- No additional follow-up issue was needed from this checkpoint because the
  live queue already covers the concrete gaps identified.

**Next step**

- Publish the checkpoint PR closing #1455.

**Blockers**

- None.
