**Accomplished**

- Claimed issue #1321, assessed it as too broad for one session, and
  decomposed it into #1350, #1351, #1352, and #1353 with the required
  parent breadcrumb before releasing #1321 for replan triage.
- Claimed issue #1350 for the determinant row-scale term algebra.
- Proved `detTerm_rowScale` in `HexMatrix/Determinant.lean` by adding
  private product-fold helper lemmas, a `rowScale_get` entry lemma, and
  `detProduct_rowScale`.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans for `native_decide`,
  `axiom`, `TODO`, and `FIXME` in `HexMatrix/Determinant.lean`.

**Current frontier**

- `HexMatrix/Determinant.lean` now has three remaining private proof-body
  `sorry`s in this determinant row-operation area:
  `permutationVectors_identity_sum`, `permutationVectors_rowSwap_sum`, and
  `permutationVectors_rowAdd_sum`.
- The public `det_rowScale` path is fully proved through
  `det_rowScale_leibniz`.

**Next step**

- Work the successor issues independently: #1351 for identity enumeration,
  #1352 for row-swap sign pairing, and #1353 for row-add duplicate-row
  cancellation.

**Blockers**

- None.
