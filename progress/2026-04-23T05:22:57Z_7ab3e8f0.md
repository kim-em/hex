**Accomplished**

- Claimed feature issue `#187` and checked the live `HexPoly` layout against the plan, noting that the issue's assumed `HexPoly/Conformance.lean` file was absent on `main`.
- Added [HexPoly/Conformance.lean](/home/kim/hex/worktrees/7ab3e8f0/HexPoly/Conformance.lean) with deterministic `core` conformance checks for `DensePoly.eval`, `DensePoly.derivative`, and `DensePoly.compose`, including typical, edge, and adversarial fixtures plus committed property checks.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/7ab3e8f0/HexPoly.lean) to import the new conformance module so `lake build HexPoly` elaborates the checks.
- Verified the slice with `lake build HexPoly` after switching the evaluator spot checks to `#eval!` to tolerate upstream scaffolded `sorry` theorems.

**Current frontier**

- `HexPoly` now has a real `Conformance.lean`, but this turn only covered the evaluation / derivative / composition slice; broader Phase 3 coverage for the rest of the advertised API remains separate work.

**Next step**

- Commit this branch and publish the PR for `#187` so the new `HexPoly` conformance slice can merge and advance the Phase 3 front.

**Blockers**

- None for this slice. The library still has existing upstream `sorry` declarations, which is why the committed spot checks use `#eval!` rather than plain `#eval`.
