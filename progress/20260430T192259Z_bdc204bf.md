**Accomplished**

- Claimed #1428 and restructured the three issue-owned raw-factor congruence obligations in `HexHensel/Linear.lean`.
- Replaced the proof bodies of `linearHenselStep_first_order_congr`, `linearHenselStep_scaled_first_order_congr`, and `linearHenselStep_product_expansion_congr` with compiled proofs that compose narrower private helper statements.
- Added and proved `linearHenselStep_recombine_error_congr`, isolating the exact additive recombination step `g*h + (f - g*h)`.
- Ran `lake build HexHensel`, `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern scan for `axiom`, `native_decide`, `TODO`, and `FIXME` in `HexHensel/Linear.lean`.

**Current frontier**

- The original three raw-factor bridge theorem bodies no longer contain direct `sorry`s.
- The remaining owned proof leaves are now narrower helpers:
  `modP_add_lift_mul`,
  `scale_congr_of_congr_mod_base`, and
  `linearHenselStep_product_expansion_identity_congr`.
- Existing unrelated `HexHensel/Linear.lean` proof-body `sorry`s remain in the monicity and iterative-wrapper area.

**Next step**

- Prove `modP_add_lift_mul` first by adding coefficient-level `modP` compatibility for lifted multiplication and addition.
- Then prove `scale_congr_of_congr_mod_base` from coefficient divisibility by `p`, and finish `linearHenselStep_product_expansion_identity_congr` by expanding the product and absorbing the `p^(2*k)` cross term modulo `p^(k+1)`.

**Blockers**

- Full completion of #1428 was not reached. The missing facts are local modular bridge lemmas for the `ZPoly.modP`/`FpPoly.liftToZ` boundary and the scaled product expansion.
