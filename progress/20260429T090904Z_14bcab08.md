**Accomplished**

- Claimed `#927`.
- Added the initial `HexBerlekampZassenhaus` module surface with the shared `PrimeChoiceData` and `LiftData` records.
- Restored the local `.lake/packages/mathlib` checkout enough for Lake to move it to the manifest revision, preserving the previous package-local state in a mathlib stash.
- Verified `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-term scan.

**Current frontier**

- `HexBerlekampZassenhaus.lean` now exposes only the pipeline records requested by the first Phase 1 slice.
- Algorithmic entry points such as prime selection, Hensel handoff, recombination, and top-level factorization remain absent, as intended for this issue.

**Next step**

- Add the next narrow `HexBerlekampZassenhaus` API slice, likely prime-choice helper declarations or Hensel/recombination handoff functions, once planned as a separate issue.

**Blockers**

- No blocker for this slice.
