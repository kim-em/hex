**Accomplished**

- Claimed issue `#14` and added [HexPoly/Division.lean](/home/kim/hex/worktrees/ea6f7620/HexPoly/Division.lean:1) with the Phase 1 dense-polynomial quotient/remainder scaffold.
- Introduced `DensePoly.DivRem`, `DensePoly.Monic`, monic-division helpers, general `/` and `%` exposure over the existing dense representation, and theorem stubs for the division algorithm and remainder-degree bounds.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/ea6f7620/HexPoly.lean:1) to re-export the new division module.
- Verified this slice with `lake build HexPoly`; the build succeeds, with `sorry` warnings only in theorem/proof declarations.

**Current frontier**

- `HexPoly` now exposes representation, arithmetic, and quotient/remainder scaffolding, but gcd/extended-gcd, evaluation/composition/derivative, CRT, and `Int`-specific content work are still unscaffolded.

**Next step**

- Add the next narrow `HexPoly` Phase 1 slice from `SPEC/Libraries/hex-poly.md`, likely the gcd/extended-gcd API block or the evaluation/composition block.

**Blockers**

- None.
