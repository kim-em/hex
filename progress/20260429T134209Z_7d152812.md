**Accomplished**

- Claimed feature issue `#1091` after `#1074` was already claimed by another agent.
- Reworked `HexBerlekamp/Irreducibility.lean` so `IrreducibilityCertificate` is self-contained with public `p : Nat` and `n : Nat` fields.
- Added a same-prime certificate view for executable checking after the stored `p` matches the ambient field.
- Updated certificate accessors, Rabin checker helpers, and checker theorem signatures to use the self-contained certificate surface.
- Verified `lake build HexBerlekamp`, `lake build HexBerlekampZassenhaus`, `python3 scripts/status.py HexBerlekamp`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-token/public-shape greps.

**Current frontier**

- `HexBerlekamp` now exposes the SPEC-faithful heterogeneous certificate shape needed by downstream Zassenhaus certificate data.
- Remaining `sorry` entries in `HexBerlekamp/Irreducibility.lean` are proof-only theorem bodies.

**Next step**

- Use the repaired certificate shape in the pending `HexBerlekampZassenhaus` irreducibility certificate checker surface work.

**Blockers**

- GitHub GraphQL rate limiting was encountered while checking for an existing PR, but issue claiming succeeded.
