**Accomplished**

- Claimed feature issue `#795`.
- Proved `Hex.GFqField.mul_inv_cancel` by transporting `reduceMod_repr_mul_invPoly_eq_one` through the finite-field and quotient-ring wrappers.
- Proved `Hex.GFqField.inv_mul_cancel` from quotient-ring commutativity and `mul_inv_cancel`.
- Verified `lake build HexGfqField` and `python3 scripts/check_dag.py`.

**Current frontier**

- The quotient-to-field inverse-cancellation slice from `#795` is complete.
- Remaining `HexGfqField/Operations.lean` `sorry`s are outside this issue's scope: nontriviality, `zpow`, and characteristic transport.

**Next step**

- Open a PR closing `#795`.

**Blockers**

- None.
