**Accomplished**

- Claimed #1507 and repaired the `ZMod64` polynomial division-law theorem surface.
- Added `ZMod64.PrimeModulus` plus `primeModulusOfPrime` so `DensePoly.DivModLaws (ZMod64 p)` is available only under a prime-modulus witness, not plain `[ZMod64.Bounds p]`.
- Updated proof-only Frobenius and modular-composition theorem surfaces to require `ZMod64.PrimeModulus p`; executable definitions remain unchanged.
- Verified `lake build HexPolyFp.Basic`, `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of touched `HexPolyFp` files.

**Current frontier**

- `HexPolyFp.Basic` no longer exports unsound field-style division laws for composite moduli.
- Existing `DensePoly.DivModLaws` proof placeholders remain, but they now live behind the corrected prime-field assumption.

**Next step**

- Continue the remaining HexPolyFp Phase 5 proof work, especially the square-free proof issues that can now rely on the corrected division-law boundary by installing `ZMod64.primeModulusOfPrime hp` locally when needed.

**Blockers**

- None for this issue.
