**Accomplished**

- Claimed issue `#854`.
- Replaced the conformance workflow stub with push, pull request, and manual triggers.
- Wired the core conformance workflow through `leanprover/lean-action@v1` using a target matrix for `HexArith`, `HexPoly`, and `HexMatrix`.
- Added the same required system dependency installation used by the main CI workflow.

**Current frontier**

- Core conformance checks for the current Phase 3 libraries are now CI-gated through root module builds.
- Broader per-library fixture expansion remains separate, including `#856` for the `HexPoly` Euclid/content/CRT surface.

**Next step**

- Let the PR CI run the new conformance matrix after publication; follow-up workers can expand individual `Conformance.lean` modules without changing this workflow wiring.

**Blockers**

- None.
