**Accomplished**

- Claimed #1361, verified its dependencies were closed, and tested the wrapper induction shape in `HexHensel/Linear.lean`.
- Marked #1361 for replan after confirming the public wrapper theorem lacks the per-step monicity/correction-degree invariant needed by `linearHenselStep_spec` and `linearHenselStep_monic`.
- Claimed #1410 and replaced the `permutationVectors_duplicateRow_sum` proof-body `sorry` in `HexMatrix/Determinant.lean`.
- Added filtered-fold and duplicate-row transpose helpers proving cancellation by splitting permutations on `perm[src] < perm[dst]`.
- Ran `lake build HexMatrix.Determinant`, `lake build HexMatrix`, `lake build HexMatrix.Conformance`, `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-placeholder scan of `HexMatrix/Determinant.lean`.

**Current frontier**

- The duplicate-row determinant cancellation proof is complete in `HexMatrix/Determinant.lean`.
- Existing unrelated HexMatrix proof-body `sorry`s remain in `RowEchelon`, `RREF`, and `Bareiss`.
- #1361 needs replanning before another worker attempts the linear iterative wrappers.

**Next step**

- Open the PR for #1410 and let CI validate the determinant cancellation change.

**Blockers**

- None for #1410.
- #1361 is blocked on a theorem-surface/invariant mismatch around intermediate monicity.
