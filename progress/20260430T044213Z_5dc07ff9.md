**Accomplished**

- Claimed human-oversight issue #1270 for the `HexLLL` size-reduction and swap-step targeted-update performance fix.
- Reworked `HexLLL/Basic.lean` so `LLLState.sizeReduceColumn` updates only row `k` with `Vector.set` instead of rebuilding the full `ν` matrix.
- Reworked `LLLState.swapStep` so `d[k]`, the swapped row-prefix entries, the pivot entry, and the two affected columns for rows `i > k` are updated through targeted writes instead of rebuilding `d` and `ν`.
- Added a small private `setEntry` helper local to `LLLState` for the targeted matrix writes.
- Verified `lake build HexLLL`, full `lake build`, `python3 scripts/status.py HexLLL`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-shape scan over `HexLLL/Basic.lean`.

**Current frontier**

- The forbidden `Matrix.ofFn` / `Vector.ofFn` rebuild shapes are removed from the two targeted LLL step functions.
- `HexLLL` remains at Phase 4-ready with `libraries.yml[HexLLL].done_through = 3`; this issue fixed a specific performance blocker and did not complete the full Phase 4 benchmark-exe review.

**Next step**

- Review and merge the PR for #1270.
- Follow-up Phase 4 work still needs the full `HexLLL.Bench` executable surface and scientific benchmark review before promoting `HexLLL.done_through` to 4.

**Blockers**

- No blocker for this session.
- The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
