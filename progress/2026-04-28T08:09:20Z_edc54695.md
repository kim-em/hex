**Accomplished**

- Claimed issue `#581` (`human-oversight`) and replaced the `UInt64` extended-GCD runtime detour in [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/edc54695/HexArith/ExtGcd.lean:1) with a native tail-recursive Euclidean loop over `UInt64`, keeping only the Bezout coefficients in `Int`.
- Repointed `HexArith.UInt64.extGcd` at the native implementation and updated the surrounding docstring to match the runtime contract.
- Verified with `lake build HexArith`, `lake build`, and `#eval` checks for `HexArith.UInt64.extGcd 17 5`, `0 7`, and `7 0`.

**Current frontier**

- The `UInt64` runtime path now matches the human-oversight directive, but the prose in `SPEC/Libraries/hex-arith.md` still describes the older `Int.extGcd` delegation route.

**Next step**

- Commit this `UInt64` implementation change and open the PR closing `#581`.

**Blockers**

- None.
