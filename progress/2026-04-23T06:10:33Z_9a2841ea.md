**Accomplished**

- Claimed issue `#216` (`Scaffold HexPolyFp Frobenius modular-power theorem surface`) after checking the live unclaimed queue and selecting the critical-path item.
- Added [HexPolyFp/Frobenius.lean](/home/kim/hex/worktrees/9a2841ea/HexPolyFp/Frobenius.lean) with the requested theorem surface for `powModMonic`, `frobeniusModMonic`, and `frobeniusPowModMonic`, including normalized-output, reduced-remainder, repeated-multiplication, and iterated-Frobenius contracts.
- Updated [HexPolyFp.lean](/home/kim/hex/worktrees/9a2841ea/HexPolyFp.lean) to re-export the new module and to keep the root docstring aligned with the expanded public surface.
- Verified the slice with `lake build HexPolyFp` and checked `python3 scripts/status.py` to confirm `HexPolyFp` remains the active Phase 1 frontier with no accidental `libraries.yml` changes.

**Current frontier**

- `HexPolyFp` still needs additional Phase 1 scaffold slices before a completion review can bump `libraries.yml`.
- Downstream `HexGfqRing`, `HexHensel`, and `HexBerlekamp` remain blocked on `HexPolyFp.done_through ≥ 1`, so finishing the remaining `HexPolyFp` scaffold surface is still high leverage.

**Next step**

- Commit the `HexPolyFp` Frobenius scaffold and this progress note, then publish the PR closing `#216`.

**Blockers**

- None on this slice.
