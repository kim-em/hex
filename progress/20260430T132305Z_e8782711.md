**Accomplished**

- Listed unclaimed work, selected the determinant identity follow-up, and
  claimed #1351.
- Assessed #1351 as still too broad for one session and decomposed it into
  #1355 and #1356, with #1356 depending on #1355.
- Claimed #1355 and proved the zero side of the identity insertion split in
  `HexMatrix/Determinant.lean`: identity entry lookup, zero-factor product
  folds, mismatch-zero determinant products, `insertAt` self/castSucc access,
  and the non-last insertion zero product.
- Created #1357 for the residual last-insertion product/sign preservation and
  added it as a dependency of #1356.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-pattern scans for `native_decide`,
  `axiom`, `TODO`, and `FIXME` in `HexMatrix/Determinant.lean`.

**Current frontier**

- `detProduct_identity_insertAt_not_last_zero` is available for the successor
  case of `permutationVectors_identity_sum`.
- The remaining #1355/#1357 work is to prove that insertion at `Fin.last n`
  preserves the identity determinant term, including product preservation and
  inversion parity/sign preservation.

**Next step**

- Work #1357, then use #1356 to finish the fold induction for
  `permutationVectors_identity_sum` and unblock `det_one`.

**Blockers**

- None.
