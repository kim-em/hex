**Accomplished**

- Claimed issue `#894`.
- Extended `HexArith/Bench.lean` with deterministic shared extended-GCD inputs for `Nat`, nonnegative `Int`, and `UInt64`.
- Added compact normalized gcd/Bezout summaries plus `setup_benchmark` registrations for `runNatExtGcdShapes`, `runIntExtGcdShapes`, and `runUInt64ExtGcdShapes`.
- Verified `lake build HexArith`, `lake build HexArith.Bench`, `lake exe hexarith_bench verify`, the three-way extended-GCD `compare` command, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-pattern scan.

**Current frontier**

- `HexArith.Bench` now covers the advertised extended-GCD API surface for this Phase 4 slice and exposes a common compare domain for gcd/Bezout conformance across the three variants.
- The compare command reports hash agreement across the common parameters. On this run, `UInt64` was consistent with the declared linear model, while `Nat`/`Int` timing verdicts were noisy/inconclusive despite the conformance agreement.

**Next step**

- Continue Phase 4 coverage for other advertised `HexArith` operations or tune the extended-GCD scientific schedule further if clean per-variant timing verdicts become a completion blocker.

**Blockers**

- None.
