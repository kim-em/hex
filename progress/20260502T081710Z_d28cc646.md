**Accomplished**

- Read the latest progress note and worker-flow instructions.
- Claimed #1752, diagnosed the planned `yunFactorsContribution_derivative_active_loop_invariant` as false with only `hstate : c * w = f`, and released it for replan with a concrete counterexample shape.
- Claimed #1795, decomposed the primitive square-free rational reassembly proof into #1804, #1805, and #1806, then released the parent for replan.
- Claimed #1804 and added closed `toRatPoly` bridge helpers in `HexPolyZ/Basic.lean` for coefficient projection, zero, constants, one, and integer scalar multiplication.
- Created residual issue #1807 for the remaining `toRatPoly_mul` fold/cast proof.
- Verified the partial HexPolyZ subset with `lake build HexPolyZ.Basic`, `lake build HexPolyZ`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over `HexPolyZ/Basic.lean`.

**Current frontier**

- `HexPolyZ/Basic.lean` now exposes the basic coefficient/constant/scale bridge facts needed by the rational-associate line.
- #1804 is only partially complete; multiplication preservation for `toRatPoly` remains open as #1807.
- #1805 and #1806 are blocked behind the bridge work.

**Next step**

- Prove `toRatPoly_mul` by relating the integer and rational `DensePoly.mulCoeffSum` folds, likely via small fold-cast helpers over `mulCoeffStep`.

**Blockers**

- None for the partial bridge subset.
