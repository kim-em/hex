**Accomplished**

- Claimed `#1001`, found its dependency was stale because the right-monomial theorem was decomposed into `#1022`/`#1023`/`#1024`, and released it for replan.
- Claimed `#1025`, decomposed the Nat Barrett reduction proof into `#1035` quotient bounds and `#1036` final correctness assembly, then released the parent for replan.
- Claimed `#1030` and tuned the HexGramSchmidt update-helper benchmarks.
- Switched `HexGramSchmidt.Int.gramDet` and `scaledCoeffs` from generic determinant evaluation to the existing Bareiss executable determinant.
- Added a fixed hot loop for the two row-update checksum benchmarks and retuned the adjacent-swap scalar-helper schedules to the tractable `3..6` band with small deterministic update fixtures.
- Verified the affected benchmark registrations produce consistent verdicts:
  `runSizeReduceChecksum`, `runAdjacentSwapChecksum`, `runAdjacentSwapDenom`,
  `runAdjacentSwapPivotCoeff`, `runAdjacentSwapGramDetNumerator`,
  `runAdjacentSwapGramDetQuotient`,
  `runAdjacentSwapScaledCoeffAbovePrevNumerator`, and
  `runAdjacentSwapScaledCoeffAboveCurrNumerator`.
- Ran `lake build HexGramSchmidt.Bench`, `lake exe hexgramschmidt_bench list`,
  `lake exe hexgramschmidt_bench verify`, `python3 scripts/status.py HexGramSchmidt`,
  `python3 scripts/check_dag.py`, `git diff --check`, and
  `rg -n "native_decide" HexGramSchmidt -S`.

**Current frontier**

- `HexGramSchmidt` remains at `done_through: 3`; this PR does not complete the full Phase 4 review or bump `libraries.yml`.
- The update-helper benchmark slice is now scientifically usable on the checked machine.

**Next step**

- Open the PR for `#1030`. A follow-up worker can continue the remaining HexGramSchmidt Phase 4 scientific review across all registrations.

**Blockers**

- None for the `#1030` slice.
