**Accomplished**

- Claimed #1537 and kept the work scoped to `HexPolyFp/SquareFree.lean`.
- Added a total `pthRoot_coeff` lemma proving formal p-th-root coefficient access both in bounds and out of bounds.
- Replaced the body of `pthRoot_pow_prime_coeff` with a proof from `pow_eq_powLinear`, the new `pthRoot_coeff`, divisibility arithmetic, `ZMod64.pow_prime`, and a new foundational `powLinear_prime_coeff` theorem boundary.
- Created follow-up #1541 for the remaining dense-polynomial Frobenius convolution proof and added the decomposition breadcrumb to #1537.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of touched `HexPolyFp` files.

**Current frontier**

- The p-th-root-specific Frobenius reconstruction proof is now routine once `powLinear_prime_coeff` is proved.
- The remaining hard obligation is isolated as the general coefficient-support theorem for a prime `p`th power over `F_p[x]`.

**Next step**

- Prove #1541: replace the `powLinear_prime_coeff` proof-body `sorry` using dense-polynomial multiplication coefficients, repeated-product coefficient support, and prime binomial divisibility.

**Blockers**

- No technical blocker; this session landed a partial reduction because the remaining convolution proof is the standalone foundational result.
