**Accomplished**

- Claimed feature issue `#127` and verified the `HexModArith` Phase 1 inverse/exponentiation slice was still unimplemented and on the critical path.
- Extended `HexModArith/Core.lean` with `ZMod64.one`, `ZMod64.invCandidate`, `ZMod64.inv?`, and `ZMod64.pow`, plus the corresponding range/spec theorem declarations allowed in Phase 1.
- Updated `HexModArith.lean`'s root docstring to reflect the expanded public API and verified the result with `lake build HexModArith`.

**Current frontier**

- The implementation is ready to publish as the issue's PR.

**Next step**

- Comment the completed scope on issue `#127`, commit the `HexModArith` changes together with this progress note, and run `coordination create-pr 127`.

**Blockers**

- None.
