**Accomplished**

- Claimed issue #1612 and added `HexPolyMathlib/Conformance.lean`.
- Re-exported the conformance module from `HexPolyMathlib.lean`.
- Bumped `libraries.yml[HexPolyMathlib].done_through` from `2` to `3`.
- Verified `HexPolyMathlib` now advances to Phase 4 in `scripts/status.py`.

**Current frontier**

- `HexPolyMathlib` has Phase 3 bridge conformance coverage for conversion round trips,
  `equiv`/`equiv.symm`, and gcd/xgcd correspondence theorem surfaces over committed
  `Rat` fixtures.
- The bridge proof bodies still contain their existing Phase proof `sorry`s.

**Next step**

- Pick up Phase 4 work for `HexPolyMathlib` if benchmarking/performance coverage is
  desired next.

**Blockers**

- None.
