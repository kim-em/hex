**Accomplished**

- Claimed issue `#199` and restored [HexMatrix/Conformance.lean](/home/kim/hex/worktrees/6a088837/HexMatrix/Conformance.lean) as a current-contract Phase 3 core conformance module for `HexMatrix`.
- Replaced the old metadata/fixture-heavy style with docstring-declared coverage, direct `#guard_msgs in #eval` spot checks, and `#guard` property assertions for `det`, `bareiss`, `rowSwap`, `rowScale`, and `rowAdd`.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/6a088837/HexMatrix.lean) to re-export the conformance module so `lake build HexMatrix` elaborates the checks.
- Fixed a broken generated `.lake/packages/verso` checkout locally so `lake build` could run again in this worktree.
- Verified the slice with `lake build HexMatrix`.

**Current frontier**

- `HexMatrix` now has a live core conformance module again, aligned with the anti-pattern bans from `SPEC/testing.md` for the determinant and elementary row-operation surface.
- The separate Phase 3 follow-up for `rref`, `spanCoeffs`, `spanContains`, and `nullspace` conformance remains open frontier outside this issue's scope.

**Next step**

- Commit this rewrite and open the PR for `#199` so the refreshed Phase 3 conformance surface can land and unblock later `HexMatrix` work.

**Blockers**

- None in the repo after regenerating the broken `.lake` dependency checkout; `lake build HexMatrix` is green with only the expected existing `sorry` warnings in scaffold modules.
