**Accomplished**

- Claimed feature issue `#193` after `#192` was concurrently claimed by another agent.
- Extended [HexGramSchmidt/Update.lean](/home/kim/hex/worktrees/cdc3f8f1/HexGramSchmidt/Update.lean) with the missing adjacent-swap theorem surface for downstream `HexLll`: explicit pivot-row formulas for `basis`, a non-affected `coeffs` case, and pivot/row-swap/tail formulas for rational `coeffs` alongside the existing `scaledCoeffs` theorems.
- Verified the new scaffold with `lake build HexGramSchmidt`.

**Current frontier**

- `HexGramSchmidt` now exposes the adjacent-swap basis and coefficient statements the Phase 2 review identified as missing, so the next downstream work can consume this surface from `HexLll` or resume the remaining review/completion flow.

**Next step**

- Land issue `#193` so downstream `HexLll` work can rely on the new `basis_adjacentSwap_*` and `coeffs_adjacentSwap_*` declarations.

**Blockers**

- None locally; the target build passes and the remaining warnings are the expected scaffold `sorry` declarations.
