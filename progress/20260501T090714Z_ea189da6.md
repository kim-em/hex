**Accomplished**

- Claimed issue #1579 for the `HexPolyFp` Freshman's-dream coefficient theorem.
- Moved `powLinear_prime_coeff` below the bounded coefficient-fold Frobenius API and replaced its proof-body `sorry`.
- Proved `powLinear_prime_coeff` by reconstructing `g` from `coeffFold g g.size`, applying `powLinear_coeffFold_prime_coeff`, and using the size bound to discharge out-of-range coefficients.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-token scan over `HexPolyFp/SquareFree.lean`.

**Current frontier**

Issue #1579 is complete. The public `powLinear_prime_coeff` theorem no longer has its own `sorry` and `pthRoot_pow_prime_coeff` continues to finish through it. The remaining `HexPolyFp/SquareFree.lean` proof-body `sorry`s are the pre-existing bounded coefficient-fold cancellation theorem and later square-free/Yun proof frontiers.

**Next step**

Use the completed `powLinear_prime_coeff` theorem to finish `pthRoot_frobenius_of_derivative_zero` in #1538.

**Blockers**

None.
