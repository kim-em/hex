**Accomplished**

- Claimed issue #1379.
- Proved `detProduct_rowSwap_transposeValues` in `HexMatrix/Determinant.lean`.
- Added local fold/product and `finTranspose` permutation helpers supporting the row-swap reindexing proof.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`, `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- The row-swap determinant term lemma now has a proved product reindexing component and uses the existing restricted sign-flip lemma.
- Remaining determinant row-swap work is the permutation-vector sum reindexing placeholder, which is outside #1379.
- Existing unrelated `.claude/CLAUDE.md` worktree modification remains uncommitted.

**Next step**

- Prove the permutation-vector sum reindexing for `transposePermutationValues` so `permutationVectors_rowSwap_sum` no longer depends on its placeholder.

**Blockers**

- None.
