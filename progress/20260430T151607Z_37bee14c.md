**Accomplished**

- Claimed repair PR #1372, diagnosed it as a conflict-only repair against
  `origin/main`, and merged `origin/main` into `agent/1931b346`.
- Resolved the `HexMatrix/Determinant.lean` conflict by keeping the PR's
  row-swap determinant term split and the newer base transpose-involution
  helper lemmas, while dropping the duplicate involution helper from the PR.
- Verified `lake build`, `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern
  scan of the touched Lean files for `axiom`, `native_decide`, `TODO`, and
  `FIXME`.

**Current frontier**

- PR #1372 is locally merge-resolved and verification passes. The branch still
  needs the merge repair commit pushed and marked salvaged.

**Next step**

- Commit the merge repair, push `agent/1931b346` with `--force-with-lease`, and
  mark PR #1372 salvaged.

**Blockers**

- None.
