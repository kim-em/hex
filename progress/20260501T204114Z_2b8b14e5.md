**Accomplished**

- Claimed #1643 and kept the work scoped to `HexPolyFp/SquareFree.lean`.
- Split `yunFactorsContribution_derivative_active_split_algebra` into:
  - a proved wrapper that eliminates the impossible zero-fuel case from
    `hfuel` and `hzero`;
  - the remaining successor-fuel frontier
    `yunFactorsContribution_derivative_active_split_algebra_succ`.
- Verified the edit with `lake build HexPolyFp.SquareFree`,
  `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token
  scan for `axiom`, `native_decide`, `TODO`, and `FIXME` in the touched file.

**Current frontier**

- The derivative-active reconstruction bridge no longer has to handle
  unreachable zero-fuel/nonzero inputs.
- The only remaining obligation in this cluster is the successor-fuel Yun
  algebra lemma, which must use the step/descent helpers to reconstruct
  `pow f multiplicity` through the reachable repeated-tail path.
- Opened follow-up #1690 for that successor-fuel algebra proof.

**Next step**

- Prove `yunFactorsContribution_derivative_active_split_algebra_succ` by
  case-splitting on `isOne c` and `isOne (c / gcd c w)`, then applying the
  existing step split and repeated-tail descent helpers to the recursive
  target. This is tracked by #1690.

**Blockers**

- None; this session is partial because the successor-fuel product algebra
  remains the hard proof.
