**Accomplished**

- Claimed issue `#353` and rewrote [`HexGramSchmidt/Conformance.lean`](/home/kim/hex/worktrees/2f982ece/HexGramSchmidt/Conformance.lean) to cover only the executable row-operation slice requested after the rewind.
- Removed the stale basis/coefficients/Gram-determinant checks that were locking in wrong scaffold behavior, and replaced them with committed typical, edge, and adversarial `#guard`s for `Hex.GramSchmidt.Int.sizeReduce` and `Hex.GramSchmidt.Int.adjacentSwap`.
- Verified `lake build HexGramSchmidt` succeeds with the new conformance module.

**Current frontier**

- `HexGramSchmidt` Phase 3 conformance is now narrowed to the honest executable row-operation surface, but the Phase 2 rewind remains open until the scaffold cleanup in `#332` and the fresh review pass in `#351` finish.

**Next step**

- Land `#353`, then continue the `HexGramSchmidt` rewind path by clearing the remaining wrong scaffold bodies from `#332` and rerunning the Phase 2 review.

**Blockers**

- No blocker for `#353` itself.
- Project-wide, `#332` is still claimed elsewhere, so the broader `HexGramSchmidt` scaffold cleanup is not complete in this session.
