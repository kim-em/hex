**Accomplished**

- Claimed issue #1564 for the `HexPolyFp` two-summand Frobenius additivity bridge.
- Replaced the `powLinear_add_prime_coeff` proof-body `sorry` in `HexPolyFp/SquareFree.lean`.
- Proved the bridge by rewriting each side through `powLinear_prime_coeff`, splitting on `n % p = 0`, using coefficient additivity in the divisible branch, and closing with `zmod64_add_pow_prime`.
- Hardened the proof to use explicit `if_pos`/`if_neg` rewrites after second-opinion review.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over `HexPolyFp/SquareFree.lean`.

**Current frontier**

Issue #1564 is complete. Issue #1556 remains open and correctly marked `blocked` until this PR lands; it owns the upstream `powLinear_prime_coeff` proof that this bridge currently depends on.

**Next step**

After the #1564 PR merges, unblock or re-check #1556 and prove `powLinear_prime_coeff` using the finite coefficient-fold/cancellation APIs.

**Blockers**

None.
