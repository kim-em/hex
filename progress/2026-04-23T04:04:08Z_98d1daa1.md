**Accomplished**

- Claimed feature issue `#145` and verified the Phase 1 `HexMatrixMathlib` rank-bridge scope still matched `SPEC/Libraries/hex-matrix-mathlib.md`, `PLAN/Phase1.md`, and the current `HexMatrix` / `HexMatrixMathlib` module layout.
- Added `HexMatrixMathlib/Rank.lean` with the scaffolded `rank_eq` theorem relating `HexMatrix.RowEchelonData.rank` for an `IsEchelonForm` witness to Mathlib's `Matrix.rank` after transport by `matrixEquiv`.
- Updated `HexMatrixMathlib.lean` to re-export the new rank module and refreshed the root docstring so the public bridge surface mentions the rank correspondence.
- Verified the result with `lake build HexMatrixMathlib`.

**Current frontier**

- The `HexMatrixMathlib` Phase 1 rank-bridge scaffold is complete and ready to publish as the PR for issue `#145`.

**Next step**

- Land the scaffold via PR; later Phase 2 work can fill the proof of `HexMatrixMathlib.rank_eq` and add the separate nullspace and span bridges.

**Blockers**

- None.
