**Accomplished**

- Claimed #1470 and isolated the quadratic g-update monicity proof in
  `HexHensel/Quadratic.lean`.
- Replaced the `quadraticHenselStep_g_update_monic` proof body with a
  reduction to a private helper about monicity after adding the modular
  divmod remainder.
- Verified `lake build HexHensel.Quadratic`, `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token
  scan over `HexHensel/Quadratic.lean` for `axiom`, `native_decide`, `TODO`,
  and `FIXME`.

**Current frontier**

- The public quadratic g-update monicity theorem compiles through the new
  helper.
- The remaining local quadratic proof placeholder is
  `addModSquare_divModMonicModSquare_remainder_monic`, which needs the
  representation-level argument that the modular divmod remainder has support
  below monic `g` and coefficient reduction preserves the leading coefficient
  `1`.

**Next step**

- Prove `addModSquare_divModMonicModSquare_remainder_monic` by deriving the
  remainder degree/support bound for `divModMonicModSquare` and a small
  `reduceModPow` monicity-preservation lemma.

**Blockers**

- None.
