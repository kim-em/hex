**Accomplished**

- Claimed feature issue #1316 for `HexLLL` Phase 4 benchmark signal-floor windows.
- Updated `HexLLL/Bench.lean` to wrap the five existing scientific targets in fixed hot loops with rolling checksums, preserving each declared complexity model up to a parameter-independent constant factor.
- Retuned scientific schedules so four registrations are verdict-eligible and consistent:
  - `Hex.LLLBench.runSwapStepChecksum`
  - `Hex.LLLBench.runSizeReduceChecksum`
  - `Hex.LLLBench.runGramSchmidtCoeffChecksum`
  - `Hex.LLLBench.runSizeReduceColumnChecksum`
- Confirmed `Hex.LLLBench.runPotential` is now verdict-eligible but remains inconclusive against the declared linear `potentialComplexity n` model.
- Filed follow-up issue #1318 for the remaining `runPotential` complexity mismatch.
- Left `libraries.yml[HexLLL].done_through` at `3`.

**Current frontier**

- `HexLLL` remains in Phase 4 because `runPotential` still fails the scientific verdict criterion.
- The signal-floor calibration problem from #1316 is resolved for the four non-potential registrations and clarified for `runPotential` as a substantive model or implementation issue.

**Next step**

- Work #1318: decide and fix the correct textbook model or executable implementation for `LLLState.potential`, then rerun the full `hexlll_bench` suite and bump `HexLLL` to Phase 4 only if all five registrations are consistent.

**Blockers**

- `runPotential` measured verdict-eligible rows at params `192`, `208`, and `224`, but constants increased from about `2.26e6` to `3.52e6`, yielding an inconclusive verdict against the current linear model.
- `python3 scripts/check_phase4.py --all` still fails on pre-existing unrelated adjacent cost-model comment gaps in other benchmark modules.
- The worktree still has a pre-existing unrelated `.claude/CLAUDE.md` modification that this session did not touch.
