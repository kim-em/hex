**Accomplished**

- Claimed issue `#20` and added [HexArith/Barrett/ReduceNat.lean](/home/kim/hex/worktrees/3d590cb3/HexArith/Barrett/ReduceNat.lean:1) with the Phase 1 Nat-level Barrett reduction scaffold: `barrettRadix`, `barrettReduceNat`, and theorem statements for exactness and remainder bounds.
- Added [HexArith/Barrett/Reduce.lean](/home/kim/hex/worktrees/3d590cb3/HexArith/Barrett/Reduce.lean:1) with the executable `UInt64` bridge `barrettReduce` and the bridge theorem `toNat_barrettReduce`.
- Updated [HexArith.lean](/home/kim/hex/worktrees/3d590cb3/HexArith.lean:1) to re-export the new Barrett reduction layers.
- Verified this slice with `lake build HexArith`; the build succeeds, with `sorry` warnings only in theorem declarations.

**Current frontier**

- `HexArith` now has the wide-word helpers, the user-facing Barrett context, and the missing Layer 3/4 Barrett scaffolding, but `BarrettCtx.mulMod` still computes its remainder directly rather than delegating to `barrettReduce`.
- The remaining arithmetic Phase 1 gaps include the Nat `extGcd` API and the Montgomery layers described in `SPEC/Libraries/hex-arith.md`.

**Next step**

- Either claim issue `#19` for the Nat extended-GCD scaffold, or add the next narrow Montgomery reduction module that now naturally follows the completed Barrett lower layers.

**Blockers**

- None.
