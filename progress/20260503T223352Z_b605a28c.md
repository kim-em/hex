**Accomplished**

- Claimed #1859 and replaced the `HexLLL.Basic` `lllAux` executable placeholder with the SPEC-shaped integer LLL outer loop.
- The loop now size-reduces row `k`, checks the integer Lovasz condition with `δ.num`/`δ.den`, advances on success, and swaps/decrements on failure.
- Removed the stub wording from the `lllAux` docstring while preserving the public `lll`, `lll.firstShortVector`, and `lll.shortVectors` signatures.

**Current frontier**

- Verification passed:
  - `lake build HexLLL`
  - `python3 scripts/status.py HexLLL`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - forbidden-token scan over touched HexLLL files
- `HexLLL` remains Phase 1 ready in `scripts/status.py`; this issue covers the outer loop, not the full Phase 1 exit criteria.

**Next step**

- Continue HexLLL Phase 1 by replacing any remaining wrong-shape executable surfaces or by adding focused conformance around the LLL entry points once lightweight proof witnesses for small independent bases are available.

**Blockers**

- None for #1859.
