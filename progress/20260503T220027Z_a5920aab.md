**Accomplished**

- Claimed #1837 as the highest-value unclaimed work item because it advances
  the executable Berlekamp-Zassenhaus recombination path and unlocks the
  downstream scale conformance work.
- Updated `HexBerlekampZassenhaus/Basic.lean` so `recombine` now tries an
  LLL-based recombination path before falling back to the existing exhaustive
  search.
- Added private helpers for building the lifted-factor lattice basis with
  coefficient columns and a `p^k`-scaled identity block, decoding short vectors
  back to `ZPoly` candidates, and accepting candidates only through exact
  divisibility against the current target.
- Preserved the old subset search as `recombineExhaustive` for fallback and
  oracle use.

**Current frontier**

- `lake build HexBerlekampZassenhaus` passes; existing conformance checks still
  pass under that build.
- `python3 scripts/check_dag.py` passes.
- `python3 scripts/status.py HexBerlekampZassenhaus` still reports Phase 1
  readiness with `done_through: 0`; this issue does not bump
  `libraries.yml`.
- `git diff --check` passes.
- Forbidden-token scan over touched Berlekamp-Zassenhaus Lean files found no
  `native_decide`, `TODO`, `FIXME`, or `axiom`.

**Next step**

- Land the PR for #1837.
- The follow-up #1839 scale conformance issue can exercise the new LLL-first
  recombination surface once this lands.

**Blockers**

- None.
