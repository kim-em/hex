**Accomplished**

- Claimed issue #1707 for the HexArith Nat prime/binomial proof cluster.
- Proved `choose_succ_mul_eq` for the local Pascal-recursive
  `Hex.Nat.choose`, adding the local `choose_one_right` helper.
- Proved `pow_prime_mod_from_add_pow`, closing the Fermat induction once
  additive prime-power congruence is available.
- Opened successor issue #1708 for the remaining standalone
  `add_pow_prime_mod_of_choose_dvd` binomial expansion/congruence proof.
- Verified `lake build HexArith.Nat.Prime`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token scan on
  `HexArith/Nat/Prime.lean`.

**Current frontier**

- `HexArith/Nat/Prime.lean` has one remaining placeholder:
  `add_pow_prime_mod_of_choose_dvd`.
- Public `Hex.Nat.add_pow_prime_mod` and `Hex.Nat.pow_prime_mod` remain wired
  through that proof; `pow_prime_mod` will close automatically once #1708 lands.

**Next step**

- Prove #1708 by adding a local binomial expansion/evaluation lemma for
  `Hex.Nat.choose`, then use `hchoose` to eliminate all middle terms modulo
  the prime.

**Blockers**

- None.
