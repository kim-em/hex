**Accomplished**

- Claimed issue #1321 for the `HexMatrix` determinant row-operation proof cluster.
- Replaced the four public direct `sorry`s in `HexMatrix/Determinant.lean`
  with proofs of `det_one`, `det_rowSwap`, `det_rowScale`, and `det_rowAdd`
  from named private Leibniz-sum helper obligations.
- Verified `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-pattern scan for `native_decide`,
  `axiom`, `TODO`, and `FIXME` in the touched determinant file.

**Current frontier**

- The public determinant row-operation surface is now structured through
  helper lemmas, but `HexMatrix/Determinant.lean` still has four theorem-level
  `sorry`s: `det_identity_leibniz`, `det_rowSwap_leibniz`,
  `det_rowScale_leibniz`, and `det_rowAdd_leibniz`.
- The issue's requested `lake exe hexmatrix_conformance` target is not
  registered in `lakefile.lean`; `lake build HexMatrix.Conformance` is the
  available conformance target and passes.

**Next step**

- Split or prove the four private Leibniz helper obligations, starting with
  permutation-vector enumeration facts for the identity and row-swap cases,
  then the fold/multilinearity facts needed by row scaling and row addition.

**Blockers**

- None.
