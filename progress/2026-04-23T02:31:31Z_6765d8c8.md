**Accomplished**

- Claimed review issue `#89` and audited `SPEC/Libraries/hex-arith.md` against the current `HexArith` scaffold, including the residual binomial/Fermat declarations and the GMP-backed `Int.extGcd` extern contract.
- Verified Phase 1 completeness conditions: the spec-named declarations are present, the root and component modules carry docstrings, `HexArith/ffi/mpz_gcdext.c` exists, and `rg` found no `TODO`/`FIXME`/`...` placeholders under `HexArith`.
- Updated [libraries.yml](/home/kim/hex/worktrees/6765d8c8/libraries.yml) so `HexArith.done_through` is `1`, and confirmed the downstream readiness change with `python3 scripts/status.py`.
- Recorded the review outcome on issue `#89` and re-ran `lake build HexArith`.

**Current frontier**

- The review work is complete locally; only publication remains for the `libraries.yml` bump and this progress note.

**Next step**

- Stage `libraries.yml` and this progress file, commit them, and run `coordination create-pr 89` so the queue can pick up the newly unblocked `HexModArith` work.

**Blockers**

- None.
