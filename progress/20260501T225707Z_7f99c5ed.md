**Accomplished**

- Claimed issue #1710 for the HexArith local binomial expansion helper.
- Added private `chooseTerm` and `chooseSum` helpers in
  `HexArith/Nat/Prime.lean`.
- Proved local Pascal-row boundary helpers `choose_eq_zero_of_lt` and
  `choose_self`.
- Proved the successor-row partial-sum recurrence and the exact expansion
  `(a + b) ^ n = chooseSum n a b (n + 1)`.
- Verified `lake build HexArith.Nat.Prime`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the forbidden-token scan on
  `HexArith/Nat/Prime.lean`.

**Current frontier**

- `HexArith/Nat/Prime.lean` still has the existing placeholder
  `add_pow_prime_mod_of_choose_dvd`.
- The new exact expansion helper is available for the modulo cancellation
  follow-up.

**Next step**

- Use `add_pow_chooseSum` plus divisibility of middle coefficients to prove
  `add_pow_prime_mod_of_choose_dvd`.

**Blockers**

- None.
