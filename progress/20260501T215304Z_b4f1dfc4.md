**Accomplished**

- Claimed issue #1701 for the Montgomery multiplication user-facing theorems.
- Proved `MontCtx.toNat_mulMont` in `HexArith/Montgomery/Context.lean` using
  `toMont_lt`, `mulMont_repr`, and `fromMont_toMont`.
- Proved `MontCtx.mulMont_eq` from the Nat theorem via `UInt64.toNat_inj` and
  the residue bound below `UInt64.size`.
- Committed the proof as `9e8994f` (`feat: prove Montgomery multiplication theorems`).
- Verified `lake build HexArith.Montgomery.Context`, `lake build HexArith`,
  `python3 scripts/status.py HexArith`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-token scans for touched additions.

**Current frontier**

- The immediate Montgomery multiplication conversion bridge from issue #1701 is
  complete.
- Existing out-of-scope HexArith Phase 5 placeholders remain, including
  `powMod_eq` and unrelated Nat/ExtGcd proof gaps.

**Next step**

- Continue HexArith Phase 5 proof work on the remaining placeholders, with
  `powMod_eq` now able to consume the completed Montgomery multiplication
  theorem.

**Blockers**

- None.
