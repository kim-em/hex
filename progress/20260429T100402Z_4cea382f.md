**Accomplished**

- Claimed `#940`.
- Proved the remaining `Lean.Grind.Semiring.pow_succ` field for `PolyQuotient f hf` without changing the public square-and-multiply `pow`.
- Added local private helpers connecting the square-and-multiply `pow.go` to a structurally recursive `linearPow` model, then used that bridge for `pow_succ`.
- Verified `lake build HexGfqRing`, `lake build HexGfqField`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexGfqRing/Operations.lean` now has no `sorry` in the quotient `Lean.Grind.Semiring` or `Lean.Grind.CommRing` instance regions.
- The remaining `sorry` warnings in `HexGfqRing/Operations.lean` are the out-of-scope `Lean.Grind.Ring` fields tracked by `#941`.

**Next step**

- Continue with `#941` to prove the quotient `Lean.Grind.Ring` negation, subtraction, `zsmul`, and integer-cast compatibility fields.

**Blockers**

- None for this slice.
