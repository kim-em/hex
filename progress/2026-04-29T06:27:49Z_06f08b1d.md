**Accomplished**

- Claimed issue `#838` after confirming its prerequisites `#835` and `#836` are closed.
- Added `FpPoly.zero_mul` and `FpPoly.mul_zero` for executable polynomial multiplication.
- Decomposed the remaining multiplication algebra surface into `#846`, `#847`, and `#848`, with a breadcrumb comment on `#838`.
- Verified `lake build HexPolyFp`, `python3 scripts/check_dag.py`, the targeted placeholder scan, and `git diff --check`.

**Current frontier**

- `HexPolyFp/Basic.lean` now exposes zero-multiplication laws needed by downstream quotient proofs.
- The remaining `#838` deliverables are intentionally split: one-multiplication laws, coefficient/distributivity laws, then associativity/commutativity.

**Next step**

- Land this partial PR for `#838`, then follow-up workers can claim `#846` and continue toward unblocking `#839`.

**Blockers**

- None for the partial slice.
