**Accomplished**

- Claimed issue `#460` and added [HexModArithMathlib/Basic.lean](/home/kim/hex/worktrees/00ddc64a/HexModArithMathlib/Basic.lean:1), introducing the `Hex.ZMod64 p ↔ ZMod p` conversion functions, the local `NeZero p` bridge from `ZMod64.Bounds`, simp lemmas for the round-trips and arithmetic preservation, and the Phase 1 ring equivalence.
- Filled in the root [HexModArithMathlib.lean](/home/kim/hex/worktrees/00ddc64a/HexModArithMathlib.lean:1) import/module docstring so the new bridge is exposed as the library surface.
- Extended [HexModArith/Basic.lean](/home/kim/hex/worktrees/00ddc64a/HexModArith/Basic.lean:142) with the minimal `Zero`/`One`/`Add`/`Sub`/`Mul`/`Pow`/`Inv` instances needed to state the bridge as a `≃+*`.
- Verified with `lake build HexModArithMathlib`, `lake build`, and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexModArithMathlib` now has the core `ZMod64 p ≃+* ZMod p` bridge, but the broader algebraic instance/theorem surface for `HexModArith` itself still lives in the separate Phase 1 issue `#471`.

**Next step**

- Commit the `HexModArithMathlib` bridge and open the PR closing `#460`.

**Blockers**

- None.
