**Accomplished**

- Claimed #1779 and proved `ZPoly.congr_symm`, `ZPoly.congr_trans`, and
  `ZPoly.congr_add` in `HexPolyZ/Basic.lean`.
- Used the existing coefficientwise congruence definition, `DensePoly.coeff_add`,
  and integer divisibility witnesses from `% = 0`; no executable polynomial
  behavior changed.
- Verified `lake build HexPolyZ.Basic`, `lake build HexPolyZ`,
  `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, and
  `git diff --check`.

**Current frontier**

- The elementary relation/additive closure lemmas for `ZPoly.congr` are now
  discharged.
- `HexPolyZ/Basic.lean` still has its pre-existing placeholders for
  `congr_mul`, primitive-part properties, and square-free decomposition
  properties.

**Next step**

- A follow-up HexPolyZ issue can tackle `ZPoly.congr_mul` using the same
  coefficientwise congruence surface plus the dense multiplication coefficient
  law.

**Blockers**

- None for #1779.
