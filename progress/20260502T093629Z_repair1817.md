**Accomplished**

- Claimed PR #1817 for repair after PR #1798 was already repair-claimed.
- Diagnosed the failing CI as a `DensePoly.DivModLaws (ZMod64 p)` synthesis failure in `HexGfqRing/Basic.lean`, outside the original HexPolyZ proof file.
- Applied the minimal repair by qualifying the reduction congruence lemmas through `DensePoly.DivModLaws`.
- Verified with `lake build HexGfqRing.Basic`, `lake build`, `python3 scripts/status.py HexPolyZ`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over the touched Lean files.

**Current frontier**

- PR #1817 now includes the HexPolyZ denominator-clearing proof plus the small HexGfqRing compatibility repair needed for CI.

**Next step**

- Push the repaired PR branch and mark PR #1817 salvaged.

**Blockers**

- None.
