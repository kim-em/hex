**Accomplished**

- Claimed issue `#831`.
- Added `GFqRing.zero_ne_one` for positive-degree quotient moduli, with a local bridge showing positive polynomial degree excludes the degenerate `p = 1` coefficient collapse.
- Added `GFqField.zero_ne_one` and used it to discharge the nontriviality slot in the `Lean.Grind.Field` instance.
- Verified `lake build HexGfqRing`, `lake build HexGfqField`, `python3 scripts/check_dag.py`, the requested `rg` scan, and `git diff --check`.

**Current frontier**

- The quotient and finite-field wrappers now expose the `0 ≠ 1` theorem needed by downstream field-shell work.
- Remaining `HexGfqField/Operations.lean` placeholders are still in the inverse witness, `zpow_neg`, and `Lean.Grind.IsCharP` surfaces.

**Next step**

- A follow-up worker can claim `#822` to consume `GFqField.zero_ne_one` and continue the `zpow_neg` field-shell obligations.

**Blockers**

- None.
