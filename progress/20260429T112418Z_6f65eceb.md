**Accomplished**

- Claimed feature issue `#1014`.
- Added `HexModArith/Bench.lean` with benchmark registrations for `ZMod64`
  construction/casts, `add`, `sub`, extern-backed `mul`, `pow`, `inv`,
  `BarrettCtx.mulMod`, and the Montgomery `toMont`/`mulMont`/`fromMont`
  hot-loop wrappers.
- Added the `hexmodarith_bench` Lake executable and CI smoke commands for
  `list` and `verify`.
- Linked the existing `hexmodarithffi` static library into `HexModArith` so
  executable uses of the extern-backed multiplication can resolve the C
  symbol.
- Verified `lake build HexModArith.Bench`.
- Verified `lake exe hexmodarith_bench list`.
- Verified `lake exe hexmodarith_bench verify`.
- Verified `python3 scripts/status.py HexModArith`.
- Verified `python3 scripts/check_dag.py`.
- Verified `git diff --check`.
- Verified `rg -n "native_decide" HexModArith -S` finds no matches.

**Current frontier**

- `HexModArith` remains in Phase 4 with `done_through: 3`.
- Benchmark infrastructure is now present and smoke-tested, but this session
  did not perform the full scientific Phase 4 timing review or bump
  `libraries.yml`.

**Next step**

- Run the scientific `hexmodarith_bench run ...` checks for every parametric
  registration, investigate any inconclusive verdicts, then complete the Phase
  4 review before bumping `HexModArith.done_through` to `4`.

**Blockers**

- None for this session's `#1014` infrastructure work.
