**Accomplished**

- Claimed issue `#450` and added `HexMatrix/Determinant.lean` on top of the current `origin/main` scaffold.
- Implemented an executable Leibniz-formula determinant surface for dense square matrices using an internal permutation-vector enumeration and inversion-parity sign computation, without introducing a Mathlib dependency into `HexMatrix`.
- Added the requested determinant theorem surface (`det_one`, `det_rowSwap`, `det_rowScale`, `det_rowAdd`) and exposed the new determinant module from `HexMatrix.lean`.
- Verified in a clean branch off `origin/main` with `lake build HexMatrix` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrix` now has the generic determinant definition and row-operation determinant lemmas scaffolded in the root library.
- The executable Bareiss determinant path from issue `#452` remains open, as does the downstream Mathlib bridge work that will prove equivalence with Mathlib's determinant.

**Next step**

- Land issue `#450`, then pick up `#452` for the Bareiss executable scaffold or `#469` for the determinant bridge once its dependency chain is ready.

**Blockers**

- None.
