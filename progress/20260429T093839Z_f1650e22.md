**Accomplished**

- Claimed `#946`.
- Added the executable `isGoodPrime` predicate to `HexBerlekampZassenhaus.lean`.
- Added immediate helper surfaces for reducing the integer leading coefficient and stating the modular square-free gcd precondition.
- Added theorem shells connecting a successful good-prime check to leading-coefficient admissibility and the square-free modular precondition.
- Verified `lake build HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested forbidden-pattern scan.

**Current frontier**

- `HexBerlekampZassenhaus` now has the first prime-selection admissibility predicate needed before `choosePrime` and `choosePrimeData`.

**Next step**

- Add the bounded/default prime chooser surface in a later narrow issue, using `isGoodPrime` as the admissibility filter.

**Blockers**

- No blockers for this slice.
