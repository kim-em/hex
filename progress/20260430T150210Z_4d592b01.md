**Accomplished**

- Claimed #1347 and moved the public `HexHensel/Quadratic.lean` theorem
  surface off direct `sorry`s for `quadraticHenselStep_factor_spec`,
  `quadraticHenselStep_bezout_spec`, `quadraticHenselStep_spec`, and
  `quadraticHenselStep_monic`.
- Added named private obligations for the remaining quadratic proof work:
  raw factor congruence, raw Bezout congruence, and the monic `g` update.
- Filed follow-up issues #1387, #1388, and #1389, and marked #1388 blocked on
  #1389 because the Bezout correction proof needs the updated divisor monic.
- Verified `lake build HexHensel`, `lake build HexHensel.Conformance`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a forbidden-pattern scan of
  `HexHensel/Quadratic.lean` for `axiom`, `native_decide`, `TODO`, and
  `FIXME`.

**Current frontier**

- `quadraticHenselStep_spec` is now a thin conjunction of the two public
  component theorems.
- The remaining quadratic proof frontier is concentrated in the three private
  helpers introduced in `HexHensel/Quadratic.lean`.

**Next step**

- Prove #1389 first to establish the monic update invariant, then use that
  foundation for the raw factor and Bezout congruence issues.

**Blockers**

- None for this decomposition and wrapper PR.
