**Accomplished**

- Claimed feature issue `#296` and added [`HexGramSchmidt/Conformance.lean`](/home/kim/hex/worktrees/0c35b345/HexGramSchmidt/Conformance.lean:1) as the Phase 3 core conformance slice for the foundational basis-and-determinant API.
- Covered the requested executable surface on committed fixtures: integer `basis`, `coeffs`, `gramDet`, `gramDetVec`, `scaledCoeffs`, plus rational `basis`, `coeffs`, and `gramDet`.
- Wired the new conformance module into [`HexGramSchmidt.lean`](/home/kim/hex/worktrees/0c35b345/HexGramSchmidt.lean:1) so the checks elaborate during ordinary builds.
- Verified `lake build HexGramSchmidt` succeeds after the change.

**Current frontier**

- `HexGramSchmidt` still needs follow-up Phase 3 conformance slices for the row-update formulas (`sizeReduce`, `adjacentSwap`) before the library can complete its full conformance phase and unblock a phase bump.
- The new file stays intentionally scoped to the core basis/Gram-determinant surface and does not cover the update theorems exported from `HexGramSchmidt/Update.lean`.

**Next step**

- Publish the branch as the PR for `#296` so the queue can move on to the remaining `HexGramSchmidt` conformance surface or downstream `HexGramSchmidtMathlib` work once the full phase is complete.

**Blockers**

- None in this session.
