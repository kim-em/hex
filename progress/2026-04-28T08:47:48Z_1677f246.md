**Accomplished**

- Claimed review issue `#560` and audited `HexMatrixMathlib` against `SPEC/Libraries/hex-matrix-mathlib.md` and `PLAN/Phase1.md`.
- Verified the prior `#521` failure mode is repaired: the rank/span/nullspace bridge now lives under the canonical `HexMatrixMathlib` namespace and reuses the single exported `matrixEquiv`.
- Bumped `libraries.yml` so `HexMatrixMathlib.done_through = 1`, moving the library to the Phase 2 review lane.
- Verified `lake build HexMatrixMathlib`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py`.

**Current frontier**

- The review work is complete and ready to package as the `#560` PR.

**Next step**

- Commit the phase bump with this progress note and open the PR closing `#560`.

**Blockers**

- None.
