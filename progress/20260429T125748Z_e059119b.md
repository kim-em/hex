**Accomplished**

- Claimed feature issue `#1063` for the HexGF2 fixed-row monomial no-effect proof surface.
- Added high-word one-hot false-bit support for target bits beyond the carry window.
- Added `xorClmulAt` no-effect lemmas for active monomial rows in the low-before-shift and high-after-carry cases.
- Lifted those cases through the fixed-row `(List.range (k / 64 + 1)).foldl` used by right multiplication by a monomial.
- Verified `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested banned-term scan.

**Current frontier**

- `HexGF2/Multiply.lean` now has fixed-row active-bit lemmas for low/high hits, unchanged-word lemmas for unrelated words, and no-effect lemmas for same-word bits outside the shifted monomial row contribution.
- This should unblock issue `#1062`, which can assemble the outer right-monomial coefficient bridge over source rows.

**Next step**

- Claim `#1062` and prove the public right-monomial theorem `q * monomial k = q.mulXk k` using the fixed-row active/no-effect lemmas.

**Blockers**

- None for this issue.
