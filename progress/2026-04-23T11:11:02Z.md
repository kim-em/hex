**Accomplished**

- Claimed PR `#422` for repair after confirming it was the top failed candidate in `coordination list-pr-repair`.
- Diagnosed the CI failure to `HexMatrixMathlib/Nullspace.lean`, where the bridge theorem still used the pre-PR typeclass context after `HexMatrix.rref` / `HexMatrix.Matrix.nullspace` gained a `[DecidableEq R]` requirement.
- Added the missing `[DecidableEq R]` assumption to the module-level variable context in [HexMatrixMathlib/Nullspace.lean](/home/kim/hex/worktrees/3c2bf085/HexMatrixMathlib/Nullspace.lean).
- Verified the repair with `lake build HexMatrixMathlib.Nullspace` and a full `lake build`, both passing locally.

**Current frontier**

- The PR branch is locally repaired and ready to publish.
- Remaining warnings are pre-existing `sorry` / lint warnings elsewhere in the project, not regressions from this repair.

**Next step**

- Commit the minimal repair together with this progress note.
- `git push --force-with-lease` the PR branch and run `coordination mark-pr-salvaged 422`.

**Blockers**

- No blocker.
