**Accomplished**

- Claimed feature issue `#1073`.
- Added outer right-monomial no-effect fold lemmas in `HexGF2/Multiply.lean` for low-word before-shift, high-word after-carry, and neither-adjacent-word cases.
- Added packed `mulWords xs (monomial k).words` coefficient lemmas covering `target < k` and shifted source words outside `xs`.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-token grep.

**Current frontier**

- The right-monomial raw coefficient proof now has outer no-effect cases available at both source-row fold and packed `mulWords` levels.
- The final public `mul_monomial` theorem remains out of scope for this issue.

**Next step**

- Use these lemmas in the successor issue to finish the raw coefficient bridge and then prove `q * monomial k = q.mulXk k`.

**Blockers**

- None.
