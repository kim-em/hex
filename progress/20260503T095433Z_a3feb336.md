**Accomplished**

Closed the Phase 1 scaffolding gap for `hex-lll` (issue #1836):

- Added `Hex.LLLState.ofBasis : Matrix Int n m → b.independent → LLLState n m`
  in `HexLLL/Basic.lean`. The `ν` and `d` fields draw from
  `GramSchmidt.Int.scaledCoeffs` and `GramSchmidt.Int.gramDetVec`. The
  `ν_eq`/`d_eq` proof fields are `sorry` with comments naming
  `GramSchmidt.Int.scaledCoeffs_eq` and
  `GramSchmidt.Int.gramDetVec_eq_gramDet` as the witness lemmas.
- Added the `lllAux` outer-loop entry point as a Phase 1 stub returning
  `s.b`. The full SPEC body using well-founded recursion on
  `(s.potential, n - k)` is deferred to Phase 5.
- Added `Hex.lll`, dispatching to `lllAux ∘ LLLState.ofBasis`. The
  body has no inline `sorry` placeholders; all proof debt is inside
  `LLLState.ofBasis`.
- Added `Hex.lll.firstShortVector` (returns the first row of the reduced
  basis) and `Hex.lll.shortVectors` (returns the full reduced basis as
  an `Array`). Both surface the SPEC's downstream-consumer entry points.

**Current frontier**

- `lake build HexLLL` is clean. The new `LLLState.ofBasis` adds one
  `sorry`-using declaration on top of the nine that were already in
  `HexLLL/Basic.lean`; `lllAux`, `lll`, `lll.firstShortVector`, and
  `lll.shortVectors` are all `sorry`-free.
- `python3 scripts/status.py HexLLL` still reports
  `Phase 1 (library scaffolding)` with `done_through: 0`. The
  remaining Phase 1 deliverables for `hex-lll` (the proof statements
  named in the SPEC for `lll_same_lattice`, `lll_reduced`, and
  `lll_short_vector`) are not part of this issue.
- `python3 scripts/check_dag.py` passes.

**Next step**

- BZ wiring (`hex-berlekamp-zassenhaus` recombination calling
  `lll.firstShortVector`) is the natural follow-up — that is the
  out-of-scope "issue E" referenced from #1836.
- The two `sorry`s inside `LLLState.ofBasis.ν_eq`/`d_eq` are scheduled
  for Phase 5, alongside the underlying `GramSchmidt.Int.scaledCoeffs_eq`
  and `GramSchmidt.Int.gramDetVec_eq_gramDet` proofs.
- The `lllAux` body itself remains a stub; replacing it with the SPEC's
  well-founded recursion (and discharging the `(s.potential, n - k)`
  termination) is also Phase 5 work.

**Blockers**

None for this scaffolding step.
