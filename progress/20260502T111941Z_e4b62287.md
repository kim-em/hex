**Accomplished**

- Claimed #1806, found that its top-level reassembly proof depends on missing lower-level rational polynomial algebra and division/gcd proof surfaces, and decomposed it into #1823, #1824, and #1825.
- Claimed #1823 and added the compiling HexPolyZ branch helpers `rat_scale_zero`, `rat_scale_one`, and `densePoly_eq_zero_of_isZero_true`.
- Split the remaining scale-product multiplication helper into #1826 and added it as a dependency of #1824.

**Current frontier**

- The zero and derivative-zero branch support lemmas for the future `primitiveSquareFreeDecomposition_reassembly_over_rat` proof are in place.
- The remaining #1823 work is the coefficient/fold proof of `rat_scale_mul_scale`, now tracked by #1826.

**Next step**

- Prove #1826 using the existing `toRatPoly_mulCoeffStep` / `mulCoeffSum` fold pattern, then continue with the rational gcd reconstruction helper #1824.

**Blockers**

- #1824 and #1825 should remain blocked until the scale-product multiplication helper lands.
