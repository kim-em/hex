**Accomplished**

- Claimed #1844, assessed it as too large for one session, and decomposed it into:
  #1851 for array-degree support lemmas, #1852 for shifted-subtraction cancellation,
  and #1853 for the final `HexPolyFp` remainder-degree proof.
- Released #1844 through `coordination skip` with a decomposition breadcrumb.
- Claimed #1851 and added proof-only `arrayDegreeAux` / `arrayDegree?` lemmas in
  `HexPoly/Euclid.lean` for:
  - returned degree is in bounds;
  - returned degree has nonzero coefficient;
  - coefficients above a returned degree are zero;
  - `none` means all coefficients are zero.
- Added the `ofCoeffs_degree_getD_lt_of_forall_zero_ge` bridge from raw array
  zero-above-bound facts to a normalized dense-polynomial degree bound.

**Current frontier**

- Verification passed:
  - `lake build HexPoly.Euclid`
  - `lake build HexPolyFp.Basic`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - placeholder scan over `HexPoly/Euclid.lean` found no `axiom`,
    `native_decide`, `TODO`, `FIXME`, or `sorry`.
- `lake build HexPolyFp.Basic` still reports only the pre-existing `sorry`
  warnings in `HexPolyFp/Basic.lean`.

**Next step**

- Prove #1852: `subtractScaledShift` frame facts and leading-term cancellation
  under `ZMod64.PrimeModulus`.

**Blockers**

- None for #1851.
