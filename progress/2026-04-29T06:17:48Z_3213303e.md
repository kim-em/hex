**Accomplished**

- Claimed `#836`.
- Added public `DensePoly.neg` and its coefficient theorem, implemented through the existing executable subtraction operation.
- Added proved `FpPoly` additive/subtractive/negation laws: `add_zero`, `zero_add`, `add_comm`, `add_assoc`, `neg_zero`, additive inverse cancellation, `sub_zero`, `zero_sub`, `sub_self`, and `sub_eq_add_neg`.
- Updated `HexGfqRing.Operations` to use the public polynomial negation surface instead of its private `negPoly` helper.
- Verified `lake build HexPolyFp`, `lake build HexGfqRing`, `python3 scripts/check_dag.py`, `git diff --check`, and that the source diff adds no `sorry`, `axiom`, or `native_decide`.

**Current frontier**

- `FpPoly` now has the additive law surface needed by downstream quotient-ring proofs.
- Multiplicative polynomial laws remain in blocked follow-up `#838`, with quotient reduction laws in `#839`.

**Next step**

- Open a PR closing `#836`.

**Blockers**

- None.
