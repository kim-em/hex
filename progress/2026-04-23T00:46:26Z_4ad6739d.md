**Accomplished**

- Claimed issue `#29` and added [HexArith/Montgomery/RedcNat.lean](/home/kim/hex/worktrees/4ad6739d/HexArith/Montgomery/RedcNat.lean:1) with the Phase 1 Nat-level Montgomery REDC scaffold: `montgomeryRadix`, `redcNat`, and the theorem declarations `redcNat_eq_mod`, `redcNat_lt`, and `redcNat_u_lt_two_p`.
- Updated [HexArith.lean](/home/kim/hex/worktrees/4ad6739d/HexArith.lean:1) to re-export the new Montgomery Nat layer and mention it in the root docstring.
- Verified this slice with `lake build HexArith`; the build succeeds, with `sorry` warnings restricted to theorem placeholders in existing scaffold modules and the new REDC theorem statements.

**Current frontier**

- `HexArith` now exposes the Montgomery Nat-level REDC scaffold below the user-facing context layer, but the inverse-computation layer (`Montgomery/InvNat`) and executable `UInt64` REDC bridge (`Montgomery/Redc`) are still absent.
- The current Montgomery context remains a direct modular scaffold; later Phase 1 work should refactor it to delegate through the new REDC and inverse layers.

**Next step**

- Claim the next Montgomery follow-on issue for `HexArith/Montgomery/InvNat` or `HexArith/Montgomery/Redc`, then connect `MontCtx` to those lower-level modules.

**Blockers**

- None.
