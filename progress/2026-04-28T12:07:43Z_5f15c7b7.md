**Accomplished**

- Claimed `#745` and replaced the duplicate `LinearLiftResult.deltaG` / `deltaH` helpers in `HexHensel/Linear.lean` with a single `liftScaledIncrement` definition.
- Updated both `linearHenselStep` call sites to use the shared helper.
- Verified `lake build HexHensel` and `python3 scripts/check_dag.py`.
- Confirmed `rg -n "deltaG|deltaH" HexHensel -g '*.lean'` returns no hits after the refactor.

**Current frontier**

- The repair is complete and ready to publish as a PR closing `#745`.

**Next step**

- Commit `HexHensel/Linear.lean` together with this progress note and run `coordination create-pr 745`.

**Blockers**

- A direct runtime smoke test of `linearHenselStep` is still blocked by existing `sorry`-backed normalization proofs in the polynomial stack, so executable evaluation hits `sorryAx` even though the module builds successfully.
