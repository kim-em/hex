**Accomplished**

- Claimed issue #1147.
- Added `HexBerlekampMathlib.Basic` theorem-level API for the `FpPoly` to `Polynomial (ZMod p)` bridge: coefficient transport through `ZMod64.equiv`, the reusable `HexPolyMathlib` coefficient bridge, monic transfer, basis-size/nat-degree transfer, derivative transfer, gcd transfer, and square-free-to-coprime transfer.
- Preserved the existing final theorem names for Berlekamp factor irreducibility and Rabin irreducibility.
- Verified `lake build HexBerlekampMathlib`, `python3 scripts/status.py HexBerlekampMathlib`, `python3 scripts/check_dag.py`, `git diff --check`, policy scans, and proof-position-only `sorry` use in `HexBerlekampMathlib`.

**Current frontier**

- `HexBerlekampMathlib.Basic` now exposes the missing transfer surface needed by downstream Berlekamp correctness work, with proof bodies intentionally left as theorem-position `sorry`s.

**Next step**

- Prove the new bridge lemmas, starting with coefficient transport and using those to discharge monic, degree, derivative, gcd, and square-free/coprime facts.

**Blockers**

- No blocker for this API-surface step. Full proof completion still depends on reconciling the executable `ZMod64`/`FpPoly` instances with Mathlib algebraic instances cleanly.
