**Accomplished**

- Claimed #1584 and replaced the proof-body `sorry` in
  `normalizeMonic_squareFreeContributionReachable`.
- Added local prime-field helper facts for `1 ≠ 0`, inverse nonzeroness, and
  multiplication by zero over `ZMod64 p`.
- Proved that if the normalized monic part has stored size one, the original
  nonzero input also has stored size one, so the normalized constant coefficient
  is `1`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`,
  `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`,
  `git diff --check`, and the requested forbidden-token scans over `HexPolyFp`.

**Current frontier**

#1584 is complete. `HexPolyFp/SquareFree.lean` still has the out-of-scope
proof-body gaps for contribution reconstruction, derivative-zero p-th-root
descent, and pairwise/factor square-freeness work.

**Next step**

Proceed to #1585, which owns the p-th-root recursive contribution descent facts
now that normalized monic reachability is available.

**Blockers**

None.
