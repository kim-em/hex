# 2026-05-04 04:51Z — work session, issue #2007

## Accomplished

- Claimed and resolved #2007 (HexPoly: schoolbook vs Karatsuba/FFT
  cross-check at degree 64-1024) by selecting Path B per the issue
  body — deferred and closed with a forward-pointing note.
- Verified the issue's premise still holds:
  - `HexPoly/Operations.lean:127` is the only multiplication strategy
    (schoolbook).
  - `grep -r 'Karatsuba|FFT|Toom' HexPoly` returns nothing.
  - `SPEC/Libraries/hex-poly.md:21` lists `(schoolbook, Karatsuba for
    large degree)` but the SPEC mention is informational; no
    planner-issued feature issue currently requests the implementation.
- HexPoly sits at `libraries.yml: done_through: 4`. Filing a Karatsuba
  implementation issue from a worker session (Path A) would preempt the
  planner's roadmap, so Path B (defer + close with breadcrumb) was the
  conservative choice.

## Current frontier

The fast-vs-fast cross-check cluster (#2006-#2011) plus the conformance
size-bump cluster (#2012-#2020) remain on the human-oversight queue.
Several were already claimed during this session window. The oracle
infrastructure bootstrap (#1986) is also claimed and unblocks the
python-flint oracle issues (#1987-#2004).

## Next step

When a future feature issue introduces a Karatsuba (or FFT) path in
`HexPoly/Operations.lean`, the cross-check ticket should be refiled
with the fixture sketch (degrees 64, 128, 256, 512, 1024; sparse +
dense; <2s elaboration target) preserved from #2007.

## Blockers

None for this session — closure is the resolution.
