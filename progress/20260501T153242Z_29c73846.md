**Accomplished**

- Claimed #1637 and proved the Montgomery REDC UInt64 bridge lemmas in `HexArith/Montgomery/Redc.lean`.
- Added local private bridge facts for the Montgomery low-word correction, `addCarry` exactness, `mulFull` projection equivalence, and Nat-level modulus positivity/bounds.
- Verified `redc_m_spec`, `redc_u_spec`, `redc_sub_spec`, and the public `toNat_redc` bridge without changing executable REDC behavior.

**Current frontier**

- `HexArith/Montgomery/Redc.lean` no longer contains `sorry`, `axiom`, `native_decide`, `TODO`, or `FIXME`.
- Existing Phase 5 proof placeholders remain in other HexArith modules, especially Montgomery inverse/context and Nat prime/ext-gcd surfaces.

**Next step**

- Continue HexArith Phase 5 with the downstream Montgomery context proofs or the remaining Montgomery inverse bridge, depending on planner priority.

**Blockers**

- None for #1637.
