**Accomplished**

- Claimed feature issue `#1032` for `HexModArith` Phase 4 benchmark signal tuning.
- Added fixed hot-loop amplification in `HexModArith/Bench.lean` for the low-signal checksum-style registrations while preserving the `n => n` declared model.
- Moved `runConstructChecksum`, `runAddChecksum`, `runSubChecksum`, `runMulChecksum`, `runPow`, and `runBarrettMulModChain` onto high-signal scientific schedules.
- Verified `runConstructChecksum`, `runAddChecksum`, `runSubChecksum`, `runMulChecksum`, and `runBarrettMulModChain` now report `consistent with declared complexity`.
- Re-ran the remaining `HexModArith` registrations; `runCastChecksum`, `runInvChecksum`, `runMontToChecksum`, `runMontMulChain`, and `runMontFromChecksum` reported `consistent with declared complexity`.
- Found that `runPow` now produces verdict-eligible rows but remains inconclusive, with residual superlinear slope against the declared linear model.
- Filed follow-up issue `#1043` for the `runPow` scaling finding.
- Left `libraries.yml` unchanged with `HexModArith.done_through: 3`.
- Verified `lake build HexModArith.Bench`, `lake exe hexmodarith_bench verify`, `python3 scripts/status.py HexModArith`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and `rg -n "native_decide" HexModArith -S`.

**Current frontier**

- `HexModArith` Phase 4 is no longer blocked by too-fast measurements for construction, addition, subtraction, multiplication, or Barrett multiplication.
- `HexModArith` Phase 4 remains blocked by the real `runPow` benchmark finding tracked in `#1043`.
- `HexPolyFp` Phase 4 remains blocked on `HexModArith.done_through >= 4`.

**Next step**

- A worker should claim `#1043`, diagnose and fix the `ZMod64` exponentiation scaling issue or prove the benchmark is measuring avoidable overhead, then re-run the full `HexModArith` Phase 4 scientific review.

**Blockers**

- `Hex.ModArithBench.runPow` reports an inconclusive verdict on verdict-eligible rows at params `8192`, `16384`, and `32768`.
