**Accomplished**

- Claimed `#832`, assessed it as oversized, decomposed it into `#835`, `#836`, `#838`, and `#839`, then released `#832` for replan triage with the required breadcrumb.
- Claimed `#835`.
- Added proved `DensePoly` coefficient-extensionality helpers in `HexPoly/Dense.lean`: equality from matching size and coefficients, out-of-support coefficients are zero, last stored coefficient is nonzero for positive size, coefficient equality forces size equality, and full coefficient extensionality.
- Verified `lake build HexPoly`, `lake build HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and that the source diff adds no `sorry` or `axiom`.

**Current frontier**

- `HexPoly/Dense.lean` now has the foundational extensionality surface needed by the additive and multiplicative polynomial-law follow-ups.
- The remaining algebra-law work is tracked by blocked follow-ups `#836`, `#838`, and `#839`.

**Next step**

- Open a PR closing `#835`.

**Blockers**

- None for `#835`.
