**Accomplished**

- Claimed issue `#473` and scaffolded the initial `HexGfqRing` quotient core in [HexGfqRing/Basic.lean](/home/kim/hex/worktrees/43c04664/HexGfqRing/Basic.lean:1), adding `FpPoly.degree`, the executable `reduceMod` wrapper, the reduced-representative `PolyQuotient` type, and the first `ofPoly`/`repr` lemma surface.
- Exported the new module from [HexGfqRing.lean](/home/kim/hex/worktrees/43c04664/HexGfqRing.lean:1) with a concise library docstring describing the current scope.
- Verified `lake build HexGfqRing` and `python3 scripts/check_dag.py` succeed on this branch.

**Current frontier**

- `HexGfqRing` now has the representation layer needed for follow-on quotient operations, but the degree bound and reduction-idempotence facts remain scaffold theorems.

**Next step**

- Land the issue `#473` PR so the dependent ring-operations issue can build on the new quotient core.

**Blockers**

- None.
