**Accomplished**

- Claimed `#647` and reworked the `HexModArith` additive core so `ZMod64.add`, `ZMod64.sub`, and `ZMod64.neg` now use native `UInt64` arithmetic paths instead of converting through `Nat`.
- Added the small `modulusWord` / `complementWord` helpers in `HexModArith/Basic.lean` to support the `p < 2^64` correction path while keeping the `p = 2^64` case on direct wrapped machine-word operations.
- Updated the downstream theorem wrappers affected by the new bodies and kept the existing theorem surface compiling.
- Verified the result with `lake build HexModArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexModArith` now matches the intended machine-word execution shape for the additive residue core, but the proof obligations for the new bound/canonical-representative lemmas remain scaffolded with `sorry`.

**Next step**

- Publish the `#647` PR so the `HexModArith` review chain can move on to the remaining hot-loop wrapper / characteristic-`p` follow-up in `#646`.

**Blockers**

- None.
