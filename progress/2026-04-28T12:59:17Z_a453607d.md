**Accomplished**

- Claimed issue `#775` and added the committed-entry Conway wrapper surface in `HexConway/Basic.lean`.
- Introduced `SupportedEntry`, `supportedEntry_2_1`, `conwayPoly`, and the corresponding nonconstant / irreducible theorem surface by reusing `luebeckConwayPolynomial?` rather than widening the imported table.
- Imported `HexGfqRing.Basic` so the wrapper theorems can expose `0 < FpPoly.degree ...`, then verified `lake build HexConway` and `python3 scripts/check_dag.py`.
- Bumped `libraries.yml` so `HexConway.done_through = 1`; `python3 scripts/status.py` now advances `HexConway` to Phase 2 review and unblocks `HexGfq` Phase 1.

**Current frontier**

- The issue work is complete and ready to publish as a PR.

**Next step**

- Commit the Conway wrapper and phase-state bump, then open the PR closing `#775`.

**Blockers**

- The `second-opinion` wrapper did not return output in this session, so final validation relied on the local Lean/DAG/status checks above.
