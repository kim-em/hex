**Accomplished**

- Claimed #1423 and added the private `linearHenselStep_correction_identity` helper in `HexHensel/Linear.lean`, proving the `FpPoly` raw correction identity from the division equation and Bezout equality.
- Added the private `modP_one` bridge lemma in `HexHensel/Linear.lean`, proving that reducing integer `1` modulo `p` is the field-polynomial `1`.
- Created follow-up #1426 for the remaining transport/scaling layer of `linearHenselStep_raw_factor_congr`.
- Ran `lake build HexHensel`, `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern scan of `HexHensel/Linear.lean`.

**Current frontier**

- `linearHenselStep_raw_factor_congr` remains the active proof-body placeholder.
- The `FpPoly` correction identity and `modP_one` bridge are now available for instantiating the final raw congruence proof.
- Existing unrelated `HexHensel/Linear.lean` proof-body `sorry`s remain.

**Next step**

- Use `DensePoly.divMod_spec`, the lifted Bezout congruence, `FpPoly.modP_liftToZ`, and `modP_one` to derive the `FpPoly` correction equality, transport it back to `ZPoly.congr ... p`, then combine with `scale_coeffwiseDiv_sub_of_congr` and the `hk : 1 ≤ k` divisibility calculation for the `p^(k+1)` proof.

**Blockers**

- No build blockers.
