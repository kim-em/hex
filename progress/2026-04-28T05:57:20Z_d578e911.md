**Accomplished**

- Claimed issue `#434` and scaffolded `HexPoly`'s first real module in [HexPoly/Dense.lean](/home/kim/hex/worktrees/d578e911/HexPoly/Dense.lean), adding the dense coefficient representation, normalization helper, normalized constructors, coefficient/degree queries, and support extraction.
- Replaced the empty root module with [HexPoly.lean](/home/kim/hex/worktrees/d578e911/HexPoly.lean) importing the dense scaffold and carrying the required module-level summary.
- Verified `lake build HexPoly` and `python3 scripts/check_dag.py` both succeed.

**Current frontier**

- `HexPoly` now has its normalized dense storage surface, but arithmetic, evaluation, derivative/composition, and Euclidean-algorithm layers remain for follow-up issues.

**Next step**

- Land this issue PR, then take the dependent polynomial arithmetic scaffold issue that builds on `DensePoly`.

**Blockers**

- None.
