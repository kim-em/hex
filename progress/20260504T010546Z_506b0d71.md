**Accomplished**

- Claimed #1953 and reviewed `HexBerlekamp/Bench.lean` against `SPEC/benchmarking.md`, `PLAN/Phase4.md`, and `SPEC/Libraries/hex-berlekamp.md`.
- Confirmed the benchmark executable is wired in `lakefile.lean` and CI smoke runs `lake exe hexberlekamp_bench list` plus `verify`.
- Ran the HexBerlekamp benchmark smoke checks and all four scientific benchmark registrations.
- Confirmed `runRabinTestChecksum` is consistent with its declared cubic model.
- Found inconclusive scientific verdicts for `runBerlekampMatrixChecksum`, `runBerlekampFactorChecksum`, and `runDistinctDegreeChecksum`.
- Filed bench-found follow-up #1963 with the observed verdicts and timing rows.

**Current frontier**

- `HexBerlekamp.done_through` remains `3`; Phase 4 is not complete.
- `status/hex-berlekamp.benchmarks-reviewed` was not added because not every registered benchmark returned a consistent verdict.
- `python3 scripts/check_phase4.py HexBerlekamp` is not a valid command in this checkout; the script currently accepts `--all` instead.

**Next step**

- Work #1963: diagnose whether the inconclusive verdicts come from the implementations, benchmark fixture families, or invalid declared models, then rerun the full HexBerlekamp Phase 4 validation.

**Blockers**

- HexBerlekamp Phase 4 advancement is blocked on resolving #1963.
