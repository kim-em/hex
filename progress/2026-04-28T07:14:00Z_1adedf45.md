**Accomplished**

- Claimed issue `#540` and replaced the forbidden `axiom` placeholders for `DensePoly.polyCRT_mod_fst` and `DensePoly.polyCRT_mod_snd` with proper `theorem` declarations in [HexPoly/Euclid.lean](/home/kim/hex/worktrees/1adedf45/HexPoly/Euclid.lean).
- Added short docstrings documenting the first- and second-modulus CRT congruence surfaces.
- Verified the change with `lake build HexPoly`, `python3 scripts/check_dag.py`, and `rg -n "\\baxiom\\b" HexPoly`.

**Current frontier**

- `HexPoly` no longer exposes the CRT modulo-equality API as axioms, but the corresponding theorem proofs remain scaffolded with `sorry` alongside the rest of the library's proof surface.

**Next step**

- Publish the `#540` PR and let a later review determine whether this closes the final remaining Phase 1 gap in `HexPoly` or whether additional root-library follow-up is still needed before any `libraries.yml` bump.

**Blockers**

- None.
