**Accomplished**

- Claimed #1647 and added `Hex.clmul_comm` in `HexGF2/Clmul.lean`.
- Proved commutativity by reconstructing words as XOR folds over one-hot bits,
  using the existing one-hot left/right CLMUL formulas and fold linearity.
- Kept `pureClmul`, `clmul`, and the extern boundary unchanged.
- Verified `lake build HexGF2.Clmul`, `lake build HexGF2.Multiply`,
  `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned `HexGF2/Clmul.lean` for forbidden
  placeholders.

**Current frontier**

- #1647 is ready for PR.
- `HexGF2.Multiply` still has its existing proof placeholders for packed
  multiplication commutativity and associativity; the new word-level
  `clmul_comm` is intended to support the next packed multiplication issue.

**Next step**

- Create the PR closing #1647.

**Blockers**

- None.
