**Accomplished**

- Claimed #1907 and added final-column determinant expansion support in `HexMatrix.Determinant`.
- Added reusable fold helpers for accumulator congruence, right scalar extraction, nonzero-start sums, and final-element splitting.
- Added `detFinalColumnOffDiagonal`, `det_finalColumn_diagonal_sum`, and `det_finalColumn_expansion` for partitioning the Leibniz determinant by the row mapped to the final column.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Determinant.lean`
- `lake build HexMatrix` still reports pre-existing warnings/sorries in `HexMatrix.RREF` and `HexMatrix.Bareiss`; no new placeholders were added in `HexMatrix.Determinant`.

**Next step**

- Use `det_finalColumn_expansion` to specialize bordered-minor determinant expansions and factor the off-diagonal terms into the Desnanot-Jacobi/Bareiss recurrence shape.

**Blockers**

- None for this slice.
