**Accomplished**
- Claimed #1125 and identified it as the critical-path GF2 multiplication prerequisite for the quotient-step chain.
- Added `pureClmul_xor_left` / `clmul_xor_left` in `HexGF2/Clmul.lean`, proving carry-less multiplication is left-linear over word XOR.
- Added private bit and `xorClmulAt` coefficient lemmas in `HexGF2/Multiply.lean` using the new `clmul_xor_left` support.
- Created follow-up issue #1127 for the remaining nested `mulWords` left-XOR coefficient fold proof, and commented the decomposition breadcrumb on #1125.

**Current frontier**
- The word-level and single-step accumulator prerequisites for raw `mulWords` left-additivity are now available and build.
- The full private theorem
  `coeffWords (mulWords (xorWords xs ys) zs) n = (coeffWords (mulWords xs zs) n != coeffWords (mulWords ys zs) n)`
  remains to be proved in #1127.

**Next step**
- Claim #1127 and finish the nested fold proof in `HexGF2/Multiply.lean`, handling the `mulWords` early-empty branch and differently sized raw accumulators.

**Blockers**
- No technical blocker for the current partial patch.
- The remaining proof is bookkeeping-heavy enough to need a focused follow-up rather than being bundled into this support slice.
