**Accomplished**

- Claimed issue `#443` and added [HexPoly/Euclid.lean](/home/kim/hex/worktrees/903e1f9e/HexPoly/Euclid.lean) with executable long-division scaffolding (`divModMonic`, field-based `divMod`, quotient/remainder wrappers), the derived `gcd` / `xgcd` surface, integer `content` / `primitivePart`, and `polyCRT`.
- Added the corresponding theorem shells for division correctness, gcd divisibility, Bezout, content reconstruction, and the two CRT modulus contracts expected by the spec.
- Updated [HexPoly.lean](/home/kim/hex/worktrees/903e1f9e/HexPoly.lean) to export the new Euclidean layer and refreshed the root module docstring.
- Verified `lake build HexPoly` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexPoly` now exposes the full Phase 1 Euclidean-algorithm and CRT/content API surface needed by downstream polynomial libraries, but the new theorem statements in [HexPoly/Euclid.lean](/home/kim/hex/worktrees/903e1f9e/HexPoly/Euclid.lean) remain scaffolded with `sorry`.

**Next step**

- Land issue `#443` so `HexPolyZ`, `HexPolyFp`, `HexPolyMathlib`, and `HexHensel` follow-up work can build on the completed core polynomial API.

**Blockers**

- None.
