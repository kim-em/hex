**Accomplished**

- Claimed #1619, verified the target theorem still exists, and skipped it for replan after finding a statement-level counterexample at `fuel = 0`.
- Claimed #1620 and added `HexPolyMathlib.Bench` with three deterministic bridge conversion benchmark registrations:
  `runToPolynomialChecksum`, `runOfPolynomialChecksum`, and `runRoundTripChecksum`.
- Added the `hexpolymathlib_bench` Lake executable.
- Verified the new benchmark module, executable registration, library build, status, DAG, diff hygiene, and placeholder scan.

**Current frontier**

- `HexPolyMathlib` now has Phase 4 bridge conversion benchmark coverage.
- The dense-to-Polynomial benchmark uses an executable finite-support payload for the Mathlib polynomial because the public `toPolynomial` wrapper sits behind Mathlib's noncomputable semiring surface.
- Existing upstream `sorry` warnings remain in `HexPoly`, `HexPolyMathlib.Basic`, and `HexPolyMathlib.Euclid`.

**Next step**

- Review/merge the PR for #1620.
- Replan #1619 with stronger reachable-state/fuel hypotheses or a narrower invariant tied to `yunFactorsContribution_reconstruct_core`.

**Blockers**

- #1619 is blocked as written: at `fuel = 0`, `yunFactorsContribution c w multiplicity 0 = (1, w)`, so the nonterminal branch would force `1 = pow (c * w) multiplicity` for arbitrary non-unit `w`.
