**Accomplished**

- Claimed `#969`.
- Proved the `IsEchelonForm` pivot/free-column partition cluster in `HexMatrix/RowEchelon.lean`: `pivotCols_injective`, `freeColsList_length`, `freeCols_sorted`, `freeCols_injective`, `colPartition`, `colPartition_exclusive`, and `pivotCols_disjoint_freeCols`.
- Added local list/vector bridge helpers for pivot-column pairwise/nodup facts, free-column list indexing, and sorted filtered `finRange` lists.
- Verified `lake build HexMatrix.RowEchelon`, `lake build HexMatrix`, `python3 scripts/check_dag.py`, the requested `rg` scan, and `git diff --check`.

**Current frontier**

- The targeted column-partition proofs are complete.
- `HexMatrix/RowEchelon.lean` still has the pre-existing `transform_mul_inv` proof placeholder, which was outside this issue.

**Next step**

- Proceed to the downstream RREF span/nullspace correctness placeholders or the independent `transform_mul_inv` proof issue when dispatched.

**Blockers**

- No blockers for this slice.
