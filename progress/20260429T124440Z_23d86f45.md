**Accomplished**

- Checked unclaimed work and first claimed `#1001`, then released it as stale
  because its prerequisite `#1024` is still open.
- Claimed `#1054`, identified that the outer right-monomial assembly needs a
  missing bit-level fixed-row no-effect surface, and decomposed it into `#1061`
  and `#1062`.
- Claimed `#1061` and added two proved low-word one-hot CLMUL bit lemmas in
  `HexGF2/Multiply.lean`.
- Created residual issue `#1063` for lifting those bit-level facts through the
  fixed-row monomial fold, and updated `#1062` to depend on `#1063`.

**Current frontier**

- `#1061` has a coherent partial proof slice: low product bits before the
  one-hot shift are false, and low product bits at/after the one-hot shift read
  the corresponding source bit.
- The remaining fixed-row work is the `xorClmulAt` / inner-fold lift in
  `#1063`; the final outer theorem remains `#1062`.

**Next step**

- Finish `#1063` by proving the bit-level no-effect lemmas at the fixed-row
  fold level, then use them in `#1062` to prove `mul_monomial`.

**Blockers**

- None for the partial `#1061` slice.
