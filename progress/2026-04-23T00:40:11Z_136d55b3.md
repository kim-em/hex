**Accomplished**

- Claimed issue `#23` and added [HexArith/Montgomery/Context.lean](/home/kim/hex/worktrees/136d55b3/HexArith/Montgomery/Context.lean:1) with the Phase 1 Montgomery context scaffold: `MontCtx`, `MontCtx.mk`, `MontCtx.toMont`, `MontCtx.fromMont`, `MontCtx.mulMont`, and the user-facing theorem declarations.
- Updated [HexArith.lean](/home/kim/hex/worktrees/136d55b3/HexArith.lean:1) to re-export the new Montgomery context layer and extend the root docstring accordingly.
- Verified this slice with `lake build HexArith`; the build succeeds, with `sorry` warnings restricted to proof obligations.

**Current frontier**

- `HexArith` now exposes both the Barrett scaffold and the user-facing Montgomery context API, but the Montgomery lower layers from the spec (`RedcNat`, `InvNat`, `Redc`) are still absent.
- The executable Montgomery bodies currently encode the intended API shape using direct modular computations and helper witness searches; later Phase 1 issues should replace these with the dedicated REDC and inverse layers promised by the spec.

**Next step**

- Claim the next narrow `HexArith` issue for `Montgomery/RedcNat` or `Montgomery/InvNat`, then refactor the context layer to delegate to those lower-level modules.

**Blockers**

- None.
