**Accomplished**

- Claimed human-oversight issue `#541` and updated `HexModArith/Basic.lean` to the SPEC-mandated bounded core: added `ZMod64.Bounds`, moved `ZMod64` to `[Bounds p]`, and removed explicit `(hp, hword)` threading from `ofNat`, `zero`, `one`, `add`, and `sub`.
- Added the default `@[extern "lean_hex_zmod64_mul"]` multiplication surface with its Lean fallback contract plus the new `toNat_mul` / modulus lemmas and concrete smoke-test examples for the two issue-specified moduli.
- Added the C shim at `HexModArith/ffi/zmod64_mul.c` and wired it into the active Lake configuration in `lakefile.lean` (and the matching `lakefile.toml` entry for consistency with the issue wording).
- Verified `lake build HexModArith`, `lake build`, and `python3 scripts/check_dag.py`; also compiled the C shim under `clang -Wall -Wextra` and ran a native harness that returned residues `2` and `1813` for the requested sample multiplications.

**Current frontier**

- The fix is ready to publish as the closing PR for `#541`.

**Next step**

- Commit the bounded-`ZMod64` reshape and extern shim, then open the PR closing `#541` so downstream `HexModArith` follow-up issue `#453` can proceed on the new core shape.

**Blockers**

- None.
