**Accomplished**

- Claimed #1472 and narrowed the modular monic divmod remainder support proof
  in `HexHensel/Quadratic.lean`.
- Added the private aux invariant
  `divModMonicModSquareAux_remainder_coeff_eq_zero_of_monic` and proved
  `divModMonicModSquare_remainder_coeff_eq_zero_of_monic` by unfolding the
  wrapper and applying that invariant at `fuel = pRed.size`.
- Created follow-up #1476 for the remaining recursive aux support invariant.
- Verified `lake build HexHensel.Quadratic`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and scanned `HexHensel/Quadratic.lean` for
  `sorry`, `axiom`, `native_decide`, `TODO`, and `FIXME`.

**Current frontier**

- The original wrapper theorem from #1472 no longer has its own proof
  placeholder.
- The remaining placeholder is now the narrower aux theorem
  `divModMonicModSquareAux_remainder_coeff_eq_zero_of_monic`, which must prove
  support descent for the recursive modular monic division loop.

**Next step**

- Work #1476 by proving that each active `divModMonicModSquareAux` step clears
  the current leading coefficient modulo `m^2` for a monic divisor and lowers
  the normalized remainder support while preserving the `rem.size ≤ fuel`
  induction measure.

**Blockers**

- None, but #1472 is only partially completed and should land with
  `coordination create-pr --partial`.
