**Accomplished**

- Claimed issue `#468` and added [HexMatrixMathlib/RankSpanNullspace.lean](/home/kim/hex/worktrees/9734ed75/HexMatrixMathlib/RankSpanNullspace.lean) with the `vectorEquiv` / `matrixEquiv` conversion layer plus Phase 1 theorem scaffolding for `rank_eq`, row-span membership, coefficient witnesses, and nullspace-vs-kernel correspondence.
- Updated [HexMatrixMathlib.lean](/home/kim/hex/worktrees/9734ed75/HexMatrixMathlib.lean) to import the new bridge module and carry the library docstring.
- Verified `lake build HexMatrixMathlib` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexMatrixMathlib` now exposes the rank/span/nullspace bridge API expected by the Phase 1 plan, but all new bridge theorems in [HexMatrixMathlib/RankSpanNullspace.lean](/home/kim/hex/worktrees/9734ed75/HexMatrixMathlib/RankSpanNullspace.lean) remain proof scaffolds with `sorry`.

**Next step**

- Land issue `#468` so downstream matrix consumers can depend on the bridge signatures while later proof-focused issues fill in the Mathlib equivalence proofs.

**Blockers**

- None.
