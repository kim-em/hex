**Accomplished**

- Claimed issue `#304` and added [`HexBerlekamp/Kernel.lean`](/home/kim/hex/worktrees/230ea9af/HexBerlekamp/Kernel.lean:1) to introduce the Phase 1 Berlekamp-kernel boundary on top of the existing matrix scaffold.
- Implemented the executable `Q_f - I` helper `berlekampKernelMatrix`, the field-parameterized nullspace wrapper `berlekampKernel`, and entry theorems for the full, off-diagonal, and diagonal matrix formulas.
- Updated [`HexBerlekamp.lean`](/home/kim/hex/worktrees/230ea9af/HexBerlekamp.lean:1) to re-export the new kernel module.
- Verified `lake build HexBerlekamp` succeeds.

**Current frontier**

- `HexBerlekamp` now exposes the matrix-to-kernel bridge promised by the spec, but the kernel soundness theorem is still a Phase 1 stub and the module remains parameterized by a `Lean.Grind.Field (HexModArith.ZMod64 p)` instance rather than providing a concrete field instance for prime moduli.

**Next step**

- Add the prime-modulus field packaging needed to instantiate `berlekampKernel` on executable `ZMod64 p`, then continue to the later irreducibility / factor-splitting surfaces.

**Blockers**

- No blocking build failures remain in this session.
- A concrete executable `berlekampKernel` call path over `ZMod64 p` is still blocked on the missing field-instance layer for prime moduli.
