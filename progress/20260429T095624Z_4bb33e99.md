**Accomplished**

- Claimed `#955`.
- Added deterministic polynomial CRT benchmark fixtures in `HexPoly/Bench.lean`.
- Registered `runPolyCRTChecksum` with the schoolbook `O(n^2)` model.
- Updated the `HexPoly/Bench.lean` module docstring so polynomial CRT is listed as covered.
- Verified with `lake build HexPoly.Bench`, `lake exe hexpoly_bench list`, `lake exe hexpoly_bench verify`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` check.

**Current frontier**

- HexPoly Phase 4 benchmark registration now includes polynomial CRT.
- The remaining unclaimed queue still includes the summarize checkpoint and the GF2 Euclid chain, but the Euclid issue should wait for its current shift/mulXk prerequisites.

**Next step**

- Review and merge the PR for `#955`.

**Blockers**

- No blocker for this slice.
