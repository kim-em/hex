**Accomplished**

- Claimed #1877 and added private no-pivot Bareiss loop preservation helpers in `HexMatrix/Bareiss.lean`.
- Proved `noPivotLoop_matrix_entry_of_row_le_or_col_lt`, showing later no-pivot iterations preserve entries in completed rows or completed columns.
- Added the diagonal corollary `noPivotLoop_diag_of_le_step` and row-swap bookkeeping lemma `noPivotLoop_rowSwaps`.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Bareiss`
  - `lake build HexMatrix`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Bareiss.lean`
- The placeholder scan reports only the pre-existing `bareiss_eq_det` `sorry`.

**Next step**

- Use the new `noPivotLoop` preservation helpers from #1880/#1893 when packaging the public no-pivot prefix API and bordered-minor loop induction.

**Blockers**

- None for this issue.
