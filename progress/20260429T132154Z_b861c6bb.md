**Accomplished**

- Claimed feature issue `#1072`.
- Added the missing right-monomial active-row proof layer in
  `HexGF2/Multiply.lean`:
  - `words_monomial_size`
  - zero-shift active `xorClmulAt` and inner monomial-fold lemmas
  - outer `foldl_mulWords_monomial_active_zero`,
    `foldl_mulWords_monomial_active_low`, and
    `foldl_mulWords_monomial_active_high`
- Verified with:
  - `lake build HexGF2.Clmul`
  - `lake build HexGF2.Multiply`
  - `lake build`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|sorry|admit" HexGF2/Multiply.lean -S`
- Ran the `second-opinion` review wrapper on the diff; it reported no
  blockers.

**Current frontier**

- The active source row now toggles the expected source coefficient into
  target coefficient `n + k`, split across the zero shift, same-word, and
  carry-word cases.
- The existing no-effect inner lemmas are still available for successor
  work, but outer no-effect/suffix composition remains outside this issue.

**Next step**

- Continue with `#1073`, proving the right-monomial no-effect coefficient
  theorem needed to compose the full public right monomial multiplication
  result.

**Blockers**

- None.
