**Accomplished**

- Claimed #1435 and repaired the `HexHensel/Linear.lean` iterative wrapper
  theorem surface without changing `henselLift` or `henselLiftLoop`.
- Added public `LinearLiftLoopInvariant` and
  `LinearLiftStepDegreeInvariant` predicates so loop proofs can carry factor
  congruence, reusable Bezout data, monicity, and the explicit per-step
  correction-degree bound required by the one-step monicity theorem.
- Added the private `henselLiftLoop_invariant` induction surface and threaded
  the new invariant hypotheses into `henselLift_spec` and `henselLift_monic`.
- Verified with `lake build HexHensel.Linear`, `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and scans for forbidden
  placeholders in `HexHensel`.

**Current frontier**

- The Linear iterative wrapper now exposes the missing invariant shape needed
  for later induction work.
- Proof bodies remain intentionally open for the new loop invariant theorem and
  the adjusted wrapper theorems, matching #1435's theorem-surface scope.
- The separate one-step raw factor congruence bridge leaves remain owned by
  #1428.

**Next step**

- A follow-up proof issue can prove `henselLiftLoop_invariant`, then close
  `henselLift_spec` and `henselLift_monic` by projecting its factor congruence
  and monicity fields.

**Blockers**

- None for the theorem-surface repair.
