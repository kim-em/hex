**Accomplished**

- Claimed feature issue `#120` and verified it was the highest-value unclaimed item because `HexLll` is currently blocked on `HexGramSchmidt.done_through ≥ 1`.
- Added [HexGramSchmidt/Update.lean](/home/kim/hex/worktrees/0e31979c/HexGramSchmidt/Update.lean) with the Phase 1 row-operation scaffold for integer Gram-Schmidt data: executable `sizeReduce` and `adjacentSwap` helpers, named swapped-basis row projections, and theorem declarations for size-reduction, swap, `gramDet`, and `scaledCoeffs` update formulas.
- Updated [HexGramSchmidt.lean](/home/kim/hex/worktrees/0e31979c/HexGramSchmidt.lean) to re-export the new update module and mention the expanded row-operation surface in the root docstring.
- Verified the implementation with `lake build HexGramSchmidt`.

**Current frontier**

- The issue work is complete locally and ready to publish.

**Next step**

- Stage the new update module, root re-export, and this progress note; commit them; then run `coordination create-pr 120`.

**Blockers**

- None.
