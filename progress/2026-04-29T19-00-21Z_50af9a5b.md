**Accomplished**

- Claimed issue #1118 for `HexHenselMathlib`.
- Added `HexHenselMathlib/Correctness.lean` with the Mathlib-facing Hensel correctness, extension, degree, and uniqueness theorem statements.
- Imported the new correctness module from `HexHenselMathlib.lean` and updated the module description without workflow phase wording.
- Advanced `HexHenselMathlib.done_through` to `1` in `libraries.yml`.
- Verified with `lake build HexHenselMathlib`, `python3 scripts/status.py HexHenselMathlib`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested `rg` scans.

**Current frontier**

- `HexHenselMathlib` is ready for its next status item: scaffolding review of the new theorem surface and existing coprimality-lifting statements.

**Next step**

- Review whether the theorem hypotheses should be tightened before replacing the proof-only `sorry` bodies with actual Mathlib proofs.

**Blockers**

- None for this session.
