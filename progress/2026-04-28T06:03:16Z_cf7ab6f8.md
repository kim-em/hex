**Accomplished**

- Claimed issue `#447` and scaffolded the first `HexModArith` implementation slice in [HexModArith/Basic.lean](/home/kim/hex/worktrees/cf7ab6f8/HexModArith/Basic.lean).
- Added `Hex.ZMod64 (p : Nat)` with `UInt64` storage, reduced-Nat/word accessors, mod-`p` smart construction, and executable zero/one/add/sub definitions under explicit `0 < p` and `p ≤ 2^64` hypotheses.
- Added the basic reduction/bounds lemmas for the new surface and updated [HexModArith.lean](/home/kim/hex/worktrees/cf7ab6f8/HexModArith.lean) to export the module.
- Verified `lake build HexModArith`, `lake build`, and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexModArith` now has the core reduced-representation and additive scaffold needed for later modular multiplication, inversion, and ring-instance work.

**Next step**

- Publish the `#447` PR, then continue with the blocked follow-up work once the remaining `HexArith` dependencies land.

**Blockers**

- None.
