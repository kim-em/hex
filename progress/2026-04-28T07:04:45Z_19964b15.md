**Accomplished**

- Claimed issue `#438` and added the executable `HexArith` extended-GCD scaffold in [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/19964b15/HexArith/ExtGcd.lean:1), covering the `Nat` recursion plus the `Int` and `UInt64` API surfaces and their immediate correctness theorem declarations.
- Added the `Int.extGcd` extern boundary stub in [HexArith/ffi/mpz_gcdext.c](/home/kim/hex/worktrees/19964b15/HexArith/ffi/mpz_gcdext.c:1), wired it into [lakefile.toml](/home/kim/hex/worktrees/19964b15/lakefile.toml:14), and re-exported the new module from [HexArith.lean](/home/kim/hex/worktrees/19964b15/HexArith.lean:1).
- Verified the landed subset with `lake build HexArith`, `python3 scripts/check_dag.py`, and a small `#eval` probe for the new `extGcd` entry points.
- Decomposed the unresolved prime/binomial theorem remainder into follow-up issue `#537` and left the required breadcrumb on `#438`.

**Current frontier**

- The remaining scope from `#438` is now isolated in `#537`: the mathlib-free theorem surface for `Nat.choose`, `Nat.Prime.dvd_mul`, `Nat.choose_prime_dvd`, `Nat.add_pow_prime_mod`, and `Nat.pow_prime_mod`.
- The key constraint is architectural, not mechanical: bare Lean imports in `HexArith` do not currently expose `Nat.choose` or `Nat.Prime`, so the follow-up needs an explicit computational-core design rather than a silent Mathlib dependency leak.

**Next step**

- Commit and publish this extGcd/extern subset as a partial PR for `#438`, leaving `#537` as the successor issue for the remaining number-theory scaffold.

**Blockers**

- None for the extGcd subset.
