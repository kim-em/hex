**Accomplished**

- Claimed #1764 for the HexGramSchmidt basis decomposition facts.
- Reordered the public rational/integer namespaces in `HexGramSchmidt/Basic.lean`
  so integer theorems can reuse rational theorem surfaces.
- Proved `GramSchmidt.Int.basis_decomposition` by reducing it to
  `GramSchmidt.Rat.basis_decomposition` over `GramSchmidt.castIntMatrix`.
- Created follow-up issue #1765 for the remaining rational decomposition
  invariant.
- Verified `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt`,
  `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`,
  `git diff --check`, and forbidden-token scans on `HexGramSchmidt/Basic.lean`.

**Current frontier**

- `GramSchmidt.Int.basis_decomposition` no longer has its own proof
  placeholder.
- `GramSchmidt.Rat.basis_decomposition` remains the decomposition blocker and
  needs the row-prefix reconstruction invariant connecting `basisRowsAux`,
  `reduceAgainstBasis`, `coeffMatrix`, and `prefixCombination`.

**Next step**

- Work #1765: prove the rational decomposition theorem, likely using the
  existing orthogonality theorem surface to show residual projection
  coefficients agree with original-row coefficients.

**Blockers**

- None.
