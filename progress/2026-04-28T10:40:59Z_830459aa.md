**Accomplished**

- Claimed feature issue `#679` and confirmed the only remaining Phase 2 vocabulary hits in `HexPolyFp` were exported docstrings in `HexPolyFp.lean`, `HexPolyFp/Basic.lean`, and `HexPolyFp/SquareFree.lean`.
- Rewrote those docstrings to describe the current executable polynomial API directly, removing `scaffold` / `scaffolding` language without changing any code paths.
- Re-ran `rg -n "scaffold|for now|eventual|placeholder|Phase 1|bridge for" HexPolyFp*.lean HexPolyFp -S`, which returned no matches in the reviewed files.
- Ran `lake build HexPolyFp` successfully.

**Current frontier**

- The docstring-only follow-up for `HexPolyFp` is ready to publish as the completion PR for `#679`.
- Once this lands, blocked re-review issue `#687` can proceed against the cleaned public surface.

**Next step**

- Commit the three docstring edits together with this progress note and create the PR closing `#679`.

**Blockers**

- None.
