**Accomplished**

- Claimed issue `#380` and diagnosed PR `#372` as a stale-branch repair problem rather than a broken distinct-degree design: the PR branch carried unrelated file churn from an old base.
- Reapplied only the intended `HexBerlekamp` distinct-degree scaffold on current `main` by adding `HexBerlekamp/DistinctDegree.lean` and re-exporting it from `HexBerlekamp.lean`.
- Verified `python3 scripts/check_dag.py` completed successfully and `lake build HexBerlekamp` succeeds on the clean transplant.

**Current frontier**

- The repair is ready to land as a clean supersede of PR `#372`, keeping scope limited to the original distinct-degree shell from `#366`.
- A full `lake build` is in progress and has advanced well past the affected dependency cone without surfacing errors so far.

**Next step**

- Let the full build finish, then commit the two-file salvage plus this progress note and open the repair PR for `#380`, referencing that it supersedes the stale red PR `#372`.

**Blockers**

- No code blocker. Waiting on the long-running full `lake build` verification to finish before publishing.
