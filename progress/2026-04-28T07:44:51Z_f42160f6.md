**Accomplished**

- Claimed issue `#513` and added [`HexGramSchmidt/Update.lean`](/home/kim/hex/worktrees/f42160f6/HexGramSchmidt/Update.lean) as the dedicated row-update module promised by `SPEC/Libraries/hex-gram-schmidt.md`.
- Introduced executable `sizeReduce` and `adjacentSwap` row-operation helpers for `Hex.GramSchmidt.Int`, together with the theorem surface for basis, coeffs, scaledCoeffs, and gramDet updates under size reduction and adjacent swaps.
- Exported the new update module from [`HexGramSchmidt.lean`](/home/kim/hex/worktrees/f42160f6/HexGramSchmidt.lean) and advanced `libraries.yml` so `HexGramSchmidt.done_through = 1`.
- Verified with `lake build HexGramSchmidt`, `python3 scripts/check_dag.py`, and `python3 scripts/status.py`; status now reports `HexGramSchmidt -> Phase 2` and newly unlocks `HexLLL -> Phase 1` plus `HexGramSchmidtMathlib -> Phase 1`.

**Current frontier**

- The `HexGramSchmidt` Phase 1 surface now includes the row-update API expected by downstream LLL work and is ready to publish as the PR closing `#513`.

**Next step**

- Commit the update module, `libraries.yml`, and this progress note, then open the worker PR for `#513`.

**Blockers**

- None.
