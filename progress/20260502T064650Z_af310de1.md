**Accomplished**

- Claimed #1791 and proved the `basisRowsAux` pairwise orthogonality invariant in `HexGramSchmidt/Basic.lean`.
- Added length and row-index bridge lemmas connecting `basisRows`/`basisMatrix` to indexed public rows.
- Replaced `GramSchmidt.Rat.basis_orthogonal` and `GramSchmidt.Int.basis_orthogonal` placeholders with proofs.
- Verified with `lake build HexGramSchmidt.Basic`, `lake build HexGramSchmidt`, `python3 scripts/status.py HexGramSchmidt`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-placeholder scans on `HexGramSchmidt/Basic.lean`.

**Current frontier**

- Gram-Schmidt public orthogonality is wired through the executable row construction.
- The remaining `HexGramSchmidt/Basic.lean` placeholders are the out-of-scope reconstruction and prefix-span surfaces.

**Next step**

- Work the blocked follow-up #1775: use the completed orthogonality result to prove `basisMatrix` reconstruction from prefix coefficients.

**Blockers**

- None.
