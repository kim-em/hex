**Accomplished**

- Claimed PR #1248 for repair and diagnosed the failing CI job as `scripts/check_phase4.py`.
- Amended the PR commit message with the required cost-model derivation for the six new `HexPolyFp` Phase 4 benchmark registrations.
- Verified `python3 scripts/check_phase4.py`, `python3 scripts/check_dag.py`, `git diff --check origin/main...HEAD`, `lake build`, and `lake exe hexpolyfp_bench list && lake exe hexpolyfp_bench verify`.

**Current frontier**

- PR #1248 is locally repaired and ready to force-push after the final amended progress note check.

**Next step**

- Push the amended PR branch with `--force-with-lease` and mark PR #1248 salvaged.

**Blockers**

- None. The pre-existing `.claude/CLAUDE.md` worktree modification was not touched.
