**Accomplished**

- Claimed `#334`, verified it was not actionable because dependency `#332` is still open and the issue body is stale (`HexMatrix.done_through` is already `3`), then released it with a precise replan reason.
- Claimed `#341` and replaced the wrong-but-plausible `HexGfqField.Irreducible` and `HexGfqField.FiniteField.inv` bodies with honest `sorry`-backed placeholders.
- Marked the dependent inverse/division and field-power declarations as `noncomputable`, updated the surrounding docstrings to describe placeholder boundaries accurately, and rebuilt `HexGfqField` successfully.

**Current frontier**

- The `#341` fix is implementation-complete and verified locally with `lake build HexGfqField`; it is ready to be committed and published as a narrow PR.

**Next step**

- Commit the `HexGfqField` edits plus this progress note, then run `coordination create-pr 341` to publish the fix.

**Blockers**

- No blocker for `#341`.
- `#334` remains blocked on open dependency `#332` and needs replanning because its state snapshot no longer matches `libraries.yml`.
