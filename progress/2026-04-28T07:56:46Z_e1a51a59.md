**Accomplished**

- Claimed issue `#477` and added [HexPolyZMathlib/Basic.lean](/home/kim/hex/worktrees/e1a51a59/HexPolyZMathlib/Basic.lean:1), specializing the existing `HexPolyMathlib` bridge to `Hex.ZPoly` with `toPolynomial`, `ofPolynomial`, the integer-polynomial ring equivalence, and the immediate simp lemmas for coefficients, zero, constants, addition, multiplication, and the inverse conversions.
- Updated [HexPolyZMathlib.lean](/home/kim/hex/worktrees/e1a51a59/HexPolyZMathlib.lean:1) to expose the new Phase 1 surface with a module docstring describing the `ZPoly ↔ Polynomial ℤ` bridge.
- Verified the slice with `lake build HexPolyZMathlib` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexPolyZMathlib` now has the base `DensePoly Int ≃+* Polynomial ℤ` bridge, but the library is still waiting on the separate Mignotte-bound proof slice before any Phase 1 completion review.

**Next step**

- Commit the bridge scaffold and open the PR closing `#477`.

**Blockers**

- None.
