**Accomplished**

- Claimed `#963`.
- Added `HexLLL/Conformance.lean` with Lean-only core checks for lattice
  membership witnesses, independence, integer `LLLState` construction,
  size-reduction updates, adjacent swaps, recovered Gram-Schmidt coefficients,
  and potential values.
- Imported `HexLLL.Conformance` from `HexLLL.lean`.
- Bumped `HexLLL.done_through` from `2` to `3`.
- Verified `lake build HexLLL`, `python3 scripts/check_dag.py`,
  `python3 scripts/status.py HexLLL`, `python3 scripts/status.py`,
  `git diff --check`, the `HexLLL.lean` conformance import scan, and the
  requested banned-pattern scan for `HexLLL/Conformance.lean`.

**Current frontier**

- `HexLLL` is ready for Phase 4 once its `HexGramSchmidt.done_through >= 4`
  dependency is satisfied.
- The new conformance module stays Lean-only with oracle `none` / mode
  `always`; external LLL oracle checks remain out of scope.

**Next step**

- Advance `HexGramSchmidt -> Phase 4` benchmarking, which unblocks
  `HexLLL -> Phase 4`.

**Blockers**

- `HexLLL -> Phase 4` is currently blocked by `HexGramSchmidt.done_through >= 4`.
