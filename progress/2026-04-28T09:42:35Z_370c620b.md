**Accomplished**

- Claimed issue `#564` after checking the current unclaimed queue and filtering out items that were already claimed or blocked on unmet dependencies.
- Added `lean-bench` as the first Phase 4 benchmarking dependency in [lakefile.lean](/home/kim/hex/worktrees/370c620b/lakefile.lean), registered the new `hexmatrix_bench` executable, and refreshed [lake-manifest.json](/home/kim/hex/worktrees/370c620b/lake-manifest.json).
- Added [HexMatrix/Bench.lean](/home/kim/hex/worktrees/370c620b/HexMatrix/Bench.lean) with a deterministic square-matrix multiplication benchmark family, a flattened hashable prep payload, and a `setup_benchmark` declaration for the textbook `n^3` complexity model.
- Extended [.github/workflows/ci.yml](/home/kim/hex/worktrees/370c620b/.github/workflows/ci.yml) to run `lake exe hexmatrix_bench list` and `lake exe hexmatrix_bench verify` on CI.
- Verified the slice with `lake update`, `lake build hexmatrix_bench`, `lake exe hexmatrix_bench list`, `lake exe hexmatrix_bench verify`, `python3 scripts/check_dag.py`, and `lake build`.

**Current frontier**

- `HexMatrix` now has the initial Phase 4 benchmark harness and a first registered benchmark family for dense matrix multiplication.
- The library is not Phase 4 complete yet: the remaining `HexMatrix` operations and any required compare/comparator groups still need follow-up issues and registrations.

**Next step**

- Commit this benchmark-harness slice and open the PR closing `#564`.

**Blockers**

- None for this issue.
