**Accomplished**

- Claimed #1900 and added `det_two_by_two` in `HexMatrix.Determinant`.
- Proved `det_borderedMinor_one_mul_prevPivot_zero`, the `k = 0` bordered-minor Desnanot-Jacobi convention using `det_leadingPrefix_zero`.
- Split the remaining general proof into #1907 for the missing final-border determinant expansion API and #1909 for the full bordered-minor Desnanot-Jacobi theorem, with a decomposition breadcrumb on #1900.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix.Bareiss`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Determinant.lean HexMatrix/Bareiss.lean`
- The scan still reports the pre-existing `sorry` in `HexMatrix/Bareiss.lean`; no new placeholders were added in `HexMatrix/Determinant.lean`.

**Next step**

- Prove #1907's determinant expansion API, then use it in #1909 to finish the general bordered-minor Desnanot-Jacobi identity.

**Blockers**

- The full #1900 identity needs the final-border determinant expansion API that is not yet present.
