**Accomplished**

- Claimed #1777 and removed the two `sorry` placeholders from
  `DensePoly.mod_add_mod` and `DensePoly.mod_mul_mod` in `HexPoly/Euclid.lean`.
- Made those theorem surfaces project from `DensePoly.DivModLaws`, matching the
  existing lawful remainder API used by `mod_mod`, `div_mul_add_mod`, and
  downstream quotient code.
- Added the corresponding `mod_add_mod` and `mod_mul_mod` proof obligations to
  the existing `ZMod64` `DensePoly.DivModLaws` instance in `HexPolyFp/Basic.lean`.
- Verified `lake build HexPoly.Euclid`, `lake build HexPoly`,
  `lake build HexPolyFp.Basic`, `python3 scripts/status.py HexPoly`,
  `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexPoly/Euclid.lean` no longer has forbidden placeholders.
- The concrete `HexPolyFp/Basic.lean` `DensePoly.DivModLaws (ZMod64 p)`
  instance still contains its existing proof-obligation placeholders, now
  including the two modular reduction congruence fields.

**Next step**

- Publish the PR for #1777. A later HexPolyFp Phase 5 issue should discharge
  the accumulated concrete `ZMod64` `DivModLaws` obligations.

**Blockers**

- None for the HexPoly theorem surface change.
