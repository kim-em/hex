**Accomplished**

- Claimed issue `#19` and added [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/96413253/HexArith/ExtGcd.lean:1) with the Phase 1 Nat extended-GCD scaffold: a real recursive `extGcd` definition plus theorem statements `extGcd_fst` and `extGcd_bezout`.
- Updated [HexArith.lean](/home/kim/hex/worktrees/96413253/HexArith.lean:1) to re-export the new `HexArith.ExtGcd` module while preserving the root-module docstring.
- Verified this slice with `lake build HexArith`; the build succeeds, with `sorry` warnings only in theorem declarations.

**Current frontier**

- `HexArith` now exposes the pure Nat extended-GCD entry point described in the spec, but the corresponding `Int`, `UInt64`, and `@[extern]` GMP-backed variants remain unscaffolded.
- The arithmetic Phase 1 queue still has the Montgomery layers and modular exponentiation / number-theory APIs left to add around this core.

**Next step**

- Add the next narrow `HexArith` Phase 1 issue covering either the pure-`Int`/`UInt64` extended-GCD surfaces or one Montgomery proof layer from `SPEC/Libraries/hex-arith.md`.

**Blockers**

- None.
