**Accomplished**

- Claimed #1512 and isolated the remaining factor square-freeness proof obligation in `HexPolyFp/SquareFree.lean`.
- Added private `yunFactors_factors_squareFree_of_derivative_split`, scoped to the nonzero-derivative split used by `squareFreeAuxRev`.
- Replaced the direct `sorry` in `squareFreeAuxRev_factors_squareFree` with a structural induction over fuel that handles zero input, derivative-zero `pthRoot` descent, Yun-loop accumulator preservation, and repeated-part descent.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexPolyFp/SquareFree.lean`.

**Current frontier**

- The public `squareFree_factors_squareFree` theorem still delegates through `squareFreeAuxRev_factors_squareFree`.
- The remaining private placeholder is now `yunFactors_factors_squareFree_of_derivative_split`, which must prove that factors discovered by the Yun loop from `c = f / gcd f f'` are square-free when `f'` is nonzero and the incoming accumulator already satisfies the invariant.
- `HexPolyFp/SquareFree.lean` still has three private/public sorries total: pairwise-coprime invariant, derivative-split Yun factor square-freeness, and weighted-product reconstruction.

**Next step**

- Prove `yunFactors_factors_squareFree_of_derivative_split` by adding narrowly scoped lemmas connecting `z = c / gcd c w` to `gcd z z' = 1` for the derivative split, likely using the existing Euclidean gcd/divisibility theorem surface.

**Blockers**

- No build blockers. The remaining work is algebraic proof work around Yun-loop square-freeness.
