**Accomplished**

- Claimed issue `#846`.
- Added `DensePoly.coeff_C` as a reusable coefficient formula for constant polynomials.
- Proved `FpPoly.one_mul` and `FpPoly.mul_one` for executable multiplication over `ZMod64 p`.
- Verified `lake build HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested placeholder scan.

**Current frontier**

- `HexPolyFp.Basic` now has zero and one multiplication laws for `FpPoly p`.
- Right identity is proved by a coefficient reconstruction fold over the stored coefficient range.

**Next step**

- Continue the multiplication-law surface with distributivity work tracked separately from `#846`.

**Blockers**

- None.
