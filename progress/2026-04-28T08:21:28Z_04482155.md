**Accomplished**

- Claimed issue `#592` and replaced the fake `HexArith.Int.extGcd` FFI trampoline in [HexArith/ffi/mpz_gcdext.c](/home/kim/hex/worktrees/04482155/HexArith/ffi/mpz_gcdext.c) with a real GMP-backed `mpz_gcdext` bridge.
- Updated [lakefile.lean](/home/kim/hex/worktrees/04482155/lakefile.lean) so the HexArith FFI compile step adds GMP's include directory discovered from `nixpkgs#gmp.dev.outPath`.
- Verified `lake build HexArith` succeeds, `libhexarithffi.a` exports `lean_hex_mpz_gcdext`, and runtime smoke tests succeed when loading the built HexArith module dynlibs into `lean`.

**Current frontier**

- The branch is ready to commit and open as the PR for `#592`.

**Next step**

- Commit the HexArith GMP bridge and `lakefile.lean` include-path wiring, then run `coordination create-pr 592`.

**Blockers**

- `lake env lean /tmp/test_extern.lean` still does not auto-load HexArith module dynlibs on its own; the verified smoke test currently needs explicit `--load-dynlib=.lake/build/lib/lean/Hex_HexArith_UInt64_Wide.so --load-dynlib=.lake/build/lib/lean/Hex_HexArith_ExtGcd.so`.
