**Accomplished**

- Claimed #1925 after several queue races on earlier issues.
- Added `det_borderedMinor_finalColumn_expansion` in `HexMatrix.Determinant`, specializing `det_finalColumn_expansion` to an arbitrary bordered minor and rewriting the diagonal term as `det (leadingPrefix M k _) * M[i][j]`.
- Verified the helper as a partial slice toward the bordered-minor Desnanot-Jacobi product identity.

**Current frontier**

- Verification passed:
  - `lake build HexMatrix.Determinant`
  - `lake build HexMatrix.Bareiss HexMatrix`
  - `python3 scripts/status.py HexMatrix`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexMatrix/Determinant.lean`
- `lake build HexMatrix.Bareiss HexMatrix` still reports pre-existing sorries in `HexMatrix.RREF` and `HexMatrix.Bareiss`; no new placeholders were added in `HexMatrix.Determinant`.

**Next step**

- Replan #1925 around the remaining off-diagonal/product algebra: combine the generic bordered-minor final-column expansion, the next-minor specialization, and the leading-prefix determinant rewrites to prove the full Bareiss-friendly bordered-minor Desnanot-Jacobi theorem.

**Blockers**

- None for this helper slice.
