**Accomplished**

- Claimed issue #1194 for the HexGF2 comparison benchmark cost-model audit.
- Added adjacent derivation comments in `HexGF2Bench.lean` for the packed/generic GCD and Berlekamp comparison registrations, keeping the declared `O(n^2)` models.
- Rolled `HexGF2.done_through` back from 4 to 3 in `libraries.yml`.
- Ran the four scientific benchmark registrations and both compare groups on `carica`; the compare groups still agreed on common parameters.
- Filed follow-up finding #1204 for the registrations whose corrected derivations did not produce stable `consistent with declared complexity` verdicts.

**Current frontier**

- `HexGF2Bench.lean` now documents why the four comparison registrations are declared `O(n^2)`.
- `HexGF2` is back at Phase 3 while the implementation-level benchmark verdict instability is tracked separately.

**Next step**

- Investigate #1204 without retuning schedules or warmup fractions solely for green verdicts.

**Blockers**

- Three affected scientific runs were inconclusive after the derivation update: packed GCD, packed Berlekamp, and generic Berlekamp.
