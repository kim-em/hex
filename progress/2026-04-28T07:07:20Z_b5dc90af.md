**Accomplished**

- Claimed issue `#456` and added [HexGramSchmidt/Int.lean](/home/kim/hex/worktrees/b5dc90af/HexGramSchmidt/Int.lean) with executable Phase 1 scaffolding for `Hex.GramSchmidt.Int.gramDet`, `gramDetVec`, and `scaledCoeffs`, plus the corresponding rational `gramDet` wrapper and the immediate theorem surface (`gramDet_zero`, `gramDetVec_eq_gramDet`, `scaledCoeffs_eq`, `scaledCoeffs_diag`, `scaledCoeffs_upper`).
- Added the determinant-driven helper surface that those definitions need: leading Gram-matrix builders and the replaced-column determinant matrix for the integral scaled-coefficient formula.
- Updated [HexGramSchmidt.lean](/home/kim/hex/worktrees/b5dc90af/HexGramSchmidt.lean) to export the new module and document the expanded library surface.
- Verified with `lake build HexGramSchmidt` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexGramSchmidt` now covers the basis/coefficient scaffold and the executable Gram-determinant / scaled-coefficient layer, but the row-operation update formulas remain separate follow-on work.

**Next step**

- Publish the `#456` changes, then continue with the downstream update-formula slice (`#513`) and the blocked review item `#530` once the Gram-Schmidt Phase 1 surface is complete.

**Blockers**

- None.
