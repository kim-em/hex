**Accomplished**

- Claimed issue `#537` and added [HexArith/Nat/Prime.lean](/home/kim/hex/worktrees/cd3deaef/HexArith/Nat/Prime.lean) to own the mathlib-free combinatorial and prime-number scaffold promised by `SPEC/Libraries/hex-arith.md`.
- Defined the local recursive `Nat.choose` surface with immediate simp lemmas, introduced the root-library `Nat.Prime` predicate, and added the requested theorem declarations `Nat.Prime.dvd_mul`, `Nat.choose_prime_dvd`, `Nat.add_pow_prime_mod`, and `Nat.pow_prime_mod`.
- Updated [HexArith.lean](/home/kim/hex/worktrees/cd3deaef/HexArith.lean) to re-export the new module and document the expanded number-theory layer.
- Verified with `lake build HexArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexArith` now exposes the remaining Phase 1 prime/binomial theorem surface, but the new theorem bodies remain scaffold declarations backed by `sorry`, consistent with the library's current Phase 1 style.

**Next step**

- Publish the `#537` branch/PR, then queue the completion review that can decide whether `HexArith.done_through` can advance to Phase 1 or whether any additional arithmetic surface is still missing.

**Blockers**

- None.
