**Accomplished**

- Claimed `#583` after `#561` was unclaimable (`needs replan`) and `#581` was already claimed.
- Removed the fake `@[extern "lean_hex_mpz_gcdext"]` boundary from [HexArith/ExtGcd.lean](/home/kim/hex/worktrees/47fedb9b/HexArith/ExtGcd.lean:1), keeping `HexArith.Int.extGcd` as the pure-Lean reference definition and tightening the nearby docstrings to match.
- Deleted [HexArith/ffi/mpz_gcdext.c](/home/kim/hex/worktrees/47fedb9b/HexArith/ffi/mpz_gcdext.c:1) and updated [lakefile.lean](/home/kim/hex/worktrees/47fedb9b/lakefile.lean:1) so `hexarithffi` now builds only `wide_arith.c` and `HexArith` no longer links `-lgmp`.
- Opened follow-up issue `#592` for the real GMP-backed `mpz_gcdext` bridge.
- Verified with `lake build HexArith`, `lake build`, `python3 scripts/check_dag.py`, and a grep confirming the removed fake-extern identifiers no longer appear in `HexArith` or `lakefile.lean`.

**Current frontier**

- `HexArith.Int.extGcd` is now pure Lean only. The future GMP-backed runtime path is tracked separately in `#592`.

**Next step**

- Commit these changes and open the PR closing `#583`.

**Blockers**

- None for `#583`. A direct `#eval HexArith.Int.extGcd ...` still reports an interpreter/native-implementation quirk, but the public `UInt64` wrapper and pure Lean helpers evaluate; that follow-up is outside this issue's requested cleanup.
