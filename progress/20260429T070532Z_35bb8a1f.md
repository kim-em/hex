**Accomplished**

- Claimed issue `#870`.
- Added proved coefficient-preservation lemmas for `DensePoly.ofCoeffs` over arrays and lists.
- Reworked `DensePoly.scale` to map over the stored normalized coefficient list and added proved coefficient lemmas for `scale`, `shift`, and `shift (scale ...)`.
- Verified `lake build HexPoly`, `lake build HexPolyFp`, `python3 scripts/check_dag.py`, and `git diff --check`.

**Current frontier**

- `HexPoly` now exposes reusable coefficient formulas for normalized scaling and shifting without adding new proof placeholders.
- The downstream `HexPolyFp` multiplication/distributivity issue can use `DensePoly.coeff_shift_scale` for schoolbook multiplication terms.

**Next step**

- Continue parent issue `#847` by proving the coefficient-level multiplication formula in `HexPolyFp/Basic.lean`, then use it for `FpPoly.left_distrib` and `FpPoly.right_distrib`.

**Blockers**

- None.
