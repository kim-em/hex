**Accomplished**

- Claimed issue `#11` and added [HexMatrix/RowOps.lean](/home/kim/hex/worktrees/8730e231/HexMatrix/RowOps.lean:1) with executable dense-matrix row operations: `Matrix.rowSwap`, `Matrix.rowScale`, and `Matrix.rowAdd`.
- Added the determinant scaffold needed by this API slice: a temporary `det` definition plus theorem statements `det_rowSwap`, `det_rowScale`, and `det_rowAdd` with `sorry` proofs only.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/8730e231/HexMatrix.lean:1) to re-export the new module.
- Verified the slice with `lake build HexMatrix`.

**Current frontier**

- `HexMatrix` now has the basic executable row-operation layer, but determinant semantics are still a placeholder and row-reduction APIs like `rref` and free-column partitioning remain unscaffolded.

**Next step**

- Land the next Phase 1 reduction-facing slice: `HexMatrix/Rref.lean` for `rref`, `rref_isRREF`, and the free-column partition API from the SPEC.

**Blockers**

- None.
