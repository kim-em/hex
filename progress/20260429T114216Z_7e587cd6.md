**Accomplished**

- Claimed review issue `#1026` for the `HexModArith` Phase 4 scientific benchmark review.
- Confirmed the registered benchmark surface in `HexModArith/Bench.lean` matches the advertised `HexModArith` operations in `SPEC/Libraries/hex-mod-arith.md`.
- Verified `lake build HexModArith.Bench`, `lake exe hexmodarith_bench list`, and `lake exe hexmodarith_bench verify`.
- Ran the full requested benchmark set. `runCastChecksum`, `runAddChecksum`, `runInvChecksum`, `runMontToChecksum`, `runMontMulChain`, and `runMontFromChecksum` reported `consistent with declared complexity`.
- Found inconclusive scientific verdicts for `runConstructChecksum`, `runSubChecksum`, `runMulChecksum`, `runPow`, and `runBarrettMulModChain`, all consistent with the benchmark being below the lean-bench child-process signal floor at the committed scientific settings.
- Left `libraries.yml` unchanged with `HexModArith.done_through: 3`.
- Filed follow-up issue `#1032` to raise or amplify the too-fast `HexModArith` benchmark schedules before retrying Phase 4 completion.
- Verified `python3 scripts/status.py HexModArith`, `python3 scripts/status.py`, `python3 scripts/check_dag.py`, `git diff --check`, and `rg -n "native_decide" HexModArith -S`.

**Current frontier**

- `HexModArith` remains at Phase 4, blocked on benchmark schedule/signal tuning rather than an implementation phase bump.
- `HexPolyFp` Phase 4 remains blocked on `HexModArith.done_through >= 4`.

**Next step**

- A worker should claim `#1032`, tune the five too-fast benchmark registrations so their scientific settings produce verdict-eligible rows, then re-run the full `HexModArith` Phase 4 review.

**Blockers**

- The committed scientific settings for five `HexModArith` registrations are too fast for the lean-bench signal floor on the review host.
