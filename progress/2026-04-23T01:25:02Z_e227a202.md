**Accomplished**

- Claimed and completed issue `#45` (`feature`): added [HexArith/Montgomery/Redc.lean](/home/kim/hex/worktrees/e227a202/HexArith/Montgomery/Redc.lean) with the executable `MontCtx.redc` bridge plus theorem declarations `redc_m_spec`, `redc_u_spec`, `redc_sub_spec`, and `toNat_redc`.
- Updated [HexArith.lean](/home/kim/hex/worktrees/e227a202/HexArith.lean) to re-export the Montgomery `UInt64` REDC bridge and mention that layer in the root-module docstring.
- Verified with `lake build HexArith` (success; only existing scaffold `sorry` warnings remain, including the new theorem placeholders).

**Current frontier**

- `HexArith` now exposes the missing Phase 1 Montgomery REDC `UInt64` bridge, but [HexArith/Montgomery/Context.lean](/home/kim/hex/worktrees/e227a202/HexArith/Montgomery/Context.lean) still uses placeholder executable arithmetic for `toMont`, `fromMont`, and `mulMont` rather than the new `redc` API.

**Next step**

- Land the PR for `#45`, then continue the remaining `HexArith` Phase 1 queue, likely the `Int` extended-GCD surface or the Montgomery context follow-through onto the new REDC bridge.

**Blockers**

- None in this session.
