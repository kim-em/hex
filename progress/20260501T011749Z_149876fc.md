**Accomplished**

- Claimed #1493 and repaired the public `multifactorLift_spec` contract in `HexHensel/Multifactor.lean`.
- Added `MultifactorLiftInvariant`, a recursive split invariant carrying the `henselLift_spec` start, degree-preservation, and Bezout-preservation hypotheses at each nontrivial split.
- Replaced the false product-congruence-only theorem shape with a theorem proved from a narrower private `multifactorLiftList_spec` helper.
- Updated `SPEC/Libraries/hex-hensel.md` to record that product congruence modulo `p` alone is insufficient for multifactor lifting.
- Verified `lake build HexHensel.Multifactor`, `lake build HexHensel`, `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`, `git diff --check`, and a focused placeholder scan.

**Current frontier**

- `multifactorLift_spec` now has a true theorem shape aligned with the iterative binary Hensel API.
- The remaining local proof obligation is the private recursive `multifactorLiftList_spec` proof over the sequential split/product structure.

**Next step**

- Prove `multifactorLiftList_spec` by induction over the factor list, using `henselLift_spec` at each nontrivial split plus product lemmas for `Array.polyProduct` over singleton append and recursive tails.

**Blockers**

- None.
