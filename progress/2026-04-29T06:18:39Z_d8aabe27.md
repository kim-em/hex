**Accomplished**

- Claimed `#826` after `#836` was already claimed by another agent.
- Added raw packed-word lookup lemmas in `HexGF2/Basic.lean` for `xorWords` size, `Array.ofFn`/`getD`, and defaulted optional lookup.
- Added coefficient-level characteristic-two self-cancellation facts for raw `xorWords` and `GF2Poly` addition.
- Verified `lake build HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and that this diff adds no `sorry`, `axiom`, or `native_decide`.

**Current frontier**

- `HexGF2/Basic.lean` now exposes the first reusable raw-word facts needed by the later packed add-cancellation and division prerequisite work.
- Full polynomial equality for `p + p = 0`, normalized-polynomial extensionality, and Euclidean division invariants remain outside this slice.

**Next step**

- Open a PR closing `#826`.

**Blockers**

- None for this slice.
