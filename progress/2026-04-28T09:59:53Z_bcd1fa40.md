**Accomplished**

- Claimed `#652` and added a canonical field-level remainder API to `HexPoly/Euclid.lean` by introducing `DensePoly.mod`, the `%` instance, and the reconstruction lemma `div_mul_add_mod`.
- Added the immediate reduction/congruence theorem surface for downstream quotient users: `mod_eq_divMod`, `modByMonic_eq_mod`, `mod_mod`, `congr_mod`, and `mod_eq_mod_of_congr`.
- Restated the CRT-facing public surface through `%` with `polyCRT_mod_fst` and `polyCRT_mod_snd`, keeping monic-reduction helper lemmas available under `polyCRT_modByMonic_fst` and `polyCRT_modByMonic_snd`.
- Verified the change with `lake build HexPoly` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexPoly` now exposes the canonical remainder/reduction layer that `HexGFqRing` and CRT-style downstream code were missing.
- The theorem bodies in this area remain scaffold-level proofs (`sorry`) consistent with Phase 1/2 expectations; no additional `libraries.yml` update is part of this issue.

**Next step**

- Commit `HexPoly/Euclid.lean` and this progress file, then publish a PR closing `#652`.
- A future downstream worker can consume `%` directly when adding quotient-ring normalization in `HexGFqRing`.

**Blockers**

- None for this issue.
- The optional `second-opinion` helper was invoked on the diff but did not return before publish time, so no external review feedback was incorporated in this session.
