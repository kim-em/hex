**Accomplished**

- Claimed issue `#361` and rewrote the dishonest HexMatrix free-column/nullspace executable scaffolds as honest placeholders: `HexMatrix.IsEchelonForm.freeCols`, `HexMatrix.IsRREF.nullspaceMatrix`, `HexMatrix.IsRREF.nullspace`, and `HexMatrix.Matrix.nullspace` are now `noncomputable def ... := sorry`.
- Marked dependent executable wrappers as `noncomputable` so the tree no longer presents the old nullspace output path as implemented: `HexMatrix.Benchmark.runNullspaceById`, `runNullspaceCase`, `runNullspaceCases`, and `HexBerlekamp.berlekampKernel`.
- Verified `lake build HexMatrix`, `lake build HexBerlekamp`, and `python3 scripts/status.py` all succeed after the rewrite.

**Current frontier**

- `HexMatrix` Phase 2 review is still blocked on the remaining human-oversight scaffold cleanup in `#332`; once that lands, `#351`/`#361`-style follow-up review work can restore the attestation.
- The executable free-column complement and nullspace algorithms remain unimplemented by design; this session only removed the wrong-but-plausible behavior.

**Next step**

- Land `#332`, then re-run the `HexMatrix` Phase 2 review to confirm no other dishonest executable scaffolds remain and restore the review token only if the library passes.

**Blockers**

- `/reflect` was not available in this session, so no reflection pass was run.
