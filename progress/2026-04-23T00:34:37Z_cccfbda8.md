**Accomplished**

- Claimed issue `#12` and added [HexMatrix/Rref.lean](/home/kim/hex/worktrees/cccfbda8/HexMatrix/Rref.lean:1) with the Phase 1 scaffold for `rref`, `rref_isRREF`, and the free-column partition API.
- Updated [HexMatrix.lean](/home/kim/hex/worktrees/cccfbda8/HexMatrix.lean:1) to re-export the new `HexMatrix.Rref` module while preserving the root-module docstring.
- Repaired the local `.lake/packages/{mathlib,verso}` cache so `lake build` could run again in this worktree.
- Verified this slice with `lake build HexMatrix`; the build succeeds, with `sorry` warnings only in theorem declarations.

**Current frontier**

- `HexMatrix` now exposes the row-reduction and free-column scaffold, but the executable reduction algorithm and the downstream span/nullspace APIs remain unimplemented.

**Next step**

- Take the next narrow `HexMatrix` Phase 1 slice from `SPEC/Libraries/hex-matrix.md`, likely the span/nullspace scaffold that consumes `rref` and `freeCols`.

**Blockers**

- None.
