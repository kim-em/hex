**Accomplished**

- Claimed #1456 and completed the `divModMonicModSquare_reconstruct_congr`
  proof in `HexHensel/Quadratic.lean`.
- Added small reusable DensePoly coefficient facts:
  `coeff_monomial`, `add_mul_sub_cancel_right`, `zero_mul`, and `zero_add`.
- Added the recursive `divModMonicModSquareAux` reconstruction invariant for
  nonzero divisors and handled the zero-divisor top-level branch.
- Created follow-up issue #1458 for the remaining
  `divModMonicModSquare_zero_mod_base` proof and left a decomposition breadcrumb
  on #1456.
- Verified `lake build HexHensel.Quadratic`, `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a scan of touched
  files for `axiom` / `native_decide` / `TODO` / `FIXME`.

**Current frontier**

- `divModMonicModSquare_reconstruct_congr` is closed.
- `divModMonicModSquare_zero_mod_base` remains the active modular division
  helper frontier and is isolated in #1458.
- The next proof needs coefficient divisibility preservation through
  reduction modulo `m*m` and through the quotient/remainder loop updates.

**Next step**

- Claim #1458 after the partial PR for #1456 lands, then prove the
  zero-mod-base invariant by induction over `divModMonicModSquareAux`.

**Blockers**

- None for the reconstruction congruence patch.
