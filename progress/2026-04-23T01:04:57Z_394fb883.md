**Accomplished**

- Claimed issue `#36` after releasing stale repair issue `#38`, whose PR branch was already mergeable with CI pending.
- Added `HexArith/PowMod.lean` with an executable Phase 1 `powMod` scaffold based on exponentiation by squaring, plus the adjacent `powMod_eq` theorem declaration.
- Updated `HexArith.lean` to re-export the modular exponentiation module and mention it in the root-module documentation.
- Verified the slice with `lake build HexArith`, which succeeded with only expected theorem-level `sorry` warnings.

**Current frontier**

- Issue `#36` is ready to be committed and packaged as a PR.
- Remaining `HexArith` Phase 1 work continues on separate arithmetic slices such as the Montgomery inverse layer claimed in issue `#37`.

**Next step**

- Commit the `powMod` scaffold changes together with this progress note and create the PR closing issue `#36`.

**Blockers**

- None.
