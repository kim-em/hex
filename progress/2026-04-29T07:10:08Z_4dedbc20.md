**Accomplished**

- Claimed issue `#874`.
- Added `HexModArith/Conformance.lean` with core `ZMod64` Phase 3 checks for `ofNat`, `zero`, `one`, `add`, `sub`, `mul`, `pow`, `inv`, `neg`, nat/int casts, and nat/int scalar multiplication.
- Imported the conformance module from `HexModArith.lean`.
- Opened follow-up issue `#877` for `HexModArith/HotLoop.lean` Phase 3 conformance, leaving `libraries.yml` unchanged for this core-only slice.
- Verified with `lake build HexModArith`, `python3 scripts/check_dag.py`, `python3 scripts/status.py HexModArith`, and a forbidden-pattern scan over the new conformance module.

**Current frontier**

- Core `ZMod64` executable/ring-facing operations now have always-on Lean conformance checks.
- `HexModArith` remains in Phase 3 until hot-loop conformance is added.

**Next step**

- Create the PR for `#874`.

**Blockers**

- None.
