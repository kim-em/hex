**Accomplished**

- Claimed issue `#857`.
- Added `HexArith.powMod` to the HexArith core conformance contract and covered odd word-sized Montgomery, even-modulus fallback, large-`Nat` fallback, exponent-zero, base-larger-than-modulus, and modulus-`1` cases.
- Moved the `powMod` helper block into the `HexArith` namespace so the public API matches the issue and conformance surface.
- Bumped `libraries.yml` so `HexArith.done_through = 3`.

**Current frontier**

- `HexArith` now reports as ready for Phase 4 after the Phase 3 conformance signoff.
- Existing theorem-level `sorry` warnings in HexArith remain outside this Phase 3 conformance slice.

**Next step**

- Publish the PR for `#857`; follow-up workers can take the Phase 4 benchmarking work reported by `scripts/status.py`.

**Blockers**

- None.
