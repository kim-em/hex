**Accomplished**

- Claimed issue `#850`.
- Added downstream-facing `HexGF2.Basic` lemmas for coefficientwise addition,
  equal-coefficient cancellation, `mulXk` degree shifting, shifted leading-term
  alignment, and long-division leading-term cancellation.
- Verified the partial proof surface with `lake build HexGF2.Basic`,
  `lake build HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and
  `rg -n "axiom|native_decide" HexGF2 -S`.

**Current frontier**

- The new API shape needed by `HexGF2.Euclid` exists, but five new proof
  obligations in `HexGF2/Basic.lean` remain as `sorry`.
- `rg -n "axiom|native_decide" HexGF2 -S` still reports the pre-existing
  private smoke-test axiom in `HexGF2/Smoke.lean`; this session did not add
  any `axiom` or `native_decide`.

**Next step**

- Discharge the new `HexGF2.Basic` proof obligations, starting with the
  UInt64 bit-level XOR lemma behind `coeff_add_eq_bne`, then the packed
  `shiftLeft`/`mulXk` degree-shift proof.

**Blockers**

- The exact packed `mulXk` proof is larger than this session could complete:
  it needs explicit bit-level reasoning about `shiftLeftBitsList` and carry
  propagation across `UInt64` words.
