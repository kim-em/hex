**Accomplished**

- Claimed issue `#336` and removed the redundant literal-copy `#guard` assertions from `HexPoly/Conformance.lean`, leaving the corresponding `#guard_msgs` output checks and the substantive algebraic property checks in place.
- Verified the cleanup with `lake build HexPoly`.

**Current frontier**

- Issue `#336` is ready to publish as a narrow conformance cleanup touching only `HexPoly/Conformance.lean` plus this session progress note.

**Next step**

- Commit the cleanup and progress note, then create the PR closing `#336`.

**Blockers**

- No blocking errors in this session.
- The build still emits pre-existing `sorry` warnings from `HexPoly` modules outside this cleanup.
