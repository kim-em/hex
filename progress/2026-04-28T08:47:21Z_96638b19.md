**Accomplished**

- Claimed issue `#608` after checking the current unclaimed queue and confirming the higher-priority `human-oversight` item `#561` was not claimable because it had already been marked for replan.
- Added `Hex.GramSchmidt.prefixRows` and `Hex.GramSchmidt.prefixSpan` in `HexGramSchmidt/Basic.lean` to express prefix row-span membership over the existing executable `HexMatrix` API.
- Added the missing theorem declaration `Hex.GramSchmidt.Int.basis_span` with a statement matching the spec's intended prefix-span invariance, and updated the root `HexGramSchmidt` docstring to mention the prefix-span surface.
- Verified `lake build HexGramSchmidt` succeeds and `python3 scripts/check_dag.py` passes.

**Current frontier**

- `HexGramSchmidt` now exposes the reviewed `basis_span` theorem surface needed to make the Phase 1 root-library API match `SPEC/Libraries/hex-gram-schmidt.md`.
- The theorem is scaffold-only, consistent with the existing Phase 1 proof surface in this library; no downstream `HexLLL` theorem movement was pulled back into `HexGramSchmidt`.

**Next step**

- Commit the `basis_span` scaffold, push `agent/96638b19`, and create the PR closing `#608`.

**Blockers**

- None.
