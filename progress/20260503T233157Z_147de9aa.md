**Accomplished**

- Claimed #1866, determined it combined an independent array-shape proof with a diagonal theorem blocked on the no-pivot Bareiss prefix API, and decomposed it into #1888 and #1890.
- Corrected a coordination typo while creating #1890: removed the accidental #1880 dependency from #1889, restored its `blocked` label for #1887, and added the #1880 dependency to #1890.
- Claimed #1888 and proved `Hex.GramSchmidt.Int.scaledCoeffs_upper`.
- Added private array-loop invariants showing `setArrayEntry`, `writeScaledColumn`, and `scaledCoeffArrayLoop` preserve zero entries strictly above the diagonal.

**Current frontier**

- Verification passed:
  - `lake build HexGramSchmidt.Int`
  - `lake build HexGramSchmidt`
  - `python3 scripts/status.py HexGramSchmidt`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexGramSchmidt/Int.lean`
- The placeholder scan reports only pre-existing `sorry`s in `HexGramSchmidt/Int.lean`; the `scaledCoeffs_upper` `sorry` was removed.

**Next step**

- Land #1890 after #1880 exposes the no-pivot Bareiss leading-prefix determinant API, then use that API to prove `scaledCoeffs_diag`.

**Blockers**

- `scaledCoeffs_diag` remains blocked on #1880, which depends on the no-pivot Bareiss control and bordered-minor work.
