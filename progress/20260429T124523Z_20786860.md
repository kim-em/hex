**Accomplished**

- Claimed feature issue `#1050` for the `HexBerlekamp` irreducibility certificate surface.
- Added public `RabinBezoutWitness` and `IrreducibilityCertificate` records in `HexBerlekamp/Irreducibility.lean`.
- Added executable certificate helpers and `checkIrreducibilityCertificate`, including prime/degree checks, pow-chain recomputation, the Rabin divisibility leg, and Bezout identity checks for maximal proper divisors.
- Added proof skeletons connecting the checker to the pow-chain contract, `rabinTest`, and the executable irreducibility predicate.
- Verified `lake build HexBerlekamp`, `git diff --check`, and the requested banned-term scan.

**Current frontier**

- `HexBerlekamp` now exposes the SPEC-facing irreducibility certificate data and checker surface.
- The new correctness connections are theorem shells with `sorry`, matching the project pattern for proof work at this phase.
- The full Berlekamp factorization driver and distinct-degree factorization surfaces remain separate unclaimed issues.

**Next step**

- Land this certificate/checker PR, then continue with `#1051` or `#1052` for the remaining `HexBerlekamp` factoring surfaces.

**Blockers**

- None for this issue.
