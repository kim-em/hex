**Accomplished**

- Claimed #1678 and closed the remaining `montPosInv_spec` and
  `montInv_spec` placeholders in `HexArith/Montgomery/InvNat.lean`.
- Added private Nat/Int bridge lemmas for converting wrapped Newton
  divisibility into Nat modulo equalities and for translating the positive
  inverse into the negated Montgomery inverse.
- Proved the executable five-step refinement chain from an odd seed through
  6, 12, 24, 48, and 64 bits without changing Montgomery executable
  definitions or public APIs.

**Current frontier**

- `HexArith/Montgomery/InvNat.lean` now has no placeholders.
- HexArith still has existing unrelated placeholders in `ExtGcd`,
  `Nat/Prime`, and `Montgomery/Context`.

**Next step**

- Continue HexArith Phase 5 by addressing the remaining Montgomery context
  or arithmetic proof placeholders tracked by the queue.

**Blockers**

- None.
