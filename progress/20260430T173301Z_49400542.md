**Accomplished**

- Claimed issue #1363 for the HexHensel linear one-step factor congruence proof.
- Added `ZPoly.scale_coeffwiseDiv_sub_of_congr` in `HexHensel/Linear.lean`.
- The helper converts a coefficientwise product congruence into the exact
  scaled `coeffwiseDiv` identity needed to recover `f - g * h` from the
  Hensel error term.
- Verified the partial slice with `lake build HexHensel`,
  `lake build HexHensel.Conformance`, `python3 scripts/status.py HexHensel`,
  `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-pattern
  scan of `HexHensel/Linear.lean`.

**Current frontier**

- The raw theorem `linearHenselStep_raw_factor_congr` remains the active
  placeholder for #1363.
- The next missing layers are the `modP`/`liftToZ` bridge for addition and
  multiplication modulo `p`, and transport of `DensePoly.divMod_spec` through
  that bridge.

**Next step**

- Prove the `FpPoly`-to-`ZPoly` bridge lemmas showing that lifting an
  `FpPoly` addition/product gives a `ZPoly.congr` modulo `p` to the
  corresponding lifted operands.
- Then use `DensePoly.divMod_spec` for `DensePoly.divMod (t * eMod) gMod` to
  prove the raw correction identity modulo `p`.

**Blockers**

- None technical, but the full #1363 proof is larger than a single local
  calculation and should continue through the bridge lemmas before the raw
  theorem is attempted again.
