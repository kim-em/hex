**Accomplished**

- Claimed #1557 for the HexBerlekampMathlib finite-field polynomial transport surface.
- Added `fpPolyToPolynomial`, `polynomialToFpPoly`, and public `fpPolyEquiv : Hex.FpPoly p ≃+* Polynomial (ZMod p)` in `HexBerlekampMathlib/Basic.lean`.
- Kept `toMathlibPolynomial` theorem-linked to `fpPolyEquiv` and added simp lemmas for the equivalence and its inverse.
- Restated the gcd and square-free theorem boundaries to use canonical `gcd` over `Polynomial (ZMod p)` with `[Fact (Nat.Prime p)]`, removing the explicit `GCDMonoid` parameter and the unsuppliable `[Field (Hex.ZMod64 p)]` assumption.
- Verified `lake build HexBerlekampMathlib`, `python3 scripts/status.py HexBerlekampMathlib`, `python3 scripts/check_dag.py`, `git diff --check`, and the forbidden-term scan for `HexBerlekampMathlib`.

**Current frontier**

- `fpPolyEquiv` exposes the missing transport boundary, but its ring-equivalence laws remain admitted theorem-level obligations, matching the existing scaffold style in this mathlib-facing layer.
- A direct theorem composing `HexPolyMathlib.toPolynomial` with `ZMod64.equiv` is still blocked by the absence of a Mathlib `Semiring`/`CommRing` instance surface for `Hex.ZMod64 p`; the available executable operation instances do not line up with `Polynomial.map`.

**Next step**

- Fill the `fpPolyEquiv` law proofs after deciding whether `Hex.ZMod64 p` should expose standard Mathlib ring typeclass instances or whether HexBerlekampMathlib should continue using the direct coefficientwise transport.

**Blockers**

- No build blocker. The remaining issue is proof/API depth: the executable `ZMod64` stack currently exposes `Lean.Grind` algebra classes and a custom `RingEquiv`, but not the standard `Semiring`/`CommRing` instances needed to literally compose the generic dense-polynomial Mathlib bridge.
