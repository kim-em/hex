**Accomplished**

- Claimed feature issue `#1036` for Nat-level Barrett reduction correctness.
- Proved `barrettReduceNat_eq_mod` in `HexArith/Barrett/ReduceNat.lean` using the quotient approximation bounds from `#1035`.
- Proved `barrettReduceNat_lt` by reducing to `barrettReduceNat_eq_mod` and `Nat.mod_lt`.
- Added two private remainder-shape lemmas for the exact-quotient and one-below-quotient cases.

**Current frontier**

- `HexArith/Barrett/ReduceNat.lean` now has no `sorry`, `admit`, `axiom`, or `native_decide` occurrences.
- Adjacent Barrett bridge and context files still have existing proof placeholders outside this issue.

**Next step**

- Prove the UInt64 bridge theorem `toNat_barrettReduce` in `HexArith/Barrett/Reduce.lean`, then lift the result into the public `BarrettCtx` API.

**Blockers**

- None.
