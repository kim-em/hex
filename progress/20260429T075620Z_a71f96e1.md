**Accomplished**

- Claimed issue `#839`.
- Added one-sided `reduceMod` add/mul compatibility lemmas in `HexGfqRing.Basic`.
- Added `PolyQuotient` constructor and representative bridge lemmas for zero, one, constants, add, mul, negation inverse facts, subtraction self, and quotient `sub = add neg` in `HexGfqRing.Operations`.
- Verified `lake build HexPolyFp`, `lake build HexGfqRing`, `python3 scripts/check_dag.py`, `git diff --check`, and a diff scan for new `sorry`, `axiom`, or `native_decide`.

**Current frontier**

- `HexGfqRing` now has a prerequisite quotient-reduction theorem surface for the downstream `PolyQuotient` ring-instance proofs.
- The existing `Lean.Grind.*` instance placeholders in `HexGfqRing.Operations` remain out of scope for this issue.

**Next step**

- Use the new reduction bridges to discharge the `PolyQuotient` `Lean.Grind` semiring/ring/comm-ring instance proofs.

**Blockers**

- None.
