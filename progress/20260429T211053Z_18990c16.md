**Accomplished**

- Claimed issue #1142 for the HexBerlekampZassenhaus normalization surface.
- Added executable normalization data in `HexBerlekampZassenhaus.lean` for integer content, primitive part, initial `X`-power extraction, square-free core selection, and factor reassembly.
- Updated `factorWithBound` to run prime selection, Hensel lifting, and recombination on the normalized square-free core instead of the raw public input.
- Added proof-only theorem statements for normalization and reassembly product invariants.
- Verified `lake build HexBerlekampZassenhaus`, `python3 scripts/status.py HexBerlekampZassenhaus`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested placeholder/sorry scans.

**Current frontier**

- The public Berlekamp-Zassenhaus entry point now has a SPEC-shaped normalization boundary before the existing square-free pipeline.
- Remaining `sorry` hits in `HexBerlekampZassenhaus.lean` are theorem/proof positions, including the new normalization product invariants.

**Next step**

- Create the PR for #1142.

**Blockers**

- No technical blockers found.
