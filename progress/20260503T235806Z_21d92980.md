**Accomplished**

- Claimed #1898, assessed the target proof dependency, and decomposed it into #1908 and #1910.
- Claimed #1908 and added `zmod64_natCast_choose_prime_eq_zero` in `HexPolyFp.SquareFree`, exposing the prime binomial cancellation helper needed for the upcoming polynomial binomial proof.
- Created follow-up issues #1912 and #1913 for the remaining #1908 work.

**Current frontier**

- Verification passed:
  - `lake build HexPolyFp.SquareFree`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME" HexPolyFp/SquareFree.lean`
  - `rg -n "sorry" HexPolyFp/SquareFree.lean`
- The only `sorry`s reported in `HexPolyFp/SquareFree.lean` are pre-existing, including the original `coeffFoldPowerCoeff_prime_coeff` placeholder now tracked by #1910.

**Next step**

- Prove #1912: a Mathlib-free binomial expansion for `powLinear (f + g) n`; then use it in #1913 with `zmod64_natCast_choose_prime_eq_zero` to close `powLinear_add_prime_coeff`.

**Blockers**

- None. The original #1898 target is blocked on the newly separated additivity foundation.
