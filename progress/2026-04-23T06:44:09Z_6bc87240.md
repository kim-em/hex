**Accomplished**

- Claimed and completed issue `#251` for the first `HexHensel` algorithm scaffold.
- Added [`HexHensel/Linear.lean`](/home/kim/hex/worktrees/6bc87240/HexHensel/Linear.lean:1) with `ZPoly.coeffwiseDivPow`, `ZPoly.linearError`, `LinearLiftResult`, an executable `linearHenselStep`, and the initial `linearHenselStep_spec` theorem surface.
- Updated [`HexHensel.lean`](/home/kim/hex/worktrees/6bc87240/HexHensel.lean:1) to re-export the new linear-lifting module.
- Verified the change with `lake build HexHensel` and `python3 scripts/status.py`.

**Current frontier**

- `HexHensel` now includes both the bridge layer and the first linear-lifting step, but the library remains on the active Phase 1 frontier until the remaining scaffold slices such as quadratic, iterative, and multifactor lifting are added.

**Next step**

- Land the PR for `#251`, then pick up the next narrow `HexHensel` Phase 1 issue or dispatch one for the next algorithm/API slice if none exists.

**Blockers**

- None in this session.
