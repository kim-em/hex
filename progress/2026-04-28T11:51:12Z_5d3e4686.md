**Accomplished**

- Claimed human-oversight issue `#721` and replaced the linear-search `floorSqrt` in `HexPolyZ/Mignotte.lean` with a Newton-step implementation bounded by `O(log n)` fuel.
- Preserved the existing `floorSqrt_zero` simp lemma and kept the `coeffNormSq` / `coeffL2NormBound` callers unchanged.
- Verified the repair with `lake build HexPolyZ`, `python3 scripts/check_dag.py`, and direct evaluation of `floorSqrt 0`, `floorSqrt 100`, and `floorSqrt (2^40)`.

**Current frontier**

- The `floorSqrt` repair is ready to publish; no further code changes are pending in this session.

**Next step**

- Commit the `HexPolyZ/Mignotte.lean` repair and open the PR closing `#721`.

**Blockers**

- None.
