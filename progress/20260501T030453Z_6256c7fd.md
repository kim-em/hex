**Accomplished**

- Claimed #1514 and isolated the Yun-loop factor square-freeness proof into a structural preservation lemma in `HexPolyFp/SquareFree.lean`.
- Replaced the direct `sorry` in `yunFactors_factors_squareFree_of_derivative_split` with an induction over the Yun loop that preserves the accumulator square-freeness invariant through list reversal and newly consed factors.
- Left the remaining algebraic gap in the new private `yunFactor_step_squareFree_of_derivative_split`, which is the exact step connecting a discovered factor `c / gcd c w` to `gcd z z' = 1`.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexPolyFp/SquareFree.lean`.

**Current frontier**

- `squareFreeAuxRev_factors_squareFree` and the public `squareFree_factors_squareFree` theorem now compile through the structural Yun-loop preservation proof.
- The proof still depends on the algebraic Yun-step placeholder `yunFactor_step_squareFree_of_derivative_split`.
- `HexPolyFp/SquareFree.lean` still has three proof placeholders total: pairwise-coprime invariant, Yun-step factor square-freeness, and weighted-product reconstruction.

**Next step**

- Prove `yunFactor_step_squareFree_of_derivative_split` from a narrower reachable-state statement for Yun's algorithm, likely after adding Euclidean derivative/divisibility lemmas sufficient to show a factor stripped by `gcd c w` is coprime to its derivative.

**Blockers**

- No build blockers. The missing API is algebraic: the current proved theorem surface does not yet expose enough derivative/gcd/divisibility facts to derive the Yun-step square-freeness criterion directly.
