**Accomplished**

- Claimed #1479 and proved `linearHenselStep_monic` in
  `HexHensel/Linear.lean`.
- Proved `linearHenselStep_g_degree?_eq` in `HexHensel/Linear.lean`.
- Added local degree/monicity helpers showing that adding a lower-degree
  correction to a monic integer polynomial preserves the leading term, and
  that `reduceModPow` preserves monicity and degree when reducing modulo
  `p^(k+1)` with `1 < p`.
- Verified `lake build HexHensel.Linear`, `lake build HexHensel`,
  `python3 scripts/status.py HexHensel`, `python3 scripts/check_dag.py`,
  `git diff --check`, and a targeted forbidden-placeholder scan of
  `HexHensel/Linear.lean`.

**Current frontier**

- The `g`-side linear Hensel step monicity and degree placeholders are closed.
- Remaining local `HexHensel/Linear.lean` placeholders are the loop invariant,
  `linearHenselStep_h_degree?_eq`, `henselLift_spec`, and `henselLift_monic`.
- `HexHensel/Multifactor.lean` still has its Phase 5 placeholder.

**Next step**

- Continue HexHensel Phase 5 by proving `linearHenselStep_h_degree?_eq` or
  the linear loop invariant, depending on the next issue selected by the
  queue.

**Blockers**

- None.
