**Accomplished**

- Claimed issue `#398` and chose the honest deletion path for the placeholder `HexMatrix` span API rather than extending the stubbed `spanCoeffs` / `spanContains` surface.
- Reduced [HexMatrix/Span.lean](/home/kim/hex/worktrees/6783508a/HexMatrix/Span.lean) to the executable row-linear-combination helper still used by theorem statements, deleting the placeholder span declarations and their local theorem shell.
- Updated the downstream cascade by removing the dead `HexMatrixMathlib` span bridge ([HexMatrixMathlib.lean](/home/kim/hex/worktrees/6783508a/HexMatrixMathlib.lean), deleted [HexMatrixMathlib/Span.lean](/home/kim/hex/worktrees/6783508a/HexMatrixMathlib/Span.lean)), switching [HexMatrix/Nullspace.lean](/home/kim/hex/worktrees/6783508a/HexMatrix/Nullspace.lean) to import `HexMatrix.Rref` directly, and deleting the Gram-Schmidt prefix-span scaffolding from [HexGramSchmidt/Int.lean](/home/kim/hex/worktrees/6783508a/HexGramSchmidt/Int.lean).
- Verified `lake build HexMatrix`, `lake build HexGramSchmidt`, `python3 scripts/status.py`, and `git diff --check`.

**Current frontier**

- The branch is ready to commit and publish for issue `#398`.

**Next step**

- Commit the span-surface deletion together with this progress file and open the PR closing `#398`.

**Blockers**

- None.
