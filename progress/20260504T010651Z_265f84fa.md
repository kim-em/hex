**Accomplished**

- Claimed #1954, assessed the proof surface, and decomposed it into #1958, #1959, and #1960 with explicit dependencies.
- Released #1954 as `replan` with the required `Decomposed into #1958, #1959, #1960` breadcrumb.
- Claimed #1958 and added a proof-only coefficient theorem for `subtractScaledShift` in `HexPoly/Euclid.lean`.
- Verified `lake build HexPoly`, `git diff --check`, and a forbidden-placeholder scan of `HexPoly/Euclid.lean`.

**Current frontier**

- #1958 now has the bounded coefficient update fact needed to reason about the array-backed remainder update.
- #1959 remains blocked on #1958 and should use this helper to assemble the recursive `divModArrayAux` reconstruction invariant.

**Next step**

- Create the PR for #1958 and let the next worker continue with #1959 after this lands.

**Blockers**

- #1959 depends on #1958.
- #1960 depends on #1959.
