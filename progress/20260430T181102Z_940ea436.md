**Accomplished**

- Claimed issue #1392 and replaced the `permutationVectors_transposeValues_neg_sum`
  proof placeholder in `HexMatrix/Determinant.lean`.
- Added private fold and enumeration helpers:
  `foldl_det_sum_perm`, `permutationVectors_nodup_list`, and
  `transposePermutationValues_map_permutationVectors_perm`.
- Verified the row-swap determinant sum now reindexes across
  `transposePermutationValues` using the permutation-vector enumeration.
- Ran `lake build HexMatrix`, `lake build HexMatrix.Conformance`,
  `python3 scripts/status.py HexMatrix`, `python3 scripts/check_dag.py`,
  and `git diff --check`.

**Current frontier**

- `det_rowSwap` no longer depends on the transpose reindexing placeholder.
- The remaining `HexMatrix/Determinant.lean` proof placeholder is
  `permutationVectors_duplicateRow_sum`, which is out of scope for #1392.

**Next step**

- Proceed with #1393, which can now use the completed row-swap reindexing
  bridge toward determinant transpose negative-sum extraction.

**Blockers**

- No blockers for this slice.
