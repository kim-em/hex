**Accomplished**

- Claimed issue `#453` and extended [HexModArith/Basic.lean](/home/kim/hex/worktrees/ed3aa30e/HexModArith/Basic.lean:1) with executable `ZMod64.pow` via exponentiation by squaring and a narrow `ZMod64.inv` surface derived from integer extended GCD coefficients.
- Added the immediate contract surface for the new operations: a `toNat_inv` simp theorem plus scaffold theorems `toNat_pow` and `inv_mul_eq_one` for the downstream mathematical API.
- Expanded [HexModArith/Smoke.lean](/home/kim/hex/worktrees/ed3aa30e/HexModArith/Smoke.lean:1) with fixed examples covering `pow`, `inv`, and `inv * a`, and updated the root docstring in [HexModArith.lean](/home/kim/hex/worktrees/ed3aa30e/HexModArith.lean:1).
- Verified with `lake build HexModArith` and `python3 scripts/check_dag.py`.

**Current frontier**

- `HexModArith` now has the Phase 1 executable power/inversion surface needed to unblock downstream `HexPolyFp` and `HexModArithMathlib` work.
- `ZMod64.inv` currently calls `Hex.pureIntExtGcd` rather than the public `HexArith.Int.extGcd` wrapper because the `mpz_gcdext` extern is still unresolved in this worktree and breaks `HexModArith.Smoke` when referenced directly.

**Next step**

- Review whether issue `#561` lands before PR creation; if it does, consider rebasing `ZMod64.inv` onto the public `HexArith.Int.extGcd` wrapper before opening the issue `#453` PR.
- Otherwise, commit and open the PR for `#453` with the pure helper rationale called out explicitly.

**Blockers**

- No blocker for `HexModArith` itself.
- Direct runtime use of `HexArith.Int.extGcd` remains blocked on the separate HexArith FFI wiring work.
