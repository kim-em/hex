**Accomplished**

- Claimed issue `#62` and inspected `HexMatrix/Determinant.lean`, the `hex-matrix` spec, and the existing row-operation surface to confirm the remaining Phase 1 determinant API gap.
- Added scaffolded Bareiss determinant declarations in `HexMatrix/Determinant.lean`: `Matrix.NonzeroBareissPivots`, `Matrix.bareissNoPivot`, `Matrix.bareissDet`, `Matrix.bareiss`, and theorems `bareissNoPivot_eq_det`, `bareissDet_eq_det`, and `bareiss_eq_det`.
- Verified the updated `HexMatrix` surface with `lake build HexMatrix`.

**Current frontier**

- The public Bareiss API names required by the spec now exist and compile, but the implementation still aliases the Leibniz determinant scaffold and the deeper correctness proof remains for later phases.

**Next step**

- Commit the determinant scaffold update and open a PR closing `#62`.

**Blockers**

- None.
