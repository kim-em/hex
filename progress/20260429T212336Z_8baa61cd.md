**Accomplished**

- Claimed issue #1164 for HexGF2 Phase 3 core conformance.
- Added `HexGF2/Conformance.lean` with deterministic Lean-side checks for packed GF(2) constructors, coefficient and degree accessors, XOR addition, shifts, multiplication, division/remainder, gcd, xgcd, `pureClmul`/`clmul`, and small AES-field `GF2n` operations.
- Imported the conformance module from `HexGF2.lean`.
- Bumped `HexGF2.done_through` from 2 to 3 in `libraries.yml`.
- Verified `lake build HexGF2`, `python3 scripts/status.py HexGF2`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested conformance banned-pattern / `#eval!` / `sorry` scans.

**Current frontier**

- `HexGF2` now reports ready for Phase 4 performance and benchmarking.
- `HexGF2/Conformance.lean` has one proof-only `sorry` for the AES irreducibility fixture needed to instantiate the small-field wrapper tests.

**Next step**

- Start HexGF2 Phase 4 benchmark coverage from the newly ready status target, or pick another unclaimed critical feature issue if Phase 4 is already covered.

**Blockers**

- No technical blockers found.
