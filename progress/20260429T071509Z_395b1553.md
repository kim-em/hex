**Accomplished**

- Claimed issue `#871`.
- Added public `FpPoly.mulCoeffTerm`, `FpPoly.mulCoeffSum`, and `FpPoly.coeff_mul` for the executable schoolbook multiplication coefficient formula.
- Proved `FpPoly.left_distrib` and `FpPoly.right_distrib` using the coefficient formula and bounded fold helpers.
- Verified `lake build HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and the requested placeholder scan.

**Current frontier**

- `HexPolyFp.Basic` now has multiplication distributivity over addition and a reusable coefficient formula for downstream quotient-ring work.
- Existing placeholders remain in `HexPoly/Operations.lean`; none were added in `HexPolyFp/Basic.lean`.

**Next step**

- Continue the polynomial multiplication law surface with issue `#846` (`FpPoly.one_mul` / `FpPoly.mul_one`) or downstream quotient-ring integration, depending on queue priority.

**Blockers**

- None.
