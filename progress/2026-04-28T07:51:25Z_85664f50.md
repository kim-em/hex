**Accomplished**

- Claimed issue `#567` and updated [HexPoly/Euclid.lean](/home/kim/hex/worktrees/85664f50/HexPoly/Euclid.lean:1) to remove the incorrect commutative-ring `Mod` instance whose non-monic branch returned the dividend unchanged.
- Replaced that surface with the explicit `DensePoly.modByMonic` helper so the exported remainder API now matches the implemented monic long-division contract.
- Tightened `polyCRT_mod_fst` and `polyCRT_mod_snd` to require monic divisors and to state their conclusions via `modByMonic`, preventing downstream code from depending on fake `%` semantics for unsupported divisors.
- Verified with `lake build HexPoly` and `python3 scripts/check_dag.py`.

**Current frontier**

- The `HexPoly` Euclidean/CRT surface no longer exposes placeholder remainder behavior over general commutative rings.

**Next step**

- Commit the `HexPoly` remainder-surface repair and open the PR closing `#567`.

**Blockers**

- None.
