**Accomplished**

- Claimed `#950`.
- Added the missing `UInt64` low-bit carry lemma for shifted words.
- Added `shiftLeftBitsList` word/bit bridge lemmas covering same-word and cross-word carry cases.
- Added coefficient bridge lemmas for raw shifted bit arrays and for the `Array.replicate (k / 64) 0` prefix used by `shiftLeft`, including the `k % 64 = 0` companion.
- Verified `lake build HexGF2.Basic`, `python3 scripts/check_dag.py`, `git diff --check`, the requested `shiftLeftBitsList`/`sorry` scan, and the forbidden `axiom|native_decide` scan.

**Current frontier**

- `GF2Poly.degree?_mulXk_of_degree?_eq_some` remains intentionally out of scope for this issue.
- The new coefficient lemmas provide the lower bridge needed by the dependent degree proof.

**Next step**

- Use `coeffWords_replicate_append_shiftLeftBitsList_add` and `coeffWords_replicate_append_add_of_mod_eq_zero` in a follow-up proof of `GF2Poly.degree?_mulXk_of_degree?_eq_some`.

**Blockers**

- No blockers for this shifted-word coefficient bridge.
