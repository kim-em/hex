**Accomplished**

- Claimed `#934` and fast-forwarded `agent/49ab3b63` to `origin/main`.
- Added word/highest-bit helpers in `HexGF2/Basic.lean`, including
  `highestSetBit?_isSome_of_ne_zero`.
- Added normalized polynomial degree-existence and coefficient-top inverse
  lemmas:
  `degree?_isSome_of_isZero_false` and
  `degree?_eq_some_of_coeff_eq_true_of_forall_gt_false`.
- Verified `lake build HexGF2.Basic`; the remaining diagnostic is the
  pre-existing `degree?_mulXk_of_degree?_eq_some` `sorry`.
- Opened follow-up issue `#943` for the remaining `shiftLeftBitsList`
  list/array coefficient bridge and linked it from `#934`.

**Current frontier**

- `degree?_mulXk_of_degree?_eq_some` still needs the coefficient semantics
  bridge through `shiftLeftBitsList`, `Array.replicate (k / 64) 0 ++ ...`,
  and `ofWords` normalization.

**Next step**

- Prove the shift-left coefficient bridge from raw shifted words to
  `p.coeff (n - k)`, then apply
  `degree?_eq_some_of_coeff_eq_true_of_forall_gt_false` to finish the
  `mulXk` degree theorem.

**Blockers**

- None. The remaining work is proof plumbing, now isolated in `#943`.
