**Accomplished**

- Claimed feature issue #1294 for the first `HexLLL` Phase 4 benchmark smoke surface.
- Added `HexLLL/Bench.lean` with deterministic prepared `LLLState` fixtures and five lean-bench registrations covering `sizeReduceColumn`, `sizeReduce`, `swapStep`, stored Gram-Schmidt coefficient recovery, and `potential`.
- Added the `hexlll_bench` executable to `lakefile.lean`.
- Added `lake exe hexlll_bench list` and `lake exe hexlll_bench verify` to the CI benchmark smoke workflow.
- Verified `lake build HexLLL`, `lake build hexlll_bench`, `lake exe hexlll_bench list`, `lake exe hexlll_bench verify`, `python3 scripts/status.py HexLLL`, `python3 scripts/check_phase4.py`, `python3 scripts/check_dag.py`, `git diff --check`, and a touched-file scan for forbidden executable/proof placeholders.

**Current frontier**

- `HexLLL` remains at `done_through: 3`; this issue intentionally adds the smoke executable surface without running all Phase 4 scientific benchmarks or bumping `libraries.yml`.
- The benchmark module uses a computable wrapper for the body of `LLLState.gramSchmidtCoeff` because the public projection is marked `noncomputable` and cannot be used directly as an executable benchmark target.
- The worktree still has a pre-existing unrelated `.claude/CLAUDE.md` modification that this session did not touch.

**Next step**

- Open the PR for #1294 with the smoke verification evidence.

**Blockers**

- None for this session's scope.
