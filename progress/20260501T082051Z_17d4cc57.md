**Accomplished**

- Claimed issue #1567 for the `HexPolyFp` square-free accumulator coprimeness invariant.
- Added a reusable `pairwise_append_of_cross` list lemma in `HexPolyFp/SquareFree.lean`.
- Replaced the direct `sorry` in `squareFreeAuxRev_pairwise_coprime_of_acc` with a call through the named proof boundary `squareFreeAuxRev_pairwise_coprime_core`, making the remaining proof obligation explicit.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan over `HexPolyFp`.

**Current frontier**

The accumulator theorem itself now has a stable wrapper, but the substantive Yun/recursive coprimeness proof remains in `squareFreeAuxRev_pairwise_coprime_core`.

**Next step**

Prove `squareFreeAuxRev_pairwise_coprime_core` by splitting out the `yunFactors` reverse-accumulator append relation and the cross-coprimeness facts between Yun factors, incoming accumulator entries, and the p-th-root recursive tail.

**Blockers**

None.
