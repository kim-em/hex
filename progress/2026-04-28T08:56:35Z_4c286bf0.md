**Accomplished**

- Claimed issue `#612` after checking the unclaimed queue and confirming the only open `human-oversight` item (`#561`) was not worker-claimable because it is already marked `replan`.
- Added [HexGF2/Field.lean](/home/kim/hex/worktrees/4c286bf0/HexGF2/Field.lean:1), introducing the Phase 1 `HexGF2` small-field surface: `GF2Poly.lowerMask`, `GF2Poly.ofUInt64Monic`, `GF2Poly.Irreducible`, and the single-word `GF2n` wrapper with executable reduction, addition, subtraction, negation, multiplication, and power APIs.
- Updated [HexGF2.lean](/home/kim/hex/worktrees/4c286bf0/HexGF2.lean:1) to export the new `GF2n` module and document that the root library now includes the first packed `GF(2^n)` wrapper surface.
- Verified `lake build HexGF2` succeeds and `python3 scripts/check_dag.py` stays clean.
- Ran a direct smoke test via `lake env lean` against `GF2n.reducePoly` using `pureClmul`; the reduction path evaluated to `87` for the canonicalized input word `0x57` and `49` for the reduced product of `0x57 * 0x83` modulo `x^8 + x^4 + x^3 + x^2 + 1`.

**Current frontier**

- Issue `#612` now has the spec-named helper surface and the small-field wrapper, but it has not yet been packaged into a PR.
- The sibling large-degree wrapper issue `#613` remains blocked on `#612`.

**Next step**

- Review the new `GF2n` API for any missing Phase 1 surface expected by `SPEC/Libraries/hex-gf2.md`, then commit and open the PR closing `#612`.

**Blockers**

- The interpreter still cannot resolve the pre-existing `Hex.clmul` extern via `lake env lean`, so runtime smoke testing had to use `pureClmul` instead of the `@[extern]` entry point. That wiring problem appears separate from `#612` and predates this change.
