**Accomplished**

- Claimed #1812 and split the non-circular `HexPolyFp/Basic.lean` canonical-remainder path into concrete helpers.
- Proved the local algebraic bridge `mod_remainders_congr_of_congr`, so congruent inputs now yield congruent computed remainders without using `DensePoly.DivModLaws` projections from the instance under construction.
- Routed `mod_eq_mod_of_congr_core` through explicit positive-degree and non-positive-degree helper surfaces, and routed the instance remainder-degree field through `divMod_remainder_degree_lt_core`.
- Created follow-up issues #1818 and #1819, with #1819 depending on #1818, and added the decomposition breadcrumb to #1812.
- Verified with `lake build HexPolyFp.Basic`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and forbidden-token scans over touched polynomial files.

**Current frontier**

- `mod_eq_mod_of_congr_core` no longer contains the monolithic placeholder; the remaining proof holes are isolated as `divMod_remainder_degree_lt_core`, `canonical_remainder_unique_of_pos_degree`, and `mod_eq_mod_of_congr_not_pos_degree`.
- The partial PR should mark #1812 for replan while preserving the concrete algebra bridge already proved.

**Next step**

- Claim #1818 to prove the concrete positive-degree `divMod` remainder-degree bound, then #1819 to finish canonical-remainder uniqueness and the non-positive-degree edge case.

**Blockers**

- None.
