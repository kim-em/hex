**Accomplished**

- Claimed #1835 and replaced the `ZPoly.multifactorLift` call inside
  `HexBerlekampZassenhaus.henselLiftData` with `ZPoly.multifactorLiftQuadratic`
  (the Phase 1 production lifter from #1834). Added the corresponding
  `import HexHensel.QuadraticMultifactor`. No other BZ surface changes.
- Verified locally: `lake build HexBerlekampZassenhaus` (538 jobs) builds
  cleanly with the new call. `HexBerlekampZassenhaus.Conformance` still
  passes — both lifters produce the same canonical output on the existing
  conformance set, so the SPEC-mandated correctness preservation holds.
- `python3 scripts/check_dag.py` is clean.

**Current frontier**

- BZ pipeline now lifts via the quadratic multifactor scaffold all the way
  through to `recombine`. The proof obligation
  `multifactorLiftQuadratic_spec` is still a Phase 5 `sorry`; nothing in
  `henselLiftData` consumes the spec lemma at definition time, so this is
  pure executable wiring.
- Only `multifactorLift` callsite in `HexBerlekampZassenhaus` was the one
  in `henselLiftData`; no further BZ-side renames needed.

**Next step**

- Issue F (scale conformance for `multifactorLiftQuadratic`) and the
  Phase 5 discharge of `multifactorLiftQuadratic_spec` remain open.
- Issue E (LLL-based replacement of `recombine`) is independent of this
  change.

**Blockers**

- Same local `bv_decide` timeout in `HexArith/Montgomery/InvNat.lean` as
  noted in the previous session: with the in-tree `timeout := 30` setting
  the build aborts at module 438/531 on this worktree's machine. Bumping
  the two timeouts to 240 lets the full BZ build complete in ~3 minutes;
  reverted before commit. CI presumably has the headroom. Worth a
  separate follow-up to either raise the in-tree timeout or factor InvNat
  out of the BZ closure.
