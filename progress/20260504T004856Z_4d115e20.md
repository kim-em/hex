**Accomplished**

- Claimed #1932 and added proof-only HexPoly division helpers in `HexPoly.Euclid`.
- Exposed `divModArray_scaleLead_congr` for pointwise-equivalent leading-coefficient scaling functions.
- Added `divModMonic_eq_divMod_of_monic_core` for the non-shortcut monic/general division agreement case.
- Added `divMod_reconstruction_step` as a public reconstruction-preservation lemma for one long-division update.

**Current frontier**

- Verification passed:
  - `lake build HexPoly`
  - `python3 scripts/status.py HexPoly`
  - `python3 scripts/status.py`
  - `python3 scripts/check_dag.py`
  - `git diff --check`
  - `rg -n "axiom|native_decide|TODO|FIXME|sorry" HexPoly/Euclid.lean`
- The helpers are intentionally proof-only; executable division, modulo, gcd, multiplication, derivative, content, primitive-part, and decomposition definitions were unchanged.

**Next step**

- Use these helpers in the HexPolyZ Rat `DensePoly.DivModLaws` instance, especially the remaining `divMod_spec` and monic-agreement fields.

**Blockers**

- None for this helper slice.
