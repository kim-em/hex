**Accomplished**

- Claimed #1626 and added `yunFactorsContribution_step_preserves_target` in
  `HexPolyFp/SquareFree.lean`.
- The new theorem packages the nonterminal Yun recursive step across both
  `isOne (c / gcd c w)` branches, preserving the common tail repeated part and
  exposing the split equations `(c / gcd c w) * gcd c w = c` and
  `(w / gcd c w) * gcd c w = w`.
- Verified `HexPolyFp.SquareFree`, `HexPolyFp`, `scripts/status.py HexPolyFp`,
  `scripts/check_dag.py`, `git diff --check`, and a touched-diff forbidden
  placeholder scan.

**Current frontier**

- #1626 now has the one-step preservation surface needed by the remaining
  `yunFactorsContribution_initial_state_product_invariant` proof.
- The initial-state invariant itself still contains the existing Phase 5
  `sorry`; no new placeholder was introduced.

**Next step**

- Use `yunFactorsContribution_step_preserves_target` to drive the induction for
  `yunFactorsContribution_initial_state_product_invariant`, then let #1627
  finish the initial-state reconstruction invariant.

**Blockers**

- None for this step. Existing upstream Phase 5 `sorry` warnings remain in
  `HexArith`, `HexModArith`, `HexPoly`, `HexPolyFp.Basic`, and later
  `HexPolyFp.SquareFree` proof bodies.
