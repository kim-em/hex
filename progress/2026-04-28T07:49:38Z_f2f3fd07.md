**Accomplished**

- Claimed `#561` and wired HexArith's FFI into [lakefile.lean](/home/kim/hex/worktrees/f2f3fd07/lakefile.lean:1) via a new `extern_lib hexarithffi` target that compiles `HexArith/ffi/wide_arith.c` and `HexArith/ffi/mpz_gcdext.c`.
- Enabled `HexArith` module precompilation and preserved GMP link args in the active Lake config.
- Deleted the dead root [lakefile.toml](/home/kim/hex/worktrees/f2f3fd07/lakefile.toml:1) and updated [scripts/check_dag.py](/home/kim/hex/worktrees/f2f3fd07/scripts/check_dag.py:1) plus [scripts/libgraph.py](/home/kim/hex/worktrees/f2f3fd07/scripts/libgraph.py:1) to read the active Lake config from `lakefile.lean`.
- Repaired [HexArith/ffi/mpz_gcdext.c](/home/kim/hex/worktrees/f2f3fd07/HexArith/ffi/mpz_gcdext.c:1) to call the actual exported boxed Lean symbol, which makes the `HexArith.Int.extGcd` extern work under `lake lean`.
- Verified `lake build HexArith`, `lake build`, `python3 scripts/check_dag.py`, `nm .lake/build/lib/libhexarithffi.a`, and `lake lean /tmp/test_extern.lean`.

**Current frontier**

- The repository-side FFI wiring is fixed and runtime smoke tests pass under `lake lean`, but the exact issue-body command `lake env lean /tmp/test_extern.lean` still fails to load precompiled module wrappers.

**Next step**

- Decide issue disposition on `#561`: either close with the stronger repo fix plus a note that `lake lean` is the working external-file runner, or mark partial/replan because raw `lake env lean` appears to be an upstream Lake behavior mismatch.

**Blockers**

- `lake env` only augments environment variables and does not run Lake's external-file setup path; even after building `libHex_HexArith.so`, `lake env lean` still reports missing `lp_Hex_*` wrapper symbols while `lake lean` succeeds on the same script.
