**Accomplished**

- Claimed human-oversight issue `#399` and verified its prerequisite `#384` was already merged while `HexMatrix/Nullspace.lean` still carried the forbidden data-level placeholder surface.
- Deleted the illegal executable nullspace API from `HexMatrix`: removed `IsRREF.nullspaceMatrix`, `IsRREF.nullspace`, `Matrix.nullspace`, and the theorem statements that depended on them, leaving only the matrix-times-column-vector helper in [HexMatrix/Nullspace.lean](/home/kim/hex/worktrees/edc4a122/HexMatrix/Nullspace.lean).
- Removed the downstream placeholder-dependent surfaces that named the deleted API: nullspace benchmark runners/results from [HexMatrix/Benchmark.lean](/home/kim/hex/worktrees/edc4a122/HexMatrix/Benchmark.lean), the deferred bridge theorem body from [HexMatrixMathlib/Nullspace.lean](/home/kim/hex/worktrees/edc4a122/HexMatrixMathlib/Nullspace.lean), and the Berlekamp kernel wrapper/theorem from [HexBerlekamp/Kernel.lean](/home/kim/hex/worktrees/edc4a122/HexBerlekamp/Kernel.lean), with matching root-module docstring updates.
- Verified the resulting tree with `lake build HexMatrix`, `lake build HexBerlekamp`, and `lake build HexMatrixMathlib`.

**Current frontier**

- The branch is ready to commit and publish for issue `#399`.

**Next step**

- Commit the nullspace-surface deletion together with this progress file and open the PR closing `#399`.

**Blockers**

- None.
