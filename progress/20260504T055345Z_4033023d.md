# 2026-05-04 05:53Z — feature session (issue #1997)

Branch: `agent/4033023d`. Pod session UUID `4033023d-53a5-...`.

## Accomplished

Landed the `hex-conway` triple cross-check from issue #1997
(`HexConway: cypari2 + Luebeck-cache triple cross-check`) — a
`feature` + `human-oversight` plan replacing the SPEC's Sage
assignment with cypari2 + a committed Lübeck flat-file cache.  Two
commits:

* `feat: scaffold HexConway Lübeck cache + JSONL emit driver` —
  `scripts/oracle/conway_lubeck_cache.py` (one-shot generator),
  `scripts/oracle/conway_lubeck.json` (36-entry committed cache,
  p ∈ {2,3,5,7,11,13}, n ∈ {1..6}, parsed from upstream
  `CPimport.txt`), `HexConway/EmitFixtures.lean` (writes one
  `prime` + one `result` JSONL record per `(p, n)` carrying Lean's
  `luebeckConwayPolynomial?` answer; `null` where Lean has not
  committed an entry yet), `lakefile.lean` exe target, committed
  fixture snapshot under `conformance-fixtures/HexConway/`.
* `feat: add HexConway PARI + Lübeck triple cross-check oracle` —
  `scripts/oracle/conway_pari.py` (reads the JSONL stream, queries
  `pari.ffinit(p, n)`, loads the cache, asserts PARI ≡ Lübeck
  always and Lean ≡ Lübeck where Lean has a committed entry),
  `HexConway/Conformance.lean` `#guard`s for the full oracle
  coverage matrix, and an `oracle-pari` CI job extension that
  builds `hexconway_emit_fixtures` and runs the new oracle next to
  the existing HexHensel one.

## Current frontier

PR open against #1997.  Lean side currently only commits to
`(2, 1)`; the oracle still continuously cross-checks PARI vs. the
committed Lübeck cache for every `(p, n)` in coverage so the cache
is regression-protected as Lean's committed table grows.

## Next step

Once Lean grows additional `luebeckConwayPolynomial?` entries (a
separate Tier 1 work item — each requires an irreducibility proof,
currently `sorry`'d for `(2, 1)` already), the existing oracle will
automatically promote those `null` results into Lean ≡ Lübeck
checks.  No further oracle work needed for that growth.

## Blockers

None.  Local self-tests with a stubbed `cypari2` pass (37 / 37
comparisons; failure-record path verified by perturbing the cache).
Real PARI binding only exercised in CI.
