**Accomplished**

- Claimed `#954`.
- Added the bounded small-prime selection surface in `HexBerlekampZassenhaus.lean`:
  `PrimeCandidateScore`, `choosePrimeScore?`, and public `choosePrime`.
- The chooser scans fixed small primes, filters by `isGoodPrime`, scores accepted
  candidates through the available modular square-free decomposition surface, and
  keeps the smaller earlier prime on score ties.
- Added proof-shell links from successful scored selection back to `isGoodPrime`.

**Current frontier**

- `choosePrime` now exists and compiles, but the scoring surface should later be
  strengthened from the current square-free decomposition count to the eventual
  full modular factor count once the Berlekamp factor payload API is available.
- Literal small-prime proofs and selection soundness links are theorem-only
  shells.

**Next step**

- Follow-up Berlekamp-Zassenhaus work should build `choosePrimeData` around the
  selected prime and modular factors once the full `F_p[x]` factorization surface
  is ready.

**Blockers**

- No build blocker. Full modular factor-count scoring depends on a completed
  Berlekamp factorization payload API rather than only split-step and square-free
  helpers.
