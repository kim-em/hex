**Accomplished**

- Claimed #1524, identified that its pth-root contribution theorem is too strong for non-one constant derivative-zero inputs, and marked it for replan with the counterexample shape.
- Claimed #1514 and narrowed the Yun factor square-freeness proof boundary in `HexPolyFp/SquareFree.lean`.
- Replaced the arbitrary `c`/`w` step theorem with `yunFactorsStepsSquareFree`, a recursive reachable-state invariant for the actual Yun loop states.
- Threaded that invariant through `yunFactors_factors_squareFree_of_steps` and the derivative-split wrapper.
- Verified `lake build HexPolyFp.SquareFree`, `lake build HexPolyFp`, `python3 scripts/status.py HexPolyFp`, `python3 scripts/check_dag.py`, `git diff --check`, and a forbidden-token scan of `HexPolyFp/SquareFree.lean`.

**Current frontier**

- The Yun factor square-freeness boundary no longer claims an unrelated derivative split proves square-freeness for arbitrary `c` and `w`.
- The remaining proof leaf is now the narrowed reachable-state theorem `yunFactorsStepsSquareFree_of_derivative_split`.
- The pth-root contribution issue #1524 needs re-planning around a monic/reachable-state invariant or a revised derivative-zero branch boundary.

**Next step**

- Prove `yunFactorsStepsSquareFree_of_derivative_split` from the actual Yun-loop state invariant starting at `c = f / gcd f f'` and `w = gcd f f'`.

**Blockers**

- No blocker for #1514 after this boundary repair.
